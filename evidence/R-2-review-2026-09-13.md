# R-2 内容管线过敏原纵深校验卡 · 独立技术审查报告

> 审查角色：fm-reviewer（工具级只读）。审查对象：git diff HEAD 8 文件（4 修改 + 4 新建，均在 tools/content-pipeline）。
> 落盘说明：审查会话无本地文件写入工具，按 DUP-KEY-review / T-C06-review 先例由主控代落盘（原文照录，未改动）。

---

## 一、结论

**最终判定：FAIL（需修改）**

唯一阻塞项 **C-1**：任务卡 AC2 明文要求"package.json bin 注册 fm-publish-check"，实际 [package.json:L14-L19](file:///d:/codex/family-menu/tools/content-pipeline/package.json#L14-L19) 的 `bin` 字段仅含 fm-coverage / fm-draft / fm-import / fm-menu 四项，**无 fm-publish-check**；而开发报告在 [L30](file:///d:/codex/family-menu/evidence/R-2-dev-2026-09-13.md#L30)、[L40](file:///d:/codex/family-menu/evidence/R-2-dev-2026-09-13.md#L40)、[L136](file:///d:/codex/family-menu/evidence/R-2-dev-2026-09-13.md#L136) 三处声称"bin 注册"且 AC2 自检 [✓]——**AC 字面未满足 + 自检失实**，按项目纪律"只认 [✓]/[✗] AC 自检"必须打回。修复成本极低（bin 补一行 + 报告三处勘误），修复后仅需轻量复验（核对 bin 实文即可），无需全套重审。

除此之外，核心交付全部成立：匹配口径与 engine 对齐、只读红线零违反、DEC-006 双保险未动、退出码契约实现正确、31 例单测覆盖齐备、动态全量读取规则无硬编码。

## 二、审查范围与方法声明

- 依据链：[R-2-task](file:///d:/codex/family-menu/evidence/R-2-task-2026-09-13.md)（AC 权威口径）→ [R-2-dev](file:///d:/codex/family-menu/evidence/R-2-dev-2026-09-13.md) → [CURRENT.md](file:///d:/codex/family-menu/CURRENT.md) R-1 段 → 实际代码 8 文件全文。
- 本会话无 Bash 工具，`pnpm test / test:taboo / build / dry-run / publish-check` 均无法独立复跑，相关数字一律标注"未验证"；以静态核查 + 交叉佐证（dist 构建产物实存、.workflow-verify/r2 验证产物实存、GetDiagnostics 类型检查）补强。
- git diff HEAD 口径以主控提供的 8 文件清单为准；越界检查以 Glob mtime 排序做近似印证（方法学局限已如实标注）。

## 三、逐审查点结论

| # | 审查点 | 结论 | 摘要 |
|---|---|---|---|
| 1 | 越界检查 | [✓] | 改动严格限于 tools/content-pipeline；shared/api/engine/prisma/seed 零触碰；无未报告源码改动 |
| 2 | 红线检查（只读/DEC-006） | [✓] | 新增代码仅 findMany + $disconnect，零写语句；status:'DRAFT' 字面量未动；无 LLM API |
| 3 | AC1 匹配口径与分级 | [✓] | INGREDIENT 精确等值含 DB join 补全；TAG 子串宽口径；五分支分级正确；先检后写真实成立 |
| 4 | AC2 退出码契约 | [✗]（C-1） | 实现 0/2/1 三态正确；但 bin 注册未满足 + dev 报告三处声称失实 |
| 5 | AC3 category 警告 | [✓] | 警告不阻断不改退出码；只消费 shared CATEGORIES；publish-check 报告面计数 |
| 6 | 数字复核 | [?] | 31 例单测静态清点吻合；431/431、95/95、build、五/四分支退出码等命令执行类未验证（无工具），佐证链见 §五.6 |
| 7 | 测试质量 | [✓] | 恰 31 个 it，覆盖五分支+退出码+警告面；mock prisma 属常规形态，真实库盘点由 AC4 附录承接 |
| 8 | 卡面笔误备案 | [✓] | 实现为 `exclusionRule.findMany()` 动态全量读取，无任何硬编码行数/规则 id，3 vs 4 行差异不影响行为 |

## 四、证据详情

### 4.1 越界检查 [✓]
- Glob（mtime 降序）显示 tools/ 最新条目为 dist/ 构建产物（dist/cli/publish-check.js、dist/allergen.js 等），**间接佐证 dev 报告 3.4 build EXIT=0 真实执行过**；其后恰为本卡 8 文件，再往前为 tc06/tc05 历史产物。
- packages/ 最新文件（engine/test/safety-ingredient-realform.spec.ts 等）与 apps/ 最新文件均对应更早卡片已提交交付物；[shared constants/index.ts](file:///d:/codex/family-menu/packages/shared/src/constants/index.ts) 六类 CATEGORIES 原文未动；seed-data.ts、schema.prisma 无本卡改动痕迹。
- 方法学说明：无法直接跑 git status，以主控 git diff HEAD = 8 文件口径 + Glob mtime 印证双重确认。

### 4.2 红线检查 [✓]
- [allergen.ts:L214](file:///d:/codex/family-menu/tools/content-pipeline/src/allergen.ts#L214) `exclusionRule.findMany()`、[L228](file:///d:/codex/family-menu/tools/content-pipeline/src/allergen.ts#L228) `ingredient.findMany({ where: { id: { in: ... } } })`、[L249](file:///d:/codex/family-menu/tools/content-pipeline/src/allergen.ts#L249) `ingredient.findMany({ where: { name: { in: ... } } })`——全程只读。
- [publish-check.ts:L233-L235](file:///d:/codex/family-menu/tools/content-pipeline/src/publish-check.ts#L233-L235) `dish.findMany({ where: { status: { in } }, include: { ingredients: { include: { ingredient: true } } } })`——只读联查。
- [cli/publish-check.ts](file:///d:/codex/family-menu/tools/content-pipeline/src/cli/publish-check.ts) 全文零写语句，`finally` 中 `$disconnect`。
- [cli/import.ts:L157](file:///d:/codex/family-menu/tools/content-pipeline/src/cli/import.ts#L157) 写路径仍为既有 `importDraft(content, writer, importOptions)`，未新增任何写库语句；[db.ts](file:///d:/codex/family-menu/tools/content-pipeline/src/db.ts) 仅在 writer 返回值新增 `prisma` 字段（单连接复用供只读校验）。
- DEC-006：[import.ts:L78](file:///d:/codex/family-menu/tools/content-pipeline/src/import.ts#L78) / [L196](file:///d:/codex/family-menu/tools/content-pipeline/src/import.ts#L196) `status: 'DRAFT'` 字面量双保险原样保留；新增代码无 LLM API 调用；package.json dependencies 零新增（AC6）。

### 4.3 AC1 匹配口径与分级 [✓]
- **INGREDIENT 精确等值**：[allergen.ts:L61-L74](file:///d:/codex/family-menu/tools/content-pipeline/src/allergen.ts#L61-L74) nameSet = {targetId, target 行.name, ...target.aliases}；[L96-L105](file:///d:/codex/family-menu/tools/content-pipeline/src/allergen.ts#L96-L105) 食材侧 names = {name, ...aliases, dbId?, ...dbAliases} 与 nameSet 严格等值求交（`filter(n => nameSet.has(n))`）——对齐 engine `buildHardIngredientNameSets`/`checkIngredientScope`。join 不到时 nameSet 仅含 targetId（[L206 注释](file:///d:/codex/family-menu/tools/content-pipeline/src/allergen.ts#L206)），不会误伤其他食材。
- **TAG 子串宽口径**：[L118-L127](file:///d:/codex/family-menu/tools/content-pipeline/src/allergen.ts#L118-L127) `t.includes(tag)` 扫 name/aliases/dbAliases——补位 engine `checkTagScope` 只查 flavorTags+category 的已知缺口（[safety.ts](file:///d:/codex/family-menu/packages/engine/src/safety.ts) 证实），与卡面"纵深补位"口径一致。
- **已标注放行**：[L121](file:///d:/codex/family-menu/tools/content-pipeline/src/allergen.ts#L121) `if (dish.flavorTags.includes(tag)) continue;`——engine 会拦、管线放行，与卡面"flavorTags 含 targetTag 即视为已标注"一致。
- **先检后写真实成立**：[cli/import.ts](file:///d:/codex/family-menu/tools/content-pipeline/src/cli/import.ts) 流程为 prepareDraftDish → category 警告（L78）→ loadAllergenRules（L85）→ checkDishAllergen（L107）→ blocked 则输出明细 + exitCode=2（L168）**且不调用 importDraft**；仅 `decision.blocked === false` 才走 [L157 importDraft](file:///d:/codex/family-menu/tools/content-pipeline/src/cli/import.ts#L157)。content 为同一内存字符串重解析，无 TOCTOU 窗口。
- **豁免 flag**：[L43](file:///d:/codex/family-menu/tools/content-pipeline/src/cli/import.ts#L43) `--allow-allergen-draft` 注册，[L140](file:///d:/codex/family-menu/tools/content-pipeline/src/cli/import.ts#L140) 拒绝时提示显式追加，豁免放行保留警告。
- **空规则集零影响 / DISH 跳过**：无规则时校验空转；scope=DISH 跳过有注释（[L84](file:///d:/codex/family-menu/tools/content-pipeline/src/allergen.ts#L84)、[L142](file:///d:/codex/family-menu/tools/content-pipeline/src/allergen.ts#L142)），理由（新导入菜无稳定 id）成立。

### 4.4 AC2 退出码契约 [✗]（C-1）
- **实现面正确**：[cli/publish-check.ts:L64](file:///d:/codex/family-menu/tools/content-pipeline/src/cli/publish-check.ts#L64) `gapDishCount > 0 → process.exitCode = 2`；扫完无缺口缺省 0；[L74](file:///d:/codex/family-menu/tools/content-pipeline/src/cli/publish-check.ts#L74) `parseAsync().catch()` → `process.exit(1)`（fail-closed）。`--status` 缺省全扫/大小写归一/去重保序/白名单外抛错（[publish-check.ts:L34](file:///d:/codex/family-menu/tools/content-pipeline/src/publish-check.ts#L34)）；`--json` 结构化输出（L57）。
- **C-1 阻塞**：任务卡 [L17](file:///d:/codex/family-menu/evidence/R-2-task-2026-09-13.md#L17) AC2 原文"**新增 CLI（package.json bin 注册 fm-publish-check）**"；实文 [package.json:L14-L19](file:///d:/codex/family-menu/tools/content-pipeline/package.json#L14-L19) bin 仅 4 项无 fm-publish-check，仅 [L28](file:///d:/codex/family-menu/tools/content-pipeline/package.json#L28) scripts 增加 `"publish-check": "tsx src/cli/publish-check.ts"`——scripts 不构成 bin 注册。dev 报告 [L30](file:///d:/codex/family-menu/evidence/R-2-dev-2026-09-13.md#L30)（"fm-publish-check CLI（bin 注册 fm-publish-check）"）、[L40](file:///d:/codex/family-menu/evidence/R-2-dev-2026-09-13.md#L40)（"bin 注册 fm-publish-check；scripts 增加 publish-check"）、[L136](file:///d:/codex/family-menu/evidence/R-2-dev-2026-09-13.md#L136)（AC2 自检 [✓] "bin 注册（package.json diff）"）三处声称与实文矛盾。dist/cli/publish-check.js 已构建存在，补注册无技术障碍。

### 4.5 AC3 category 警告 [✓]
- [import.ts:L17-L21](file:///d:/codex/family-menu/tools/content-pipeline/src/import.ts#L17-L21) `findNonStandardCategories` 消费 `(CATEGORIES as readonly string[]).includes`（只读不修改 shared）；[L26-L30](file:///d:/codex/family-menu/tools/content-pipeline/src/import.ts#L26-L30) 警告含数量/六类清单。
- [cli/import.ts:L78](file:///d:/codex/family-menu/tools/content-pipeline/src/cli/import.ts#L78) `console.warn` 走 stderr，不设置 exitCode——不阻断不改变退出码。
- [publish-check.ts:L120-L129](file:///d:/codex/family-menu/tools/content-pipeline/src/publish-check.ts#L120-L129) collectNonStandardCategories 报告面计数 + [L192](file:///d:/codex/family-menu/tools/content-pipeline/src/publish-check.ts#L192) 格式化输出。与 CURRENT.md R-1 段"管线侧消费常量非修改 shared、嫩豆腐存量不动"裁决一致。

### 4.6 数字复核 [?]
- **静态吻合**：test/allergen.spec.ts 逐 it 清点恰 31 例；卡面预期 2 道含花生缺口与 dev 报告附录 B（31 道菜、缺口 2、非标 category 1 = 嫩豆腐 = 豆制品）与 seed 三规则（花生 TAG HARD + 花生米 INGREDIENT HARD + 内脏 SOFT）逻辑自洽。
- **交叉佐证**：dist/ 构建产物实存（佐证 build）；`.workflow-verify/r2/` 下 db-baseline.mjs + r2-import-{clean,hard,soft,tagged}.json 共 5 文件实存（佐证五分支 dry-run 验证过程真实发生）。
- **未验证**：431/431、95/95、build EXIT=0、五分支 exit 2/0/0/0/0、四分支 exit 2/2/0/1、AC4 附录 A 是否真实库产出——本会话无命令执行工具，无法独立复跑，**全部标未验证**，仅确认报告内命令+退出码记录形态合规。

### 4.7 测试质量 [✓]
- 31 例覆盖：AC1 五分支（HARD 拒绝/豁免放行/SOFT 警告/已标注放行/无命中）、INGREDIENT 等值细节（name/aliases/dbAliases/dbId/join 不到/子串反例）、TAG 子串、DISH 跳过、空规则集、normalizeStatusFilter 三例、scanDishesForGaps/buildPublishCheckReport/formatReport/loadDishesForCheck、AC3 三函数+去重。
- mock prisma 属常规单测形态（对齐管线既有 spec），spec 头部明示"真实库盘点属 AC4 另行实跑"——符合任务卡"禁止 Mock 冒充真实环境**出报告**"的要求（报告以 AC4 实跑承接）。

### 4.8 卡面笔误备案 [✓]
- [allergen.ts:L214](file:///d:/codex/family-menu/tools/content-pipeline/src/allergen.ts#L214) `exclusionRule.findMany()` 无 where 全量动态读取，代码中无任何硬编码规则行数或规则 id 清单。本地 3 行（缺腊肉 ex-1788658316681）vs RDS 4 行的差异不影响行为——腊肉规则在 RDS 存在时同样会被动态纳入。备案成立。

## 五、问题清单

**C 级（阻塞）**
- **C-1**：AC2 的 bin 注册未满足 + dev 报告三处声称失实（证据见 §4.4）。

**S 级（建议，不阻塞）**
- **S-1**：CLI 层（cli/publish-check.ts 退出码三态、--status 过滤语义）无单测，目前仅靠真实库实跑（dev 报告 3.3）佐证。建议后续补 commander 层轻量测试或函数化抽出可测。
- **S-2**：[publish-check.ts:L91](file:///d:/codex/family-menu/tools/content-pipeline/src/publish-check.ts#L91) 注释明示"HARD/SOFT 均算缺口——盘点面不做豁免裁量"。卡面 AC2 未明确盘点面 SOFT 是否计入缺口，此为合理设计选择，但**请主控确认口径**并在卡面/文档固化，避免后续 verify 环节口径争议。
- **S-3**：loadAllergenRules"无 where 全量读取"这一关键点仅有静态核实，单测 mock 未断言 `findMany` 调用参数（无 where）。建议补一条断言防回归。
- **S-4**：dry-run 现也会建立 DB 连接并加载规则（loadAllergenRules 在 dry-run 分支之前执行），对"无 DB 也能 dry-run"的旧用法是行为变化；help 文本已披露，提示 verify 环节知悉即可。

## 六、未验证清单（工具限制，如实声明）

pnpm test（431/431）、pnpm test:taboo（95/95）、build EXIT=0、fm-import 五分支 dry-run 退出码、fm-publish-check 四分支退出码实跑、AC4 附录 A 真实库产出复核——均未独立执行。已有佐证：dist 构建产物实存、.workflow-verify/r2 验证产物实存、GetDiagnostics 对 4 个新建/修改 TS 文件零诊断（类型级验证通过）、31 it 静态清点。

## 七、最小修复建议（交主控派发 fm-dev，本审查不代改）

1. [tools/content-pipeline/package.json](file:///d:/codex/family-menu/tools/content-pipeline/package.json) `bin` 字段补一行：`"fm-publish-check": "./dist/cli/publish-check.js"`（dist 产物已存在，补后建议跑一次 `pnpm --filter @family-menu/content-pipeline build` 确认无副作用）。
2. [evidence/R-2-dev-2026-09-13.md](file:///d:/codex/family-menu/evidence/R-2-dev-2026-09-13.md) 三处勘误：L30、L40、L136——将"bin 注册 fm-publish-check"改为"bin 注册**缺失，补丁后达成**"或补丁后改回 [✓] 并注明补丁时间。
3. 复验方式（轻量）：仅核对 package.json bin 实文 + `fm-publish-check --help` 可经 bin 入口执行即可，无需重跑全套测试。
