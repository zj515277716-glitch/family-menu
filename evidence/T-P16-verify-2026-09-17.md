# T-P16 独立验收报告（fm-verify）

- 验收日期：2026-09-17
- 验收角色：fm-verify（产品验收，独立于 DEV/REVIEW）
- 被验收任务：T-P16 推荐套餐 MealRole 槽位动态组合（任务卡：docs/workflow/tasks/T-P16-推荐套餐MealRole槽位动态组合.md）
- 被测版本：分支 `feat/tp-16-role-slot-composition`，代码 HEAD `e2e8b92`，dev 报告提交 `5e7a71f`，基线 `a095661`（下文各用例 Version 均指此版本，不再重复）
- 环境：Windows + PowerShell 5.1；便携 PostgreSQL 端口 54329（`postgresql://postgres@127.0.0.1:54329/family_menu`）；API 端口 3000；鉴权 cookie（值与 .env ACCESS_TOKEN 一致，报告中脱敏为 `family-menu-local-****`）
- 独立性声明：本验收不采信 dev/review 自检数字，全部独立复跑与真实接口调用取证；全程未修改任何业务代码/UI 基线文件；唯一新增文件即本报告。临时探测脚本一律写入 `%TEMP%`（未入仓库）。

## 0. 沿用的主控裁决

- 裁决①：AC9 h5 豁免口径 = h5 typecheck 错误数 ≤ 基线 6 错即达标。
- 裁决②：seed 七表基线断言收敛为内容四表 Dish=49 / Menu=16 / MenuDish=52 / Ingredient=81；Plan / Event 仅登记、不作达标断言。
- 其余裁决（文档归属、数字复跑移交 verify、P3 挂账）按 evidence/T-P16-review-2026-09-17.md 附录执行。

## 1. 第一部分：数字复跑（全部独立复跑）

| # | 项目 | 命令 | 退出码 | 复跑结果 | dev 报告值 | 比对 |
|---|------|------|--------|----------|-----------|------|
| 1 | 全仓单测 | `pnpm test` | 0 | 21 test files / 471 passed / 0 failed | 21 / 471 | 一致 |
| 2 | 禁忌集 | `pnpm test:taboo` | 0 | 6 test files / 108 passed / 0 failed | 6 / 108 | 一致 |
| 3 | API 类型检查 | `tsc --noEmit`（apps/api） | 0 | 0 错误 | 0 错 | 一致 |
| 4 | h5 类型检查 | `tsc --noEmit`（apps/h5，EXIT=2 为预期） | 2（预期） | 6 个 pre-existing 错误 | 6 错 | ≤6，裁决①豁免命中 |
| 5 | seed 幂等闭环 | 人工破坏 2 行 → `pnpm db:seed` 重放 → 七表计数脚本复查 | 0 | 四表恢复 Dish=49 / Menu=16 / MenuDish=52 / Ingredient=81；Plan/Event 仅登记（裁决②） | 49/16/52/81 | 一致 |

seed 闭环明细：
- 破坏操作（只改数据、不改代码）：`seed-dish-potato-ribs.status → TESTED`；`cmtvkq3dl00009cpgaeksvumo.mealRole → MAIN`。
- 重放 `pnpm db:seed` 后，四内容表计数恢复 49/16/52/81，破坏字段被 seed 纠正（幂等性成立，AC8 命中）。

第一部分结论：5/5 全部一致，无偏差。

## 2. 第二部分：接口级产品验收（真实 HTTP 调用，非数据库直查替代）

通用 Expected 依据：PRODUCT-CONFIRMATION.md C-3/C-3a/C-7/C-7a、docs/ai-rebuild/PRODUCT-DECISIONS.md PD-018、任务卡 AC1~AC8。
DB 前置事实（只读探测）：PUBLISHED 菜品 22 道，按 mealRole 池 MAIN=16 / SIDE=2 / SOUP=3 / STAPLE=1；食材 `seed-ing-ribs`=排骨、`seed-ing-cucumber`=黄瓜、`seed-ing-beef`=牛腩；苦瓜不在食材库；ExclusionRule 基线 3 条 seed 行。

### R1 基线 + 人数映射（AC1）
- Operation：`GET /api/health`（豁免鉴权）；`POST /api/recommend` 分别以 people=2/4/6、timeBudgetMin=60 调用。
- Expected：PD-018 骨架=主菜+配菜+汤；主菜数=⌈people/2⌉ → 2人=3道、4人=4道、6人=5道。
- Actual：
  - people=2：cand#1 = 3 道 [MAIN,SIDE,SOUP]（命中 3 道）；cand#2 = 2 道；cand#3 = 1 道。
  - people=4：cand#1 = 3 道 [MAIN,MAIN,SOUP]（配菜槽留空降级）；完整 4 道虚拟菜单（virt-001/002 含主菜+配菜+汤）确已由组合层生成，仅评分排序未进 top-3 → 见观察项 V-1。
  - people=6：cand#1 = 1 道 [MAIN]（主菜槽缺 2 道降级）；cand#2 = 5 道 [MAIN,MAIN,MAIN,SIDE,SOUP]（命中 5 道）；cand#3 = 4 道。
- Evidence：响应内逐菜 `menu[].mealRole` 取证（DishView 含 mealRole）；三次调用响应哈希记录于会话日志。
- Coverage boundaries：验证了 top-3 候选构成与 mealRole 标注；未验证全部候选分页（接口本身只返回 3 候选）。
- 判定：AC1 命中（2 人=3 道、6 人=5 道均在候选集内出现；4 人见 V-1 观察项，属降级语义而非映射错误）。

### R2 必消硬要求（AC3，排骨）
- Operation：`POST /api/recommend` {people:4, timeBudgetMin:60, mustUse:["排骨"]}。
- Expected：所有返回候选的菜单均包含含"排骨"食材的菜。
- Actual：3 候选全覆盖，allCandidatesCoverRibs=True。
- Evidence：逐候选扫 `menu[].ingredients[].ingredientName`。
- Coverage boundaries：单食材必消；多食材组合贪心覆盖轮转有单测覆盖（471 用例内），未在本轮接口级重复。
- 判定：命中。

### R3 空手两型口径——无菜型 C-7（苦瓜）
- Operation：`POST /api/recommend` {people:4, timeBudgetMin:60, mustUse:["苦瓜"]}（苦瓜不在库）。
- Expected：返回空候选（不凑数），unmetReasons 原样透传且标注 NO_DISH。
- Actual：candCount=0；unmetReasons={"苦瓜":"NO_DISH"}。
- Evidence：响应 JSON。
- 判定：命中（未映射原文→空手→侧车原因回译，planService resolveMustUseIds 链路真实生效）。

### R7a/R7b 空手两型口径——时间型 C-7（牛腩）
- Operation：R7a {people:4, timeBudgetMin:15, mustUse:["牛腩"]}；R7b 对照 {people:4, timeBudgetMin:60, mustUse:["牛腩"]}。前置探测：含牛腩食材的菜最短 activeMinutes=20，15min 档必然无法承载。
- Expected：R7a 空手且 unmetReasons 标注 TIME_BUDGET；R7b 应正常出菜且全候选含牛腩。
- Actual：R7a candCount=0 且 unmetReasons={"牛腩":"TIME_BUDGET"}；R7b 3 候选全部含牛腩菜。
- Evidence：响应 JSON；DB 最短时长探测脚本（%TEMP%）。
- Coverage boundaries：NO_DISH/TIME_BUDGET 两型均已实测；其余 reason 枚举值（如器具型）有单测覆盖，未在本轮接口级重复。
- 判定：命中，C-7 两型口径完整。

### R4 时长档 + 缺槽降级（AC4/AC5，排骨+tb=15）
- Operation：`POST /api/recommend` {people:4, timeBudgetMin:15, mustUse:["排骨"]}。
- Expected：AC5 不凑数——无整套可行时槽位留空降级；但因存在 ≤15min 的单主菜可行套，不应空手。
- Actual：非空手，返回 2 套单主菜菜单（各菜 activeMinutes 之和=15 ≤ 15），配菜/汤槽位留空。
- Evidence：响应内逐菜 activeMinutes 求和 + mealRole 缺槽。
- 判定：命中（留空降级、不无谓凑数）。

### R5 时长档求和（AC4，tb=30）
- Operation：`POST /api/recommend` {people:4, timeBudgetMin:30}。
- Expected：候选内所有菜 activeMinutes 之和 ≤ 30。
- Actual：3 候选中最大求和 maxSum=14 ≤ 30，withinBudget=True。
- Evidence：逐候选求和计算记录。
- 判定：命中。

### G 硬禁忌一票否决（AC2，黄瓜）+ 数据还原
- Operation：`PUT /api/family/exclusions` 全量替换（3 条 seed 规则 + 1 条新增 HARD 禁忌黄瓜）→ `POST /api/recommend` {people:4} → 还原为 3 条 seed 规则 → `GET /api/family/exclusions` 复查。
- Expected：设 HARD 禁忌后任何候选不含黄瓜（安全过滤>一切评分）；seed 前缀规则受保护、还原后基线完整。
- Actual：设禁忌后 anyCandidateContainsCucumber=False；还原后返回 3 条 seed 行完整（seed-excl-organ / seed-excl-peanut / seed-excl-peanut-ing），familyId/主角字段无损。
- Evidence：PUT/GET 响应 JSON；禁用前后候选扫描结果。
- Coverage boundaries：HARD 级接口级实测；SOFT-TAG 降权（不排除）由单测覆盖（471 用例内），未在本轮接口级重复。
- 判定：命中，且验收过程未污染数据（已还原）。

### H 换一批真实变化（AC6，×2 次）
- Operation：创建 plan 后连续 2 次 `POST /api/plans/:id/swap` {swapType:"全换"}；对三次结果做菜组合集合比对。
- Expected：换了就是真的换了——第 1 次 ≠ 初次；第 2 次 ≠ 第 1 次且 ≠ 初次。
- Actual：s1Changed=True；s2ChangedAgain=True；s2DiffBase=True（三次菜组合两两不同）。
- Evidence：三次响应的菜品集合哈希比对记录。
- Coverage boundaries：本轮接口级实测"全换"；"单菜换"路径未在本轮接口级重复（其候选五层过滤逻辑在 471 单测内覆盖），见第 4 节边界。
- 判定：命中。

第二部分结论：8 组实测全部命中，无 FAIL 项。

## 3. 第三部分：浏览器/界面实操——如实降级

- Operation：拟对 h5 推荐页做真实浏览器点击/输入/提交取证。
- Actual：本验收会话可用工具集中无桌面浏览器自动化驱动（无 Playwright/Puppeteer 类工具；mobile-mcp 需任务书指定的真实设备/模拟器，本次未提供目标设备）。按用户任务书"浏览器实操或如实降级说明"授权，本项如实降级，未做浏览器实操。
- 替代证据：页面数据源即本报告第二部分的 recommend/swap/exclusions 接口，接口级全链路已按最终用户视角参数（人数/时长/必消/禁忌/换一批）实测命中。
- 残余风险：UI 呈现层（槽位留空的展示、侧车原因文案渲染、换一批按钮交互）未在浏览器核验，建议后续具备浏览器自动化条件时补充一轮。

## 4. 覆盖边界汇总

已测：数字复跑 5 项；人数映射 2/4/6 三组；必消（排骨）覆盖；空手 NO_DISH/TIME_BUDGET 两型；时长档求和（tb=30 及 tb=15 求和=15 边界）；HARD 禁忌一票否决+还原；换一批×2 真实变化；seed 幂等闭环。
未测/降级：浏览器 UI 实操（降级，见第 3 节）；单菜换接口级重复实测（单测覆盖）；SOFT-TAG 降权与器具型空手 reason 的接口级重复实测（单测覆盖）；Plan/Event 表仅按裁决②登记、未断言。

## 5. 观察项与问题清单

- V-1（P3 观察项，不计 FAIL）：people=4 时组合层已产出完整 4 道虚拟菜单（virt-001/002，含主菜+配菜+汤），但评分排序未进 top-3，主推为 3 道 [MAIN,MAIN,SOUP]（配菜槽留空）。属 AC5 降级语义正常运作，非功能缺陷；建议 PM/DEV 评估是否在排序层对"完整套"加权以改善主推完整度。超出本验收修改范围，不顺手修改。
- V-2（P3 观察项，不计 FAIL）：tb=60+必消牛腩（R7b）出现单菜候选（仅 1 道主菜），系必消定向套贪心覆盖结果，符合 C-7/AC3 口径，但与"骨架"完整度的体验预期有差异，同 V-1 一并建议评估。
- 无 P0/P1/P2 问题。

## 6. 环境清理确认

- API 服务：已停止（无残留错误输出）。
- PostgreSQL：`pg_ctl -m fast stop` 成功，exit 0，postmaster.pid 已消失（"server stopped"）。
- 数据还原：ExclusionRule 已还原为 3 条 seed 基线行；seed 闭环复跑本身即幂等恢复。
- 临时脚本全部位于 `%TEMP%`，未进入仓库；本验收唯一新增文件为本报告。

## 7. 总结论

**PASS** —— 数字复跑 5/5 一致；接口级产品验收 8/8 命中（人数映射、必消、空手两型、时长档、缺槽降级、硬禁忌一票否决、换一批真实变化）；浏览器实操按授权降级并已如实声明；附 2 条 P3 观察项供后续评估，不构成拒收理由。
