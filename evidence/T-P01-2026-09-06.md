# T-P01 必消输入→推荐结果 试点闭环证据（2026-09-06）

> 四角色工作流首个试点闭环：主控 PM 派发 → fm-dev 开发 → fm-reviewer 静态复审 → fm-verify 本地验收 → 主控收敛。
> 任务卡：[docs/workflow/tasks/T-P01-必消输入推荐回归.md](../docs/workflow/tasks/T-P01-必消输入推荐回归.md)
> 基线：HEAD `58f7535`（分支 feat/tp-06-menu-expansion）

## 1. 闭环记录（三方独立结论）

| 角色 | 结论 | 要点 |
|---|---|---|
| 开发 fm-dev | 交付 | `tests/tp01-mustuse-regression.cjs`（17 断言，真实 HTTP+PG 直查）+ `tests/.tp01-probe.cjs`；自测 17 PASS / 0 FAIL / EXIT=0；全量 pnpm test 295/296（唯一失败 AC12 性能抖动 54.35ms>50ms，单跑 51/51 过，与本卡无关）；零业务改动 |
| 技术审查 fm-reviewer（工具级只读） | **通过（静态）** | 逐 AC 4/4 ✓；对抗性审查确认失败分支有效（500→FAIL、字段缺失→FAIL、苦瓜错误返回非空→三层齐断、Plan/Event 零增量为真库 COUNT 非响应推断）；无 Mock/无条件跳过；断言数与回报一致（运行时 17 条）；6 条建议级问题，无阻断（见 §3） |
| 产品验收 fm-verify（本地独立） | **通过** | 独立启动 API（/health=200）后重跑回归 17 PASS / 0 FAIL / EXIT=0；独立抽样复核 8 PASS；停服后端口复测 ECONNREFUSED；孤儿 tsx watcher×4 已清理；未触碰生产 |
| 主控 git 核查 | AC4 ✓ | `git status --short -- apps packages prisma` 零输出 |

## 2. 运行时证据（fm-verify 实测，原文落盘 `.workflow-verify/`）

- `node tests/tp01-mustuse-regression.cjs` → EXIT_CODE=0，`== 结果: 17 PASS / 0 FAIL ==`（tp01-run1.txt）
- AC1-a 西红柿：status=200 candidates=3，含西红柿候选 3/3（DB 归一：西红柿→seed-ing-tomato，name=番茄）
- AC1-b 西蓝花：status=200 candidates=1，含西蓝花候选 1/1（seed-ing-broccoli）
- AC1-c 组合：200，candidates=0，unmetMustUse=[]（如实空手，见 §4 产品议题）
- AC2-a 空手：200，candidates=3，unmetMustUse 空
- AC2-b 苦瓜：200 + candidates=0 + unmetMustUse=["苦瓜"] + planId=undefined + Plan 54→54 / Event 93→93（真库零增量）
- 独立抽样（不经测试脚本）：S1 西红柿 planId=cmtpimh0d…、S2 空手 planId=cmtpimh17…、S3 苦瓜不建 Plan（tp01-sample-out.txt）

## 3. 复审建议级问题（6 条，均非阻断，记录在案）

1. AC1-c 空分支仅断言 200，未断言 unmetMustUse 空/缺省（L188-L194）
2. 测试对成功场景产生的 Plan/Event 无自清理（单次跑 +3 Plan/+3 Event）——已被验收实证
3. AC1 成功路径未断言 planId 存在（TP-02 e2e 已覆盖，边界说明）
4. 归一映射排序差异（服务层 findMany 无排序 vs 测试 ORDER BY id，当前种子无冲突）
5. .env 手写解析器不处理引号/行内注释（失败形态为显式 FAIL，风险低）
6. `process.exit` 紧跟 console.log 有管道截断风险（建议 process.exitCode）

**主控裁决**：本卡不返工（建议级非阻断，闭环首次跑通优先收敛）；问题 1、2、6 纳入下张测试卡强制项（teardown 必带）。

## 4. 产品议题待裁决（卡外发现#5，已移交产品负责人）

组合必消「西红柿+西蓝花」→ 推荐返回 `candidates=0 且 unmetMustUse=[]`（两食材各自可达，但无单菜单同时覆盖）。与契约 DEC-012 边界兜底一致，**非缺陷**；是否需要「必消无法同时满足」的产品提示/引擎保证组合可达，待产品负责人裁决。测试已如实暴露该形态（未掩盖）。

## 5. 主控处置：验收残留测试数据清理（已完成）

fm-verify 实证残留 5 Plan(PROPOSED) + 5 Event（回归 3 + 抽样 2）。主控一次性脚本 `.workflow-verify/cleanup-tp01-residual.cjs`（安全校验：目标须存在且 PROPOSED 且 mustUse 匹配，任一不符整体中止）：

```
BEFORE Plan=56 Event=95
SAFETY_OK: 5/5 目标均为 PROPOSED 且 mustUse 匹配
DELETED Events=5 Plans=5
AFTER Plan=51 Event=90
RESIDUAL_CHECK: 目标 planId 残留=0
```

## 6. 未验证项

- 浏览器页面渲染/截图/真机：**未验证**（挂账，待授权 Playwright）
- 生产环境：未触碰（本轮验收仅本地）
- 别名词覆盖：仅西红柿/西蓝花/苦瓜及组合；TP-08 其余别名词未逐一实测
- 「同时含两食材」正向组合场景：本地数据集不存在，未验证
