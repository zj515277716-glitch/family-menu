# T-C07-FIX 花生过敏缺口处置实现卡（方案 A）任务卡

> 派发：主控 PM｜日期：2026-09-13｜执行：fm-dev｜审查：fm-reviewer｜验收：fm-verify
> 上游：evidence/T-C07-dev-2026-09-13.md（评估定案）+ evidence/T-C07-review-2026-09-13.md（审查，移交清单 2.a~2.f）+ evidence/T-C07-verify-2026-09-13.md（验收 PASS 6/6）
> 主控裁决（按 MIGRATE-CHK-FIX 先例，用户总授权「按挂账队列依次执行」）：按评估推荐序 A>B>C 采纳方案 A 执行。

## 一、目标

消除花生过敏安全规则「拦截力为零」的口径缺口：seed 层补一条 HARD+INGREDIENT(花生米) 规则，使含花生米食材的菜在推荐链与换菜链被真实拦截；补 engine 真实形态回归用例防回退；端到端验证全链生效；putExclusions 全量替换语义风险书面评估入档。

## 二、方案 A 内容（review 移交清单 2.a~2.e 逐项落实；2.f RDS 另立卡不在本卡）

- **a. seed 补规则**：apps/api/prisma/seed-data.ts 恰 +1 行——`{ id: 'seed-excl-peanut-ing', scope: 'INGREDIENT', targetId: 'cmtvnuvxvc2oy5fdrzhpxxe69', severity: 'HARD' }`（字段名/形态以既有两行 seed-excl-peanut / seed-excl-organ 同构为准，先读 seed-data.ts L27-46 与 prisma schema ExclusionRule 定义确认；reason/active 等可选字段按既有行口径补齐）。**保留**原 seed-excl-peanut（TAG）行原样不动。**ExclusionRule 基线 2→3 预声明**（全表扫描类脚本重跑时行数口径随之变化）。
- **b. 重放纪律**：seed.ts 全 upsert 幂等已核实（评估卡 d 项）；本卡**禁引入 deleteMany/清库式迁移**；本地库应用方式 = `pnpm db:seed`（upsert 重放）。
- **c. 端到端拦截验证（验收项，运行时真实链）**：重放后以真实库规则行走 loadExclusionViews（planService 仅 INGREDIENT 行 join Ingredient）→ toExclusionView（mappers 注入 targetName/targetAliases）→ safetyFilter（packages/engine dist 生产代码）全链：宫保鸡丁（花生米 50g optional=false）与蚝油生菜（花生米 10g optional=true）**2/2 blocked**；PUBLISHED 池不误伤（拦截清单中不含 PUBLISHED 菜）；重跑评估卡 v5（0 食材行菜=0 道，checkUnconfirmedIngredients 影响面仍 0）与 v4（嫩豆腐 category=豆制品 现状记录）并**落盘结果 JSON**（补 review S-2）。
- **d. putExclusions 全量替换语义（review S-4）书面评估**：源码级分析 planService.ts putExclusions（事务内 deleteMany+createMany）与 seed 层规则的关系——若用户经 PUT 保存一次禁忌列表，seed 三规则是否被整体清除、前端/文档口径、复现方式（只读或可回滚方式验证，禁留下污染态）；给出处置建议（保留 seed 规则的合并策略 / API 写入路径 / 文档口径），**本卡不改 putExclusions 代码、不改 shared 契约**，处置建议转主控挂账裁决。
- **e. engine 真实形态用例**：packages/engine 测试新增「食材名≠品类」用例——构造 INGREDIENT 规则 + 菜的食材名/别名含目标串而 flavorTags/category 均不含目标串的真实形态，断言 HARD 拦截（含 optional=true 行同样拦截的断言）；既有测试用例零改动；表述性注释如涉及 review S-1 口径（「既有 TAG 用例为 flavorTags+category 两类构造场景」）可一并修正，仅限注释/描述字符串。

## 三、AC（逐条 [✓]/[✗] 自检，完成报告必须含）

- AC1 规则落地：seed-data.ts 恰 +1 行（保留 TAG 行原样）；`pnpm db:seed` 重放成功 EXIT=0；重放前后 13 表**逐行对账**（不只计数——Dish/Menu/MenuDish/Ingredient/DishIngredient 全字段比对）：预期差异仅 ExclusionRule 2→3，其余 12 表逐行零变化；任何计划外差异 → 立即按升级路径停止报主控。重放前先做全表快照落盘（.workflow-verify/t-c07-fix/）。
- AC2 端到端拦截：真实库规则行 → loadExclusionViews → toExclusionView → safetyFilter 全链运行时验证 2/2 拦截（宫保鸡丁+蚝油生菜，reason 含 HARD 食材禁忌口径）+ PUBLISHED 池零误伤 + v4/v5 重跑落盘结果 JSON。
- AC3 engine 用例：新增真实形态用例通过；pnpm test 全绿（391+新增，数字如实记录）+ pnpm test:taboo 89/89 EXIT=0。
- AC4 putExclusions 评估：源码级分析+风险场景验证（不留污染态）+ 处置建议三条以上候选入报告。
- AC5 收尾对账：13 表终态 = T-C07 基线 + ExclusionRule=3（Dish 49/Menu 13/MenuDish 42/Ingredient 81/Plan 54/Event 93/CookLog 6/DishIngredient 375/Family 1/FamilyRule 1/Substitution 0/_prisma_migrations 4）；git diff 范围恰 = seed-data.ts +1 行 + engine 测试新增（无其他源码改动）；新增依赖 0（如测试脚本需要 node 内置或既有 pg，不新增包）。

## 四、边界约束

- 只许改：apps/api/prisma/seed-data.ts（+1 行）、packages/engine 测试文件（新增用例；表述性注释修正仅限注释/描述字符串）。其余源码零改动；packages/shared 禁改（ExclusionRuleSchema 已支持 INGREDIENT，无需动）。
- 禁止：deleteMany/清库/DROP/reset 类操作；写库仅限 pnpm db:seed 重放与测试自身的自建自清；RDS 零触碰；不碰 Plan/Event/CookLog（seed 不含这三表，预期零接触）；不碰 9222 CDP 浏览器；不删 %TEMP%\fm-rds.env；不停本地 PG。
- 证据落盘：脚本与结果落 .workflow-verify/t-c07-fix/（gitignore 内）或 evidence/；报告落 evidence/T-C07-FIX-dev-2026-09-13.md（主控派发时直接写入该文件，R-1 纪律）。
- 环境坑位：PowerShell 5.1 下 node -e 内嵌引号解析失败——写临时 .mjs/.cjs 脚本执行；stdout 管道/重定向丢输出——脚本内变量捕获 + writeFileSync 落盘。DATABASE_URL 在根 .env（非 apps/api/.env，T-C07 verify 口径勘误）。

## 五、异常升级路径

1. seed 重放出现任何计划外表差异 → 立即停止、保留现场、报主控（不自行回滚数据）。
2. INGREDIENT 通道在任一消费链（推荐/换菜/checkUnconfirmedIngredients）缺失或行为与评估不符 → 停止报主控（不改引擎代码——引擎改动属方案 B 范畴，越界即停）。
3. 测试失败且与本卡改动相关 → 修复重跑；与本卡无关的既有失败 → 如实记录报主控裁决。
4. 发现卡外问题 → 写进完成报告，不顺手修改。

## 六、完成报告要求

evidence/T-C07-FIX-dev-2026-09-13.md：逐 AC [✓]/[✗] 自检 + 关键命令与退出码 + 数字结果 + 重放前后对账明细 + putExclusions 评估专节 + 卡外发现段。
