# T-P17 独立产品验收报告（fm-verify）

> 验收角色：fm-verify（独立于 dev/review，不采信其自检数字，全部独立复跑与真实接口调用取证）
> 验收执行：2026-09-17 至 2026-09-18；报告落盘路径按任务书指定
> 被测对象：分支 `feat/tp-17-p3-followups`，commit `59139dd`（`git rev-parse` 核实 HEAD=59139dd）；diff 基线 `d63027c`（T-P16 终态补登，非 e2e8b92，纠偏说明见第 1.2 节）
> 任务卡：[docs/workflow/tasks/T-P17-P3挂账修复完整套加权与h5类型归零.md](../docs/workflow/tasks/T-P17-P3挂账修复完整套加权与h5类型归零.md)（AC1~AC7）
> 环境：便携 PG 17.5 @ 127.0.0.1:54329/family_menu；API @ :3000（固定本地测试令牌鉴权）；真实 seed 库，全程未 Mock
> 脱敏声明：本报告仅含 seed 库内容与本地测试令牌，无真实用户数据；原始 JSON 证据存于 %TEMP%（tp17-* 系列文件），不进入仓库

---

## 1. 第一部分：数字复跑（5/5 一致）

### 1.1 复跑结果表

| # | 项目 | 命令 | 独立复跑结果 | dev 报告数字 | 一致 |
|---|---|---|---|---|---|
| 1 | 全量单测 | 根 `pnpm test` | exit 0；**22 files / 478 tests**（462 passed + 16 skipped），0 fail | 22 files / 478 | ✓ |
| 2 | 禁忌集（一票否决口径） | 根 `pnpm test:taboo` | exit 0；**7 files / 115 passed**，0 fail | 7 files / 115 | ✓ |
| 3 | api 类型检查 | `pnpm --dir apps/api exec tsc --noEmit` | exit 0，0 错 | 0 错 | ✓ |
| 4 | h5 类型检查 | `pnpm --dir apps/h5 exec tsc --noEmit` | exit 0，**0 错（修复前 6 错，归零）** | 0 错 | ✓ |
| 5 | 契约零改动 | `git diff d63027c..59139dd -- packages/shared/src` | **空输出** | 空 | ✓ |

数字自洽性核对：478 = 基线 471（T-P16 verify 登记）+ 新增 7 用例；115 = 基线 108 + 新增测试文件 7 用例；与 numstat 中 `packages/engine/test/shortage-rank.spec.ts` 新增 220 行/7 用例吻合。

### 1.2 diff 精确取证（含基线纠偏说明）

- **纠偏**：验收早期曾误用 e2e8b92（T-P16 dev 提交）作 diff 基线；经任务卡核实，基线应为 d63027c（T-P16 终态补登）。已用 `git rev-parse` 核实三哈希（d63027c / 59139dd / HEAD）后以 `d63027c..59139dd` 重算，结论不变。
- 源码级 numstat（d63027c..59139dd）：

| 文件 | 增/删 | 归属 |
|---|---|---|
| apps/api/src/services/planService.ts | 10 / 1 | 主推荐+换批传入缺槽计数、兜底 reasons 并入（AC1/AC4） |
| apps/h5/src/pages/dish/index.tsx | 1 / 1 | L282 类型收窄（AC5，仅此一处） |
| packages/engine/src/index.ts | 1 / 1 | 导出 SHORTAGE_PENALTY（AC1） |
| packages/engine/src/recommend.ts | 16 / 2 | 罚分排序（AC1） |
| packages/engine/src/types.ts | 7 / 0 | engine 内部类型 menuShortageCounts（AC1） |
| packages/engine/test/shortage-rank.spec.ts | 220 / 0（新增） | 7 用例（AC1） |

- 另含：packages/shared/dist 重建产物 31 文件（AC5 授权范围）+ dev 报告 1 文件入库；全量 **39 files / 1711+ / 5-**。
- **禁改区核对**：packages/shared/src 空 diff、prisma/schema.prisma 与现有迁移零变更、package.json 零变更（无新增依赖）。组合层 composition.ts 与可行性层 feasibility.ts 零 diff（R3 定性的关键前提，见 R3）。

---

## 2. 第二部分：接口级验收（7 项实测全命中，无 FAIL）

通用说明：全部经真实 HTTP（curl.exe，原因见观察项 O1）打到 :3000 真实 API + 真实 PG；recommend 走 POST /api/plans/recommend，换批走 POST /api/plans/:id/swap {mode:"ALL"}；响应原文均存 %TEMP%。每条记录如下。

### R1 完整套登顶（AC2）——命中

- Operation：POST /api/plans/recommend，body {people:4, timeBudgetMin:60, mustUseIngredientIds:[]}
- Expected（任务卡 AC2）：库中存在完整可行套时，cand#1 为完整 4 道套 [MAIN×2, SIDE, SOUP]（T-P16 V-1「完整套未进 top-3」消除）
- Actual：cand#1 = **virt-001，score=0.65，完整 4 道**（青椒炒鸡丁 MAIN 8min + 家常蒸蛋羹 MAIN 5min + 凉拌黄瓜 SIDE 5min + 冬瓜排骨汤 SOUP 15min），totalActiveMinutes=33 ≤ 60；cand#2 = virt-003（3 道，1 条缺槽文案）；cand#3 = virt-004（2 道，2 条缺槽文案）。展示 score（0.65/0.75/0.75）未变，排序按罚分后键（0.65/0.65/0.55）。
- Version：59139dd（feat/tp-17-p3-followups）
- Evidence：%TEMP%\tp17-r1-res.json（planId=cmu719xgo…）
- Coverage boundaries：仅核对 top-3；top-3 之外候选未展开。
- 判定：**命中**

### R2 必消牛腩单菜候选消除（AC3）——命中

- Operation：POST /api/plans/recommend，body {people:4, timeBudgetMin:60, mustUseIngredientIds:["seed-ing-beef"]}
- Expected（任务卡 AC3「完整可行解存在」分支）：完整含牛腩套进 top-3，全候选含牛腩，单菜候选消除
- Actual：**3 候选全部含牛腩**。cand#1 = virt-009 score=0.745，完整 4 道（番茄土豆炖牛腩 MAIN + 青椒炒鸡丁 MAIN + 凉拌黄瓜 SIDE + 冬瓜排骨汤 SOUP），totalActiveMinutes=50 ≤ 60；cand#2 = virt-005（2 道，缺配菜+汤槽，0.785 → 罚分 0.585）；cand#3 = virt-006（2 道，同 0.585）。0.745 展示分低于 0.785 却登顶——罚分生效的又一直接证据；**T-P16 V-2 单菜候选已消除**。
- Version：59139dd
- Evidence：%TEMP%\tp17-r2-res.json（planId=cmu71a97z…）
- Coverage boundaries：「确无完整可行解 → 如实保留单菜套」分支当前库无法构造（含牛腩完整套存在），由单测与 R3 空手不凑数取证侧证，见第 4 节。
- 判定：**命中**

### R3 空手行为与 T-P16 差异定性（非本卡回归）——行为正确

- Operation：POST recommend {people:4, timeBudgetMin:15, mustUseIngredientIds:["seed-ing-ribs"]} **连跑 3 次**；差分 R3B：同参数 people=2
- Expected（本卡口径）：行为确定性；空手时如实返回 0 候选 + 归因文案（不凑数红线 C-13/PD-018）
- Actual：3 连跑全部 0 候选空手，unsatisfiable=[{seed-ing-ribs: **TIME_BUDGET**}]，确定性成立；R3B（people=2/tb=15）→ 2 套通过（virt-010/virt-014，activeMinutes 求和=15）。
  **定性证据链（三重）**：
  1. 确定性三连跑排除偶发；
  2. d63027c..59139dd 中 composition.ts / feasibility.ts 零 diff，且 composeMenusByRole 两处调用点两版本逐字节一致（均未传 timeBudgetMin）——排除本卡改动引发；
  3. 纯函数实验（%TEMP%\tp17-pure-exp.cjs，当前 engine dist + 当前库 22 道 PUBLISHED 菜 + 当前 rules 直跑 composeMenusByRole→feasibilityFilter）：people=4 时 MAIN=⌈4/2⌉=2 槽不变量 → 库中含排骨菜的 MAIN 全为 15min → 含排骨最小可行 total=25min（virt-007=西兰花炒虾仁10+土豆烧排骨15）→ tb=15 必空手；people=2 时 virt-010/014 求和 15 通过。
  **结论**：T-P16 verify 报告「people=4/tb=15 得 2 套」的观察无法以当前代码+数据复现（同形态仅在 people=2 复现），属 T-P16 时点观察与当前语义的差异，**非 T-P17 回归**。
- Version：59139dd（实验同时覆盖当前 dist 与当前库）
- Evidence：%TEMP%\tp17-r3-run1/2/3.json、tp17-r3b-res.json、tp17-pure-exp.cjs 及其实验输出
- Coverage boundaries：仅排骨必消+极短时长组合；其他必消食材×短时长未逐一展开。
- 判定：**行为正确**（空手+TIME_BUDGET 如实）；历史差异定性说明，不计 FAIL

### R4 换批兜底缺槽侧车（AC4）——命中

- Operation：建 plan {people:2, timeBudgetMin:15, mustUseIngredientIds:["seed-ing-ribs"]}（初次 2 候选，触发 freshCandidates<3 兜底）→ POST /api/plans/:id/swap {mode:"ALL"} ×2
- Expected（任务卡 AC4）：兜底候选 reasons 并入 composition.slotShortages 缺槽文案，非仅「替换候选」
- Actual：swap#1 返回 3 候选全 score=0.5 兜底；CAND#3 virt-003 reasons = **[替换候选, 配菜槽留空：配菜池无更多可用菜]**；swap#2 CAND#3 virt-001 reasons = **[替换候选, 配菜槽留空：配菜池无可用菜（安全过滤后）, 汤槽留空：汤池无可用菜（安全过滤后）]**。「替换候选」与缺槽侧车并存，且缺槽文案两变体均实证（语义区分：中途耗尽 vs 池初始为空）。
- Version：59139dd
- Evidence：%TEMP%\tp17-r4-rec-resp.json / tp17-r4-swap1-resp.json / tp17-r4-swap2-resp.json（planId=cmu71x14v…）
- Coverage boundaries：people=2/tb=15 场景；主推荐路径的缺槽侧车已在 R1/R2 候选中见到同文案（两条路径均传参的结论由 diff+单测佐证）。
- 判定：**命中**

### R5 HARD 禁菜一票否决 + PUT 全量替换 + 还原（AC6 接口级取证，验收自加用例）——命中

- Operation：GET /api/family/exclusions 快照（3 条 seed）→ PUT 4 规则（3 seed + verify-excl-cucumber-hard：INGREDIENT 黄瓜 HARD）→ recommend {people:4, tb:60} → PUT 还原 3 seed → GET 复核
- Expected（AC6 安全零回归的接口级佐证）：HARD 禁忌食材全链路零出现；PUT 为全量替换语义；还原后与基线一致
- Actual：禁黄瓜后 3 候选共 9 道菜中「黄瓜」命中 = **0**（库中唯一含黄瓜 PUBLISHED 菜 = 凉拌黄瓜 SIDE 5min，未出现于任何候选——而 R1 未禁时它在 cand#1 中出现过，对照成立）；还原后 GET 返回 3 条 seed 规则与快照逐字段一致。
- Version：59139dd
- Evidence：%TEMP%\tp17-r5-excl-snapshot.json / tp17-r5-put-resp.json / tp17-r5-rec-resp.json / tp17-r5-restore-resp.json
- Coverage boundaries：仅 HARD-INGREDIENT 级；SOFT 降权与 TAG 级未在本轮接口级重复（T-P16 已实测 + 单测覆盖）。
- 判定：**命中**

### R6 量纲与罚分算术核对（AC1 佐证，验收自加用例）——命中

- Operation：独立复测一次 recommend（people=4/tb=60），对响应逐字段核算
- Expected：score 与 breakdown 6 维均在 [0,1]；罚分仅影响排序、不改 score/breakdown 展示值；rankValue = score − 0.1×缺槽数 可复算
- Actual：virt-001 score=0.65（无缺槽完整套）登顶；virt-003/virt-004 原始 score=0.75；算术复算：0.75−0.1×1=0.65（与 virt-001 同分，按 menuId 字典序 virt-001 先）、0.75−0.1×2=0.55；全部 breakdown 6 维 ∈[0,1]；reasons/breakdown 展示值与基线一致。
- Version：59139dd
- Evidence：%TEMP%\tp17-r6-rec-resp.json（planId=cmu722rwo…）
- Coverage boundaries：抽查 top-3 全部候选；SHORTAGE_PENALTY=0.1 常量由 engine 导出核对。
- 判定：**命中**

**第二部分结论：7 项实测（R1/R2/R3/R3B/R4/R5/R6）全部命中，无 FAIL 项。**

---

## 3. 第三部分：浏览器/界面实操——如实降级

- Operation：拟对 h5 推荐页做真实浏览器点击/输入/提交取证。
- Actual：本验收会话可用工具集中无桌面浏览器自动化驱动（无 Playwright/Puppeteer 类工具；mobile-mcp 需任务书指定的真实设备/模拟器，本次未提供目标设备）。按任务书「浏览器实操或如实降级说明」授权，本项如实降级，未做浏览器实操；未以任何原型/设计稿截图充当运行时证据。
- 替代证据：h5 页面数据源即本报告第二部分的 recommend/swap/exclusions 接口，接口级已按最终用户视角参数（人数/时长/必消/禁忌/换一批）实测命中。
- 残余风险：UI 呈现层（槽位留空展示、缺槽两变体文案渲染、换一批按钮交互）未在浏览器核验，建议后续具备浏览器自动化条件时补充一轮。

---

## 4. 覆盖边界汇总

**已测**：数字复跑 5 项；完整套登顶（R1）；必消牛腩 3 候选全含/单菜候选消除（R2）；空手 TIME_BUDGET 确定性 + people=2 差分（R3/R3B）；兜底缺槽侧车两变体（R4）；HARD 禁菜零出现 + PUT 全量替换 + 还原（R5）；量纲/罚分算术（R6）；shared/src 与 prisma/迁移零 diff；无新增依赖。

**未测/降级**：
1. 浏览器 UI 实操（降级，见第 3 节）；
2. AC3「确无完整可行解 → 如实保留单菜套」分支（当前库存在完整含牛腩套，无法构造反例；由单测与 R3 空手不凑数取证侧证）；
3. 单菜换（部分换）路径接口级未重复（单测覆盖，T-P16 已实测）；
4. SOFT 禁忌降权接口级未重复（T-P16 已实测 + 单测覆盖）；
5. top-3 之外候选排序未展开；
6. Plan/Event 表仅登记、不作达标断言（T-P16 裁决②）。

---

## 5. 观察项与问题清单

- **O1（环境怪癖，非产品缺陷）**：PowerShell 5.1 `Invoke-RestMethod` 本会话丢弃 Cookie 请求头——同一令牌 curl.exe 返回 200、PowerShell 返回 401（对照证据 tp17-nocookie.json：WITH_COOKIE=200 / NO_COOKIE=401）。本验收全部 HTTP 改用 curl.exe + UTF-8 报文文件。后续验证任务请留意此坑。
- **O2（提示项）**：缺槽文案存在两变体——「配菜槽留空：配菜池**无更多可用菜**」（中途耗尽）与「配菜槽留空：配菜池**无可用菜（安全过滤后）**」（池初始为空），语义区分正确且 R4 双双实证；h5 渲染需兼容两种文案（浏览器降级未验）。
- **O3（提示项）**：packages/shared/dist 31 个构建产物文件随本卡提交入库（AC5 授权范围）。后续 shared/src 任何变更必须同步重建 dist 并入库，否则 h5 类型将再次漂移（本次 6 错中 5 错即 dist 过期所致）。
- **O4（登记）**：本次验证新建 4 个 plan（cmu719xgo… / cmu71a97z… / cmu71x14v… / cmu722rwo…），Plan=79、Event=123、CookLog=6 为登记性终态；四内容表口径不受影响（裁决②）。
- **O5（提示项）**：R2 cand#2/cand#3 为 2 道短套（缺配菜+汤槽），排序已正确降权并如实标注——符合不凑数红线；短套体验是否需继续优化由 PM 评估，不构成缺陷。
- 无 P0/P1/P2 问题。

---

## 6. 环境清理确认

- **禁忌规则还原（双重复核）**：API GET /api/family/exclusions 返回 200 + 3 条 seed 规则（tp17-final-excl.json）；pg 直查 ExclusionRule=3（seed-excl-organ / seed-excl-peanut / seed-excl-peanut-ing），与快照一致。
- **DB 四内容表**：pg 直查 **Dish=49 / Menu=16 / MenuDish=52 / Ingredient=81**，与 T-P16 基线一致；CookLog=6 一致。
- **API**：node 进程（PID 23972）已停止，:3000 复核 **PORT_FREE**。
- **PostgreSQL**：`pg_ctl -m fast stop` 成功，**exit 0**，:54329 复核 **PORT_FREE**。
- **仓库纪律**：临时脚本与全部原始证据均位于 %TEMP%（tp17-* 共 34 项），未进入仓库；本验收唯一新增文件 = 本报告。

---

## 7. 总结论

**PASS** —— 数字复跑 5/5 一致；接口级产品验收 7/7 命中（完整套登顶、必消牛腩单菜候选消除、空手确定性、兜底缺槽侧车两变体、HARD 禁菜一票否决、量纲/罚分算术）；任务卡 AC1~AC7 逐条验收全命中（AC1→R1/R6+单测7用例；AC2→R1；AC3→R2；AC4→R4；AC5→h5 tsc 6→0+shared/src 零 diff；AC6→数字复跑+R5+diff 核对；AC7→dev 报告已落盘+本独立报告）。R3 定性为 T-P16 时点观察与当前语义的差异（三重证据链），非本卡回归。浏览器实操按授权降级并已如实声明。观察项 5 条（O1~O5）均不构成拒收理由。

建议主控按流程进行 T-P17 终态补登。
