# T-P10 dev 完成报告（空手文案区分时长原因 · STEP-2 实现）

- 日期：2026-09-11　执行角色：fm-dev　分支：`feat/tp-06-menu-expansion`（按卡直接工作，未新建分支、未提交 git）
- 任务卡：`docs/workflow/tasks/T-P10-空手文案区分时长原因.md`；实施蓝本：`docs/workflow/T-P10-技术方案-空手文案区分时长原因.md`（D-1~D-8 按推荐执行）；验收基准：`PRODUCT-CONFIRMATION.md` C-7（PD-017 修订版）
- **结论：7/7 AC 全 [✓]，自检无 [✗]。** 回归链全绿（shared/engine build exit 0、vitest 389/389、taboo 89/89、tp01 54 PASS/0 FAIL、h5 typecheck+build exit 0）；teardown 七表回基线 + REL=29 不动 + 双端口归零。**新增依赖 0，运行时零 LLM API，零 Mock（全部真实进程/真实 DB/真实 HTTP）。**

---

## 1. 做了 / 没做

### 做了
1. **AC1 引擎**（packages/engine）：feasibilityFilter 宽集收集（器具齐全且经安全过滤菜单的食材并集，不限时长）+ 对 `unsatisfiable` 二次归类（∈宽集→`TIME_BUDGET`，∉宽集→`NO_DISH`，缺器具菜单不进宽集）；`unsatisfiable: string[]` 输出零变化；`recommend()` 一行透传 `unsatisfiableMustUseReasons`；types 新增 `MustUseUnmetReason` 并从 index.ts 导出。
2. **AC2 契约**（packages/shared）：`RecommendResponseSchema` 增 optional 侧车字段 `unmetReasons: z.record(z.string(), z.enum(['TIME_BUDGET','NO_DISH'])).optional()`；契约账本版本记 v0.8（api.ts 头部）。
3. **AC3 API**（apps/api）：planService 空手响应组装 `unmetReasons`，并用 idToRaw 回译为用户原文（与 unmetMustUse 同键集由同一映射构造保证）；routes/recommend.ts 显式透传（规避 zod strip）；tp01 回归脚本追加 R1-R4。
4. **AC4 h5**（apps/h5）：`RecommendResult` 增 `unmetReasons?: Record<string, UnmetReason>` 与 `UnmetReason` 类型；屏⑥仅细分 `emptyReason.length > 0` 分支内部——缺省防御（同仓同发不可达，防 h5 先发/旧后端）→全按无菜型；全 `NO_DISH`→现状文案逐字不变；全 `TIME_BUDGET`→新文案（标题含当次档位分钟数 `tonightContext.timeBudgetMin` + 正文出路暗示）；混合→分句。`emptyReason.length===0` 分支（C-7a/DEC-012）与按钮组原样未动。
5. **UI 基准同步**：`docs/ai-rebuild/ui/e-final.html` 屏⑥新增时间型（土豆丝 30min）与混合型（土豆丝+苦瓜）两帧，复用既有 .empty-card/.btn 结构；`design-spec.md` L102 状态列与 L113 关键跳转同步。
6. **AC6 回归链全绿 + teardown**：见 §3 证据。
7. **AC7**：netstat/端口收尾（见 §3.4）+ 本报告落盘。

### 没做（按卡边界，均为有意不做）
- 未提交 git、未新建分支（按卡要求留待审）。
- 未改 prisma schema/migration、评分/多样化/换菜逻辑、migrate/抓取脚本、全换分支 planService L486-504。
- 未改既有文案措辞（无菜型文案逐字保留，tp01 既有 35 断言零变化佐证）。
- 未删任何失败测试、未引入任何模拟数据。
- 未触碰 packages/shared 契约语义（仅按批准方案加 optional 字段，属任务卡授权范围）。
- 工作树中其他任务/主控的在途改动（CURRENT.md、PRODUCT-CONFIRMATION.md、docs/ai-rebuild/PRODUCT-DECISIONS.md、docs/design/*、docs/ui-redesign/*、tools/* 等）本卡未触碰、保持原样。

---

## 2. 逐条 AC 自检

| # | AC | 结论 | 关键证据（命令+退出码+数字） |
|---|---|---|---|
| 1 | 引擎宽集+归类，`unsatisfiable` 零变化，feasibility.spec E1-E7 | [✓] | `pnpm --filter @family-menu/engine build` EXIT=0；`pnpm test` 389/389（含 E1-E7 7 断言全过，E6 混合两型、E7 集成透传）；tp01 既有断言零变化佐证行为零变化 |
| 2 | 契约 v0.8 optional `unmetReasons` + 账本 | [✓] | `pnpm --filter @family-menu/shared build` EXIT=0；`pnpm test` 含 schemas.spec S1-S4 4 断言全过；账本记于 api.ts 头部 v0.8 |
| 3 | planService 回译 + routes 显式透传 + tp01 R1-R4 | [✓] | `node tests/tp01-mustuse-regression.cjs` EXIT=0，**54 PASS / 0 FAIL**（详见 §3.2）；R1 `unmetReasons={"苦瓜":"NO_DISH"}`、R2 `unmetReasons={"土豆丝":"TIME_BUDGET"}`（原文回译）、R3/R4 无 unmetReasons 键 |
| 4 | h5 屏⑥三态细分，`emptyReason.length===0` 分支不动，分钟数取当次档位 | [✓] | `pnpm --filter @family-menu/h5 typecheck` EXIT=0；`pnpm --filter @family-menu/h5 build` EXIT=0（25 warnings 均 NutUI CSS calc/体积基线提示，0 error，与 T-P08/T-P09 基线一致）；分钟数绑定 `tonightContext.timeBudgetMin` |
| 5 | 确认书先行（C-7 / PD-017 修订） | [✓] | 主控已落 `PRODUCT-CONFIRMATION.md` C-7，开工时已读；非 dev 改动项 |
| 6 | 回归链全绿 + teardown 七表回基线（REL=29 不动）+ 双端口零遗留 | [✓] | 顺序：shared build → engine build → `pnpm test`（15 files/389 tests 全过）→ `pnpm test:taboo`（89/89）→ tp01（54 PASS）→ h5 typecheck（EXIT=0）→ h5 build（EXIT=0）→ teardown（Plan 62→54、Event 103→93；七表 48/13/42/79/54/93/6；MIGRATION rel=29/abs=0）→ `pg_ctl stop` EXIT=0 → `PORT_3000_LISTEN=0`、`PORT_54329_LISTEN=0` |
| 7 | netstat 收尾 + 完成报告落盘 | [✓] | §3.4 端口证据；本文件即落盘物 `evidence/T-P10-dev-2026-09-11.md` |

---

## 3. 证据明细（命令均可复制复跑）

### 3.1 回归链（依序执行）

| 步骤 | 命令 | 退出码 | 数字结果 |
|---|---|---|---|
| shared 构建 | `pnpm --filter @family-menu/shared build` | 0 | tsc 零错误，dist 产出（T-P09 假绿教训：先构建再回归） |
| engine 构建 | `pnpm --filter @family-menu/engine build` | 0 | tsc 零错误 |
| 全仓单测 | `pnpm test` | 0 | **15 files / 389 tests 全过** = 既有 378 + feasibility.spec E1-E7（7）+ schemas.spec S1-S4（4），数字与增量吻合 |
| 禁忌集 | `pnpm test:taboo` | 0 | **89/89**（taboo.spec 18 断言 100%，一票否决机制未受影响） |
| 必消回归 | `node tests/tp01-mustuse-regression.cjs` | 0 | **54 PASS / 0 FAIL** = 既有 35（T-P05 轮全量，零改动零回归）+ R1 7 + R2 7 + R3 2 + R4 3 |
| h5 类型检查 | `pnpm --filter @family-menu/h5 typecheck`（= tsc --noEmit） | 0 | 零错误 |
| h5 构建 | `pnpm --filter @family-menu/h5 build` | 0 | `webpack 5.91.0 compiled with 25 warnings in ~44.9s`，0 error；25 warnings 全为 NutUI CSS calc 解析与 asset size 提示，与 T-P08/T-P09 基线完全一致 |

### 3.2 tp01 关键断言证据（R1-R4 全链路：matcher→宽集归类→idToRaw 回译→routes 透传）

- **R1 无菜型（苦瓜，60min）**：200 + `candidates=[]` + `unmetMustUse=["苦瓜"]`（原文）+ **`unmetReasons={"苦瓜":"NO_DISH"}`** + 响应无 planId（不建 Plan）——7 断言。
- **R2 时间型（土豆丝，30min）**：200 + `candidates=[]` + `unmetMustUse=["土豆丝"]`（原文回译）+ **`unmetReasons={"土豆丝":"TIME_BUDGET"}`** + 无 planId——7 断言。
- **R3 时间型对照（土豆丝，60min）**：候选 **2** 套非空 + 响应**无 unmetReasons 键**——2 断言。
- **R4 正常推荐对照（西红柿，60min）**：候选 **3** 套非空 + 无 unmetReasons 键 + `unmetMustUse` 空——3 断言。
- 既有 35 断言（含 T-P05 全部 18 断言与更早 17 断言）**零变化**，证明空手判定行为零变化（仅新增侧车字段）。

### 3.3 teardown（tp01 回归产物精确清理）

定位依据：8 个新 Plan 全部创建于 `2026-09-11T06:52:46.xxxZ`（tp01 唯一一次实际运行窗口，首次运行因语法笔误未执行任何语句），第 9 新行为 `2026-09-06`，分隔清晰。

```
node .tmp_teardown_t10_del.cjs        # 临时脚本，跑完即删
EXIT=0
BEFORE {"plans":"62","events":"103"}
WHITELIST_FOUND 8 / 8                 # 8 个 planId 全部存在且 createdAt 均在 2026-09-11 窗口（防呆校验，越界即抛错）
EVENTS_UNDER_WHITELIST 10             # 防呆：≠10 停手不删
DELETED_Events 10
DELETED_Plans 8
AFTER_SEVEN_TABLES {"dish":"48","menu":"13","menudish":"42","ingredient":"79","plan":"54","event":"93","cooklog":"6"}
RESIDUAL_Events 0
```

迁移数据复核：

```
node .tmp_teardown_t10_rel.cjs        # 临时脚本，跑完即删
EXIT=0
MIGRATION {"rel":29,"abs":0,"fetched":29}   # REL=29 不动 / ABS=0，29 道存量迁移数据零触碰
```

注：REL/ABS 脚本首跑曾因误用 `status='FETCHED'` 报枚举错（FETCHED 属 `origin` 枚举），修正为 `origin='FETCHED'` 后通过——该脚本为临时产物非交付代码。

### 3.4 端口收尾（AC6 尾 + AC7）

- API dev（tsx watch）：teardown 时已不在运行——`Get-NetTCPConnection` 显示 :3000 无监听；现存 8 个 node 进程逐一核实命令行，均为 Trae MCP 服务（obsidian-mcp / server-github / mobile-mcp / notion-mcp-server 及其 npx 启动器），无项目进程，故无需 taskkill。
- PG 停止：`D:\codex\family-menu\.pg\bin\pg_ctl.exe stop -D d:/codex/family-menu/.pg/data -m fast` → EXIT=0，输出「等待服务器进程关闭 .... 完成 / 服务器进程已经关闭」。
- 终验：`Get-NetTCPConnection -State Listen | Where LocalPort -in 3000,54329` → **`PORT_3000_LISTEN=0`、`PORT_54329_LISTEN=0`**（netstat 在部分沙箱终端输出被截断/吞行，改用 PowerShell 原生 Get-NetTCPConnection 复核，见 §7-6）。

---

## 4. 文件清单（本卡 dev 改动，共 15 个）

**修改（13）**
1. `packages/engine/src/feasibility.ts` — 宽集收集 + unsatisfiableReasons 归类（FeasibilityFilterResult 增字段）
2. `packages/engine/src/types.ts` — `MustUseUnmetReason` 类型
3. `packages/engine/src/recommend.ts` — 一行透传 `unsatisfiableMustUseReasons`
4. `packages/engine/src/index.ts` — 导出 `MustUseUnmetReason`（+1 行）
5. `packages/shared/src/schemas/api.ts` — 契约 v0.8 `unmetReasons` optional + 账本
6. `packages/shared/test/schemas.spec.ts` — S1-S4
7. `apps/api/src/services/planService.ts` — unmetReasons 组装 + idToRaw 回译
8. `apps/api/src/routes/recommend.ts` — 显式透传（zod strip 规避）
9. `apps/h5/src/types/index.ts` — `unmetReasons?` 字段 + `UnmetReason` 类型 + PD-017 JSDoc
10. `apps/h5/src/pages/tonight/index.tsx` — classifyUnmet 三态判定 + 屏⑥标题/正文细分（`emptyReason.length===0` 分支与按钮组未动）
11. `docs/ai-rebuild/ui/e-final.html` — 屏⑥新增时间型/混合型两帧
12. `docs/ai-rebuild/ui/design-spec.md` — s6 状态列与关键跳转同步
13. `tests/tp01-mustuse-regression.cjs` — 追加 T-P10 R1-R4（19 断言）

**新增（2）**
14. `packages/engine/test/feasibility.spec.ts` — E1-E7
15. `evidence/T-P10-dev-2026-09-11.md` — 本报告

（`docs/workflow/T-P10-技术方案-*.md` 与 `docs/workflow/tasks/T-P10-*.md` 为主控创建，非 dev 产物。）

## 5. 新增依赖清单

**0 个。** 全部改动基于既有依赖（zod / pg / vitest / taro / webpack 均既有）。

## 6. 未验证项 / 遗留问题

1. **屏⑥三态未做浏览器人工点验**：h5 侧经 typecheck（tsc --noEmit EXIT=0）与 build（EXIT=0）静态验证；运行时视觉渲染属 fm-verify 阶段动作（可用 e-final.html 三帧对照）。
2. **API 级混合态无 tp01 回归用例**：任务卡 AC3 仅要求 R1-R4；混合归类已在引擎层覆盖（feasibility.spec E6「同一批必消各归各型」、E7 集成透传混合两型均过），h5 混合分支经 typecheck/build。如验收需要可补 R5（必消=土豆丝+苦瓜 @30min 预期 `{"土豆丝":"TIME_BUDGET","苦瓜":"NO_DISH"}`），属卡外扩展，未自行加入。
3. 无其他遗留。DB 已回基线、双端口归零、无残留临时文件（3 个 .tmp_teardown_t10*.cjs 均已删除）。

## 7. 披露项（过程透明，非缺陷）

1. **方案勘误**：方案 §3.2 称 `FeasibilityFilterResult` 位于 types.ts，实际在 feasibility.ts L5-10，就地修改；两文件均在任务卡允许范围内。
2. **引号口径**：方案 §3.5 文案表格用『』为文档书写惯例；代码与 e-final.html 屏⑥统一落「」，保持屏内一致（屏⑥既有文案本用「」）。
3. **tp01 引号笔误**：首跑 `node tests/tp01-mustuse-regression.cjs` 报 SyntaxError（L424/L468 断言消息单引号嵌套 `['苦瓜']`），外层改双引号后重跑 54 PASS——语法错误发生在解析期，首次运行未执行任何语句、未写库。
4. **pnpm test 首跑 3 用例失败为环境问题**：PG 未启动（`Can't reach database server at 127.0.0.1:54329`），按 T-C03 证据口径启动便携 PG 后重跑 389/389 全绿；非代码缺陷，未用 Mock 规避。
5. **teardown 清理规模修正**：实际清理 8 Plan + 10 Event（此前预估 3+3 不准确——tp01 全量重跑含 T-P05 段与全换用例）；按 id 白名单精确删除，白名单 8/8 命中、createdAt 全部落在 2026-09-11T06:52:46Z 窗口、防呆校验（Event≠10 即停手）齐备。
6. **netstat 输出异常**：部分沙箱终端下 netstat 输出被吞/截断（重定向后仅剩 1 行 UDP）；改用 PowerShell `Get-NetTCPConnection` 完成全部端口判定，结论可靠。
7. **API dev 进程状态**：h5 build 之后、teardown 之前 API dev 已退出（:3000 无监听、无项目 node/tsx 进程）；不影响任何测试结果（tp01 等均在其存活期完成）。
8. **REL 脚本枚举笔误**：临时脚本误用 `status='FETCHED'`（应为 `origin`），报错即修，见 §3.3 注。

---

**自检人**：fm-dev　**待**：fm-reviewer 技术审查 → fm-verify 产品验收。按 DEC-005，用户批准前不合并。
