# T-P14 dev 完成报告：时区口径统一——6 处 DateTime 列迁移 timestamptz（挂账④）

> 角色：fm-dev ｜ 日期：2026-09-12 ｜ 基线：git HEAD `7fe1fe2`（T-P13 提交），分支 feat/tp-06-menu-expansion
> 状态：实现完成，固定待审版本（未 commit，提交由主控执行）；环境已还原（PG -m fast stop、dev server 已停、临时诊断脚本已删、端口 3000/54329 双闭）
> 术语约定：本报告「方案 A」= 任务卡批准的迁移方案（timestamptz + USING 分支）；「环境处置方案 D / 备选 A」= 主控 2026-09-12 批复的 PG 会话时区处置，两者不混用。

## 0. 5→6 列勘误确认（任务卡 L20 已声明，此处确认入档）

报批消息写「5 列」系主控漏数，实际 **6 处** DateTime 列：漏计 [FamilyRule.updatedAt](../../apps/api/prisma/schema.prisma#L40)（`@updatedAt` 由 Prisma client 生成 `new Date()` → 驱动 UTC 序列化，与 Plan/Event 同机制同处置）。用户批准的「方案 A 迁 Timestamptz」是方向批准，列数修正属事实勘误非方案变更。**本卡按 6 列实施**，与任务卡 AC1 列清单逐一对应。

## 1. 改动摘要

| 文件 | 改动 | 说明 |
|---|---|---|
| [schema.prisma](../../apps/api/prisma/schema.prisma) | +6/−6（L29/L40/L178/L193/L200/L217 六处 `@db.Timestamptz`） | git diff --numstat 实测；shared/engine 零改动（AC1 红线） |
| [migration.sql](../../apps/api/prisma/migrations/20260912000000_timezone_timestamptz/migration.sql)（新增目录） | 45 行：6 条 `ALTER ... TYPE TIMESTAMPTZ(3)` 全部带 USING 分支 + 头部钟面归属注释 | `prisma migrate dev --create-only` 生成骨架后按 AC2 改写（Prisma 自动生成无 USING，会按会话时区解释旧钟面致 Plan/Event 全错 8h） |
| [.pg/data/postgresql.conf](../../.pg/data/postgresql.conf) | L740 `timezone = 'Asia/Shanghai'` → `timezone = 'UTC'`（环境处置方案 D，主控批复） | **纯环境级，不入库**（.gitignore L23 覆盖 .pg/）；L622 `log_timezone = 'Asia/Shanghai'` 未动 |
| [seed.ts](../../apps/api/prisma/seed.ts) | **0 改动** | AC6 评估：Family/FamilyRule upsert `update: {}` 空更新不触发 `@updatedAt`，无钟面断言冲突，无需最小修 |
| tests/**、apps/api/test/** | **0 改动** | AC5 逐处评估结论（见 §6），0 处裸钟面字符串比较时间列 |
| apps/api/.tmp-tp14-diag.ts | 临时诊断脚本（AC3 读回/迁移验证/teardown），**已删除** | 任务卡边界「apps/api/src/** 禁改」的例外工具，收尾清偿；git status 不再有该文件 |

git status 全量核对：`M CURRENT.md`（主控维护文件，dev 未触碰）+ 上述 schema.prisma / 新迁移目录；其余 untracked（docs/design/、tools/* 等）开工前已存在原样保留。**禁区零越界**：packages/shared、packages/engine、apps/h5/src、apps/api/src 无任何改动。

## 2. 环境处置方案 D 落实记录（主控批复 1）

1. **conf 修改**：[postgresql.conf](../../.pg/data/postgresql.conf) 增加注释块并改 `timezone = 'UTC'`（注明 T-P14 依据：adapter-pg 硬编码假设会话 UTC + 云 RDS 默认 UTC，与生产口径对齐；log_timezone 不动；纯环境级不入库）。
2. **过程披露（conf 误改与恢复）**：首次 SearchReplace 的 old_str 匹配到 L622 `log_timezone = 'Asia/Shanghai'` 行内子串（`log_timezone` 包含 `timezone`），把 L622 破坏为 `log_# T-P14...` 且目标行未改；随即反向恢复 L622 原值，改用带唯一上下文的锚点（`#intervalstyle = 'postgres'` 行 + 目标行）精确修改。grep 复核最终态：L622 `log_timezone = 'Asia/Shanghai'`、L740 `timezone = 'UTC'`，两行均正确。
3. **重启**：`pg_ctl -D .pg\data -m fast stop` → EXIT=0；启动 `pg_ctl -D "d:\codex\family-menu\.pg\data" -o "-p 54329" -l "d:\codex\family-menu\.pg\pg-tp14dev.log" start` → EXIT=0（启动命令仍按任务卡带 `-o "-p 54329"`）。
4. **会话时区验证**（node pg 驱动直查，便携 PG 无 psql.exe）：
   - `SHOW timezone` → `{"TimeZone":"UTC"}`（EXIT=0）
   - `SELECT now()` → 钟面偏移渲染 +00，与 UTC 会话一致
   - `log_timezone` 未动：仅会话时区钉 UTC，日志钟面口径不变

## 3. 迁移应用与 AC2 判定过程

### 3.1 应用

```
pnpm db:migrate          （prisma migrate deploy）
EXITCODE=0
```

DB 复核（diag 脚本 migrations 模式，node pg 直查）：

- `_prisma_migrations` 4 条全部 `finished=true`；本卡迁移 checksum = `801f2066b13e4918dd8fc975d906991bc36f07f7a64dea9ea9f4d176c925d79b`
- information_schema：6 列全部 `timestamp with time zone`

### 3.2 每列钟面归属判定（双重确认，写读码 + 基线库直查）

| 列 | 判定 | 依据 1（写入路径读码） | 依据 2（基线库钟面直查） |
|---|---|---|---|
| Plan.planDate / Plan.createdAt / Event.createdAt | UTC | Prisma client 显式传 JS Date（planService `new Date()`；seed 显式传值），pg 驱动把 JS Date 序列化为 UTC 钟面 | max=2026-09-06 09:04:09.745/.747/.754（UTC 渲染口径） |
| Family.createdAt / FamilyRule.updatedAt | UTC | seed-data L6 `new Date('2026-08-06T00:00:00Z')`，seed 显式传入两列 | 两列钟面均=2026-08-06 00:00:00；若为本地钟面应渲染 08:00 → UTC 判定成立 |
| CookLog.cookedAt | 本地 | INSERT 不带该列，DB `DEFAULT CURRENT_TIMESTAMP` 填充（服务器时区钟面） | 6 行 09-05 01:03 ~ 09-10 17:55，与「当日做饭记录」业务时刻吻合 → 本地判定成立 |

### 3.3 迁移 SQL（AC2 验收点）

6 条 ALTER 全部显式带 USING，防 PG 按会话时区（原 Asia/Shanghai）解释旧钟面：

- 5 条 UTC 钟面列：`USING "col" AT TIME ZONE 'UTC'`（Family.createdAt、FamilyRule.updatedAt、Plan.planDate、Plan.createdAt、Event.createdAt）
- 1 条本地钟面列：`USING "cookedAt" AT TIME ZONE 'Asia/Shanghai'`（CookLog.cookedAt）

## 4. AC3 双向抽查断言真实输出

### 4.1 SQL 侧（迁移后，+00 渲染对照卡数字）

| 断言 | 实测 | 卡面预期 | 判定 |
|---|---|---|---|
| Plan.max(createdAt) | `2026-09-06 09:04:09.747+00` | 同 | 精确命中 |
| Event.max(createdAt) | `2026-09-06 09:04:09.754+00` | 同 | 精确命中 |
| Plan.max(planDate) | `2026-09-06 09:04:09.745+00` | 同 | 精确命中 |
| CookLog.max(cookedAt) | `2026-09-10 09:55:58.267+00`（本地 17:55 − 8h） | 同 | 精确命中 |
| Family.createdAt / FamilyRule.updatedAt | `2026-08-06 00:00:00+00` | 同 | 精确命中 |

### 4.2 Prisma 侧读回 `Date.toISOString()`（主控批复：默认连接，不带任何 options 参数）

连接方式：`new Pool({ connectionString: 'postgresql://postgres@127.0.0.1:54329/family_menu' })` + `new PrismaClient({ adapter: new PrismaPg(pool) })`——无 `-c timezone` 注入，会话时区完全由环境处置方案 D（conf）钉住，7 项逐一与卡面预期精确对照：

| # | 断言 | 实测 toISOString() | 卡面预期 | 判定 |
|---|---|---|---|---|
| 1 | Plan.max(createdAt) | `2026-09-06T09:04:09.747Z` | 同 | 命中 |
| 2 | Plan.max(planDate)（聚合） | `2026-09-06T09:04:09.745Z` | 同 | 命中 |
| 3 | Plan.maxRow.planDate（最新行字段） | `2026-09-06T09:04:09.745Z` | 同 | 命中 |
| 4 | Event.max(createdAt) | `2026-09-06T09:04:09.754Z` | 同 | 命中 |
| 5 | CookLog.max(cookedAt) | `2026-09-10T09:55:58.267Z` | 同 | 命中 |
| 6 | Family.max(createdAt) | `2026-08-06T00:00:00.000Z` | 同 | 命中 |
| 7 | FamilyRule.max(updatedAt) | `2026-08-06T00:00:00.000Z` | 同 | 命中 |

7/7 命中 = 读回即写入真实绝对时刻，AC3 全过。这一组数字同时闭环 AC2：CookLog 本地钟面 17:55 经 `AT TIME ZONE 'Asia/Shanghai'` 迁移后在 UTC 会话下读回 09:55Z，双向语义一致。

## 5. AC4 全量回归（真实命令 + 退出码 + 数字）

| # | 命令 | 退出码 | 数字 |
|---|---|---|---|
| 1 | `pnpm test`（根，串行） | 0 | Test Files **15 passed (15)**；Tests **390 passed (390)**，0 skipped，0 FAIL；Duration 5.17s |
| 2 | `pnpm test:taboo` | 0 | Test Files 4 passed (4)；Tests **89 passed (89)**（禁忌集 100%，一票否决线安全） |
| 3 | `node tests/tp01-mustuse-regression.cjs`（:3000 dev server 已用新 client 重启） | 0 | **54 PASS / 0 FAIL** |
| 4 | `node tests/tp12-feedback-null.cjs` | 0 | **18 PASS / 0 FAIL**（端到端：submittedAt=`2026-09-12T03:34:59.631Z`，UTC 读回正确） |
| 5 | `pnpm typecheck`（apps/h5） | 0 | `tsc --noEmit` **0 错** |
| 6 | `pnpm exec tsc -p tsconfig.json --noEmit`（apps/api） | 0 | **0 错** |
| 7 | teardown（diag 脚本 teardown 模式） | 0 | 删 tp01 遗留 **8 Plan + 10 Event**（先打印待删 id、断言数量再删）；七表 `48/13/42/79/54/93/6` |
| 8 | `node tests/tp12-feedback-null.cjs baseline`（REL 口径终态复核） | 0 | `{"Dish":48,"Menu":13,"MenuDish":42,"Ingredient":79,"Plan":54,"Event":93,"CookLog":6,"REL":29}` |

**根测试 390 与 T-P13 基线 387+3skip 的算术自洽说明**（AC4「数字自洽」要求）：3 个 skip 来自 [projection-order.spec.ts](../../apps/api/test/projection-order.spec.ts#L105) 的 3 条 DB 集成用例，其 `if (!dbReady) return skip(...)`——T-P13 会话未启 PG 故 skip，本会话 PG 在跑且为 timestamptz 新库，3 条用例真实执行 → 387 + 3 = 390 passed / 0 skipped，无任何用例增删。

**tp01 teardown 说明**：tp01 无自带清理；[tp02-cleanup-test-plans.cjs](../../tests/tp02-cleanup-test-plans.cjs) 白名单口径不覆盖 tp01 新场景（小番茄/土豆丝/鸡蛋/鸡），故在临时诊断脚本内实现 teardown：识别口径 `Plan.createdAt > '2026-09-07'`（基线簇 max=2026-09-06T09:04:09.747Z，tp01 簇全部建于 2026-09-12 UTC，两簇相距 5 天零边界歧义），先打印待删 id + 数量断言（Plan=8/Event=10）再删除。删除后以 tp12 baseline 脚本复核 REL=29（上表 #8，REL=Dish.imageUrl LIKE '/images/%' 计数）。

## 6. AC5 窗口口径逐处评估清单（结论：0 改动）

全仓评估对象（grep tests/** 与 apps/api/test/**）：

| 位置 | 口径 | 评估结论 |
|---|---|---|
| [tp12-feedback-null.cjs:126](../../tests/tp12-feedback-null.cjs#L126) `CURRENT_DATE` | timestamptz 下按**会话时区**取整（UTC 会话 → UTC 日期边界） | 无需改：窗口断言围绕 tp12- 前缀自建自清的行，插入与查询同会话同口径；测试时刻远离 UTC/上海边界差 8h 的翻转点 |
| [tp12-feedback-null.cjs:152/188](../../tests/tp12-feedback-null.cjs#L152) `now()` | timestamptz 下 now() 为绝对时刻，算术安全 | 无需改 |
| [tp12-feedback-null.cjs:184](../../tests/tp12-feedback-null.cjs#L184) `now() - interval '10 minutes'` | 绝对时刻区间 | 无需改 |
| tp02/tp12 清理窗口 | 按 ID 前缀白名单（tp12- / tp07-），无时间窗口 | 无需改 |
| [projection-order.spec.ts](../../apps/api/test/projection-order.spec.ts) deleteMany | 按 tp07- 前缀 ID，无时间窗口；DB 用例显式 UTC 断言 | 无需改 |
| [contract.spec.ts:753/768](../../apps/api/test/contract.spec.ts#L753) `new Date('2026-09-05T19:00:00')`（无 Z 后缀） | vi.mock 的 mock 返回值，零 DB 触碰 | 备案不改：不触库则无钟面解释歧义；留待后续涉 DB 改造时顺带补 Z |
| tp01 / step08 / engine-perf / batch-fetch | 相对 interval 或按 ID | 无需改 |

**结论：全仓 0 处「裸钟面字符串比较时间列」的清理窗口，无一处需要随 timestamptz 迁移更新**（AC5 建议的 `AT TIME ZONE 'UTC'` 显式标注/interval 相对窗口两种改法均无落点）。teardown 七表回基线见 §5 #7/#8（含 REL=29）。

## 7. AC6 seed 重放

```
pnpm db:seed
EXITCODE=0
```

- 七表计数不变（重放前后均 48/13/42/79/54/93/6）
- Family.createdAt / FamilyRule.updatedAt 重放后钟面仍 `2026-08-06 00:00:00+00`：seed upsert 的 `update: {}` 为空更新，不触发 `@updatedAt`，显式时间仅在 create 分支 → **语义不变，seed 零改动**
- CookLog 依赖 DB `DEFAULT CURRENT_TIMESTAMP`：timestamptz 列上 CURRENT_TIMESTAMP 语义=绝对时刻，不破坏

## 8. AC7 迁移文件复核

```
pnpm exec prisma validate       （apps/api）
EXITCODE=0   ← The schema at prisma\schema.prisma is valid
pnpm exec prisma generate
EXITCODE=0   ← Generated Prisma Client (7.9.1) in 84ms
```

- 头部注释含每列钟面归属依据（双重确认：写入路径读码 + 基线库直查，与 §3.2 表一致）
- SQL 全文无本机路径/端口硬编码（L27 明示），公网 RDS 可直接 `prisma migrate deploy`（对接挂账①「迁移脚本参数化」项——**对接前提**见 §13 挂账：RDS 参数组 timezone=UTC）

## 9. CookLog 勘误（主控批复 2 采纳，写入报告）

主控读码实证：adapter-pg 的 normalize_timestamp（源码 L307-309）对**无时区钟面也当 UTC 解释**。由此修正任务卡 L18 的初判：

- **迁移前**：CookLog（本地钟面）读回**实际错 +8h**（并非「本地写+本地读恰好闭环正确」）；Plan/Event 因「UTC 钟面被当 UTC」**碰巧正确**
- **迁移后（timestamptz + 会话 UTC）**：钟面带偏移落库，normalize 无歧义可依 → **CookLog 读回 bug 被本卡真正修复**（§4 的 CookLog 7 项断言命中即实证：17:55 本地 → 09:55Z 读回）

## 10. 卡外发现（移交主控，未自行处置）

1. **migrate dev checksum 漂移**：`20260904000000_rescale_event_type` 在本机 `_prisma_migrations` 记录的 checksum 与迁移目录文件不一致（历史 migrate dev 本地行为所致）。主控已记挂账，不阻塞本卡；本卡维持 deploy-only，未做任何处置。
2. **生产对接前提（挂账①扩项）**：云 RDS 参数组 `timezone = 'UTC'` 需主控在部署对接时落实（环境处置方案 D 的批复依据，dev 侧无法处置）。

## 11. AC 逐条自检

| AC | 自检 | 证据 |
|---|---|---|
| 1 | **[✓]** schema.prisma 6 处 `@db.Timestamptz`（L29/L40/L178/L193/L200/L217）；validate 过 + generate 成功；shared/engine 零改动 | §1 numstat（+6/−6）+ §8 validate/generate EXIT=0 + git status 禁区零越界 |
| 2 | **[✓]** 手写迁移 6 列 ALTER 带 USING（5×'UTC' + CookLog×'Asia/Shanghai'）；Family/FamilyRule 钟面归属双重确认；本地基线库应用成功 | §3.1（deploy EXIT=0，4 迁移 finished，6 列 timestamptz，checksum 在案）+ §3.2 判定表 + §3.3 |
| 3 | **[✓]** 迁移后双向抽查断言与卡面数字精确对照；Prisma 默认连接（无 options）读回 7 项全命中 | §4.1（SQL 侧 +00 全中）+ §4.2（toISOString 7/7） |
| 4 | **[✓]** 根 test 390/390（数字自洽：387+3 DB 用例因 PG 在跑真实执行）+ taboo 89/89 + tp01 54 PASS + tp12 18 PASS + h5 typecheck 0 错 + api tsc 0 错 | §5 全表（8 项命令 EXIT=0）+ skip 差异算术说明 |
| 5 | **[✓]** 窗口口径逐处评估落清单（0 改动）+ teardown 七表回基线 REL=29 | §6 清单 + §5 #7/#8（48/13/42/79/54/93/6，REL=29） |
| 6 | **[✓]** seed 重放成功、语义不变、seed 零改动 | §7（EXIT=0，计数/钟面不变，update:{} 不触发 @updatedAt） |
| 7 | **[✓]** 迁移文件头部注释钟面归属依据 + 无本机路径/端口硬编码，RDS 可直接 deploy | §8（validate/generate EXIT=0）+ [migration.sql](../../apps/api/prisma/migrations/20260912000000_timezone_timestamptz/migration.sql) L6-L27 |
| 8 | **[✓]** 完成报告落盘 evidence/T-P14-dev-2026-09-12.md，含 5→6 列勘误、窗口清单、AC2 判定、AC3 真实输出、全部命令+退出码+数字 | 本文件（§0/§6/§3.2/§4/§5） |

## 12. 新引入依赖

**0 个**。schema/migration 为 Prisma 内置能力；环境处置为 conf 配置；诊断脚本复用项目既有 pg/tsx/@prisma/client 依赖，且已删除。禁引入项（Redis/消息队列/微服务/K8s）零触碰；运行时代码零 LLM API 调用。

## 13. 未验证项（移交主控/reviewer）

1. **公网 RDS 未验证**：本卡仅便携 PG 本地实证；RDS 迁移 deploy + 参数组 timezone=UTC 属部署对接（挂账①/§10-2）。
2. **checksum 漂移未处置**：§10-1，主控挂账。
3. **h5 真机回归未做**：本卡 h5 零改动，typecheck 0 错已实证；formatDate 日期提前一天问题的用户可见修复属部署后观察项。
4. **3 条 DB 集成用例历史上长期 skip**：T-P11/T-P12/T-P13 根回归均无 PG；本会话为三者后首次真实执行（390/390），建议 verify/review 知悉口径变化。
5. **conf 改动不入库的部署映射**：环境处置方案 D 是本机 .pg/ 环境级；等价物在云上=RDS 参数组（§10-2），在容器/自管部署=对应 conf 或启动参数，主控部署时按环境择一。

## 14. 环境还原记录（收尾已完成）

| 动作 | 证据 |
|---|---|
| dev server 停止（StopCommand job-365f86050b9c4fb0a3a2015322c41c61） | 工具执行成功 |
| `pg_ctl -D "d:\codex\family-menu\.pg\data" -m fast stop` | EXIT=0，`server stopped` |
| 删除 apps/api/.tmp-tp14-diag.ts | DeleteFile 成功，git status 不再出现 |
| 端口终态 `netstat -ano` 过滤 :3000/:54329 | EXIT=0，`PORTS 3000/54329: BOTH CLOSED` |

## 15. 过程披露（不采信/已恢复项）

1. **conf 误改与恢复**：§2-2，最终态 grep 复核正确，无遗留影响。
2. **便携 PG 无 psql.exe**：PowerShell CommandNotFoundException → 改用 node pg 驱动直查（SHOW 结果列名为 `TimeZone`，首取小写键得 undefined 后改打印整行 JSON 确认）。
3. **PowerShell 内联 node -e 转义失败**：seed 重放验证的内联 JS 被双引号转义破坏（SyntaxError）→ 改用 tsx 执行 diag 脚本。
4. **StopCommand 旧会话 ID 报错**：前次对 terminal 32 旧 dev server 的 command_id 操作报 "Command ID is required" → 改端口探测确认旧进程已死后直接后台启动新 server；本报告 §14 的 StopCommand 用完整 job id 执行成功。

## 16. 待审版本

工作区待审版本 = `M apps/api/prisma/schema.prisma`（+6/−6）+ 新增 `apps/api/prisma/migrations/20260912000000_timezone_timestamptz/`（migration.sql + migration_lock.toml）。未 commit，提交由主控按总授权执行；CURRENT.md 的未提交改动为主控维护内容，dev 未触碰。
