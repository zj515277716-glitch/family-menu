# BASELINE-2026-09-04.md — 只读基线检查证据

> 检查方式：纯只读（Read/Grep/Glob + 只读 git 命令），未修改任何文件、未安装依赖、未运行写入性命令。
> 环境：Windows，PowerShell 执行策略为 Restricted（与项目日志记录一致），因此部分命令经由工具内建能力完成。

## 1. 仓库状态（git 只读命令输出摘要）

- 分支：`main`；HEAD：`bc8d9ff`（feat: UI重设计-青瓷清新风格(方案F)-全页面样式更新）
- STATUS.md 记录的合并点 `5be260c` 之后另有 4 次提交：`27c56ad`、`0bb08fe`、`77e7e76`、`8d5e6ec`（UI 重设计、独立菜品做法页面、换菜 DB 修补）——**状态文档未更新**
- 未跟踪内容 11 项（原样保留，未清理）：`.pai/`、`console.error(e))`、`docs/design/preview.html`、`docs/design/preview.md`、`docs/ui-redesign/`、`h5-dist.tar.gz`、`tests/`、`tools/append-log.mjs`、`tools/content-pipeline/out/`、`tools/preview-server.mjs`、`{console.log('候选数`
- 根目录两个空残文件（0 字节，内容为空）：`console.error(e))`、`{console.log('候选数`——疑似命令残留

## 2. 断裂点代码证据

### 2.1 Mock 自动兜底
- `apps/h5/src/api/client.ts` L24-25：`USE_MOCK = !BASE_URL`——未配置 API 地址即自动进入 Mock。
- `apps/h5` 目录下无任何 `.env*` 文件（Glob 验证）——当前构建必然假数据。

### 2.2 必消食材中文名直传（失配）
- `apps/h5/src/pages/tonight/index.tsx` L48-58：`addMustUse` 把输入框原文（中文名）直接加入 `mustUse` 并随 `api.recommend` 发出。
- `apps/api/src/services/planService.ts` L197：`mustUseIngredients: context.mustUse`——服务层**无**中文名→ingredientId 映射，原样传给引擎。
- `packages/engine/src/types.ts` L32：`mustUseIngredients: string[]; // ingredientId，最多 3 个`——引擎契约按 ID 匹配。
- `packages/engine/src/feasibility.ts` L62-79：逐项比对 `ing.ingredientId`——中文名必不命中。

### 2.3 单菜换假动作
- `apps/api/src/services/planService.ts` L367-377：单菜换注释明确"记录事件，不修改候选"，返回未修改的计划。
- `apps/h5/src/pages/candidates/index.tsx` L101-118：提交换菜后用返回的未修改候选覆盖状态，并弹"已换菜"成功提示——**界面误导用户**。

### 2.4 反馈学习断链
- `apps/api/prisma/schema.prisma` `model Event`：字段仅 familyId/planId/type/payload/createdAt——**无 menuId**。
- `apps/api/src/services/mappers.ts` L167-173：`toEventView` 仅映射 id/type/createdAt，丢弃 payload 等全部内容。
- `packages/engine/src/score.ts`：`calcHistoryAcceptance` 依赖 `e.menuId === menu.id`、`e.cookedResult === 'success'`、`e.willRepeat`——因上述双重断裂，历史接受度（权重 0.35，最大评分维度）恒取中性值 0.7。

### 2.5 菜库规模
- `apps/api/prisma/seed-data.ts` L333-400：共 4 套菜单，3 套 `PUBLISHED`（工作日快手/周末丰盛/预算），1 套 DRAFT（清库存）——三选三，整套换无余量。

### 2.6 页面数量与文档漂移
- `apps/h5/src/pages/` 实际 6 个目录（setup/tonight/candidates/dish/plan/history）——README 写"5 页面"。
- `apps/admin` 仅 `main.tsx` 骨架（P1 可推迟）。

## 3. 从未真实验证项

本机无 Docker/PostgreSQL/ECS（STATUS.md 与开发日志自认）：DB 集成测试、e2e 冒烟（README F1-F7）、prod 构建、部署、回滚演练均无执行证据；230 项测试全部为 inject/mock 层；H5 独立 TypeScript 检查曾失败（开发日志 STEP-08 记录）。

## 4. 结论

产品当前状态：**原型骨架齐全、核心链路断裂、从未真实跑通**。与《产品确认书》A（已定稿）之间的差距即 TP-01 ~ TP-08 的工作范围。
