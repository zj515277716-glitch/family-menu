# R-2-S1 技术审查报告（主控代落盘）

> 审查人：fm-reviewer（审查会话 2026-09-13，本文件由主控代为落盘 2026-09-14——fm-reviewer 工具集无写入能力，先例：T-C07 / S-3 / LOGIN-DOM）
> 审查模式：**静态审查降级**（审查会话无 shell 工具，回归数字与 git 边界未独立复验 → T-1 条件移交主控/verify 闭合）。
> 审查对象：tools/content-pipeline/src/cli/publish-check.ts、src/cli/import.ts（isMain 守卫重构）+ test/cli.spec.ts（新增 12 用例）+ evidence/R-2-S1-task-2026-09-13.md + evidence/R-2-S1-dev-2026-09-13.md。

## 总体判定

**PASS WITH NOTES（条件通过）**

## 审查发现

- **R1**：任务卡 AC1~AC5 全部成立。
- **R2**：isMain 守卫重构四场景语义独立核验成立——tsx 直跑命中守卫正常执行 / node dist（bin shim）匹配执行 / vitest import 本模块不触发顶层副作用 / 守卫内 catch 回调与 process.exit(1) 逐字保留。
- **R3**：cli.spec.ts 质量通过——①9 处断言文案 grep 源码实文抽查全吻合（无自创文案）；②`vi.mock('../src/db.js')` 整体替换彻底，零 DB 触达（db.ts 头注释「CLI 入口用，测试用 mock」与设计自洽）；③commander 15 状态快照（saveStateBeforeParse/restoreStateBeforeParse + `_storeOptionsAsProperties=false`）源码级独立证实，同一 program 实例重复 parseAsync 安全，--json/--status 不跨用例泄漏。
- **R6**：CLI 语义零变更静态确认（守卫包裹前后行为面一致）。
- **R7**：dev 报告无失实。

## T-1（唯一条件）

回归数字与 git 边界未独立复验（静态审查降级所致）。**合并前须独立复跑**：

1. 根 `pnpm test` → 应 454/454（442+12）
2. `pnpm test:taboo` → 应 95/95
3. 单文件 `test/cli.spec.ts` vitest run → 应 12/12
4. content-pipeline `build` → 应 EXIT=0
5. git 核对：numstat 恰 2 文件各 13+/4- + 1 新增（cli.spec.ts）、禁区（packages/shared、packages/engine、packages/list-merger、apps/api、apps/h5）零 diff

结果与 dev 报告一致方可关闭本卡。

## 建议

- **S-1**：git diff --stat 的「17」系总变更行（13 插入+4 删除），准确口径为 numstat 13/4 每文件、合计 26+/8-。本卡全部落档统一 numstat 口径。
- **S-2**：isMain 守卫 catch 分支在 vitest 进程内技术不可达（argv[1]=vitest bin 不匹配守卫），无自动化覆盖；测试内逐字复现代码与源码守卫块无联动。建议在 CURRENT.md 备案为已知限制。

## dev 备案 5 项评估（全部评估完毕）

1. **pnpm `--` 透传**：豁免成立——该行为重构前即存在（`run publish-check -- --help` 中 pnpm 把 `--` 逐字传给脚本 → error: too many arguments EXIT=1），与本次改动无关；AC1 意图以等价无 `--` 形态（EXIT=0）+ 字面形态前后同错双路证实。
2. **isMain 守卫 vitest 不可达**：处置可接受——PC-a/IMP-c 改 action 内错误驱动 parseAsync rejection 断言 + 测试内逐字复现守卫 catch + tsx 实跑真口径三重锚定。
3. **exit mock 后 trailing rejection**：无 flaky 隐患（确定性状态触发，主断言先于 trailing 断言）。
4. **parse 阶段错误（缺参）由 commander 直接 process.exit(1) 不走守卫 catch**：事实确认。
5. **commander 15 状态快照**：独立证实（见 R3③）。

## 移交清单（4 项）

1. 测试独立复跑（根 pnpm test / taboo / 单文件 vitest / content-pipeline build）→ T-1
2. git 复核（status --porcelain + diff --numstat + 禁区零 diff）→ T-1
3. 守卫 catch 回调与重构前的 git 级逐字 diff → 主控已目检闭合（catch 文案与 process.exit(1) 逐字保留）
4. 守卫真实口径 tsx 实跑复测 2 条 + pnpm `--` 透传口径裁决 → 见下主控裁决

## 主控裁决注记（2026-09-14）

1. **T-1**：移交 fm-verify 按 T-1 清单全部独立复跑闭合（不信任 dev 数字，一律真实执行）。
2. **S-1**：采纳——本卡全部落档统一 numstat 口径（2 文件各 13+/4-，合计 26+/8-）。
3. **S-2**：采纳——守卫 catch 无自动化覆盖为已知限制，随 CURRENT.md 落段备案。
4. **pnpm `--` 透传**：裁决=豁免成立，口径入档（重构前即存在、非本卡引入；不改产品代码，留观；后续如立卡以 `pnpm --dir` 或脚本内 shift 处理）。
5. **移交 3**：主控 git diff 目检闭合。
