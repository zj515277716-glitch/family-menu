# DEPLOY-03 开发完成报告（fm-dev）

- 日期：2026-09-12
- 角色：fm-dev（开发执行者，只执行不批准交付）
- 任务卡：DEPLOY-03（三处同构裸 vitest script 补齐，挂账 C-3 核销）
- 工作目录：d:\codex\family-menu（Windows / PowerShell）
- 分支：feat/tp-06-menu-expansion（HEAD 71df054，DEPLOY-01/02 已提交，开工时工作树无已跟踪文件改动）
- 报告人：开发代理（fm-dev）
- **结论速览：AC1 [✓] / AC2 [✗]（6 跑中 LM_R1 复现 AC12 flaky，EXIT=1）/ AC3 [未执行——按卡面异常升级路径停止] / AC4 [✓]。已按异常升级路径停止后续验证，未重试刷绿，待主控裁决。**

---

## 1. 做了什么

1. **修复前基线**：三包裸 script 各实跑 1 次，全部复现空跑 `No test files found, exiting with code 1`（与 DEPLOY-01/02 定案的配置上溯继承根因一致，见 §3 基线表）。
2. **最小修复**：三处 package.json 的 test script 值改为 `vitest run --root ../.. --no-file-parallelism`，对齐 DEPLOY-01/02 先例，每文件仅 1 行。
3. **AC2 验证（部分受阻）**：三包各 2 次共 6 跑，单条串行命令一次性批执行；批内第 3 跑（LM_R1）复现 AC12 flaky（1 failed，EXIT=1），其余 5 跑全绿 391 passed (391)。
4. **按异常升级路径停止**：发现 LM_R1 失败后，未重跑任何测试、未执行 AC3 根跑，如实存档完整输出（evidence/DEPLOY-03-run-batch1-full-output.log，143,273 字节）后报告主控。

## 2. 没做什么（边界遵守）

- 未改 packages/shared、根 vitest.config.ts、根 package.json、任何 src/test 代码、任何其他配置文件。
- 未新增任何依赖（0 个）。
- 未执行 git commit（提交由主控处理；待审版本 = 当前工作树 3 文件改动 + 本报告 + 完整日志存档）。
- 发现 LM_R1 失败后**未重试刷绿**：AC3 根跑未执行，LM 未重跑。

## 3. AC 逐条自检

### AC1：三处 test script 值改为 `vitest run --root ../.. --no-file-parallelism`，其余零改动 —— [✓]

| 文件 | 改前 | 改后 |
|---|---|---|
| packages/engine/package.json（scripts.test，原 L16） | `"test": "vitest run"` | `"test": "vitest run --root ../.. --no-file-parallelism"` |
| packages/list-merger/package.json（scripts.test，原 L16） | `"test": "vitest run"` | `"test": "vitest run --root ../.. --no-file-parallelism"` |
| apps/api/package.json（scripts.test，原 L9） | `"test": "vitest run"` | `"test": "vitest run --root ../.. --no-file-parallelism"` |

- git diff --numstat 佐证（§5）：恰 3 文件、各 (1,1)，即每文件仅此 1 行差异。

### AC2：三包各实跑 2 次均 EXIT=0、15 passed、391 passed 0 skipped —— [✗]

命令（cwd=d:\codex\family-menu，单条命令串行批执行，先发命令后提取日志时才发现 LM_R1 失败；LM_R2/API_R1/API_R2 为同批后续跑，**非失败后重试**）：

```powershell
pnpm --filter @family-menu/engine test; Write-Output "ENGINE_R1_EXIT=$LASTEXITCODE"; pnpm --filter @family-menu/engine test; Write-Output "ENGINE_R2_EXIT=$LASTEXITCODE"; pnpm --filter @family-menu/list-merger test; Write-Output "LM_R1_EXIT=$LASTEXITCODE"; pnpm --filter @family-menu/list-merger test; Write-Output "LM_R2_EXIT=$LASTEXITCODE"; pnpm --filter @family-menu/api test; Write-Output "API_R1_EXIT=$LASTEXITCODE"; pnpm --filter @family-menu/api test; Write-Output "API_R2_EXIT=$LASTEXITCODE"
```

| 跑次 | 退出码 | Test Files | Tests | Duration | 备注 |
|---|---|---|---|---|---|
| ENGINE_R1 | EXIT=0 | 15 passed (15) | **391 passed (391)** | **5.28s** | 0 skipped |
| ENGINE_R2 | EXIT=0 | 15 passed (15) | **391 passed (391)** | **5.18s** | 0 skipped |
| LM_R1 | **EXIT=1** | **1 failed \| 14 passed (15)** | **1 failed \| 390 passed (391)** | **5.52s** | **AC12 flaky 复现**，唯一 FAIL 见下 |
| LM_R2 | EXIT=0 | 15 passed (15) | **391 passed (391)** | **5.36s** | 0 skipped |
| API_R1 | EXIT=0 | 15 passed (15) | **391 passed (391)** | **5.42s** | 0 skipped |
| API_R2 | EXIT=0 | 15 passed (15) | **391 passed (391)** | **5.22s** | 0 skipped |

**LM_R1 唯一 FAIL 详情**（原样摘录自完整日志）：

```text
 FAIL  packages/engine/test/score.spec.ts > 性能测试（AC12） > 千套菜单 recommend 调用性能 < 50ms（AC12）
AssertionError: expected 51.764099999999985 to be less than 50
 ❯ packages/engine/test/score.spec.ts:793:24
```

- 与任务卡异常升级路径列举的"AC12 再次 flaky"情形完全吻合：LM_R1 与 LM_R2 同命令同环境一败一成（批次内 flaky 实证）；失败文件在 packages/engine（全仓跑），与入口包（list-merger）无关。
- PG 常驻口径：全部 6 跑连真实 DB，无 skipped 字样（391 passed 或 1 failed | 390 passed，总数 391）。
- 完整输出存档：evidence/DEPLOY-03-run-batch1-full-output.log（143,273 字节，终端原样）。

### AC3：根 `pnpm test` 实跑 1 次不回归 —— [未执行，按卡面停止]

- 卡面异常升级路径明示："若任一实跑出现非预期……如实记录完整输出后**立即停止并报告**，不得自行扩大修改范围。"
- LM_R1 失败属该条款所列非预期情形，故未执行根回归跑，等待主控裁决后续口径。

### AC4：git diff --numstat 恰 3 文件且各 (1,1) —— [✓]

命令（cwd=d:\codex\family-menu）：`git diff --numstat; Write-Output "NUMSTAT_EXIT=$LASTEXITCODE"`

```text
warning: in the working copy of 'apps/api/package.json', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'packages/engine/package.json', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'packages/list-merger/package.json', LF will be replaced by CRLF the next time Git touches it
1       1       apps/api/package.json
1       1       packages/engine/package.json
1       1       packages/list-merger/package.json
NUMSTAT_EXIT=0
```

- 恰 3 文件、各 (1,1)。LF/CRLF warning 为 git 换行提示，非内容差异。工作树另存历史遗留 untracked 文件（.pai/、docs/design/preview.* 等，与 DEPLOY-01/02 报告所列一致），untracked 不进 numstat，未触碰。

### 基线（修复前，三包各 1 次）

命令（cwd=d:\codex\family-menu）：

```powershell
pnpm --filter @family-menu/engine test; Write-Output "ENGINE_BASE_EXIT=$LASTEXITCODE"; pnpm --filter @family-menu/list-merger test; Write-Output "LM_BASE_EXIT=$LASTEXITCODE"; pnpm --filter @family-menu/api test; Write-Output "API_BASE_EXIT=$LASTEXITCODE"
```

| 跑次 | 退出码 | 输出关键内容 |
|---|---|---|
| engine | ENGINE_BASE_EXIT=1 | `No test files found, exiting with code 1`；root=`D:/codex/family-menu/packages/engine`；include 打印为根配置三条 glob |
| list-merger | LM_BASE_EXIT=1 | 同上（root=packages/list-merger） |
| api | API_BASE_EXIT=1 | 同上（root=apps/api） |

- 与 DEPLOY-01/02 定案的"配置上溯继承 + include 相对子包 root 解析零匹配"根因完全同构，卡面诊断成立。

## 4. diff 清单（本次会话合计，git diff --numstat 原样见 §3 AC4）

```text
1       1       apps/api/package.json
1       1       packages/engine/package.json
1       1       packages/list-merger/package.json
```

- 新增依赖：**0 个**。
- 其余文件 0 改动（含 packages/shared、根 vitest.config.ts、根 package.json、全部 src/test 代码）。

## 5. 未验证项 / 遗留

1. **AC3 根回归未跑**：因 AC2 批内出现卡面所列非预期（AC12 flaky），按异常升级路径停止。修复改动本身（script 行）不影响根 `pnpm test`（根 script 独立为 `vitest run --no-file-parallelism`，不经过三处子包 script），回归风险低，但按卡未实测，待主控裁决后补跑或由审核侧复跑。
2. **AC2 未达"均 EXIT=0"**：6 跑中 5 绿 1 红（LM_R1 AC12 51.76ms > 50ms），AC2 判 [✗] 如实上报。
3. **AC12 概率性**：本批复现 1/6；与既有记录同族（DEPLOY-01 修复前 50.37ms、verify V1b 50.19/57.86ms、71df054 提交信息"AC12 阈值噪声敏感新挂账"）。串行化已消除文件并行 CPU 竞争诱因，但阈值噪声敏感未根治（需性能阈值调整、预热/采样统计等方案，超出本卡边界）。

## 6. 卡外发现（移交主控，未自行处理）

1. **AC12 flaky 第 N 次实证且串行下仍复现**：LM_R1 与 LM_R2 同命令同环境一败一成，说明既有挂账（AC12 阈值噪声敏感）在串行模式下依旧存在。建议主控对该挂账升级处置评估（如：阈值放宽至 P99 采样、多次采样取中位数、或将 AC12 改为独立非门禁跑），本卡未动 score.spec.ts。
2. **DEPLOY-02 报告卡外发现 2 的表述已被证伪**：其称"本次修复后仓库内已无裸 script"，实际当时 engine/list-merger/apps/api 三处仍为裸 script（即本卡范围，主控已以挂账 C-3 立项）。该历史报告表述不回改，仅在此勘误备案。
3. 工作树历史遗留 untracked 文件若干（§3 AC4 所列），与本卡无关，建议主控另行收口（与 DEPLOY-01/02 报告同项）。

---

fm-dev 签署：以上命令均真实执行，退出码与数字为终端原样输出摘录，无 Mock、无失败后重试刷绿；LM_R1 失败后即按卡停止验证并报告。
