# T-P08 开发交付报告：图片 URL 相对路径改造 + onError 降级兜底（R-10）

- 执行角色：fm-dev（开发代理）
- 日期：2026-09-10（UTC；teardown 收尾跨至 UTC 2026-09-10 深夜窗口内完成）
- 任务卡：docs/workflow/tasks/T-P08-图片URL相对路径改造.md
- 状态：全部 AC 完成，待 fm-reviewer / fm-verify 审核（本报告不自我批准）
- 验证资产：.workflow-verify/tp08/（脚本 + 日志 + 截图 + e2e-result.json）

---

## 1. 改动摘要

### 1.1 代码改动（git 工作区，`git diff --stat` 实测）

| 文件 | 改动 | 说明 |
|---|---|---|
| tools/content-pipeline/fetch2dish.mjs | M（22 行级） | imageUrl 改相对路径 `/images/dishes/<noteId>/<index><ext>` 入库；`--base-url` 参数显式报错废弃（L280-281） |
| tools/content-pipeline/test/fetch2dish.spec.ts | M（15 行级） | 断言同步为相对路径 + 废弃参数用例；本文件 33 tests |
| tools/content-pipeline/migrate-imageurl-relative.mjs | 新增 | 一次性迁移脚本：dry-run/apply 双模式、事务包裹、REPLACE 剥绝对前缀、前后对账、静态文件存在性核对 |
| apps/h5/src/pages/dish/index.tsx | M（43 行级） | R-10 方案 B：`API_BASE`（TARO_APP_API_BASE_URL）+ `toAbsoluteImageUrl()` 拼接；`imgError` state + `onError` → 切 `.dish-hero-empty` 同一降级分支 + 差异化注记；`useEffect([dish?.id])` 换菜重置 |
| apps/h5/src/pages/dish/index.css | 零改动 | git status 无此文件——降级复用既有 `.dish-hero-empty` 样式（自带底色占位） |

关键决策原文（fetch2dish.mjs）：
- L169：`// R-10：imageUrl 相对路径入库，不拼任何环境前缀（前端按 TARO_APP_API_BASE_URL 拼接）`
- L280-281：`--base-url 已废弃（R-10：imageUrl 改相对路径入库，绝对 URL 由前端拼接）；请移除该参数`（显式报错，防误用静默吞参）

### 1.2 非本卡产物声明

git status 中以下条目为先前任务遗留/主控资产，本卡未触碰：docs/workflow/tasks/T-C03-*.md（M）、.pai/、docs/design/*、docs/ui-redesign/、docs/workflow/tasks/T-C05-*.md、evidence/T-C05-dev-2026-09-10.md、tests/*、tools/append-log.mjs、tools/content-pipeline/apply-ingredients.mjs、tools/*.tar.gz、tools/preview-server.mjs、复盘报告.md。任务卡文件本身（T-P08-*.md）为主控下发。

## 2. AC 逐条证据（命令 + 退出码 + 数字）

### AC1 fetch2dish.mjs 相对路径 + baseUrl 处置 —— [✓]
- `imageUrl = '/images/dishes/' + noteId + '/' + firstImageIndex + firstImageExt`（L169-171），不拼任何环境前缀
- `--base-url` 显式报错废弃（L280-281）
- 单测：tools/content-pipeline/test/fetch2dish.spec.ts **33 tests**（rt-serial.txt L121 / pt-serial1.txt L114）

### AC2 迁移脚本 + 落地 + 幂等 —— [✓]
- 脚本：migrate-imageurl-relative.mjs（dry-run/apply 双模式；只动 `origin='FETCHED'` 且有图行；事务包裹；对账 + 静态文件核对）
- dry-run：会话内先行执行 EXIT=0（未单独落盘；与 apply 同构的 PRECHECK 见 migrate-apply1.txt L1-4）
- apply 第 1 次（migrate-apply1.txt）：
  - `PRECHECK 目标集(origin=FETCHED 且有图)=29（预期 29）`、`ABS=29 REL=0 OTHER=0`、`非 FETCHED 有图行=0`、`imageUrl 为 NULL 行=19（迁移不触碰）`
  - `UPDATE 完成：改写 29 行（事务已提交）` + 29 条 `POST ... file=OK`
  - `RECONCILE 改写行数=29（实际）`、`残留 ABS 前缀行=0`、`FETCHED 有图总数=29`、`静态文件 OK=29 MISSING=0`
  - `MIGRATION_APPLY_OK exit=0`
- apply 第 2 次（幂等重跑，migrate-apply2-idempotent.txt）：改写行数=0、残留 ABS=0、静态 OK=29 MISSING=0、**EXITCODE=0**
- 迁移后全局核查（baseline-after-migrate.txt）：七表计数不变；29 条 FETCHED_IMG 全部 `kind:"REL", file:"OK"`；`URL_KIND_ABS 0 / URL_KIND_REL 29`；`FILE_OK 29 / FILE_MISSING 0`；`NON_FETCHED_WITH_IMG_N 0`；`PLAN_CANDIDATES_WITH_ABS_URL 0`；`DISH_ABS_URL_N 0`；`EVENT_PAYLOAD_ABS_URL 0`

### AC3 迁移后真机亮图回归 —— [✓]
- 命令：`node .workflow-verify/tp08/e2e.cjs`（前台阻塞），日志 e2e-run.txt，**EXITCODE=0，21/21 checks PASS**
- 链路：tonight 输入杠杆食材「五花肉」（Input.insertText 真实键盘输入）→ 推荐（引擎硬过滤唯一入选夹具菜单）→ candidates → lock → plan → dish 详情，CDP 9222 真机浏览器（Edg/152.0.4191.66），viewport **375×500**（deviceScaleFactor 2, mobile）
- D 系列（亮图）：D1 `img.src === http://127.0.0.1:3000/images/dishes/6a8db1e5000000002003a91e/0.webp`（方案 B 前端拼接证据）；D2 store `lockedMenu.dishes[0].imageUrl === '/images/dishes/6a8db1e5000000002003a91e/0.webp'`（快照只存相对路径）；D3 `complete=true, naturalW=1080, naturalH=1440`（natural 尺寸>0）；D4 hero rect **375×500 全出血**（w>=370 达标）；D5 图上叠菜名+chips=5+fade=true
- O 系列（onError 真机触发）：evalJs 改内部 img.src 为 `http://127.0.0.1:3000/images/__broken_tp08__.webp` → 原生 error → Taro Image 转发 → React onError → O1 `cls="dish-hero dish-hero-empty", hasImg=false, emptyNote="图片加载失败 · 已自动降级占位"`；O2 菜名保留；O3 rect 375×220
- E 系列（契约无图降级 + 重置验证）：`.dish-side-nav.next` → 紫菜蛋花汤，E1 注记以「试做实拍位」开头（非「图片加载失败」，证明 imgError 随 dish.id 重置不跨菜泄漏）；E3 emptyName=紫菜蛋花汤
- F 系列（换回重新亮图）：`.dish-side-nav.prev` → F1 natural 1080×1440 亮图恢复；F2 src 恢复拼接绝对 URL；F4 dock「做完饭，记一笔」无回归
- 截图 6 张：shots/e2e-p0-candidates.png、e2e-p1-plan.png、e2e-p2-dish-imaged.png、e2e-p3-dish-onerror.png、e2e-p4-dish-contract-empty.png、e2e-p5-dish-reimaged.png

### AC4 dish 页 onError 降级兜底 —— [✓]
- index.tsx：`onError={() => setImgError(true)}`（L193）；`showHero = Boolean(heroImageUrl) && !imgError`（L181）；降级分支复用 `.dish-hero-empty`（L186）+ 注记差异化「图片加载失败 · 已自动降级占位」（L210）；`useEffect(() => setImgError(false), [dish?.id])`（L81-83）换菜重置
- 真机证据：e2e O1-O3 / E1-E3 / F1-F3（见 AC3）

### AC5 基址策略二选一 + 契约零改动 —— [✓]
- **选型：方案 B**（前端渲染层拼接），理由：库与 Plan.candidates 快照只存 `/images/...` 相对路径，不固化环境地址（dev=127.0.0.1:3000 / prod=公网域名切换零迁移）；D2 断言证明 API→store 全链路相对路径；D1 断言证明前端拼接终点正确
- 契约零改动：packages/shared 未出现在 git status（未触碰）；prisma/**、apps/api/src 均未改动

### AC6 测试全绿 —— [✓]
- 管线测试（pt-serial1.txt，`--no-file-parallelism` 串行）：`Test Files 13 passed (13)`、`Tests 353 passed (353)`、fetch2dish.spec.ts **33 tests**
- 根 `pnpm test --no-file-parallelism`（rt-serial.txt）：`Test Files 13 passed (13)`（L132）、`Tests 353 passed (353)`（L133）、score.spec.ts **51 tests 182ms**（L11，AC12 性能断言串行下稳定通过）
- h5 typecheck：`pnpm --filter @family-menu/h5 typecheck`（tsc --noEmit）→ 零错误输出，**EXITCODE=0**（h5-typecheck3.txt）
- h5 build：`pnpm --filter @family-menu/h5 build` → stderr L537 `Compiled successfully in 46.18s`，无 ERROR；25 条 warning 全部为 NutUI 库 CSS 既有 postcss-calc 解析警告（webpack 归类 WARNING，与本卡无关——dish/index.css 零改动）

### AC7 teardown —— [✓]
- DB（teardown-run.txt，`node .workflow-verify/tp08/teardown.cjs`，**EXITCODE=0，TEARDOWN_OK**）：
  - `BEFORE {plan:58, event:99, menu:14, menudish:45, dish:49, ingredient:79, cooklog:6}`
  - `PLAN_BEFORE_WINDOW 54 OK`（窗口切得准，基线 Plan 未动 → lockedMenuId=pipeline-menu-02 等基线数据未触碰）
  - `INCREMENT_TO_DELETE {event:6, plan:4}`（=本会话两轮 e2e 的全部 recommend/lock 产物）→ 已删
  - `FIXTURE_ROWS menudish=3/di=1/dish=1/menu=1`（tp08-fixture-* 全量）→ 已删；`RESIDUE_TP08 0 OK`
  - `AFTER {plan:54, event:93, menu:13, menudish:42, dish:48, ingredient:79, cooklog:6}` → 七项 RECONCILE 全 PASS
  - `MIGRATION_KEPT REL=29 ABS=0 OK`（迁移数据为真实数据，保留不回滚）
- 进程/端口：taskkill /T /F 终止 API（pid 38852 树）与 H5（pid 42648 树）共 13 进程 SUCCESS；`LISTEN_3000_10086_54329_ALL_ZERO`
- PG：`pg_ctl -D .pg/data stop -m fast` → 「服务器进程已经关闭」；54329 归零（含在上行端口核查内）
- CDP：`http://127.0.0.1:9222/json/version` → `CDP_ALIVE Browser=Edg/152.0.4191.66`（存活未杀）

### AC8 独占 PG —— [✓]
- 全程唯一 PG 实例 = 便携版 `.pg/data`（pid 41220，`-o "-p 54329"`），系统 5432 未触碰
- 独占性佐证：窗口增量恰好等于本会话产物（4 Plan + 6 Event = 两轮 e2e），`PLAN_BEFORE_WINDOW=54` 与任务卡基线逐表吻合，期间无外部写入者

## 3. 新增依赖

无。验证脚本仅复用 apps/api 已有 `pg` 依赖（绝对路径 require）与 Node 22 内置 fetch/WebSocket。

## 4. 未验证项 / 遗留 / 卡外发现（移交主控）

1. **卡外发现：/images/* 静态路由在 API 鉴权之后**——无 cookie 请求 401（e2e S0 实测 `withCookie 200 image/webp / noCookieStatus 401`）。浏览器同 host（127.0.0.1）自动携 cookie 故亮图无碍；若未来 H5 与 API 跨域部署，需主控决策静态图是否匿名放行。本卡未改 API（越界）。
2. **卡外发现：NutUI 库 CSS postcss-calc 解析警告**——h5 build 25 条 warning 均来自库 CSS 既有问题，非本卡引入（dish/index.css 零改动）；建议另立任务处理。
3. **AC12 性能测试并行模式偶发 flaky**——score.spec.ts 千套菜单 <50ms 在并行调度下负载敏感；串行（--no-file-parallelism）稳定通过（51 tests 182ms）。测试基建层问题，移交主控评估是否固化串行跑法。
4. **迁移 dry-run 未单独落盘**——会话内先行执行 EXIT=0，其 PRECHECK 逻辑与 apply 日志 L1-4 同构；apply 本身即含完整对账，风险可控。
5. 生产环境（公网域名）的 TARO_APP_API_BASE_URL 值属部署配置，本卡仅验证 dev 值（http://127.0.0.1:3000，apps/h5/.env）。

## 5. 证据文件索引（.workflow-verify/tp08/）

| 文件 | 内容 |
|---|---|
| rt-serial.txt | 根 vitest 串行 353/353（13 files）、fetch2dish 33 tests、score.spec 51 tests 182ms |
| pt-serial1.txt | 管线 vitest 串行 353/353 |
| h5-typecheck3.txt | tsc --noEmit 零输出，EXITCODE=0 |
| h5-build2.txt / h5-build2-err.txt | build Compiled successfully in 46.18s（25 warnings=库 CSS 既有） |
| migrate-apply1.txt | PRECHECK 29/ABS29/REL0 → UPDATE 29 行 → POST×29 file=OK → RECONCILE → exit=0 |
| migrate-apply2-idempotent.txt | 幂等重跑改写 0 行，EXITCODE=0 |
| baseline-after-migrate.txt | 迁移后全局核查：REL=29/ABS=0/FILE OK 29/七表不变/Plan+Event 零绝对 URL |
| e2e-run.txt / e2e-result.json | 真机回归 21/21 PASS，EXITCODE=0 |
| shots/e2e-p0~p5-*.png | 6 张真机截图（candidates/plan/亮图/onError 降级/契约降级/换回亮图） |
| teardown-run.txt | TEARDOWN_OK：七表回基线 + 迁移保留 REL=29 |
| baseline.cjs / fixture-create.cjs / cdp.cjs / e2e.cjs / teardown.cjs | 可复跑验证脚本 |

## 6. AC 自检结论

AC1 [✓] AC2 [✓] AC3 [✓] AC4 [✓] AC5 [✓] AC6 [✓] AC7 [✓] AC8 [✓] —— 8/8，全部有命令+退出码+数字证据。待审。
