# T-P12 独立产品验收报告（fm-verify）

> 日期：2026-09-12　角色：产品验收 VERIFY　任务卡：[docs/workflow/tasks/T-P12-反馈空态200响应null.md](../docs/workflow/tasks/T-P12-反馈空态200响应null.md)
> 被测版本：工作区未提交态，HEAD=`033af65`，分支 `feat/tp-06-menu-expansion`（与 review 报告待审版本一致）
> 验收方式：只读代码 + 真实环境复跑（真实 HTTP + PG 直查，零 Mock 冒充）+ 本报告落盘。验收全程未修改任何业务代码/契约/测试/配置，未执行 git add/commit/push（终态 `git status` 与开工时逐项一致：5 M 文件 +34/−11，untracked 清单无新增）。

## 总判定：**PASS**

7/7 AC 全部 [✓]；reviewer 移交复跑清单 5 项全部复核通过；S-1/S-2 补证闭环；无阻塞、无与任务卡/reviewer 结论冲突的事实。3 条口径级偏差（非代码缺陷）如实记录于「六、偏差与异常记录」。

---

## 一、改动范围行级核对（对应 reviewer 移交清单第 1 项）

命令：`git status` / `git --no-pager diff --numstat` / 逐文件 `git --no-pager diff -- <file>`，EXIT=0。

| 变更 | 文件 | numstat | 行级核对结论 |
|---|---|---|---|
| M | [packages/shared/src/schemas/api.ts](../packages/shared/src/schemas/api.ts) | +6 −2 | 头部账本追加 v0.9 段 3 行注释（紧接 v0.8，单一入口未分裂）+ `FeedbackResponseSchema` JSDoc 2 行改写；**schema 定义本体零变更**（diff 中无 schema 字段行） |
| M | [apps/api/src/services/planService.ts](../apps/api/src/services/planService.ts) | +4 −4 | `getFeedback` 签名改 `Promise<FeedbackResponse \| null>`；无反馈分支 `return null`（删 `NotFoundError('has no feedback')` throw）；plan 不存在 `throw new NotFoundError` 保留；JSDoc 同步 v0.9 |
| M | [apps/api/src/routes/plans.ts](../apps/api/src/routes/plans.ts) | +6 −1 | `feedback === null` → `reply.code(200).send(null)`；有反馈仍走 `FeedbackResponseSchema.parse(feedback)`；PlanIdParamsSchema 400 先行链未触碰 |
| M | [apps/api/test/contract.spec.ts](../apps/api/test/contract.spec.ts) | +16 −3 | 原「无反馈 404」用例改写为「200 + null body」三层断言（`statusCode===200` / `body==='null'` / `JSON.parse(body)===null`）；新增「plan 不存在 404」用例；describe 标题标注 v0.9/T-P12 |
| M | [apps/h5/src/api/client.ts](../apps/h5/src/api/client.ts) | +2 −1 | **仅注释**两行（v0.9 语义 + notFoundAsNull 兜底说明）；`getFeedback` 返回类型与第三参 `notFoundAsNull=true` 逻辑零变更 |
| 新增 | [tests/tp12-feedback-null.cjs](../tests/tp12-feedback-null.cjs) | — | 三态真实环境回归脚本（纯测试，baseline/完整双模式，自建自清 + 七表绝对基线断言） |
| 新增 | 本任务卡 / [T-P12-dev](./T-P12-dev-2026-09-12.md) / [T-P12-review](./T-P12-review-2026-09-12.md) | — | 文档类，符合任务卡允许清单 |

- 改动集**恰为**任务卡「允许改动」范围，超出范围改动 = **无**。
- 禁改区核对（`git status` 证实）：packages/engine/**、apps/h5 其余文件、prisma（schema/seed/migrations）、packages/shared 其余文件（含 test/schemas.spec.ts 零改动，见 AC1）、PRODUCT-CONFIRMATION.md、e-final.html/design-spec.md —— **零触碰**。
- 其余 untracked（`.pai/`、`docs/design/preview.*`、`docs/ui-redesign/`、`tests/engine-perf.mjs`、`tests/step08-deploy-validate.cjs`、`tools/*.tar.gz`、`tools/append-log.mjs`、`preview-server.mjs`、`复盘报告.md`）为开工前既有非本卡遗留（dev 报告 §5 披露 5 同清单），本卡未触碰，原样移交主控。

## 二、AC1~AC7 逐条自检（对照任务卡原文）

| AC | 判定 | 验收证据（本角色独立复跑） |
|---|---|---|
| AC1 契约：账本 v0.9 行 + JSDoc 与行为一致；shared build EXIT=0；schemas.spec 影响说明 | **[✓]** | 行级 diff 核对账本 v0.9 行（3 行，措辞与挂账⑤授权逐字对应）+ JSDoc（v0.9 空态语义）准确；`pnpm --filter @family-menu/shared build` EXIT=0（tsc 无错）；schemas.spec 本卡**零改动**（git status 证实无 M），不影响性成立：v0.9 仅改路由层空态语义与注释，schema 解析行为零变化，该路由用例由 contract.spec 覆盖 |
| AC2 API 三态：无反馈 200+null / plan 不存在 404 / 有反馈形状不变（真实环境非 Mock） | **[✓]** | [tests/tp12-feedback-null.cjs](../tests/tp12-feedback-null.cjs) 完整模式 **18 PASS / 0 FAIL，EXIT=0**（真实 HTTP access_token cookie + PG 直查）：态1 无反馈 200 且响应体严格 `null` 字面量；态2-a 有反馈键集恰为 `["actualMinutes","didCook","submittedAt","taste","willRepeat"]` 五键（形状零变化）+ 态2-b append-only 取最新 + 旧 payload optional 如实缺省；态3 不存在 id → 404 |
| AC3 contract.spec：404 用例改写 200+null + 新增 plan 不存在 404；apps/api 全绿 | **[✓]** | 行级 diff 核对改写与新增用例成立（三层断言杜绝「200+空对象」弱断言）；`pnpm exec vitest run apps/api/test/contract.spec.ts` **53/53，EXIT=0**（日志中 GET feedback 三用例 200 + `tp12-not-exist` 404 按序执行） |
| AC4 H5 零行为变更：仅 client.ts 注释；typecheck 0 错 | **[✓]** | client.ts diff 逐行核对为纯注释（+2/−1），`notFoundAsNull=true` 兜底与返回类型零变更；`pnpm --filter @family-menu/h5 typecheck` **EXIT=0（0 错）** |
| AC5 回归：根 vitest 全绿（389 基线允许净增）+ taboo 89/89 + tp01 54 PASS + tp12 全 PASS | **[✓]** | 根 vitest 串行 `pnpm exec vitest run --no-file-parallelism` **390/390，15 files passed，EXIT=0**（=T-P11 基线 389 + contract 净增 1，算术自洽；AC12 性能用例串行下无波动）；`pnpm test:taboo` **89/89（100%），EXIT=0**；`node tests/tp01-mustuse-regression.cjs` **54 PASS / 0 FAIL，EXIT=0**；tp12 **18 PASS / 0 FAIL，EXIT=0** |
| AC6 teardown：tp12 自清七表回基线 + REL=29 不动；PG 用完 stop（54329=0） | **[✓]** | tp12 脚本内断言：3 Event + 3 Plan 白名单精确删除，residual=0，七表 before===after 全对齐，REL=29/29；终态 `node tests/tp12-feedback-null.cjs baseline` EXIT=0 = `{Dish:48, Menu:13, MenuDish:42, Ingredient:79, Plan:54, Event:93, CookLog:6, REL:29}` 与绝对基线一致；`pg_ctl stop -m fast` EXIT=0；`Get-NetTCPConnection -LocalPort 54329 -State Listen` 监听数 = **0**（另 :3000 亦 = 0） |
| AC7 报告落盘：evidence/T-P12-dev-2026-09-12.md | **[✓]** | dev 报告存在且含逐 AC 自检 + 测试命令/退出码/数字结果；本 verify 报告同步落盘 |

## 三、复跑清单数字汇总（reviewer 第五节移交清单逐项）

| # | 复跑项 | 命令（根目录 d:\codex\family-menu） | 退出码 | 关键数字 |
|---|---|---|---|---|
| 1 | 改动范围 | `git status` / `git --no-pager diff --numstat` / 逐文件 diff | 0 | 5 M（+34/−11）+ 本卡 3 新增；超范围 = 0 |
| 2 | shared build | `pnpm --filter @family-menu/shared build` | 0 | tsc 无错 |
| 3 | contract.spec | `pnpm exec vitest run apps/api/test/contract.spec.ts` | 0 | **53/53** |
| 4 | tp12 专项 | `node tests/tp12-feedback-null.cjs` | 0 | **18 PASS / 0 FAIL**（完整输出见第五节，S-2 补齐） |
| 5 | tp01 回归 | `node tests/tp01-mustuse-regression.cjs` | 0 | **54 PASS / 0 FAIL** |
| 6 | 根 vitest 串行 | `pnpm exec vitest run --no-file-parallelism` | 0 | **390/390，15 files passed**（AC12 串行无波动，见六-3） |
| 7 | 禁忌集 | `pnpm test:taboo` | 0 | **89/89（100%）** |
| 8 | h5 typecheck | `pnpm --filter @family-menu/h5 typecheck` | 0 | **0 错** |
| 9 | 开工基线 | `node tests/tp12-feedback-null.cjs baseline` | 0 | 七表+REL = `{48,13,42,79,54,93,6}`，REL=29 |
| 10 | tp01 残留清偿（dry-run → 实删） | `node .workflow-verify/tp12/cleanup-tp01.cjs` → `… go` | 0 / 0 | W1 防呆全过；DELETED Events=10 Plans=8；AFTER 七表+REL 回基线 |
| 11 | 终态基线 | `node tests/tp12-feedback-null.cjs baseline` | 0 | 七表+REL = 绝对基线，REL=29 |
| 12 | PG 停机 + 端口 | `.\.pg\bin\pg_ctl.exe stop -D d:/codex/family-menu/.pg/data -m fast`；`Get-NetTCPConnection -LocalPort 54329 -State Listen` | 0 | 「服务器进程已经关闭」；**54329 监听=0**（:3000 亦=0） |

## 四、真实环境生命周期（verify 自建自清）

1. 开工前置探测：:3000 监听=0（遗留 terminal 的 dev server 实际已死）、54329 监听=0（fm-dev 收尾已停，符合前置）。
2. 启动 PG 17.5（`.pg/bin/pg_ctl.exe start -D d:/codex/family-menu/.pg/data -o "-p 54329"`）→「database system is ready to accept connections」。
3. 启动 API dev server（`pnpm --filter @family-menu/api dev`，tsx watch :3000）→ 探活响应正常（无有效 cookie 时 401，鉴权链生效）。
4. 真实 HTTP 测试（tp12 / tp01）在 dev server 在场时执行；随后按 dev 先例停 dev server（:3000 监听=0 复测）再跑根 vitest 串行与 taboo，规避进程干扰。
5. 结束：终态基线核对 → PG 停机（EXIT=0）→ 54329 / :3000 监听双清零。

## 五、tp12 专项脚本完整输出留存（S-2 补齐，2026-09-12 09:49 实跑）

```text
BASE=http://127.0.0.1:3000 DATABASE_URL=postgresql://postgres@127.0.0.1:54329/family_menu
[PASS] 前置：seed family 存在（自建 Plan 外键依赖）: familyId=seed-family rows=1
[PASS] 前置：七表基线快照记录: Dish=48 Menu=13 MenuDish=42 Ingredient=79 Plan=54 Event=93 CookLog=6 REL=29
[PASS] 前置：自建 3 个无反馈 Plan（tp12- 前缀白名单）: inserted=3/3
[PASS] 态1 无反馈：HTTP 200（真实请求，原为 404）: status=200（契约 v0.9 要求 200）
[PASS] 态1 无反馈：响应体严格为 null 字面量: body="null"（要求 'null'，非 '{}'/'""'/错误包裹）
[PASS] 态2-a 有反馈：HTTP 200（形状回归前置）: status=200
[PASS] 态2-a 有反馈：didCook/taste/willRepeat/actualMinutes 与事件 payload 一致: didCook=true taste=good willRepeat=true actualMinutes=30
[PASS] 态2-a 有反馈：submittedAt 为可解析 ISO 时间（= 事件创建时间）: submittedAt=2026-09-12T09:49:49.239Z
[PASS] 态2-a 有反馈：报文键集恰为 5 键（形状零变化，无新增/缺失键）: keys=["actualMinutes","didCook","submittedAt","taste","willRepeat"]
[PASS] 态2-b 取最新：HTTP 200 且返回后插入的 NOT_COOKED 事件（createdAt desc 语义）: status=200 didCook=false
[PASS] 态2-b 旧 payload 宽松：taste/willRepeat 如实缺省（undefined，不编造）: taste=null willRepeat=null
[PASS] 态2-b 旧 payload 宽松：actualMinutes=20（旧事件 payload 如实保留）: actualMinutes=20
[PASS] 态3 plan 不存在：HTTP 404（v0.9 保持不变）: status=404 body="{\"error\":\"Plan tp12-nonexistent-mtxq9dcx-uzymkf not found\"}"
[PASS] teardown：自建 Event 按白名单精确删除: deleted=3/3 [tp12-ev-full-mtxq9dcx-uzymkf, tp12-ev-old-mtxq9dcx-uzymkf, tp12-ev-new-mtxq9dcx-uzymkf]
[PASS] teardown：自建 Plan 按白名单精确删除: deleted=3/3 [tp12-plan-nofb-mtxq9dcx-uzymkf, tp12-plan-full-mtxq9dcx-uzymkf, tp12-plan-legacy-mtxq9dcx-uzymkf]
[PASS] teardown：残留行 = 0: residual=0
[PASS] teardown：七表计数回基线: Dish=48/48 Menu=13/13 MenuDish=42/42 Ingredient=79/79 Plan=54/54 Event=93/93 CookLog=6/6
[PASS] teardown：REL（imageUrl LIKE /images/%）不动: REL=29/29

== 结果: 18 PASS / 0 FAIL ==
TP12_EXIT=0
```

（DATABASE_URL 已由脚本自身脱敏输出；tp01 54 PASS 明细因篇幅略，全部 PASS 无 FAIL，关键字样：`== 结果: 54 PASS / 0 FAIL ==`、`TP01_EXIT=0`。）

## 六、偏差与异常记录（如实呈报，均不构成 T-P12 失败）

1. **「仓库 pnpm test 已带 --no-file-parallelism」口径与仓库事实不符**（派发说明 vs 代码事实）：根 [package.json](../package.json) `test` script 实为 `vitest run`，[vitest.config.ts](../vitest.config.ts) 亦无 `fileParallelism: false` 配置——即 `pnpm test` 默认**并行**，串行须显式 `pnpm exec vitest run --no-file-parallelism`。本验收即按显式串行参数执行（与 dev 报告 AC5 所述命令一致）。此为口径澄清事项，请主控知悉；若后续挂账⑥选择「固化串行跑法」，建议落点为 package.json script 或 vitest 配置。
2. **dev 报告 AC1 旁注「schemas.spec 63 用例」为过时引用**：本卡实测 `packages/shared/test/schemas.spec.ts` 为 **67 用例**（根 vitest 明细行）。git status 证实该文件本卡零改动（63 为 T-P09 时点数字，其后 T-P10 契约 v0.8 用例已并入）。无害偏差，不影响 AC1 判定（AC1 关键点是「schemas.spec 不受影响且零改动」，git 证据成立）。
3. **AC12 性能用例（挂账⑥）串行复跑实录**：按移交口径以 `--no-file-parallelism` 串行执行，`packages/engine/test/score.spec.ts` 51/51 通过（184ms），全量 390/390——**串行下未见 AC12 波动**。dev 披露的并行 flaky（默认并行 4/4 次 55~62ms>50ms 阈）属挂账⑥既有所涉，与 T-P12 改动无关（packages/engine 本卡零改动，git 证实），本卡不算失败。挂账编号按 CURRENT.md 权威口径（⑥=AC12 flaky、⑧=imageUrl regex 收紧，S-3 勘误遵循主控裁量）。
4. **tp01 真实 HTTP 回归残留清偿**：tp01 跑后 Plan 54→62、Event 93→103（+8/+10，类型构成 GENERATE×8 + SWAP_MENU×2）。复用 dev 先例脚本 `node .workflow-verify/tp12/cleanup-tp01.cjs`（cuid 前缀圈窗 + W1 三重防呆）：dry-run 实证 8 Plan 全 PROPOSED、窗口/交叉校验全过后 `go` 实删，DELETED Events=10 Plans=8，AFTER 七表+REL 回基线。输出日志留存于本报告第三节 #10。
5. **环境噪音（不影响判定）**：PowerShell 5 下 `Invoke-WebRequest`/`pnpm` 输出混入 CLIXML 编码噪音（`#< CLIXML`/NativeCommandError 包装），退出码判定不受影响（dev 报告 §5 披露 6 同款现象）； LF→CRLF git warning 为本机常规提示（T-P11 同）。

## 七、reviewer S 项落实情况

- **S-1（diff/数字复跑）**：已闭环——第一节行级 diff 核对 + 第三节全部数字独立复跑。
- **S-2（tp12 清理脚本运行输出日志留存）**：已闭环——tp12 完整 18 行 PASS 输出留存于本报告第五节。
- **S-3（挂账编号勘误）**：本报告按 CURRENT.md 权威编号（⑥=AC12 flaky）执行。
- **S-4（h5 feedback/history 两页注释过时）**：维持 reviewer 裁量——挂账随 ⑥⑦ 卡顺带，不单开卡；本卡 h5 仅 client.ts 注释，符合禁改边界。

## 八、覆盖边界

**已测**：GET /api/plans/:id/feedback 三态（真实 HTTP：无反馈 200+null 字面量 / 有反馈五键形状 / plan 不存在 404）、契约层（contract.spec 53 用例含 v0.9 改写与新增 404 用例）、schema 层（67 用例回归）、引擎与管线全量（根 vitest 390 串行）、禁忌集一票否决线（89/89）、H5 类型层（typecheck 0 错）、tp01 必消/全换/空手三态真实回归（54 断言）、数据基线（七表+REL 开工/终态双核对）、端口清零（54329/:3000 双 0）。

**未测（边界外，如实说明）**：
- 浏览器/CDP 真机 UI 回归不在本卡范围：H5 侧仅注释级改动、`notFoundAsNull` 兜底行为零变化（T-P04 起 feedback/history/dish 页消费 null 的分支均已存在），浏览器层 404→200 消除 console 网络错误的最终效果由 T-P04 V5 豁免链路的后续浏览器任务承接（本卡无 h5 行为变更可测）。
- 默认并行模式 `pnpm test` 的 AC12 flaky 未作为本卡证据重跑（挂账⑥范畴，dev 已披露 4/4 复现口径；本卡按移交要求以串行为准）。
- 公网部署环境（T-P07 链路）不在本卡范围。
- tp12 脚本对「并发重复请求」等极端边界未覆盖（超出任务卡 AC 定义）。

## 九、验收结论

T-P12 实现与任务卡 AC1~AC7 逐条相符，真实环境证据完整可复现，改动范围零越界，回归全绿，teardown 清偿完毕（数据库回基线、双端口清零）。**fm-verify 判定 PASS**，建议主控按流程进入「待用户审核」，随「卡完成即提交」总授权执行提交。
