# T-C03 fetch 内容入库链路 —— 四角色闭环证据归档（主控）

> 归档：主控 PM｜2026-09-10｜基线 HEAD `4b237d1`｜分支 feat/tp-06-menu-expansion
> fm-dev 报告：[evidence/T-C03-dev-2026-09-10.md](./T-C03-dev-2026-09-10.md)｜fm-verify 证据：.workflow-verify/tp-c03-verify/（verify-report.md 总报告）
> 本卡经历两轮 fm-dev：第一轮完成代码后返回中断（主控磁盘核验发现改动在盘），第二轮续作补齐测试/端到端/teardown/报告并修复第一轮 CLI 缺陷——双轮模式运行正常。

## 0. 三方结论

| 角色 | 结论 | 关键数字 |
|---|---|---|
| fm-dev | 8/8 AC | 转换 1055B dish.json；管线 97/97、全仓 346/346、tsc 0；端到端 FETCHED 落库；teardown Dish=19 |
| fm-reviewer | **通过**（无阻断，6 建议级） | R-4 双保险逐层核验未削弱；静态服务 root 固定+鉴权+无穿越；@fastify/static 依赖合规 |
| fm-verify | **通过**（V1~V7 全 PASS） | 双保险强制覆盖实证；SHA256×3 一致；teardown 六表逐项一致；3000/54329 清零 |

## 1. 交付内容（6 改 + 3 新增，+257/-26）

- tools/content-pipeline/fetch2dish.mjs（新增）：fetch.json → Dish 形状转换（steps 从正文切分不虚构、C-2 按 Content-Type→魔数归一扩展名、noteId 双重净化）。
- tools/content-pipeline/src/import.ts（+87）：`--origin FETCHED` 显式授权路径（R-4 处置）；默认路径双保险两层完好（编译期字面量 + 运行时覆盖，ImportOrigin 仅 LLM_DRAFT|FETCHED，显式传其他值抛错）。
- tools/content-pipeline/src/cli/import.ts（+38）：CLI 透传（第一轮缺陷——默认路径传 'LLM_DRAFT' 必挂——已修复，默认 dry-run EXIT=0）。
- apps/api/src/app.ts（+11）+ package.json（@fastify/static ^10.1.3）：静态图片服务 /images/**，root 固定 apps/api/static/images、受 authHook 鉴权。
- 测试：fetch2dish.spec.ts 31 用例（新增）+ import.spec.ts +9（双保险回归）。

## 2. fm-verify 关键实证

- V2 双保险：输入故意写 FETCHED/PUBLISHED，默认导入落库 origin=LLM_DRAFT/status=DRAFT（强制覆盖）；授权导入 origin=FETCHED/status=DRAFT。
- V4 端到端：dishId cmtvcid1s0000twpgpsbnlv0p 三字段与 fetch.json 逐字一致；三图 HTTP 200 image/webp 字节 594340/354324/358954 与源一致（SHA256 全匹配）。
- V1 安全：无 cookie 401/带 cookie 200；路径穿越（编码/明文/反斜杠）全部被拒零泄露。
- V5 测试复跑：管线 97/97（--root ../.. 定型跑法）、全仓 346/346、tsc EXIT=0。
- V6 teardown：六表 19/13/45/42/54/93 逐项一致；端口清零；CDP 9222 全程未碰。

## 3. 审查发现处置表

| 编号 | 内容 | 处置 |
|---|---|---|
| B-5 | fetch2dish downloadedImages[].index 未校验，异常产物或致写文件路径逃逸（自产输入，风险低） | 挂账：后续入库卡或修补小卡加 Number.isInteger 校验 |
| B-6 | imageUrl 拼接硬编码 index 0（当前产物 index 连续未触发） | 挂账：改从 placed[0].index 取值 |
| R-8 | 管线 package.json test script 在子目录不命中根 include（EXIT=1），需 --root ../.. | **待用户裁决**是否改 script（一行） |
| R-9 | DRAFT 内容无 HTTP 只读口（审阅 FETCHED 草稿需直查 PG） | 登记待内容轨道后续卡 |
| R-10 | 契约 z.string().url() 致 imageUrl 只能存绝对 URL（换部署地址存量失效） | **知悉项**：个人自用版可接受；未来如改域名需批量更新或改契约 |
| R-11 | apps/api/static/ 未入 .gitignore（当前 teardown 清净无实害） | **待用户裁决**是否追加 ignore |
| C-3 | PS 5.1 IWR Cookie 怪癖（curl 正常） | 测试经验备查 |
| （verify） | 明文/反斜杠穿越返回 500 而非 404（零泄露已守住） | 语义瑕疵，不处置 |
| （verify） | 静态图片路由无 cookie 401 未单独实测（401 对照在 /api/family/rules） | reviewer V6 缺口建议；verify 实测了带/无 cookie 对照（V1 PASS），缺口已实质覆盖 |

## 4. teardown 与环境终态

六表 19/13/45/42/54/93 逐项一致；测试 Dish 3 条删净、图片文件删净（2 个空目录残留，git 不跟踪无影响）；3000 先子后父杀净=0；PG stop EXIT=0 后 54329=0；CDP 9222 存活未碰。

## 5. 结论

T-C03 **通过验收**，四角色闭环第七次跑通（内容轨道链路三段式全部就绪：采集→转换→入库）。内容池填充工作流已可用：CDP 试采（T-C02）→ fetch2dish → import --origin FETCHED（DRAFT）→ 人工微调 → 既有流程升 PUBLISHED。待用户裁决：①提交 ②R-8 test script ③R-11 static gitignore ④B-5/B-6 修补时机。
