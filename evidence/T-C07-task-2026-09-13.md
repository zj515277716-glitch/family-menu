# T-C07 任务卡：花生过敏 HARD+TAG 规则口径缺口独立评估（只读评估卡，零代码改动）

> 派发：主控 PM｜日期：2026-09-13｜执行：fm-dev｜性质：只读评估（同 MIGRATE-CHK 先例——评估闭环产出处置选项，处置决策另定）
> 来源：T-C06 review S-3（审查方新增观察，建议优先级最高）+ T-C06 verify 移交印证。主控已将该评估升至挂账队列首位（涉「安全过滤>一切评分」铁律，AGENTS.md 铁律 8 家族）。

## 背景

FamilyRule 存在花生过敏 HARD 规则 `{scope:'TAG', targetTag:'花生', severity:'HARD'}`（apps/api/prisma/seed-data.ts L29-36 附近）。engine 安全过滤（packages/engine/src/safety.ts L155 附近）对 HARD+TAG 的匹配口径为「菜品 flavorTags 或食材 category」。花生米食材行 category=调料，若其 flavorTags/category 均不含「花生」字样，则含花生米的菜（宫保鸡丁 T-C05、蚝油生菜 T-C06 可选花生米 DishIngredient 行 ingredientId=cmtvnuvxvc2oy5fdrzhpxxe69 optional=true qty=10g，verify 已实证）可能不会被 HARD 规则拦截——若属实，即安全过滤铁律级缺口。

## 目标

只读评估三件事：①缺口是否属实（源码级+运行时级双重定案）；②暴露面全景量化；③处置方案收敛（≥2 案，含影响面/风险/推荐序），供主控/用户拍板。**零代码改动**。

## AC（逐条 [✓]/[✗] 自检，证据落盘 evidence/T-C07-dev-2026-09-13.md）

- **AC1 全景数据面**：全库检索花生族食材（name/aliases 含「花生」的一切 Ingredient 行，含独立食材与 aliases 引用）、相关 FamilyRule/ExclusionRule 原文、含花生成分的菜品清单（Dish→DishIngredient→Ingredient 联查，标注 optional 与否、rows 数）。
- **AC2 缺口定性（源码级+运行时级）**：①精读 safety.ts HARD+TAG 匹配全链（规则从哪来→匹配对象是什么→「花生米」能否命中），给出源码行号级结论；②运行时复现：用真实库数据（含花生米的菜）实际执行 engine 安全过滤（或最小化测试脚本/既有测试套件内复现），证实「含花生米的菜是否被 HARD 规则拦截」——不许只靠静态推理下结论。
- **AC3 暴露面量化**：列出「含花生成分但不会被 HARD 规则拦截」的菜品全集（若 AC2 证实缺口属实）；同时反向列出「会被拦截的路径」说明现口径在哪些情形仍有效（如 flavorTags 恰含花生）。
- **AC4 处置方案收敛（≥2 案）**：每案给出：改动位置与形态（seed 数据层/engine 匹配口径层/契约层）、影响面（契约版本变更?、seed 迁移路径含 RDS、engine 纯函数改动+测试影响、matcher/safety 既有用例兼容性）、风险与副作用（如 aliases 泛匹配误伤——花生油/花生酱 vs 花生米语义差异）、推荐序与理由。方案须遵守约束：运行时零 LLM、packages/shared 契约变更须主控批准路径、RDS 侧另立卡口径。
- **AC5 环境与回归**：评估全程零写库（快照前后 13 表计数一致）；跑 `pnpm test` 与 `pnpm test:taboo` 确认基线全绿（391/391 + 89/89）证明评估环境健康；证据全部落盘 evidence/T-C07-dev-2026-09-13.md（真实命令+退出码+数字）。

## 输入资源

- T-C06 审查报告：d:\codex\family-menu\evidence\T-C06-review-2026-09-13.md（S-3 条目：seed-data.ts L29-36 + safety.ts L155 定位）
- T-C06 验收报告：d:\codex\family-menu\evidence\T-C06-verify-2026-09-13.md（花生米 DishIngredient 行实证）
- 关键源码：apps/api/prisma/seed-data.ts（FamilyRule 种子）、packages/engine/src/safety.ts（安全过滤）、packages/engine/src/（matcher 上下文）、packages/shared/src/schemas/（规则/食材契约）
- 本地 PG 17.5 @ 127.0.0.1:54329 库 family_menu（只读查询；当前基线 Dish=49 / Ingredient=81 / DishIngredient=375 / Plan=54 / Event=93 / CookLog=6 / Menu=13 / MenuDish=42）

## 边界约束

- **只读评估零代码改动**：不改任何 .ts/.mjs/.prisma/.json 源码与种子数据；临时验证脚本只落 %TEMP% 或 .workflow-verify/（不入 evidence/ 之外的仓库路径），报告落 evidence/T-C07-dev-2026-09-13.md。
- PG 红线：仅只读 SELECT；禁止写库/DROP/清理；不得停止本地 PG；不碰 RDS（ssh fmsrv 只读查询若需要，须用既有只读口径并在报告披露）。
- 不改 packages/shared；不新增 npm 依赖；运行时代码禁止 LLM API。
- 测试跑既有套件即可，不为评估新写测试文件（若运行时复现需要最小脚本，落 .workflow-verify/ 并在报告内联关键输出）。
- 评估结论措辞须区分「已证实/静态推断/未验证」，禁止定性模糊。

## 异常升级路径

- 若发现花生之外存在同族缺口（其它 HARD 规则 vs 其它食材族，如鸡蛋/牛奶/海鲜等），一并全景登记（不改代码），在报告单列「同族缺口扫描」节。
- 若发现 safety.ts 逻辑与预期完全不符（如实际已能命中花生米——S-3 观察被证伪），同样如实定案（证伪也是有效结论），并解释 review 观察为何偏差。
- 任何卡外问题写入报告报主控，不顺手修改。
