# R-9 开发完成报告（fm-dev）

- 任务卡：evidence/R-9-task-2026-09-13.md（DRAFT 内容 HTTP 只读口：GET /api/dishes 列表 + 详情）
- 执行角色：fm-dev | 日期：2026-09-13 | 环境：Windows PowerShell + pnpm monorepo + 本地 PG 17.5 @ 127.0.0.1:54329/family_menu
- 状态：**待主控复核**（未 git commit，按任务卡交付要求留待主控统一提交）

---

## 一、AC 逐条自检

### AC1 GET /api/dishes 列表端点 —— [✓]

| 子项 | 证据 |
|---|---|
| 新建 apps/api/src/routes/dishes.ts（102 行） | git status 新增文件；路由薄原则对齐 plans.ts 先例（safeParse 校验 → service → 响应 parse） |
| ?status= 可选过滤（DRAFT/TESTED/PUBLISHED 白名单） | 复用 shared ContentStatusSchema 白名单；spec 用例 c：单值三桶与 DB 精确集合相等 |
| 逗号分隔多值去重保序 | routes/dishes.ts L51-78 transform 实现（Set 去重保序 + 逐段 trim）；spec 用例 d：TESTED,PUBLISHED,TESTED 去重并集无重复行 + %20 空格 trim |
| 白名单外 400 | spec 用例 e：FOO / DRAFT,FOO / 空串 / 尾随逗号 均 400（运行日志 statusCode 400 实录） |
| 缺省=全部 | spec 用例 b：缺省列表 = baselineTotal(49)+3(rm9 行) = 52 行 |
| 响应 = z.array(DishSchema) 本地组合 | routes/dishes.ts L20 `DishListResponseSchema = z.array(DishSchema)`；不改 shared（shared 无此响应形状） |
| 排序 id asc | dishesService orderBy id asc；spec 用例 b 断言 |
| app.ts 注册 + 注释实数口径 | app.ts L21 import、L125 注册；L118-121 注释更新为「17 条：family 4 / recommend 1 / plans 10 / dishes 2」并注明 R-9 依据（见卡外发现②） |

### AC2 GET /api/dishes/:id 详情端点 —— [✓]

| 子项 | 证据 |
|---|---|
| Dish 全字段 + ingredients（DishIngredient→Ingredient join） | dishesService.getDish：findUnique + include ingredients.ingredient；toDishView 投影 |
| view 形状对齐 engine DishIngredientView 8 字段 | routes/dishes.ts L27-36（ingredientId/ingredientName/aliases/category/defaultUnit/qty/unit/optional）；spec 用例 f：与 DB join 实值逐字段对照 + 8 键集精确断言 |
| 不存在 → 404 | 复用 planService NotFoundError → app.ts 错误处理映射 404；spec 用例 h：rm9-not-exist → 404（运行日志实录） |
| 响应 schema 本地组合 | routes/dishes.ts L39-41 `DishSchema.extend({ ingredients: z.array(DishIngredientViewSchema) })`，不涉契约变更 |

### AC3 origin 投影补齐 —— [✓]

| 子项 | 证据 |
|---|---|
| mappers.ts DishRow 接口补 origin | mappers.ts：DishRow.status 后新增 `origin: string`（含 R-9 注释说明缺投影的正确性缺陷） |
| toDishView 投影 origin（engine DishView 无 origin，禁改 engine） | 本地交叉类型扩展：DishMediaView 增加必填 `origin: ContentOrigin`（先例=同文件 DishMediaView 媒体字段形态）；toDishView 增加 `origin: row.origin as ContentOrigin` |
| zod default('LLM_DRAFT') 不吞 FETCHED 实值 | spec 用例 i：全列表逐行 origin 与 DB 实值对照 + API FETCHED 集合 === DB FETCHED 集合 + 抽 FETCHED 行详情验证；**实测 FETCHED=30 行**（console.log 输出实录：`[dishes] origin=FETCHED 实测行数：30（基线卡口径 29，以 DB 实测为准）`） |

### AC4 全局 authHook 自动覆盖 + 零写库 —— [✓]

| 子项 | 证据 |
|---|---|
| 无 cookie 401，未新增豁免逻辑 | app.ts 未改 authHook；spec 用例 a：列表+详情无 cookie 均 401 |
| findMany/findUnique 零写库 | dishesService.ts 全文仅 findMany/findUnique（43 行，无 create/update/delete/upsert） |

### AC5 新增 spec + 回归验证 —— [✓]

| 子项 | 证据 |
|---|---|
| spec 口径对齐 put-exclusions.spec.ts | apps/api/test/dishes.spec.ts（310 行）：buildApp+inject+真实 PG；SELECT 1 gate 防假绿（PG 不可达时 skip）；rm9- 前缀隔离；afterAll 清理+零残留断言 |
| 10 用例覆盖 | a) 401×2；b) 缺省全量 52 行+id asc+逐行 DishSchema.parse+rm9 行无 ingredients/imageUrl 键（zod strip）；c) 单值三桶=DB 精确集合；d) 多值去重+trim；e) 非法值 400×4；f) 详情 join 实值对照；g) rm9 行 ingredients=[] 空态；h) 404；i) origin 逐行投影 FETCHED 实测；j) 存量全量行（排除 rm9-）toDishView→DishSchema.parse 幂等 **49/49 零畸形** |
| 根 pnpm test | `pnpm test` → **Test Files 19 passed (19) / Tests 442 passed (442)**，EXIT=0（基线 432/432 + 新增 10/10） |
| pnpm test:taboo | `pnpm test:taboo` → **Test Files 5 passed (5) / Tests 95 passed (95)**，EXIT=0 |
| apps/api typecheck/build | `pnpm --filter @family-menu/api build`（prisma generate 7.9.1 + tsc -p tsconfig.json）→ EXIT=0 |
| 报告记录命令+退出码+数字 | 见下文「二、验证证据」 |

### AC6 改动边界 —— [✓*]（含 1 项需主控裁决的依赖声明，见「四、新增依赖声明」）

| 子项 | 证据 |
|---|---|
| 零 packages/shared 改动 | git status：无 packages/* 文件被修改 |
| 零新增 npm 依赖 | **例外 1 项：apps/api 增加 zod ^4.4.3 —— 任务卡内在矛盾，非自愿新增，详见「四」** |
| 零 LLM API | 全部改动文件无任何 LLM/网络调用 |
| 只改 apps/api 内文件 | git diff --numstat 4 个文件：3 个在 apps/api 内 + pnpm-lock.yaml（依赖声明伴生，见「四」） |

---

## 二、验证证据（命令 + 退出码 + 数字）

### 基线（改造前，已记录）
1. `pnpm test` → Test Files 18 / Tests 432/432，EXIT=0
2. `pnpm test:taboo` → 5 文件 / 95/95，EXIT=0
3. PG 只读计数（临时脚本，已删）：Ingredient=81、DishIngredient=375、ExclusionRule=3、Plan=54、rm9- 残留 0；Dish=49 分布：DRAFT/FETCHED=30、TESTED/MANUAL=1、PUBLISHED/LLM_DRAFT=9、PUBLISHED/MANUAL=9

### 改造后（本卡）
1. `pnpm --filter @family-menu/api build` → EXIT=0（✔ Generated Prisma Client 7.9.1 + tsc 无错误）
2. `pnpm test` → Test Files **19 passed (19)** / Tests **442 passed (442)**，EXIT=0，Duration 7.25s
   - dishes.spec.ts (10 tests) 360ms 全绿
   - 幂等验证输出：`[dishes] 存量全量 DishSchema.parse 幂等验证：49/49 全过`
   - origin 实测输出：`[dishes] origin=FETCHED 实测行数：30（基线卡口径 29，以 DB 实测为准）`
3. `pnpm test:taboo` → Test Files **5 passed (5)** / Tests **95 passed (95)**，EXIT=0，Duration 781ms
4. PG 零残留复查（临时只读脚本 `node tmp-r9-verify.cjs`，跑完已删）→ `{"rm9Dish":0,"dishTotal":49,"fetchedTotal":30,"exclusionRules":3,"ingredients":81}`，EXIT=0
   - rm9- 测试行零残留；Dish 总数回基线 49；ExclusionRule 三行与 seed 数据零触碰

### 过程中的失败与修复（如实记录）
- 首次 build EXIT=2，共 4 个错误：
  - `src/app.ts(125,22) TS2304: Cannot find name 'dishRoutes'` —— app.ts 注册块已改但 import 行编辑丢失 → 已补 `import { dishRoutes } from './routes/dishes.js';`
  - `src/routes/dishes.ts(8,19) TS2307: Cannot find module 'zod'` + 连锁 TS7006×2 —— **zod 在 apps/api 不可解析**（root/apps/api node_modules 均无 zod 链接，zod 仅是 shared 与 fastify-type-provider-zod 的内部依赖，pnpm 严格模式不提升）→ 见「四、新增依赖声明」
- 修复后 `pnpm install --no-frozen-lockfile` → EXIT=0（**downloaded 0**，zod@4.4.3 复用 pnpm store 既有包，零网络下载），随后 build EXIT=0

---

## 三、改动文件清单（git diff --numstat + 新增文件行数）

| 状态 | 文件 | 增/删 | 说明 |
|---|---|---|---|
| M | apps/api/package.json | +2/-1 | dependencies 增加 `"zod": "^4.4.3"`（见「四」） |
| M | apps/api/src/app.ts | +6/-1 | import dishRoutes；注册 `await app.register(dishRoutes, { prefix: '/api' })`；路由计数注释更新为 17 条实数口径（注明 R-9 依据） |
| M | apps/api/src/services/mappers.ts | +12/0 | DishRow 接口补 origin；DishMediaView 交叉类型加必填 origin；toDishView 投影 origin |
| M | pnpm-lock.yaml | +4/-1 | apps/api importer 新增 zod 记录（依赖声明伴生改动，非业务改动） |
| A | apps/api/src/routes/dishes.ts | 新增 102 行 | GET /api/dishes + GET /api/dishes/:id，本地 zod 组合响应/查询 schema |
| A | apps/api/src/services/dishesService.ts | 新增 43 行 | listDishes/getDish，零写库，复用 NotFoundError |
| A | apps/api/test/dishes.spec.ts | 新增 310 行 | 10 用例真实 PG 集成测试（rm9- 隔离+零残留+幂等验证） |

git status 另有若干与 R-9 无关的既有未跟踪文件（.pai/、docs/design/、tools/*.tar.gz 等），非本卡产物，未触碰。

---

## 四、新增依赖声明（需主控裁决）

**声明：apps/api 新增 1 个依赖 `"zod": "^4.4.3"`（伴生 pnpm-lock.yaml 一条 importer 记录）。**

- **性质：任务卡内在矛盾的被动解决，非自愿扩展。** 任务卡红线要求「零新增 npm 依赖」，同时 AC1/AC2 要求「响应 schema 本地组合（z.array(DishSchema)、DishSchema.extend）」与查询 transform——本地 zod 组合要求 `import { z } from 'zod'` 可解析。
- **事实**：apps/api 此前从未声明 zod（zod 仅是 packages/shared 与 fastify-type-provider-zod 的内部依赖；pnpm 严格模式下 apps/api 无法解析），首次 build 实测 TS2307 失败。排除的替代方案：①不 import zod 全用实例方法绕行——查询 transform 与 DishIngredientViewSchema 的 z.object 无法绕行，且 hack 写法偏离「对齐 plans.ts 先例」；②响应 schema 下沉 shared——违反「不改 packages/shared」红线。
- **采取的最小方案**：版本与 packages/shared 完全一致（^4.4.3）；该包已在 workspace 内被 shared 使用，pnpm store 既有 zod@4.4.3，安装 downloaded=0，**未引入任何新外部包、未产生新下载**。
- **请主控复核裁决**：接受此声明 → 合并时确认 package.json+lock 改动；不接受 → 需重新定义 AC1/AC2 的实现口径（放弃本地组合或改走 shared），本人按新口径返工。

---

## 五、卡外发现登记（移交主控，未自行处理）

1. **卡面数字漂移：FETCHED 实测 30 道 vs 卡面「29 道」**。DB 实测（改造前基线与改造后复查两次一致）：`DISH WHERE origin='FETCHED'` = 30。完整分布：DRAFT/FETCHED=30、TESTED/MANUAL=1、PUBLISHED/LLM_DRAFT=9、PUBLISHED/MANUAL=9，合计 49（与卡面 Dish=49 一致）。spec 用例 i 采用动态断言（API FETCHED 集合 === DB FETCHED 集合）不受影响，console.log 已实测输出 30。建议主控更新卡面口径。
2. **app.ts 原注释「10 条路由」为过时口径**：实际代码既有 family 4 + recommend 1 + plans 10 = 15 条 API 路由（原「10 条，对齐 5.1 路由清单」是设计清单数字）。本卡已更新为「17 条（family 4 / recommend 1 / plans 10 / dishes 2）」并注明 R-9 依据与统计外端点（/health、/health/db、/images/**）。
3. **apps/api 测试文件不在 typecheck 范围**：apps/api/tsconfig.json include 仅 src，test/ 下 spec 由 vitest（esbuild 仅转译）执行、ESLint 非 type-aware——类型错误在测试文件中不会被发现。此为仓库既有口径（put-exclusions.spec.ts 同），非本卡引入；如需收紧建议主控另立任务。

---

## 六、未验证项

1. 未做浏览器/人工 HTTP 手测（本卡未要求启动服务；端点行为已由 inject 集成测试全量覆盖，含 401/200/400/404 状态码运行日志实录）。
2. spec 文件不被 tsc typecheck（见卡外发现 3），用例内类型正确性由运行时行为验证兜底。
3. git commit 未执行（按任务卡要求留待主控复核后统一提交）。

## 七、遗留

- 无功能遗留。待主控裁决事项：「四」的 zod 依赖声明 + 「五」的卡面数字口径修正。

---

## 八、返工记录（T1）—— fm-reviewer 审查 PASS WITH NOTES 唯一返工项

日期：2026-09-13 ｜ 执行：fm-dev ｜ 状态：待主控复核（未 git commit）

### a) T1 内容与主控裁决

- 审查发现：GET /api/dishes/:id 详情端点声称「Dish 全字段」但 `licenseNote` 未投影——shared DishSchema L59 既有 `licenseNote: z.string().optional()`（内容授权台账字段）、schema.prisma L106 既有 `licenseNote String?` 列、fm-import 入库时写该字段（tools/content-pipeline/src/import.ts L198），但 mappers.ts 的 DishRow 接口与 toDishView 均未投影，响应 parse 时被 zod strip 丢弃——审阅 FETCHED 草稿时授权台账文案不可见（R-9 立卡动机即审阅草稿）。
- 主控裁决：**从严处理，AC2「Dish 全字段」按字面满足口径返工**，本项为唯一返工项（第 1 次返工，轻量）。

### b) 修复内容与 diff numstat

1. apps/api/src/services/mappers.ts（T1 增量 +11 行）：
   - DishRow 接口媒体字段区块新增 `licenseNote: string | null;`（注释注明 R-9 返工 T1 与缺投影缺陷）
   - DishMediaView 交叉类型新增 `licenseNote?: string;`（**连带必要改动**：toDishView 返回类型标注为 DishMediaView，不加声明则 TS excess property check 报错；与 imageUrl 先例同形态）
   - toDishView 投影新增 `licenseNote: row.licenseNote ?? undefined,`（null → undefined 缺省序列化不出现，口径对齐 imageUrl）
2. apps/api/test/dishes.spec.ts（未跟踪新文件，R-9 产物；T1 追加 +28 行断言块，用例数不变仍 10）：
   - 用例 f 补断言 `expect(parsed.licenseNote).toBe(row!.licenseNote === null ? undefined : row!.licenseNote)`（DB String? null→undefined 映射兼容写法）
   - 用例 f 补 FETCHED 行抽查：按 `origin='FETCHED' AND licenseNote IS NOT NULL` 取首行，断言详情响应 `licenseNote` 为非空 string 且等于 DB 实值；**库内若 FETCHED 行 licenseNote 全为 null 则如实 console.log 报告、不强造断言**（分支已内置）
3. evidence/R-9-dev-2026-09-13.md：追加本返工记录小节。

T1 后累计 `git diff --numstat`（未跟踪新增文件不显示，累计含 R-9 原始改动；mappers.ts 累计 +23/0 = R-9 原始 +12 + T1 +11）：

```
2       1       apps/api/package.json
6       1       apps/api/src/app.ts
23      0       apps/api/src/services/mappers.ts
4       1       pnpm-lock.yaml
```

红线遵守：仅改上述 3 个授权文件；未触碰 packages/shared / apps/h5 / tools / prisma / 其他 service/route；零新增依赖；PG 仅只读（临时查询脚本跑完已删）+ 既有 rm9- 造行清理口径不变；未 git commit。

### c) S1 补充披露（主控移交口径 + 实测定位）

- 主控披露：pnpm-lock.yaml 除 zod importer 记录外还含根 importer vitest peer 解析元数据变化（@types/node 22.20.1→26.1.2，纯类型定义无运行时影响，apps/api 自身 vitest 仍绑 22.20.1）。
- 实测定位（`git diff pnpm-lock.yaml`，+4/-1）：①+3 行为 apps/api importer 新增 zod 记录（specifier ^4.4.3 / version 4.4.3）；②-1/+1 行位于 snapshots 区 `@vitest/coverage-v8@4.1.10(vitest@4.1.10(...))` 快照键，其 vitest peer 后缀中 @types/node 由 22.20.1 变为 26.1.2（根 importer 依赖链的 peer 解析元数据）。@types/node 为纯类型定义包，无运行时影响；apps/api importer 自身 `@types/node ^22.0.0` 解析仍为 22.20.1，diff 未触碰该行。

### d) 复验证据（命令 + 退出码 + 数字）

1. `pnpm --filter @family-menu/api build`（prisma generate 7.9.1 + tsc -p tsconfig.json）→ **EXIT=0**，无类型错误
2. `pnpm test` → **Test Files 19 passed (19) / Tests 442 passed (442)**，EXIT=0，Duration 7.09s；用例数仍 442（断言增条不增用例）
3. `pnpm vitest run dishes`（根目录，dishes.spec.ts 单文件复核）→ **Test Files 1 passed (1) / Tests 10 passed (10)**，SPEC_EXIT=0，339ms
4. `pnpm test:taboo` → **Test Files 5 passed (5) / Tests 95 passed (95)**，EXIT=0，Duration 755ms
5. licenseNote 断言实况：库内只读查询（临时脚本已删，EXIT=0）——origin=FETCHED 共 30 行，**licenseNote 非空 30/30**；运行实录 `[dishes] FETCHED 行 licenseNote 实值抽查：cmtvk1w3v0000rwpgkoxz982k -> FETCHED 自xiaohongshu笔记 6a8db1e5000000002`，非空硬断言生效（null 跳过分支未触发）

### 过程中的失败与修复（如实记录）

- 首次 build EXIT=2，2 类 4 个错误：`src/services/mappers.ts(187,5) TS2353`（licenseNote 不在 DishMediaView 中）+ 连锁 TS239——原因：①DishRow 字段行首次编辑回显成功但未落盘（重编辑后经 Grep 核实落盘）；②DishMediaView 交叉类型缺 licenseNote 声明（toDishView 返回类型 excess property check）→ 补 `licenseNote?: string;` 声明后 build EXIT=0。未放宽任何类型检查（无 any/as 绕行，无 tsconfig 修改）。

### T1 未验证项

1. git commit 未执行（按任务卡要求留待主控统一提交）。
2. 未做浏览器/人工 HTTP 手测（沿用 R-9 原口径：inject 集成测试全量覆盖）。
