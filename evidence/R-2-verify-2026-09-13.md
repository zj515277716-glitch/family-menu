# R-2 产品验收复跑报告（fm-verify）

- 任务卡：`evidence/R-2-task-2026-09-13.md`（唯一指挥源）
- 执行者：fm-verify（产品验收代理，独立复跑）
- 日期：2026-09-13
- 仓库根：`d:\codex\family-menu`
- 落档说明：fm-verify 返回结论文本，本报告由主控代笔落盘（全文如实转录，未删改结论）。
- 被测版本：HEAD `5cccaf0` 工作区未提交态，含 R-2 全部改动（tools/content-pipeline 4 modified + 4 new）+ REV1 修复（bin 补注册 + S-3 断言）。
- 环境：本地 PG 17.5 @ 127.0.0.1:54329 / family_menu（在线）。测试数据：`.workflow-verify/r2/`（gitignore 内）既有 4 个测试 JSON，全程 dry-run / 只读，零写库。

---

## 逐 AC 自检表

### AC1 fm-import 入库过敏原校验五分支 —— [✓]

| # | 操作（cwd=tools/content-pipeline） | 预期 | 实际 | 退出码 |
|---|---|---|---|---|
| 1 | `pnpm exec tsx src/cli/import.ts --dry-run ..\..\.workflow-verify\r2\r2-import-hard.json` | exit 2 + 拦截明细 | `[过敏原拦截]`，双命中：`seed-excl-peanut`（TAG 花生）+ `seed-excl-peanut-ing`（INGREDIENT），命中值含 `花生米/熟花生米/油炸花生米`（DB join 补全 aliases）与 `cmtvnuvxvc2oy5fdrzhpxxe69`（targetId 等值），「未写入 DB」 | **2** |
| 2 | 同上 + `--allow-allergen-draft` | exit 0 + 豁免警告 | 2 条 `[豁免放行]` 警告保留，`校验通过（dry-run，未写入 DB）` | **0** |
| 3 | `--dry-run ..\..\.workflow-verify\r2\r2-import-soft.json` | exit 0 + SOFT 警告 | `[警告] SOFT 规则 seed-excl-organ（TAG 内脏）未标注命中……不阻断导入` | **0** |
| 4 | `--dry-run ..\..\.workflow-verify\r2\r2-import-tagged.json` | exit 0（已标注放行） | `过敏原校验通过：命中 0 条（无规则命中）` | **0** |
| 5 | `--dry-run ..\..\.workflow-verify\r2\r2-import-clean.json` | exit 0（无命中） | `过敏原校验通过：命中 0 条（无规则命中）` | **0** |

- 证据：五分支 stdout/stderr 全程留痕于终端（本次复跑命令记录），输出与 `evidence/R-2-dev-2026-09-13.md` 3.2 / 附录 B 逐字级一致。
- 覆盖边界：仅 dry-run；非 dry-run 真实写库路径未测（红线禁止对存量写处置，与 dev 未验证项口径一致）；空规则集/DISH scope 分支由单测覆盖（本地库 3 行规则无法真实复现空集）。

### AC2 fm-publish-check 只读盘点 CLI 四分支 + bin 注册 —— [✓]

| # | 操作 | 预期 | 实际 | 退出码 |
|---|---|---|---|---|
| 1 | `pnpm --filter @family-menu/content-pipeline run publish-check`（cwd=仓库根） | exit 2、31 道菜、缺口 2、非标 1 | 扫 31 道（DRAFT 30+TESTED 1），规则 3 行（HARD 2/SOFT 1），缺口恰 2 道（宫保鸡丁/蚝油生菜，各 2 条 HARD 命中），非标 1 个，「只报告不处置」 | **2** |
| 2 | 同 + `--json` | 结构化字段 | `scannedCount:31`、`gapDishCount:2`、`ruleCounts{total:3,hard:2,soft:1}`、gaps 内每菜 findings 含 `ruleId/severity/scope/targetTag/missingTag/ruleNote/matchedIngredients[].matchedAs`、`nonStandardCategory{count:1, items:[嫩豆腐=豆制品]}` | **2** |
| 3 | 同 + `--status tested` | exit 0 | `扫描范围：status IN ('TESTED')，共 1 道菜`，缺口 0，`结论：无缺口（exit 0）`（PUBLISHED 未被扫） | **0** |
| 4 | `pnpm exec tsx src/cli/publish-check.ts --status published`（cwd=tools/content-pipeline） | exit 1 非法值报错 | `--status 非法值：PUBLISHED（仅允许 DRAFT/TESTED，大小写不敏感，逗号分隔）` | **1** |

- bin 注册核对：`tools/content-pipeline/package.json` `bin` 第 5 项 `"fm-publish-check": "./dist/cli/publish-check.js"` 实文确认（C-1 修复达成）；bin 指向的 dist 产物 `node tools/content-pipeline/dist/cli/publish-check.js --help` EXIT=0，帮助文本含退出码契约与 --status/--json 选项。
- 覆盖边界：`--status draft,tested` 组合形态未单独实跑（normalizeStatusFilter 归一/去重由单测覆盖）；dist 全扫直跑 EXIT=1 为已知备案（Prisma 生成客户端路径解析限制，正式口径 tsx/scripts 路径，与 dev 报告 REV1 卡外登记一致，不判阻塞）。

### AC3 category 漂移警告面 —— [✓]

- fm-import 侧：AC1 场景 1 stderr 实测 `[警告] 1 个食材 category 不在标准六类 [蔬菜/肉类/水产/蛋奶/调料/主食]：罗勒=香草（R-1 防御面：仅警告，不阻断导入）`；五分支退出码（2/0/0/0/0）均由过敏原处置分级决定，警告未阻断流程、未改变退出码。
- fm-publish-check 侧：全扫报告附非标计数 `1 个（嫩豆腐=豆制品）`，`--json` 的 `nonStandardCategory.count=1` 结构化可核。
- 证据：场景 1 输出原文 + 全扫报告原文 + JSON 字段。覆盖边界：仅验证现存非标实例（嫩豆腐=豆制品）；六类边界穷举由单测覆盖。

### AC4 存量只读盘点一致 + 零写库 —— [✓]

- 一致性：盘点缺口**恰好 2 道含花生 DRAFT**（宫保鸡丁 `cmtvkmre4000020pglsqyzak8`、蚝油生菜 `cmtz3kdhp000070pga77gl5wt`），与卡面预期及独立库级直查（`node .workflow-verify/r2/db-baseline.mjs`：花生米引用菜恰 2 行，宫保鸡丁 50g optional=false、蚝油生菜 10g optional=true，均 DRAFT/FETCHED）三方吻合；无花生外缺口族群。
- 零写库：① 代码级——`src/publish-check.ts` 与 `src/allergen.ts` grep 全部写 API（create/update/delete/upsert/createMany/updateMany/deleteMany/executeRaw 等）**零命中**，仅 findMany；② 行为级——全部验收操作后九表基线复核与 dev 报告 3.1 基线逐一相等：`Dish=49 / Ingredient=81 / ExclusionRule=3 / Plan=54 / Event=93 / DishIngredient=375 / Menu=13 / MenuDish=42 / CookLog=6`，ExclusionRule 3 行（seed-excl-organ/seed-excl-peanut/seed-excl-peanut-ing）逐字段原样。
- 证据：db-baseline.mjs EXIT=0 输出（纯 SELECT 脚本）。覆盖边界：全程本地库；RDS 侧未验证（红线禁 ssh fmsrv）；本地库无腊肉行 ex-1788658316681（系 RDS 口径，已知备案）。

### AC5 测试与回归 —— [✓]

| 命令（cwd=仓库根） | 预期 | 实际 | 退出码 |
|---|---|---|---|
| `pnpm test` | 432/432 | Test Files 18 passed (18)，Tests **432 passed (432)**（含 `test/allergen.spec.ts` 32 例 = REV1 前 31 + S-3 findMany 无参断言 1） | **0** |
| `pnpm test:taboo` | 95/95 | Test Files 5 passed (5)，Tests **95 passed (95)**（AC12 328ms 绿） | **0** |

### AC6 约束与零侵入 —— [✓]

- `git diff --numstat`：`CURRENT.md 3+/1-`（既有）+ `tools/content-pipeline/package.json 4+/2-`（REV1 后口径与 dev 报告一致）+ `src/cli/import.ts 146+/39-` + `src/db.ts 9+/1-` + `src/import.ts 27+/1-`；untracked 本卡项 = evidence R-2 三份 + tools/content-pipeline 4 新文件（allergen.ts / cli/publish-check.ts / publish-check.ts / test/allergen.spec.ts）。
- 禁区终核：`git diff --stat -- packages/shared apps/api apps/h5 prisma` 输出为空 → **零改动**；工作区其余 untracked（.pai/、docs/design/、docs/ui-redesign/、tests/*.mjs|cjs、tools/*.tar.gz 等）与 dev 报告 §5.2 披露的既有状态一致，验收全程无新增改动。
- 红线遵守：fm-rds.env 未动（EXISTS）；PG 54329 未停止；未碰 RDS；无 DROP/清理 SQL；存量 2 道含花生 DRAFT 未写处置；测试 JSON 落于 .workflow-verify/r2/（git status 未列示 = gitignore 覆盖确认）。零新依赖（package.json dependencies 与 REV1 记录一致）、运行时零 LLM API（新增代码无 LLM import）。

---

## 总体判定：**PASS（6/6 全 ✓）**

REV1 两项修复均已实证闭环：C-1（bin 第 5 项注册 + `--help` 可执行）与 S-3（findMany 无参断言，总测试 432）。核心交付（入库 HARD 拦截/豁免/SOFT 分级、发布前置盘点退出码契约、category 警告面、存量盘点恰 2 道含花生 DRAFT、零写库、零侵入）全部与任务卡 AC 权威口径及 dev/review 报告一致。未发现新问题，无打回事项。

**已知备案（不判阻塞，沿用既有登记）**：① dist 全扫直跑 EXIT=1（Prisma 生成客户端路径解析限制），正式口径为 tsx/scripts 路径；② 本地 ExclusionRule=3 行 vs 卡面「4 行」笔误（动态读取设计不受影响）；③ fm-import 非 dry-run 真实写库路径本卡未实跑（红线约束，与 dev 未验证项一致）。
