# T-P10 独立技术审查报告（fm-reviewer）

- 审查对象：T-P10 空手文案区分时长原因，未提交工作区改动（dev 自报：修改 13 + 新增 2，新增依赖 0）
- 代码基线：HEAD = `3426a2d04d4698d0a55f32ec82488f31a65bf12b`，分支 `feat/tp-06-menu-expansion`（经 `.git/HEAD` + `refs/heads/feat/tp-06-menu-expansion` 锚定，与任务卡口径一致）
- 审查日期：2026-09-11；审查者：fm-reviewer（工具级只读）
- 依据：T-P10-技术方案（已批准，D-1~D-8 按推荐）/ T-P10 任务卡（AC 锁定版）/ PRODUCT-CONFIRMATION.md C-7（PD-017 修订版）/ T-P10-dev-2026-09-11.md（仅作线索）
- 落盘说明：审查会话无本地文件写入工具，报告全文由 fm-reviewer 产出、主控原样落盘至本路径。

## 0. 方法学声明与限制（必读）

1. 本会话无 shell 工具：**git diff/diff --stat 无法执行**；替代方法 = ①基线锚点静态对照（方案/任务卡记录的行号、断言内容、文案原文逐一对回工作区）②全仓新标识符扫描（`unmetReasons|MustUseUnmetReason|unsatisfiableReasons|UnmetReason`，覆盖 ts/tsx/prisma/sql/json/md/html/cjs）③dev 回归数字算术交叉验证。远程 GitHub 严重落后（main=bc8d9ff@2026-08-12，feat 分支未推送）无法取基线做远程 diff。
2. **未经独立复跑清单**：shared build → engine build → pnpm test 389/389、pnpm test:taboo 89/89、tp01 54 PASS、h5 typecheck+build exit 0、AC7 端口零遗留——均为 dev 自报。本会话无 shell，复跑在物理上不可执行。
3. **未经 diff 验证清单**：不含新标识符的空白级/注释级改动无法排查；「既有零改动」结论为静态锚点核对而非逐字节比对。风险评级：低（标识符扫描命中面与 dev 自报清单完全吻合，见 §6）。

## 1. 审查重点 1：feasibility.ts 归类逻辑正确性 —— 通过

**实现与方案 §3.1 一致**（feasibility.ts L59-L116）：
- 循环内 missing 器具仅提前计算不提前判定（L61-68）；时长过滤 push 后 `if (missing.size === 0) addWideIngredientIds(menu)`（L78-80）——**超时但器具齐全的菜单进宽集**，这正是 D-2 口径（宽集=通过器具过滤且不限时长的菜单食材并集）的运行时落实；器具过滤 push（L85-92）不进宽集；可达菜单先 addWide 再 push（L94-95）。
- unsatisfiable push 的条件/去重/顺序原样（L108-116），同一循环内同步写入 `unsatisfiableReasons[id] = wideIngredientIds.has(id) ? 'TIME_BUDGET' : 'NO_DISH'` —— 两字段键集恒等由构造保证（E5/E7 断言覆盖）。
- 第二轮 mustUse 硬过滤（L119-135）与返回形状（L137）原样，`unsatisfiable: string[]` 判定、形状、顺序零变化。
- **语义推演复核**：对 unsatisfiable 食材 X（含 X 的菜单全部出局），其中器具齐全者只能因超时出局→必经 L78-80 进宽集→TIME_BUDGET；器具不齐全者（无论是否同时超时）不进宽集→NO_DISH。与 E3（缺器具归 NO_DISH）、E4（双重失败归 NO_DISH）一致。
- **数据事实核验**：R2 依赖「含土豆 3 菜 totalActiveMinutes=35/38/40 全超 30min 档但器具齐全（equipment=["wok"]∈rules.equipment）」（方案 F6 引 T-P05 verify）→ 30min 档下土豆∈宽集→TIME_BUDGET ✓。库外词「苦瓜」：matcher 0 命中原文透传，宽集只含真实 ingredientId→NO_DISH ✓（与 R1 断言一致）。
- trace 顺序（时长先于器具、菜单按库序）未动，既有顺序断言 score.spec.ts L476 锚点原样。
- recommend.ts L53 仅一行透传；types.ts L140/L143-150 类型定义与方案 §3.2 一致（FeasibilityFilterResult 实际位于 feasibility.ts，系方案勘误，dev 披露属实——index.ts L7 证明基线即从 './feasibility.js' 导出）。

## 2. 审查重点 2：契约合规 —— 通过

- api.ts L193-L203：L202 `unmetReasons: z.record(z.string(), z.enum(['TIME_BUDGET', 'NO_DISH'])).optional()` 与方案 §3.3 diff **逐字一致**（zod 4 双参数语法）。
- v0.8 账本记 api.ts 头部 L13-L15，符合 D-7；dish.ts 头部仍为 v0.7、全文件无新标识符命中——未被本卡触碰。
- 向后兼容成立：optional 纯增量；空手不建 Plan/不写 Event（planService L429-L447 建库仅在 candidates 非空分支）→ 无 Plan 快照回填问题（PD-001）；旧 h5 对多余响应键无感知；新 h5 + 旧 API 由 h5 防御分支兜底（见 §4）。
- 范围说明：`packages/shared/test/schemas.spec.ts` 不在任务卡允许范围字面清单内，但 AC2 验证方法列明示「shared schemas.spec S1-S4」且方案 §6.2 明确要求——**视为卡内授权**（任务卡允许清单遗漏，非 dev 越界）。
- 保留一项偏差见问题 P2（C-7a 时字段非缺省）。

## 3. 审查重点 3：透传链 —— 通过

- planService L413-L427：空手分支内 `unmetReasons[mustUseResolved.idToRaw.get(id) ?? id] = reason`（L418）与 `unmetMustUse` 回译（L422-424）使用**同一 idToRaw 映射**，同源同序、键集一一对应由构造保证；`unmetMustUse` 生成逻辑一行未动。
- routes/recommend.ts L24-L28：显式三元条件展开透传（规避 zod parse strip），planId 逻辑（L30）原样。
- 建 Plan 分支（L429-447）与全换分支（L489-534，T-P05 D2 修复基线）未触碰——全文件无其他新标识符命中。

## 4. 审查重点 4：h5 渲染 —— 通过

- tonight/index.tsx L28-L37 `classifyUnmet` 纯判定：缺省算 NO_DISH（防御分支，同仓同发不可达、旧后端组合兜底=现状渲染）。
- L206-L220：emptyKind 仅在 `emptyReason.length > 0` 时才调 classifyUnmet；`emptyMin = tonightContext.timeBudgetMin`（L213）符合 D-6（用户当次所选档位）；两型素材拆分（L214-220）与防御口径一致。
- 渲染 L246-L279：`length === 0` 分支（L255-259 composeEmptyTitle / DEC-012 兜底）**原样**；时间型标题/正文（L250/L266）、混合型正文（L269）与确认书 C-7（L174/L175）及方案 §3.5 逐字一致（确认书『』为文档惯例，代码落「」，方案 §3.5 已声明该口径）；C-7a 正文（L275）逐字不变。
- 按钮组回调（L168-179）除清新 state 外未变；state 清理路径（L126-135 空手 set / 正常清 null）闭环。
- h5 types L96-L113 本地 UnmetReason 类型 + optional 字段，与契约对齐。

## 5. 审查重点 5：测试质量 —— 通过（回归数字为 dev 自报，未复跑）

- **E1-E7**（feasibility.spec.ts，新增）：全部 toEqual 精确断言、trace 逐字断言、E6/E7 含「超时 trace 先于必消未消耗 trace」顺序断言——断言有牙齿。fixture 前置核实（fixtures/index.ts）：MENU_LAWEI 40min 器具齐全含 ing-pork-belly、MENU_PLAIN_RICE 30min、FAMILY_RULE.equipment 三件齐全，E3「缺少器具：steamer、rice_cooker」断言与前置自洽。
- **S1-S4**（schemas.spec.ts L640-L728，追加）：带值两枚举原样通过 / 缺省向后兼容 v0.7 / 非法枚举拒（'WEEKEND'/'no_dish'）/ 非对象拒（数组、字符串）——正反两侧覆盖；既有 v0.3 unmetMustUse 正反用例（L649-668）原样。
- **R1-R4**（tp01-mustuse-regression.cjs L400-L528，追加）：JSON.stringify 精确比对（unmetMustUse 逐字现状值 + unmetReasons 键值）+ Plan/Event 落库零增量（真实 PG COUNT 前后对照）+ 键缺省断言（`!('unmetReasons' in data)`，验证正常推荐不携带）；全部经真实 HTTP（Fastify :3000 + cookie 鉴权）与真实 PG 直查，**无 Mock**。R1/R2 覆盖 NO_DISH/TIME_BUDGET 两型 + idToRaw 原文回译，R3 为 V2b 出路全链路对照。
- **计数自洽交叉验证**：tp01 段头「既有 17 断言」（L251）+ T-P05 新增 18 =「既有 35」（L400 段头）+ R1-R4 新增 19 = 54 = dev 自报 54 PASS；回归 389 = 378（基线）+ 7（E1-E7）+ 4（S1-S4）✓；taboo 89 计数不变 ✓。
- **既有测试零改动**：静态锚点核对通过（score.spec L461-477、schemas.spec v0.3 用例、tp01 既有段结构完整）；L424/L468 引号修复系脚本语法修复非断言弱化。**非逐字节 diff 验证**（见 §0）。

## 6. 审查重点 6：越界检查 —— 通过（1 项已披露的清单外改动降级为建议）

- 全仓标识符扫描命中 18 文件 = **12 代码/测试**（engine×5、shared×2、api×2、h5×2、tp01×1）+ **2 UI 基准**（e-final.html、design-spec.md）+ **4 文档**（任务卡、技术方案、PRODUCT-DECISIONS、dev 报告）。**无清单外代码文件**。
- 12 个代码文件中：11 个在任务卡允许范围（L26-34）字面内；`packages/engine/src/index.ts` 清单外（+1 行导出，dev 报告 §4 已披露）→ 问题 P1；`schemas.spec.ts` 由 AC2 验证方法授权（见 §2）。
- 禁改区核实：schema.prisma 对 `unmet|TIME_BUDGET|NO_DISH` **零命中**；prisma 目录无新 migration（最新 20260910000000 为 T-P09 期产物）；engine 评分/多样化/换菜逻辑、屏⑥既有文案、migrate/抓取脚本均无新标识符命中。
- 主控权限内改动核实：PRODUCT-CONFIRMATION.md C-7 节（L168-180）按附录 A 替换、C-7a（L181-187）原样；PRODUCT-DECISIONS.md PD-017 已登记（L25/L124）。
- 局限：不含新标识符的空白/注释级改动无法排查（§0 第 3 条）。

## 7. 审查重点 7：UI 基准同步 —— 通过

- e-final.html L584-L597 时间型帧（变体 A2）：标题「能用到「土豆丝」的菜，今晚 30 分钟内排不下」、正文与 h5 L250/L266 模板逐字一致；L600-L613 混合型帧（变体 A3）与 h5 L269 一致。
- 既有帧原样：A1 无菜型（L573-581）现状文案、B C-7a 帧（L616-628）按钮组与正文均未动。
- design-spec.md L103 s6 行已按确认书驱动同步（T-P10/PD-017 两变体描述），其余屏未涉及。

## 8. 审查重点 8：dev 报告披露项复核 —— 8/8 属实且处置合理

| # | 披露项 | 复核结论 |
|---|---|---|
| 1 | FeasibilityFilterResult 就地修改（方案称位于 types.ts 系勘误） | 属实：index.ts L7 证明基线即从 feasibility.js 导出；就地修改合理 |
| 2 | 引号口径（代码「」/文档『』） | 与方案 §3.5 声明一致，h5/tp01/e-final 落地一致 |
| 3 | tp01 L424/L468 首跑引号笔误 SyntaxError | 现为合法双引号字符串，系脚本修复非测试预期弱化 |
| 4 | PG 未启动致首轮 3 失败、重启后复跑 | 环境问题；最终 54 PASS 为自报未复跑（§0） |
| 5 | teardown 白名单 8 Plan + 10 Event | **与脚本结构精确吻合**：建 Plan 调用恰 8 处（AC1-a/AC1-b/AC2-a/T-P05 小番茄/土豆丝/全换#0/R3/R4；AC1-c 走空手分支不建）、Event = 8 GENERATE + 2 SWAP（全换#1/#2）= 10；REL=29 不动自报 |
| 6 | netstat → Get-NetTCPConnection | 环境工具替换，AC7 性质不变（自报未复跑） |
| 7 | API dev 进程提前退出后重启 | 环境问题；tp01 复跑覆盖该路径 |
| 8 | REL 脚本枚举笔误修复 | 脚本调用修复，非断言弱化（自报未复跑） |

## 9. 问题清单（0 阻塞 / 3 建议）

### P1（建议）：engine/src/index.ts 清单外 +1 行导出
- 证据：index.ts L25 新增 `MustUseUnmetReason` 导出；任务卡允许范围（L28）未列 index.ts；dev 报告 §4 已披露。
- 影响：无行为影响（apps/api planService L416 用字面量联合类型，未消费该导出；属新增公开类型的标准伴随导出）。
- 最小修复：主控在任务卡补登该项范围变更并说明理由（纯文档动作，无需改码）。

### P2（建议）：C-7a 场景响应携带 `unmetReasons: {}` 而非契约注释宣称的「缺省」
- 证据链：C-7a 时引擎 `unsatisfiable=[]`（tp01 AC1-c 注释 L168-169、方案 §3.4 风险 3）→ candidates=[] 触发 planService L413-L427 空手分支 → L416-419 构造空对象 → L425 返回 → routes L27 `{}` 为 truthy 展开进响应（同理 `unmetMustUse: []`，L26，系基线既有行为）。与 api.ts L199-L200「仅在空手且 unmetMustUse 非空时由服务端携带；正常推荐与组合凑不进（C-7a）时缺省」及方案 §3.4 L145「此时字段必然缺省」不符。
- 根因归属：**方案自身矛盾**——§3.4 L133-134 实现指引（无条件构造 + truthy 展开）必然产生该行为，dev 忠实实现；非 dev 实现错误。
- 影响评估：**无行为缺陷**——h5 C-7a 分支（emptyReason.length===0）先判定、不读该字段；契约合法（z.record().optional() 接受空对象）；旧客户端无感知；R3/R4 仅对正常推荐断言键缺省（正常推荐时服务不返回该字段，断言仍真）。
- 最小修复（二选一，主控定）：(a) 修 api.ts v0.8 注释措辞（如「C-7a 时为空对象，消费方以 emptyReason 优先判定」）——注释级改动但仍属 shared 契约文件，须走主控流程；(b) routes L27 增加空对象不携带判断。建议优先 (a) 或记挂账。

### P3（微瑕/建议）：recommend.ts 文件头注释未同步
- 证据：recommend.ts L2 仍写「输出 {candidates, filtered, unsatisfiableMustUse}」。
- 影响：纯文档，误导后续读者。
- 最小修复：注释补列 `unsatisfiableMustUseReasons`（交 dev 随下卡或主控批准后顺手改，不属本审范围）。

## 10. AC 对照意见（AC1-AC7）

| AC | 结论 | 依据 |
|---|---|---|
| AC1 引擎归类（宽集 D-2、空手判定零变化、C-7a 不混淆） | ✓ | §1 全部证据；E1-E7；score.spec 既有顺序断言锚点原样 |
| AC2 契约 v0.8 | ✓ | §2；S1-S4；dish.ts 未触碰；账本落点正确 |
| AC3 透传（unmetMustUse 语义不变、idToRaw 同源、显式透传） | ✓ | §3；tp01 既有 35 断言结构性原样 |
| AC4 屏⑥三态（档位分钟数、出路暗示、既有文案逐字不变、防御分支） | ✓ | §4；文案逐字对照确认书 C-7/C-7a |
| AC5 确认书先行 | ✓（主控已完成） | 确认书 C-7 修订 + PRODUCT-DECISIONS PD-017 登记 |
| AC6 全套回归绿 + teardown 回基线 | △ dev 自报绿 | 389/389、89/89、54 PASS、exit 0 均未独立复跑（§0）；算术自洽 + teardown 8/10 结构吻合为间接佐证；**最终独立确认依赖 fm-verify（V1-V4 真机/CDP）** |
| AC7 端口零遗留 | △ dev 自报 | Get-NetTCPConnection 记录未复跑 |

## 11. 最终结论：**通过**

理由：审查重点 1-5、7、8 全部通过——归类逻辑与批准方案逐条吻合、契约与方案 §3.3 逐字一致、透传链同源同序、h5 三态与文案逐字对齐确认书、测试断言有牙齿且真实无 Mock、UI 基准同步、dev 披露 8/8 属实；越界检查发现面干净（1 项已披露的 +1 行导出降级为建议 P1）。3 项建议级问题均不阻塞合并：P1 为范围登记动作，P2 为方案自身矛盾的文档级偏差（无行为缺陷），P3 为注释微瑕。保留声明：AC6/AC7 的「绿」以 dev 自报 + 算术交叉验证为限，未经本审查独立复跑（工具限制，§0）；合并前建议主控处置 P1，P2/P3 可随卡修正或挂账。
