# T-P12 开发完成报告（fm-dev）

> 日期：2026-09-12　角色：开发 DEV　任务卡：[docs/workflow/tasks/T-P12-反馈空态200响应null.md](../docs/workflow/tasks/T-P12-反馈空态200响应null.md)
> 结论：**7/7 AC 全部 [✓]**，任务卡状态已改「待复审」，待审版本固定在工作区（fm-dev 不执行 git commit）。
> 目标语义（契约 v0.9）：GET /api/plans/:id/feedback「plan 存在但无反馈」404 → **200 + JSON null**（响应体即 null 字面量）；「plan 不存在」保持 404；「有反馈」报文形状零变化；H5 零行为改动。

## §1 逐 AC 自检

| AC | 自检 | 证据要点 |
|---|---|---|
| AC1 契约 | **[✓]** | [api.ts](../packages/shared/src/schemas/api.ts) 头部账本追加 **v0.9** 段（L21-23，紧接 T-P11 收敛后的 v0.8，单一入口未分裂）；`FeedbackResponseSchema` JSDoc 同步 v0.9 语义（**schema 定义本身零变更**）。`pnpm --filter @family-menu/shared build` EXIT=0（改 src 后先重建 dist 再跑一切回归）。schemas.spec **不受影响、零改动**（git status 证实无 M）：v0.9 只改路由层空态语义与注释，schema 解析行为零变化；「无反馈 404/200」属路由语义，由 contract.spec 覆盖，schemas.spec（63 用例）本就不含该路由用例 |
| AC2 API 三态 | **[✓]** | 真实环境证据（非 Mock）：新增 [tests/tp12-feedback-null.cjs](../tests/tp12-feedback-null.cjs) 完整模式 **18 PASS / 0 FAIL，EXIT=0**（真实 HTTP + access_token cookie + PG 直查）：态1 无反馈 → 200 且 `body === 'null'` 严格 null 字面量；态2 有反馈 → 200 键集恰为 `['actualMinutes','didCook','submittedAt','taste','willRepeat']`（完整 payload），且旧事件（v0.5 payload 仅 actualMinutes）走 append-only 取最新 + optional 宽松缺省；态3 不存在 id → 404 |
| AC3 contract.spec | **[✓]** | 原「无反馈 404」用例改写为「无反馈 → 200 + null body」（断言 `statusCode===200`、`body==='null'`、`JSON.parse(body)===null`）；新增「plan 不存在 → 404」用例。52 → **53 用例**；`pnpm exec vitest run apps/api/test/contract.spec.ts`（根目录）EXIT=0，**53/53** |
| AC4 H5 零行为变更 | **[✓]** | [client.ts](../apps/h5/src/api/client.ts) 仅 getFeedback 前注释两行同步（v0.9 语义 + notFoundAsNull 兜底说明），`notFoundAsNull=true` 与返回类型 `FeedbackResponse \| null` 零变更，numstat +2/−1；`pnpm --filter @family-menu/h5 typecheck` EXIT=0，**0 错** |
| AC5 回归 | **[✓]** | 根 vitest **390/390 全绿**（`pnpm exec vitest run --no-file-parallelism` EXIT=0，= 基线 389 + 新增 1；默认并行模式 `pnpm test` 4 次均 389 passed + AC12 性能 1 failed，与本卡无关，见 §5 披露 1）；taboo `pnpm test:taboo` EXIT=0 **89/89（100%）**；tp01 `node tests/tp01-mustuse-regression.cjs` **54 PASS / 0 FAIL** EXIT=0；tp12 新脚本 **18 PASS / 0 FAIL** EXIT=0 |
| AC6 teardown | **[✓]** | tp12 自建 3 Plan/3 Event 按 `tp12-` 前缀白名单自清（residual=0），脚本内断言七表 before===after；另按先例清理了 tp01 回归残留 8 Plan/10 Event（§5 披露 3）；终态 `node tests/tp12-feedback-null.cjs baseline` EXIT=0 = `{"Dish":48,"Menu":13,"MenuDish":42,"Ingredient":79,"Plan":54,"Event":93,"CookLog":6,"REL":29}` 与绝对基线一致，**REL=29 未动**；`.pg/bin/pg_ctl.exe stop -m fast` EXIT=0，54329 端口监听数 = **0** |
| AC7 报告落盘 | **[✓]** | 本报告 [evidence/T-P12-dev-2026-09-12.md](./T-P12-dev-2026-09-12.md)；任务卡状态头已改「待复审」 |

## §2 改动清单与 numstat

`git --no-pager diff --numstat`（另有新增 2 文件不计入 numstat）：

| 变更 | 文件 | +/− | 说明 |
|---|---|---|---|
| M | packages/shared/src/schemas/api.ts | +6 −2 | 头部账本 v0.9 段 3 行注释 + FeedbackResponseSchema JSDoc 2 行改写；schema 定义零变更 |
| M | apps/api/src/services/planService.ts | +4 −4 | getFeedback 签名改 `Promise<FeedbackResponse \| null>`；无反馈 `return null`（删 NotFoundError throw）；plan 不存在 throw 保留；JSDoc 同步 |
| M | apps/api/src/routes/plans.ts | +6 −1 | GET /plans/:id/feedback：`feedback === null` → `reply.code(200).send(null)`；有反馈仍 `FeedbackResponseSchema.parse` |
| M | apps/api/test/contract.spec.ts | +16 −3 | 无反馈 404 用例改写 200+null；新增 plan 不存在 404 用例；describe 标题标注 v0.9/T-P12 |
| M | apps/h5/src/api/client.ts | +2 −1 | **仅注释**两行（逻辑零变更） |
| 新增 | tests/tp12-feedback-null.cjs | — | 三态真实环境回归脚本（纯测试，零业务逻辑；baseline/完整双模式；自清 + W1 防呆） |
| 新增 | evidence/T-P12-dev-2026-09-12.md | — | 本报告 |

合计 5 M + 2 新增，全部在任务卡「允许改动」8 类范围内；`git status` 证实 engine/**、h5 其余文件、prisma、shared 其余文件、PRODUCT-CONFIRMATION.md、e-final.html/design-spec.md **零触碰**。

注：git 输出 LF→CRLF 行尾 warning 为本机常规提示（T-P11 同），不影响 diff 内容与退出码。

## §4 测试证据（可复制命令 + 退出码 + 数字）

| 步骤 | 命令（根目录 d:\codex\family-menu） | 退出码 | 数字 |
|---|---|---|---|
| shared 重建 dist | `pnpm --filter @family-menu/shared build` | 0 | tsc 无错 |
| contract 单套件 | `pnpm exec vitest run apps/api/test/contract.spec.ts` | 0 | **53/53**（52+1 新增） |
| tp12 三态回归 | `node tests/tp12-feedback-null.cjs` | 0 | **18 PASS / 0 FAIL**（含自清+七表 before===after 断言） |
| tp01 回归 | `node tests/tp01-mustuse-regression.cjs` | 0 | **54 PASS / 0 FAIL** |
| 根全量（串行） | `pnpm exec vitest run --no-file-parallelism` | 0 | **390/390，15 files passed** |
| 根全量（默认并行，对照组） | `pnpm test` | 1 | 389 passed + 1 failed（恒为 AC12 性能，55.70~61.88ms 超 50ms 阈，4/4 次，见 §5 披露 1） |
| score.spec 单文件 | `pnpm exec vitest run packages/engine/test/score.spec.ts` | 0 | **51/51** |
| 禁忌集 | `pnpm test:taboo` | 0 | **89/89（100%）** |
| h5 类型检查 | `pnpm --filter @family-menu/h5 typecheck` | 0 | **0 错** |
| 终态基线核对 | `node tests/tp12-feedback-null.cjs baseline` | 0 | 七表+REL = 绝对基线 |
| tp01 残留清理 dry-run | `node .workflow-verify/tp12/cleanup-tp01.cjs` | 0 | 窗口 Plan=8/Event=10 全命中，W1 全过，未删 |
| tp01 残留清理实删 | `node .workflow-verify/tp12/cleanup-tp01.cjs go` | 0 | **DELETED Events=10 Plans=8**，AFTER 七表+REL 回基线 |
| PG 停机 | `./.pg/bin/pg_ctl.exe stop -D d:/codex/family-menu/.pg/data -m fast` | 0 | 「服务器进程已经关闭」 |
| 端口核对 | `Get-NetTCPConnection -LocalPort 54329 -State Listen` | 0 | 监听数 = **0** |

## §5 披露事项

1. **AC12 性能用例并行模式 flaky（挂账⑧，与本卡改动无关）**：默认并行 `pnpm test` 连续 4 次均为 389 passed + 「千套菜单 recommend < 50ms」1 failed（实测 58.84 / 55.70 / 61.88ms 等）；该用例单文件跑 51/51 绿、`--no-file-parallelism` 全量 390/390 绿。packages/engine 本卡零改动（git status 证实）。T-P11 披露 2 曾出现同例 53.639ms 失败、重跑即过，挂账⑧「AC12 并行模式 flaky」已存在；**差异在于当前机器并行负载下 4/4 必现**，建议主控将挂账⑧升级评估（如为该用例固定串行执行或调整阈值口径）。本卡以串行全量 390/390 + 单文件 51/51 + taboo 89/89 作为 AC5 证据，未删除/未改写该测试。
2. **tp12 脚本开发期两处自纠（均已修复并全量复跑）**：a) 首版事件 id 复用（态2-a 与态2-b 同 id）会主键冲突，改为 ev-full/ev-old/ev-new 三个独立 id，teardown 断言 2→3；b) Node v24.19.0/Windows 下 `process.exit()` 与 libuv 句柄关闭竞态致 18 PASS 仍伪崩溃退出码 0xC0000409，改为 `process.exitCode` 自然退出，复跑 EXIT=0。
3. **tp01 残留数据清理（复用 T-P11 teardown 先例）**：tp01 回归（真实 HTTP 建数据）产生 Plan 54→62（+8）、Event 93→103（+10）残留；因 PG 时钟偏移 8 小时（T-P11 披露 5）禁用时间窗口，改用 cuid 前缀 `cmtx` 圈窗 + W1 三重防呆（跑时总数必须=62/103；窗口行数必须=8/10；Event.planId ⊆ Plan 窗口）。dry-run 实证 8 Plan 全 PROPOSED 且 createdAt 同一秒批量、10 Event 交叉校验全过，后才实删 DELETED Events=10 Plans=8，七表+REL 回基线。清理脚本与输出在 `.workflow-verify/tp12/`（.gitignore 忽略目录，不入库）。
4. **dev server 生命周期**：AC2 真实环境证据采集完成后停止 tsx watch dev server；后续全量回归均在无 dev server 干扰下执行（§4 各行）。
5. **工作区存在非本卡 untracked 文件**（`.pai/`、`docs/design/preview.*`、`docs/ui-redesign/`、`tests/engine-perf.mjs`、`tests/step08-deploy-validate.cjs`、`tools/*.tar.gz`、`preview-server.mjs`、`append-log.mjs`、`复盘报告.md` 等）：本卡未触碰，原样移交主控处置。
6. **PowerShell 5 环境**：无 `-SkipHttpErrorCheck`（探活改 try/catch 读 StatusCode）；stderr 混排 CLIXML 编码影响日志可读性，不影响退出码判定。

## §6 未验证项

- 浏览器/CDP 侧 UI 回归不在本卡范围：H5 侧仅注释级改动且 `notFoundAsNull` 兜底行为不变（plan 不存在 404 仍转 null 防崩溃），feedback/history/dish 页消费 null 的分支均已存在（T-P04 起）。
- 默认并行模式下 AC12 用例在本机当前负载未能复现绿灯（4/4 超阈），以披露 1 的三重证据交主控/reviewer 裁定。

## §7 依赖声明

**新增依赖 0**：tp12 脚本经 `createRequire(apps/api/package.json)` 复用 apps/api 已有 `pg` 依赖；无任何 package.json 变更（git status 证实）；未引入 Redis/MQ/微服务/K8s；运行时代码零 LLM API 调用。

## §8 交接

待审版本已固定在工作区（未 commit，提交由主控按总授权执行）；建议 reviewer 复核点：①api.ts 账本 v0.9 段措辞与挂账⑤一致性；②routes null 透传与 `FeedbackResponseSchema.parse` 分支边界；③contract.spec 新 404 用例的 mock 边界（NotFoundError 映射）；④并行模式 AC12 flaky 处置（挂账⑧）。
