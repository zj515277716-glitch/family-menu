# T-P15 开发完成报告：DishSchema.imageUrl 站内相对路径 regex 收紧（挂账⑧）

> 报告人：fm-dev ｜ 日期：2026-09-12 ｜ 任务卡：docs/workflow/tasks/T-P15-imageUrl-regex收紧.md（唯一任务来源）
> 基线 git HEAD：`7472614`（T-P14 提交后），改动前工作区无已跟踪文件修改（git status --porcelain 仅历史遗留 untracked，非本卡产物）。

## 一、改动摘要（git diff --stat 实测 3 files, +33/-1）

| 文件 | 变化 | 内容 |
|---|---|---|
| packages/shared/src/schemas/dish.ts | +2/−1 | L52 注释区追加 v0.10 行；L54 regex `/^\/images\//` → `/^\/images\/dishes\/[A-Za-z0-9]+\/\d+\.(webp|jpg|png|gif)$/`（逐字符与卡面定稿一致）；union（URL 分支）与 optional 结构不变 |
| packages/shared/src/schemas/api.ts | +4 | 头部账本 v0.9 行之后、import 之前追加 v0.10 行（含拍板差异备注）；既有 v0.2~v0.9 行零改动 |
| packages/shared/test/schemas.spec.ts | +27 | 新增 1 个 it（T-P15 v0.10 收紧用例，11 组断言，覆盖 AC3 全部列明项） |

- 禁区核对（git status --porcelain 非untracked 部分仅上述 3 文件）：schema.prisma / apps/api/** / apps/h5/** / packages/engine/** / fetch2dish.mjs / content-pipeline src/** 均零改动；**新增依赖 0**。
- 遵循 T-P09 教训：改 shared src 后先 `pnpm --filter @family-menu/shared build` 重建 dist（EXIT=0）再跑回归。

## 二、逐条 AC 自检

### AC1 dish.ts regex 替换 + v0.10 注释 — [✓]
- L54 新值（逐字符）：`/^\/images\/dishes\/[A-Za-z0-9]+\/\d+\.(webp|jpg|png|gif)$/`，与卡面「定稿口径」一致。
- L52 注释区追加：`// v0.10（2026-09-12，T-P15，挂账⑧，用户已批准）：站内分支收紧为 ...；拍板：不含 jpeg（normalizeImageExt 归一为 jpg）、保留 gif`。
- union 结构（`z.string().url()` 分支不动）与 `.optional()` 不变。
- shared tsc 0 错：`pnpm --filter @family-menu/shared build`（即 `tsc -p tsconfig.json`）→ **BUILD_EXIT=0**。
- 同款 JS regex 在 AC5 对 DB 29 行 node 端复验 29/29（见 §四）。

### AC2 api.ts 头部账本追加 v0.10 — [✓]
- 追加 4 行（v0.10，2026-09-12，T-P15，挂账⑧，用户已批准；含拍板差异一句备注与 29 行 DB 符合说明）。
- 既有 v0.2~v0.9 行零改动（diff 仅 +4 行，插入点在 v0.9 行后、`import { z } from 'zod';` 前）。

### AC3 shared 新增收紧用例 + 存量用例值核对 — [✓]
新增 it：`DishSchema imageUrl 站内分支收紧为内容管线落盘结构（T-P15 v0.10）`，断言覆盖：

| 覆盖项 | 用例值 | 期望 | 实测 |
|---|---|---|---|
| webp 合法 | /images/dishes/6a55c68e000000001c025017/0.webp | 通过 | 通过 |
| jpg 合法 | /images/dishes/6a55c68e000000001c025017/0.jpg | 通过 | 通过 |
| png 合法（非 0 序号 12） | /images/dishes/6a55c68e000000001c025017/12.png | 通过 | 通过 |
| gif 合法（保留 gif 拍板） | /images/dishes/6a55c68e000000001c025017/3.gif | 通过 | 通过 |
| /images/ 泛前缀拒绝 | /images/abc.jpg | 拒绝 | 拒绝 |
| .jpeg 拒绝（拍板差异 1） | /images/dishes/6a55c68e000000001c025017/0.jpeg | 拒绝 | 拒绝 |
| .svg 拒绝 | /images/dishes/6a55c68e000000001c025017/0.svg | 拒绝 | 拒绝 |
| 无扩展名拒绝 | /images/dishes/6a55c68e000000001c025017/0 | 拒绝 | 拒绝 |
| 目录结构不符（无文件名） | /images/dishes/ | 拒绝 | 拒绝 |
| 目录结构不符（无文件名） | /images/dishes/abc/ | 拒绝 | 拒绝 |
| 大写扩展名拒绝（硬编码小写） | /images/dishes/6a55c68e000000001c025017/0.WEBP | 拒绝 | 拒绝 |

存量 T-P09 用例值逐一核对结果（**全部无语义冲突，未动任何存量值，无需停下报主控**）：
- packages/shared/test/schemas.spec.ts：L247 `/images/dishes/6a55c68e000000001c025017/0.webp`（新 regex 下仍合规，不动）；L257 `foo/bar.jpg`、L261 `images/dishes/abc/0.webp`、L264 `''`（新 regex 下仍拒绝，与用例期望一致）。
- tools/content-pipeline/test/import.spec.ts：L109 合规相对路径、L117 绝对 URL（union URL 分支）、L123/L126/L131 拒绝值——均无冲突。
- tools/content-pipeline/test/fetch2dish.spec.ts：L171/L178 `/images/dishes/6a55c68e000000001c025017/0.webp`、L184 `/images/dishes/6a55c68e000000001c025017/2.png`——产出值均合规。
- 结论：content-pipeline test/** 无冲突，按卡面约束**未动**。

### AC4 全量回归 — [✓]（数字算术自洽；含 1 项卡外发现，见 §五）

| # | 命令 | cwd | 结果 | 退出码 |
|---|---|---|---|---|
| 1 | `pnpm test`（改动前基线，13:00:20） | 仓库根 | 15 files 全过，**387 passed \| 3 skipped (390)** | 0 |
| 2 | `pnpm --filter @family-menu/shared build` | 仓库根 | tsc 0 错 | 0 |
| 3 | `pnpm test`（改动后，PG 未启动，13:02:48） | 仓库根 | 15 files 全过，**388 passed \| 3 skipped (391)**（=390+1 新用例） | 0 |
| 4 | `pnpm test:taboo`（13:03:01） | 仓库根 | **89 passed (89)** | 0 |
| 5 | `pnpm typecheck`（h5） | apps/h5 | 0 错 | 0 |
| 6 | `npx tsc -p tsconfig.json --noEmit`（api） | apps/api | 0 错 | 0 |
| 7 | `pnpm --filter @family-menu/content-pipeline test`（原 script） | 仓库根 | 1 failed（AC12 性能 65ms>50ms，连续 3 次；**与本卡无关，根因见 §五卡外发现 1**） | 1 |
| 8 | `npx vitest run --root ../.. --no-file-parallelism`（=第 7 项命令 + 挂账⑥串行参数，13:06:00） | tools/content-pipeline | 15 files 全过，**388 passed \| 3 skipped (391)** | 0 |
| 9 | `npx vitest run packages/engine/test/score.spec.ts`（单跑佐证） | 仓库根 | **51 passed (51)** | 0 |
| 10 | `pnpm test`（PG 常驻，13:13:30） | 仓库根 | 15 files 全过，**391 passed (391)，0 skipped**（对齐 T-P14 基线 390/390 0 skipped 口径 +1） | 0 |

- 数字自洽链：基线 390（387+3skip）→ 本卡 +1 用例 → 391；PG 常驻时 3 条 projection-order DB 用例真实执行 → 391/391 0 skipped。
- 「content-pipeline 测试全绿」以第 8 项等效命令达成：`vitest run --root ../..` 与根 `pnpm test` 同一 vitest 配置同一 include scope（T-R8 时 R-8 修复后的既定口径，T-P09 报告两者数字亦一致），显式补挂账⑥串行参数后 15 files / 391 用例全绿。

### AC5 DB 只读复核 — [✓]
- 环境：便携 PG 启动命令严格按卡面 `& "d:\codex\family-menu\.pg\bin\pg_ctl.exe" -D "d:\codex\family-menu\.pg\data" -o "-p 54329" -l "d:\codex\family-menu\.pg\pg-tp15dev.log" start`（启动 EXIT=0，输出「服务器进程已经启动」）；用后 `pg_ctl -m fast stop`（**PG_STOP_EXIT=0**，输出「服务器进程已经关闭」）。
- 便携包无 psql（.pg/bin 仅 initdb/pg_ctl/postgres），采用 node-pg 只读直查（连接串 `postgresql://postgres@127.0.0.1:54329/family_menu`，与 T-P12/T-P14 teardown 同技术路径；纯 SELECT 零写入；临时脚本放系统 TEMP，复核后已删除，仓库零残留）。
- SQL（PG ARE 正则）：`SELECT count(*) FROM "Dish" WHERE origin='FETCHED' AND "imageUrl" IS NOT NULL AND "imageUrl" <> ''` 及 `~ '^/images/dishes/[A-Za-z0-9]+/\d+\.(webp|jpg|png|gif)$'`（另以 `[0-9]+` 形式双写复核）。
- **真实输出**：

```
=== T-P15 AC5 DB 只读复核（origin=FETCHED 且 imageUrl 非空） ===
total=29
pg_regex_matched(\d 形式)=29
pg_regex_matched([0-9] 形式)=29
pg_regex_unmatched(应=0)=0
js_regex_matched(dish.ts 同款 regex node 端复验)=29/29
--- 逐行 imageUrl ---
 1  cmtvk1w3v0000rwpgkoxz982k  /images/dishes/6a8db1e5000000002003a91e/0.webp  MATCH
 2  cmtvk25jb0000ecpgwd2xd34x  /images/dishes/6a991d0e0000000028030267/0.webp  MATCH
 3  cmtvk2erk0000nopg3y2tqytq  /images/dishes/6a7447180000000026035e3b/0.webp  MATCH
 4  cmtvk2nya00008wpgf52iawwe  /images/dishes/6a93dc1d0000000025011682/0.webp  MATCH
 5  cmtvk2x4a00004wpgsyw1nn5h  /images/dishes/6a7462a20000000028005994/0.webp  MATCH
 6  cmtvk36bd0000bcpg4tt5pnqg  /images/dishes/6a1791dd0000000007013aee/0.webp  MATCH
 7  cmtvkmre4000020pglsqyzak8  /images/dishes/6a85799b000000002c002ec3/0.webp  MATCH
 8  cmtvkn13r0000t8pgcvn9cvps  /images/dishes/6a96829f000000000d027cb6/0.webp  MATCH
 9  cmtvkna8x0000kgpgmf063jp1  /images/dishes/6a058210000000003601f1f3/0.webp  MATCH
10  cmtvknjcj0000qgpg2ztg8496  /images/dishes/69e2e4bd000000001b021d0c/0.webp  MATCH
11  cmtvknshw00003gpgifiy8jgz  /images/dishes/69fff5c5000000003503949f/0.webp  MATCH
12  cmtvko1oo00006kpg2urdb3j8  /images/dishes/6a29263a000000000602328a/0.webp  MATCH
13  cmtvkoazs0000vwpg3agr82s4  /images/dishes/6a4f38a1000000001702a9db/0.webp  MATCH
14  cmtvkojz30000okpg4jailat6  /images/dishes/6a00b02800000000070118dd/0.webp  MATCH
15  cmtvkot7s0000y0pg66z2iznc  /images/dishes/69f87290000000001e00c1f1/0.webp  MATCH
16  cmtvkp2co000000pg5mn2s2wk  /images/dishes/69f07eef0000000035032f69/0.webp  MATCH
17  cmtvkpbrz0000dopga1ul1r6b  /images/dishes/6a3e0d23000000001603c627/0.webp  MATCH
18  cmtvkpl4b0000jgpg27raohxx  /images/dishes/692c2fd6000000001d0384f4/0.webp  MATCH
19  cmtvkpuc60000l8pgr50dhf84  /images/dishes/6a6decca000000002803202c/0.webp  MATCH
20  cmtvkq3dl00009cpgaeksvumo  /images/dishes/6a351368000000000803feb1/0.webp  MATCH
21  cmtvkqcem0000nspgnvqhzv53  /images/dishes/69f20f58000000003502e70a/0.webp  MATCH
22  cmtvkqlfh000080pggqq7f7p0  /images/dishes/6825e64e0000000020029573/0.webp  MATCH
23  cmtvkquqc000094pg710cahp6  /images/dishes/68e77aa9000000000700aa45/0.webp  MATCH
24  cmtvkr49o0000m0pgj3uz5zza  /images/dishes/6a3b4498000000002102050f/0.webp  MATCH
25  cmtvkrdc20000tcpg4ds0hi0x  /images/dishes/6a730d58000000000503082a/0.webp  MATCH
26  cmtvkrmcq00008kpg19e5i4f6  /images/dishes/6971cc9a000000000a031ff2/0.webp  MATCH
27  cmtvkrvam0000qspgccoq5axn  /images/dishes/683dd06e00000000120062de/0.webp  MATCH
28  cmtvku5bj0000rkpg5f154fpo  /images/dishes/6a265036000000003502cac4/0.webp  MATCH
29  cmtvkuemd0000ocpgt4uvq0eo  /images/dishes/6937ccf4000000001f005924/0.webp  MATCH
AC5_RESULT=PASS (29/29)   （脚本进程 EXIT=0）
```

- 29 行全部为 `.webp` 扩展名（DB 无 .jpeg/.gif 实例，与拍板差异依据一致）。

### AC6 完成报告落盘 — [✓]
- 本文件即 `evidence/T-P15-dev-2026-09-12.md`：影响面核对（§一/§三）、AC3 用例清单与结果（§AC3）、AC5 复核真实输出（§AC5）、全部测试命令+退出码+数字（§AC4）齐备。

## 三、影响面核对（复核卡面「影响面」节，实测与卡面一致）

- 唯一校验点 dish.ts L54；消费面：
  - tools/content-pipeline：import.ts（DraftFileSchema=DishSchema.extend）、draft.ts（DishSchema.parse）——收紧后不合规路径在入库/起草被拒（本卡目的）；其测试经 §AC4 第 8 项全绿，无存量用例冲突。
  - schemas.spec.ts / import.spec.ts / fetch2dish.spec.ts 存量值全部合规（逐值核对见 §AC3）。
- 零影响面复核：apps/api（mappers.ts L171 仅透传；projection-order.spec 用绝对 URL 走 union URL 分支，PG 常驻下 5 用例全过——见 §AC4 第 10 项 391/391）；apps/h5（typecheck 0 错，仅渲染 toAbsoluteImageUrl）；schema.prisma（String? 无 zod，未动）。

## 四、阻塞点

无。全程未触发卡面「停下报主控」情形（存量用例/DB 行与新 regex 零冲突）。

## 五、卡外发现（不越界处理，移交主控）

1. **content-pipeline test script 未继承挂账⑥串行修复（建议随挂账⑥收尾补齐）**：`tools/content-pipeline/package.json` 的 test script 为 `vitest run --root ../..`（无 `--no-file-parallelism`）。本卡实测其文件并行模式下 AC12（packages/engine/test/score.spec.ts「千套菜单 recommend < 50ms」）连续 3 次稳定失败（65.93/65.10/65.10ms > 50ms），单跑该文件 51/51 全过、根 `pnpm test`（T-P13 已加串行参数）稳定全过——根因为文件并行 CPU 竞争，与本卡改动（shared schema 纯函数）零交集。本卡禁改清单不含但允许改清单亦不含该 package.json，**未动**；证据见 §AC4 第 7/8/9 项（显式加参后同 scope 391 用例全绿）。
2. **shared 包单独 test 命令不可用（历史既有，非本卡引入）**：`pnpm --filter @family-menu/shared test` 报 `No test files found`（其 vitest include 模式 `packages/*/test/**` 相对仓库根解析，子包目录下找不到文件）。shared 用例实际由根 `pnpm test` 覆盖（与本卡无关，仅记录）。
3. **环境观察**：`pg_ctl start` 命令包装层在服务已启动后仍显示 running（PowerShell 句柄未随 spawn 释放的假象）；实际端口 54329 立即可连、stop 正常，不影响使用。`.pg/data/postgresql.conf` 的 `timezone='UTC'` 固化（T-P14 方案 D）本次启动沿用有效。

## 六、未验证项

- 公网 RDS（生产库）未复核 29 行等价性——本卡范围仅本机库；公网发布前随挂账①「公网部署清单」处理（迁移/复核脚本跑 RDS）。
- fm-reviewer 独立复审与 fm-verify 产品验收未执行（本报告仅为 dev 环节交付，待主控派发）。
