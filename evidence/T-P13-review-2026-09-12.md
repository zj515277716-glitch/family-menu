# T-P13 技术审查报告（fm-reviewer · 工具级只读，主控代落盘）

> 落盘说明：reviewer 为工具级只读角色，本文件由主控按其返回报告原文结构化落盘（2026-09-12）。审查对象：工作区未提交态，HEAD=`319327f`（分支 feat/tp-06-menu-expansion）。reviewer 无 shell 工具，git 命令与测试复跑不可行，采用依赖源码实证 + mtime 排序旁证 + 三方证据链交叉核对替代，未验证项如实标注并移交 verify。

## 总判定：通过

无阻塞（C）项。dev 自检 7/7 AC 与独立核实一致；核心事实（⑥ 标志有效性、⑦ 根因与「功能无影响」、S-4 注释语义、边界合规）均获依赖源码级或产物级独立实证，未依赖 dev 自述。2 条 S 级记录在案，测试数字与精确 numstat 未独立复跑，移交 fm-verify 按清单补证。

## 各审查点结论

1. **diff 行级核对——通过（附未验证项）**：package.json L10 直接实证现值 `"test": "vitest run --no-file-parallelism"`（改前值由 T-P12 verify 报告六-1 第三方 git 实证为 `vitest run`）；test:taboo（L11）与 verify（L14）均未动。两处注释新值直接读得（feedback/index.tsx L73、history/index.tsx L71），恰位于 T-P12 review S-4 记录行号——+1/−1 原位替换、行号不漂移。mtime 排序旁证：apps 源码序位头两名恰为 history/feedback 两页面，packages 下无源文件异动序位，根 package.json 最新、pnpm-lock.yaml 未被重写、vitest.config.ts 未触碰。**未验证**：精确 numstat +1/−1 与 untracked 清单（无 shell），移交 verify。
2. **⑥ 方案正确性——通过**：vitest@4.1.10 CLI 源码（cac.DdICfEr1.js L926）官方文档化 `--no-file-parallelism` 选项，非臆造参数；行为级双证（T-P12 verify 同参数 390/390 成功 + dev 两次连跑 5.35s/5.02s 串行特征 vs 并行基线 1.13s）。test:taboo 不动合理（taboo 648ms 短平快无 CPU 争抢场景）；verify L14 经 pnpm test 自动继承。更优落点不成立：vitest.config.ts 为禁改区且影响面更大（波及 taboo 及各包直跑）；package.json 落点与 T-P12 verify 六-1 建议一致。串行代价（墙钟 +4.2s）已披露。
3. **S-4 注释语义准确性——通过**：对照 api.ts L21-23 v0.9 账本 + L173-174 JSDoc、plans.ts L151-152 null 分支、client.ts L152 口径 + L49-51 `404 && notFoundAsNull → null` 封装 + L154 第三参 `true`，两处新注释三态表述逐项一致；两文件全文 Grep 旧 404 语义零残留；新行均为 `//` 注释行零行为改动。
4. **⑦ 评估结论——推荐成立，通过**：postcss-calc@9.0.1 transform.js L84-96 源码实证「解析失败 warn 后直接 return，不写入 node，原值保留」——「功能无影响」因果链为依赖确定性行为而非推测；构建产物 apps/h5/dist/css/app.e1d6750e4ede969614ff.css 内 `calc(...var(--nutui-` 模式计数恰 23，与 dev 存档 23 个唯一位置精确相等，`--nut-scale-f` 大量存在——警告源在库侧获证。选项 B（升级 NutUI：触发纪律 7 依赖变更须批准 + 全页 UI 回归成本，收益仅日志清洁）与 C（构建侧过滤：侵入禁改配置 + 吞掉未来真实错误信号）否决理由双重成立。挂账⑦「另立任务」口径兼容：本卡即评估落地形式，结论 A（零代码）→ 无需后续卡/批准，重评估触发条件已定义（升级 NutUI 顺带观察/警告升级为失败）。
5. **数字自洽——通过**：387 passed + 3 skipped = 390 成立；3 skip 逐一实证为 projection-order.spec.ts L106/116/194 `if (!dbReady) return skip('PG 不可达…')`（T-P11 既定口径）；数字链连续 T-P10 389 → T-P11 389 → T-P12 390 → T-P13 390（零新增）；taboo 89/89 三代一致；typecheck 0 错双重佐证（dev EXIT=0 + 审查 GetDiagnostics 全工作区 0 诊断）。
6. **边界合规——通过（untracked 清单未验证）**：新增依赖 0（pnpm-lock.yaml 未重写旁证 + package.json 依赖区零变化）；禁区无触碰迹象；dist 产物是否在 ignore 内移交 verify 甄别。
7. **AC1~AC7 核对**：dev 自检 [✓]×7 与独立证据一致（AC1/2/3 数字与 AC5 typecheck 由 verify 复跑终证；AC4/AC6/AC7 本审查已独立核实成立）。

## 问题分级

| 级别 | 编号 | 内容 | 主控裁量 |
|---|---|---|---|
| C（阻塞） | 无 | — | — |
| S（建议） | S-1 | dev 报告 §2.5 警告存档路径少写 `dist\` 一层（写作 `apps\h5\css\...`，实际在 `apps/h5/dist/css/`）；证据实质不受影响 | 采纳 reviewer 建议：dev 报告原文不改（历史报告不改先例），勘误记录于本证据 + 任务卡补登 |
| S（建议） | S-2 | 测试数字与精确 numstat 未由 reviewer 独立复跑（无 shell 工具） | 转 fm-verify 复跑清单补证 |

## 移交验收建议

**可直接转 fm-verify**，无需先补正。复跑清单：

1. `git --no-pager diff --numstat` → 恰 3 行各 `1\t1`（package.json、feedback/index.tsx、history/index.tsx）；`git status` 无新增 M，untracked 与 T-P12 verify 记录比对（h5 dist 产物若不在 ignore 内须甄别标注）。
2. `pnpm test` 连跑 2 次 → 均 EXIT=0、387 passed | 3 skipped (390)、Duration 约 5s 量级（串行特征）。
3. `pnpm test:taboo` → 89 passed (89)、EXIT=0。
4. `pnpm --filter @family-menu/h5 typecheck` → 0 错、EXIT=0。
5. （可选，S-1 勘误用）`pnpm --filter @family-menu/h5 build` → EXIT=0 警告复现，确认产物实际路径 `apps/h5/dist/css/`。
6. 终态确认 `git log -1` 仍为 319327f（主控提交前）。

## S-4 注释措辞复核结论

无需替换。两处注释三态语义逐项一致、无残留旧语义；措辞风格差异（箭头式 vs 括号补注）属风格非错误，按「不折腾」原则维持现状。
