# T-P14 技术审查报告：时区口径统一——timestamptz 迁移

> 审查人 fm-reviewer（工具级只读：无 shell、无 git、不执行测试，结论仅来自 Read/Grep/Glob 读码）；报告由主控代落盘。
> 审查对象：commit 前工作区待审版本 `M apps/api/prisma/schema.prisma`（+6/−6）+ 新增 `apps/api/prisma/migrations/20260912000000_timezone_timestamptz/migration.sql`（+ 环境级 `.pg/data/postgresql.conf`，不入库）。
> 审查日期：2026-09-12。**结论总判定：PASS-有条件**（核心交付全部核实通过；1 条建议级条件 S-1 须在 fm-verify 环节裁决，不影响迁移 SQL 与数据正确性）。

---

## 一、逐项审查结果（12 点）

### 审查点 1：schema.prisma 6 处 `@db.Timestamptz` —— [✓]

**证据**：
- 全文 `DateTime` 字段 Grep 恰好 6 处，全部带 `@db.Timestamptz`，位置与声称逐一对应：[schema.prisma L29](file:///d:/codex/family-menu/apps/api/prisma/schema.prisma#L29)（Family.createdAt）、[L40](file:///d:/codex/family-menu/apps/api/prisma/schema.prisma#L40)（FamilyRule.updatedAt）、[L178](file:///d:/codex/family-menu/apps/api/prisma/schema.prisma#L178)（CookLog.cookedAt）、[L193](file:///d:/codex/family-menu/apps/api/prisma/schema.prisma#L193)（Plan.planDate）、[L200](file:///d:/codex/family-menu/apps/api/prisma/schema.prisma#L200)（Plan.createdAt）、[L217](file:///d:/codex/family-menu/apps/api/prisma/schema.prisma#L217)（Event.createdAt）。
- 结构完整性：头注释「12 model + 8 enum」与读码实数吻合（model 12 个、enum 8 个），除 6 处注解外未见其他字段/关系/枚举改动痕迹。
- +6/−6 numstat 无 git 无法直验，但 6 处行内注解修改与 +6/−6 数学一致（静态核对）。

### 审查点 2：迁移 SQL USING 方向 —— [✓]

**证据**（[migration.sql](file:///d:/codex/family-menu/apps/api/prisma/migrations/20260912000000_timezone_timestamptz/migration.sql)）：
- 恰好 6 条 `ALTER ... TYPE TIMESTAMPTZ(3) USING ...`，方向与主控诊断基准**逐一吻合、零颠倒**：
  - [L30](file:///d:/codex/family-menu/apps/api/prisma/migrations/20260912000000_timezone_timestamptz/migration.sql#L30) Family.createdAt → `AT TIME ZONE 'UTC'` ✓
  - [L33](file:///d:/codex/family-menu/apps/api/prisma/migrations/20260912000000_timezone_timestamptz/migration.sql#L33) FamilyRule.updatedAt → `'UTC'` ✓
  - [L36](file:///d:/codex/family-menu/apps/api/prisma/migrations/20260912000000_timezone_timestamptz/migration.sql#L36) CookLog.cookedAt → `'Asia/Shanghai'` ✓（唯一本地钟面列）
  - [L39](file:///d:/codex/family-menu/apps/api/prisma/migrations/20260912000000_timezone_timestamptz/migration.sql#L39) Plan.planDate / [L42](file:///d:/codex/family-menu/apps/api/prisma/migrations/20260912000000_timezone_timestamptz/migration.sql#L42) Plan.createdAt / [L45](file:///d:/codex/family-menu/apps/api/prisma/migrations/20260912000000_timezone_timestamptz/migration.sql#L45) Event.createdAt → `'UTC'` ✓
- 钟面归属双重确认的读码侧独立复核成立：[seed-data.ts L6](file:///d:/codex/family-menu/apps/api/prisma/seed-data.ts#L6) `new Date('2026-08-06T00:00:00Z')`、[L13/L23](file:///d:/codex/family-menu/apps/api/prisma/seed-data.ts#L13) 两列显式赋值；[seed.ts L95/L110](file:///d:/codex/family-menu/apps/api/prisma/seed.ts#L95) create 分支显式传入、`update: {}`；[planService.ts L432](file:///d:/codex/family-menu/apps/api/src/services/planService.ts#L432) `planDate: new Date()`——Prisma 显式传 JS Date、驱动按 UTC 序列化，UTC 判定成立。
- PG 语义独立佐证：`AT TIME ZONE` 应用于无时区钟面即「按指定时区解释并产出 timestamptz」（PostgreSQL 官方文档 8.5/B.2 语义），USING 表达式类型与目标类型匹配，写法正确。
- 精度自洽：`TIMESTAMPTZ(3)` 与 Prisma `@db.Timestamptz` 默认精度（3，毫秒）一致；基线值均毫秒三位（如 `.747`），JS Date 毫秒精度无截断。✓
- 无本机路径/端口硬编码：SQL 全文读毕无路径/端口，[L27](file:///d:/codex/family-menu/apps/api/prisma/migrations/20260912000000_timezone_timestamptz/migration.sql#L27) 明示可移植性。✓
- 头部注释含每列钟面归属依据（[L6-L27](file:///d:/codex/family-menu/apps/api/prisma/migrations/20260912000000_timezone_timestamptz/migration.sql#L6-L27)：写入路径读码 + 基线库直查双重确认，且每条 ALTER 上方有单列依据注释）。✓

### 审查点 3：dev 报告 16 节逐节核对 —— [✓]（1 处措辞瑕疵见 S-2）

**证据**（[T-P14-dev-2026-09-12.md](file:///d:/codex/family-menu/evidence/T-P14-dev-2026-09-12.md)）：§0 勘误与任务卡 L20 呼应一致；§1 改动摘要与实测一致；§2 conf 处置与文件实核一致（见审查点 6）；§3.2 判定表三列依据与读码吻合；§4/§5 数字见审查点 8/9；§6 逐处评估清单抽查吻合（见审查点 10）；§7 seed 论证读码支撑（`update: {}` 空更新不触发 `@updatedAt`，且 seed.spec.ts 无任何钟面断言——Grep 实证 0 命中）；§8 与迁移文件实核一致；§9 见审查点 7（存疑）；§10/§13 如实移交；§11 自检表与证据链对得上；§12 零新依赖成立（迁移用 Prisma 内置能力、diag 脚本已删）；§14/§15 过程披露完整。

### 审查点 4：任务卡 AC1~AC8 对照 —— [✓]

**证据**（[任务卡](file:///d:/codex/family-menu/docs/workflow/tasks/T-P14-时区口径统一-timestamptz迁移.md) L24-L34 vs dev §11）：AC1 六列清单与 schema 实测一致；AC2 USING 分支（5×'UTC'+CookLog×'Asia/Shanghai'）与迁移 SQL 逐条一致；AC3 断言数字与卡面 L27-L29 精确一致；AC4 口径（390=387+3skip）与 T-P13 实际（387 passed|3 skipped）算术衔接；AC5 清单+teardown 基线在 §5#7/#8 与 §6；AC6 seed 重放论证成立；AC7 注释+可移植性实核成立；AC8 报告落盘且结构完整。边界约束（L44-L49）与实际改动范围一致（见审查点 5）。

### 审查点 5：边界合规 —— [✓]（静态核对）

**证据**：
- 四个禁区 Grep 零命中：packages/shared、packages/engine（合并搜索）、apps/api/src、apps/h5/src 中 `T-P14|Timestamptz|timestamptz` 均 0 匹配——无越界痕迹。
- 临时诊断脚本确已删除：`apps/api/.tmp-tp14-diag.ts` Glob 无结果。✓
- seed.ts 读码无本卡改动痕迹；tests/、apps/api/test/ 与报告 §6「0 改动」口径一致（本审查两次全量模式扫描佐证）。
- conf 改动不入库披露成立：[.gitignore L23](file:///d:/codex/family-menu/.gitignore#L23) `.pg/` 在案。conf 严格说不在卡上「允许改」清单内，但属主控批复方案 D 授权的环境级处置（CURRENT.md 执行中段在案），不计越界。
- 限制声明：无 git，`git status`/numstat 无法复核，「0 越界」以上述静态证据为准，标注**静态核对**。

### 审查点 6：环境处置披露 —— [✓]

**证据**（[postgresql.conf](file:///d:/codex/family-menu/.pg/data/postgresql.conf) 实测）：
- L740 `timezone = 'UTC'`（方案 D 落实）✓
- L622 `log_timezone = 'Asia/Shanghai'`（未被改成 UTC，与披露一致）✓
- L737 `#intervalstyle = 'postgres'` 锚点行在案（§2-2 所述精确修改锚点）✓
- §2-2/§15-1 误改 `log_timezone` 后恢复的披露如实（最终态与 grep 复核描述吻合）。

### 审查点 7：CookLog 勘误表述 —— [注意]

**证据**：dev §9 形式上如实——明写「修正任务卡 L18 的初判」并给出依据（主控读码实证 + 引用行号），未粉饰；且披露了收益归因变化（「CookLog 读回 bug 被本卡真正修复」）。但**内容正确性存在未决矛盾**：

- dev §9 断言：「迁移前 adapter-pg normalize_timestamp 对无时区钟面按 UTC 解释 → Plan/Event 读回碰巧正确、CookLog 实际错 +8h」。
- 与三条历史证据**方向相反**：
  1. [T-C05-dev-2026-09-10.md L154](file:///d:/codex/family-menu/evidence/T-C05-dev-2026-09-10.md#L154)：「node-pg 驱动按本地时区解析导致时间语义歧义」；
  2. [T-P11-dev-2026-09-11.md L80](file:///d:/codex/family-menu/evidence/T-P11-dev-2026-09-11.md#L80)：本机 09-12 01:21 写入行 createdAt 读回 `2026-09-11T09:21:20Z`「慢 8h」——UTC 钟面 17:21 若按 UTC 解释读回应为 17:21Z（正确），实测 09:21Z 恰是「无时区钟面被按本地 +08 解释」的结果，直接实证卡面模型（Plan/Event 偏 −8h）；
  3. 任务卡 L7-L9「目标」节将「Plan/Event 读回偏 −8h」与「深夜 formatDate 日期提前一天」列为待根治的两类现存问题——后者只能在读回偏 −8h 的模型下成立。
- 裁决依据（adapter-pg dist/index.js L307-312）本审查无法读取（见审查点 12），**无法判定哪一方为真**。

**关键边界**：该矛盾**不触及迁移正确性**——六列钟面归属判定由「写入路径读码 + 基线库钟面直查」双重确认（与读回模型无关），USING 方向、AC3 迁移后断言、迁移后读回无歧义性在两个模型下均成立；受影响的仅是「迁移前 bug 是什么、修复收益归因」的历史叙事准确性。

### 审查点 8：AC3 断言数字内部自洽 —— [✓]（数字独立复跑移交 fm-verify）

**证据**（dev §4 与任务卡 L27-L29 对照）：
- SQL 侧 5 组与卡面精确一致：Plan.max(createdAt)=`09:04:09.747+00`、Event.max=`09:04:09.754+00`、Plan.max(planDate)=`09:04:09.745+00`、CookLog.max=`09:55:58.267+00`（=任务卡 L16 本地钟面 17:55:58.267 − 8h，算术正确）、Family/FamilyRule=`2026-08-06 00:00:00+00`（与 seed-data L6 `...T00:00:00Z` + `AT TIME ZONE 'UTC'` 逻辑自洽）。
- Prisma 侧 7 项 toISOString 与 SQL 侧同值不同渲染，两表互洽；「默认连接无 options」的验证口径与方案 D（会话时区由 conf 钉住）逻辑闭环。
- 无 shell 不可复跑，报告内部自洽性通过；数字独立复跑明确移交 fm-verify（见移交清单）。

### 审查点 9：AC4 数字自洽 —— [✓]

**证据**：
- `387 + 3 = 390`：[projection-order.spec.ts](file:///d:/codex/family-menu/apps/api/test/projection-order.spec.ts) 实数恰 3 处 `if (!dbReady) return skip(...)`（L106/L116/L194），与「3 条 DB 集成用例」口径一致；T-P13 基线 387 passed|3 skipped（CURRENT.md L78 在案）→ 本卡 PG 在跑 390/390 0 skip，属卡上预告的合法口径变化（任务卡 L30）。
- Test Files 15：与磁盘 spec 实数吻合（apps/api 4 + engine 4 + list-merger 1 + shared 1 + content-pipeline 5 = 15）。
- taboo 89/89、tp01 54 PASS、tp12 18 PASS、h5 typecheck 0 错、api tsc 0 错：均与 T-P10~T-P13 历史终态一致（CURRENT.md L71-L78 交叉印证），无矛盾。

### 审查点 10：AC5「0 改动」合理性 —— [✓]

**证据**（dev §6 清单逐项抽查）：
- 迁移只改列类型、值语义由 +00 偏移表达，UTC 钟面列的 Date 值语义不变——「测试窗口无需随迁更新」的前提成立。
- 全仓实测：tests/ 与 apps/api/test/ 中时间函数仅 [tp12 L126](file:///d:/codex/family-menu/tests/tp12-feedback-null.cjs#L126) `CURRENT_DATE`（插入值非窗口断言，插入与断言同会话同口径）与 L152/L184/L188 `now()`（绝对时刻算术）——与报告引用行号逐一吻合。
- 清理窗口确无时间口径：[tp02 L126/L130](file:///d:/codex/family-menu/tests/tp02-cleanup-test-plans.cjs#L126) 按 `id = ANY(...)` 白名单删除；projection-order deleteMany 按 tp07- 前缀。
- [contract.spec.ts L753/L768](file:///d:/codex/family-menu/apps/api/test/contract.spec.ts#L753) 无 Z 钟面实为 `vi.mocked(...).mockResolvedValue` 内存值、零 DB 触碰，备案不改合理。
- 结论：**0 处裸钟面字符串比较时间列**与实测吻合，「0 改动」论证成立。

### 审查点 11：未验证项披露 —— [✓]

**证据**（dev §13）：五项如实列出——公网 RDS 未验证、checksum 漂移未处置、h5 真机回归未做、3 条 DB 集成用例历史长期 skip 后首次真实执行、conf 不入库的部署映射——覆盖移交要求的 4 项且多 1 项（部署映射），无遗漏无回避。

### 审查点 12：adapter-pg 根因引用核实 —— [✗ 未验证]

**证据**：
- dev 引用 `@prisma/adapter-pg` dist/index.js L307-312（normalize_timestamp 对无时区钟面按 UTC 解释 / normalize_timestamptz 无条件改写偏移后缀为 +00:00）。
- 本审查环境 `node_modules/.pnpm` 下仅存 `lock.yaml`，包内容缺失（[.modules.yaml L54-L56](file:///d:/codex/family-menu/node_modules/.modules.yaml#L54) 仅证明 `@prisma/adapter-pg@7.9.1` 在安装清单中）；unpkg / jsdelivr / npmmirror / GitHub raw 四个外部渠道均不可达。**引用真实性无法核实**，此为审查点 7 矛盾无法裁决的直接原因。
- 间接自洽佐证（不替代源码核实）：「normalize_timestamptz 无条件改写偏移后缀为 +00:00」若为真，则**必须**把会话时区钉在 UTC 才能保证读回语义正确——这正是环境处置方案 D（conf `timezone='UTC'` + RDS 参数组 UTC 为部署前提）的设计依据，两处相互印证、内部自洽。

---

## 二、S 编号问题清单

| # | 严重级 | 问题 | 证据 | 处置建议 |
|---|---|---|---|---|
| S-1 | **建议**（转验证） | dev §9 读回模型断言（迁移前 Plan/Event 碰巧正确、CookLog 错 +8h）与三条历史证据（T-C05-dev L154、T-P11-dev L80 实证「慢 8h」、任务卡 L8-L9）方向互斥；裁决所依赖的 adapter-pg L307-312 引用 reviewer 不可核实 | 见审查点 7/12 全部证据 | **转 fm-verify/主控**：在依赖可用环境 `pnpm i` 后 Read `node_modules/.pnpm/@prisma+adapter-pg@7.9.1*/node_modules/@prisma/adapter-pg/dist/index.js` L300-320 裁决；裁决后统一 §9/任务卡/T-C05/T-P11 四处叙事口径（若 dev §9 为真，须补充解释 T-P11 L80 实证与 formatDate 问题的成因，如脚本 pg 直查与 adapter parser 分叉）。不阻断迁移本身——钟面归属与 USING 方向已由写入路径+基线直查独立确认 |
| S-2 | 记录 | dev §16 括注「新增目录（migration.sql + migration_lock.toml）」易误读：migration_lock.toml 实位于 migrations/ 根（[实读](file:///d:/codex/family-menu/apps/api/prisma/migrations/migration_lock.toml)，provider=postgresql，历史既有文件，3 个历史迁移目录可证），非本卡新增目录内文件 | LS migrations 目录结构 | 备案；主控知悉即可，历史报告按项目惯例不改写 |
| S-3 | 记录 | 审查时点 node_modules 为空壳（.pnpm 仅 lock.yaml），与 dev 会话可执行测试的状态不符——疑环境快照/清理所致；node_modules 不入库，非 dev 越界 | 审查点 12 证据 | 备案；所有运行时数字断言依赖 fm-verify 在依赖完备环境复跑（本就属其职责） |

---

## 三、数字复核移交清单（移交 fm-verify 复跑，含命令口径）

1. **根回归**：`pnpm test`（根目录，串行已固化）→ 预期 Test Files 15 passed / Tests **390 passed (390)**、0 skipped。
2. **禁忌集**：`pnpm test:taboo` → **89/89**。
3. **tp01**：PG + dev server 起后 `node tests/tp01-mustuse-regression.cjs`（即卡面 test-p01 口径的实际执行形态）→ **54 PASS / 0 FAIL**。
4. **tp12**：`node tests/tp12-feedback-null.cjs` → **18 PASS / 0 FAIL**（并抽查 submittedAt 为 UTC 读回）。
5. **h5 typecheck**：apps/h5 `pnpm typecheck`（tsc --noEmit）→ 0 错。
6. **api tsc**：apps/api `pnpm exec tsc -p tsconfig.json --noEmit` → 0 错。
7. **teardown 七表计数与 REL=29**：Dish=48 / Menu=13 / MenuDish=42 / Ingredient=79 / Plan=54 / Event=93 / CookLog=6，REL（Dish.imageUrl LIKE '/images/%'）=29。
8. **AC3 复读**（迁移后）：SQL 侧 Plan.max(createdAt)=`2026-09-06 09:04:09.747+00`、Event.max=`…09.754+00`、Plan.max(planDate)=`…09.745+00`、CookLog.max=`2026-09-10 09:55:58.267+00`、Family/FamilyRule=`2026-08-06 00:00:00+00`；Prisma 侧 toISOString 7 项同值 Z 渲染。
9. **conf 生效**：`SHOW timezone` → `UTC`；`log_timezone` 仍为 Asia/Shanghai。
10. **S-1 裁决专项**：Read adapter-pg dist/index.js L300-320（见上）。

---

## 四、卡外观察（只记录不处理）

1. **node_modules 空壳**（S-3）：除影响审查能力外，若为环境常态，建议主控确认后续 verify 卡的执行前提（`pnpm i` 是否为开卡固定动作）。
2. 无其他卡外问题。PRODUCT-CONFIRMATION.md 无本卡登记不构成问题——本卡为挂账④技术级修复（用户 2026-09-12 批准方案 A，CURRENT.md 执行中段在案），非产品行为变更。

---

## 五、结语

**是否批准进入 fm-verify 验收环节：批准。**

核心理由：六列 `@db.Timestamptz` 位置精确、迁移 USING 方向与主控诊断基准零偏差且经 PG 官方语义与写入路径读码双重佐证、注释与可移植性达标、边界零越界（静态核对）、环境处置与披露如实、AC3~AC8 证据链内部自洽、未验证项披露完整。唯一条件 S-1 为历史叙事口径的未决矛盾（迁移前读回模型），不触及迁移正确性与数据安全，交 fm-verify/主控在依赖完备环境裁决即可。fm-verify 须按上表完成数字复跑，并优先处理 S-1 裁决专项。

---

## 六、主控裁决附录：S-1 已裁决（2026-09-12，主控代记）

**裁决手段**：jsdelivr CDN 渠道取得 `@prisma/adapter-pg@7.9.1/dist/index.js` 原文（reviewer 环境 node_modules 空壳 + 四渠道不可达，主控渠道可达），直接读码裁决。

**裁决结论：dev §9 断言为真。** 关键源码（conversion 段）：

```js
function normalize_timestamp(time) {
  return `${time.replace(" ", "T")}+00:00`;   // 无时区钟面 → 无条件按 UTC 解释
}
function normalize_timestamptz(time) {
  return time.replace(" ", "T").replace(/[+-]\d{2}(:\d{2})?$/, "+00:00");  // timestamptz 偏移后缀 → 无条件改写 +00:00（假设会话 UTC）
}
```

1. **迁移前（本卡 schema 下）Prisma 栈读回模型**：`normalize_timestamp` 对无时区钟面无条件标 +00:00 → Plan/Event/Family/FamilyRule（UTC 钟面）读回**碰巧正确**；CookLog（本地钟面）被当 UTC 解释 → 读回**错 +8h**。与 dev §9 及本审查审查点 7 引用的 adapter-pg 行为完全一致。
2. **T-P11-dev L80「慢 8h」实测的成因澄清**：该次读回路径为 teardown `.cjs`（`.workflow-verify/tp11/teardown-result.json` 产物在案）= **node-pg 直查**。node-pg 原生 parser 将无时区钟面按**会话本地 +08** 解释 → UTC 钟面 17:21:20 读回 09:21:20Z（−8h，与实测精确吻合）。原文「PG 服务器时钟偏移 8 小时」系当时误判：PG 服务器时钟没有偏移，是**直查读取器口径**所致。
3. **T-C05-dev L153「node-pg 按本地时区解析」**：描述对 node-pg 直查路径成立、对 Prisma 栈（adapter-pg）不成立——两套读取器行为**互为镜像**，构成「同库两种钟面 × 两套读取器」的 2×2 歧义矩阵：Plan/Event UTC 钟面 adapter-pg ✓ / node-pg 直查 ✗；CookLog 本地钟面 adapter-pg ✗ / node-pg 直查 ✓。
4. **任务卡 L7-L9 初判修正**：「Plan/Event 读回偏 −8h」仅对 node-pg 直查路径成立（Prisma 栈为碰巧正确）；「深夜 formatDate 显示日期提前一天」系基于该初判的推演，未获复现实证，随本卡「读回绝对时刻恒正确」一并根治，成因不再单独追查。
5. **修复收益终表述**（供 CURRENT.md 挂账④核销与任务卡终态引用）：本卡（timestamptz 迁移 + conf timezone='UTC' + 后续 RDS 参数组 UTC）根除 2×2 歧义矩阵——迁移后两类读取器对全部时间列读回均为与时区无关的绝对时刻，写读口径唯一。
6. dev 报告 §9 与审查点 7 的 [注意] 就此解除；S-1 闭环，无遗留叙事修正义务（历史报告 T-C05/T-P11 按项目惯例不改写原文，以本附录为勘误依据）。
7. **S-3 勘误（2026-09-12 主控实测补充）**：审查点 12 与 S-3 所述「node_modules 空壳（.pnpm 仅存 lock.yaml）」系 **Glob/LS 对 pnpm 符号链接+junction 结构穿透失败**的误判，非依赖真实缺失。主控 PowerShell 深度实测：`node_modules/.pnpm` 实际内容齐全（@babel+* 等大量包目录在列）、`apps/api/node_modules/pg` 与 `@prisma/adapter-pg` 相关路径 `Test-Path=True`，环境健康无需任何重建；fm-verify 未重建依赖、9/9 复跑全绿亦反向证实。reviewer 沙盒内 unpkg/jsdelivr/npmmirror/GitHub raw 四渠道不可达为主控工具面差异（主控 jsdelivr 渠道可达并取得源码）。S-3 与审查点 12 的「未验证」按此勘误口径归档；裁决依据以本附录上方 adapter-pg 双 normalize 函数源码摘录为准。
