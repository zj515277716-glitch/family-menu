# T-P04 采购页「开始做菜」入口 —— 四角色闭环证据归档

> 归档：主控 PM（SOLO 主会话）｜归档日期 2026-09-10（fm-dev 交付与 fm-verify 验收的实际运行日；任务卡开卡日 2026-09-06，两日期并存，以运行证据时间为准）
> 基线 HEAD：`f1b0954`（T-P03 提交后）｜分支 feat/tp-06-menu-expansion
> 运行时证据目录：`.workflow-verify/tp04-verify/ac/`（34 文件：截图 8 张 s0~s6、verify-result.json、exit4~7.txt、teardown 三份、端口/PG 复核等）

## 0. 三方结论一览

| 角色 | 结论 | 关键数字 |
|---|---|---|
| fm-dev | 交付，AC 7/7 自检 [✓] | plan/index.tsx +18/-3、e-final.html +3；typecheck 退出码 0；自测后 teardown 56→54 / 96→93；无新增依赖 |
| fm-reviewer | **通过**（无阻断） | 3 条建议级 R-1/R-2/R-3；AC2 五处独立核实无反例 |
| fm-verify | **通过**（22 PASS / 1 FAIL，唯一 FAIL 经主控裁定豁免） | 第 7 轮正式运行（exit7.txt=2）；无挂具真实 Edge 浏览器；teardown 两轮删 Plan 8 行/Event 16 行 → 54/93 含构成逐项一致；三端口监听=0；PG 已停（pgctl exit=0） |

代码改动：**6 文件 +31/-8**（T-P04 业务代码 2 + 主控立项 4），`apps/h5/src/pages/plan/index.tsx` +18/-3、`docs/ai-rebuild/ui/e-final.html` +3（仅 s7 区块）、PRODUCT-CONFIRMATION.md +4/-4（C-8 修订）、docs/ai-rebuild/PRODUCT-DECISIONS.md +8（PD-015）、CURRENT.md、T-P03 卡哈希补记。**契约/引擎/API 零改动，禁改区零越界。**

## 1. 交付内容（主控磁盘核验在盘）

- [plan/index.tsx](../../apps/h5/src/pages/plan/index.tsx)：L51 注释、L277-280 新增 `goFirstDish()`（取 `lockedMenu?.dishes[0]` 复用既有 `goDish` → `/pages/dish/index?dishId=`）、L457-464 底栏按钮 `onClick={() => (bought ? goFirstDish() : setBought(true))}`、文案 `{bought ? '开始做菜' : '就按这个买'}`；原「✓ 已按这个买」代码正文已删除（仅存 L458 注释声明两态设计）。
- [e-final.html](../../docs/ai-rebuild/ui/e-final.html)：s7 屏⑦按钮处 PD-015 两态说明注释（+3 行，他屏未动；主控与 fm-verify 双核验）。
- 主控先行立项（fm-dev 之前）：确认书 C-8 L181 按钮定义修订 + L185「历史」条目、PRODUCT-DECISIONS.md PD-015 决策表行 + 完整条目（含"上一道/下一道导航与排版改版合并到详情页改版事项"）。

## 2. AC 逐条对照

- **AC1 按钮状态机** [✓]：未确认=「就按这个买」（点击一次性确认），确认后=「开始做菜」；「✓ 已按这个买」文案不再出现（代码正文已删）。fm-verify 截图 s2/s2b 两态对照。
- **AC2 跳转 dishes[0]** [✓]：bought 态点「开始做菜」→ `/pages/dish/index?dishId=<备菜顺序第一道菜>`。fm-dev 核实 + fm-reviewer 五处独立核实无反例（见 §3）。
- **AC3 行为不回退** [✓]：ghost 反馈按钮不变（L465-467）、顶部菜卡进详情不变（L322-337）、Checkbox 勾选不变（L94-108 handleCheck 核验无异常）；bought 刷新/重进按现状保持（s4 重进截图）。
- **AC4 场景覆盖** [✓]：多道菜（≥2）跳第一道、单道菜正常、空态页不受影响（L252-267 提前 return 未动）。
- **AC5 范围纪律** [✓]：后端/契约零改动，diff 6 文件与允许清单逐一对应。
- **AC6 UI 基准同步** [✓]：e-final s7 仅 +3 注释。
- **AC7 证据与清理** [✓]：真实浏览器 8 截图 + teardown 对账（§6）。

## 3. AC2 顺序核实链（本卡关键技术论断）

**论断：`MenuView.dishes` 数组顺序 = 备菜顺序（prepSequence）顺序**，故 `dishes[0]` 即第一道菜。fm-reviewer 五处独立核实无反例：

1. seed 手写一致：seed.ts L206-216 与 seed-data.ts L399-414，dishes 排列与 buildPrepSequence 输出一致；
2. content-pipeline 同源：menu-assemble.ts L111-113，assembleOneMenu sort=i+1 与 buildPrepSequence 同一索引源；
3. 单菜换继承+重算：planService.ts L606-613，换槽位继承原序、重算 prepSequence，dishes 数组随动；
4. 整套换同源：planService.ts L517；
5. 前端备菜顺序展示直接按数组序渲染，与详情页跳转共用同一数组。

**弱点 R-2（挂账，本卡未动）**：loadMenuViews（planService.ts L121-133）与 hydrateLockedMenu（L218-226）的 include dishes 无 `orderBy:{sort:'asc'}`，当前依赖 PG 物理顺序返回恰与写入序一致；listPlans L894 已有 orderBy 口径不一。处置：独立小卡补两处 `orderBy:{sort:'asc'}`（约 2 行）。

## 4. fm-reviewer 建议 R-1~R-3 处置

| 编号 | 内容 | 处置 |
|---|---|---|
| R-1 | fm-dev 完成报告/typecheck/截图/teardown 数字仅在对话返回，evidence/ 与开发日志无落盘凭证 | **流程缺口，主控补救**：fm-verify 已独立落盘 34 文件运行时证据；本归档由主控撰写。根治：T-P05 起派发指令强制要求 fm-dev 将完成报告落盘 `evidence/T-Pxx-dev-YYYY-MM-DD.md`（援引 T-P03 B-1 先例） |
| R-2 | include dishes 无 orderBy（见 §3） | 挂账独立小卡 |
| R-3 | 连点「开始做菜」可能重复 navigateTo | 归入 UAT 反馈 3b 详情页改版（T-P06）一并处理 |

## 5. fm-verify 验收与 V5 裁定

**判定 23 项：22 PASS / 1 FAIL**（verify-result.json + exit7.txt=2，第 7 轮为正式运行）。

过程说明：第 5/6 轮退出码用 `%ERRORLEVEL%` 解析期展开得假 0 失真；第 7 轮改 `cmd /v:on /c "... & echo !errorlevel! > exit7.txt"` 延迟展开，得真实退出码 2（=22 PASS/1 FAIL）。第 5 轮 V4-4 断言曾超时，核验 handleCheck 无异常后重写为 checkbox class+全局计数+URL 复核的鲁棒断言，此后持续 PASS（测试伪影，非产品缺陷）。全程无挂具真实 Edge 浏览器（T-P03 CORS 修复后浏览器可直调 :3000）。

**唯一 FAIL（V5）与主控裁定：豁免。**

- 现象：GET `/api/plans/:id/feedback` 无反馈时返回 404，浏览器对 404 响应的原生 console 打印，判定 console 零错误不满足。
- 裁定依据：①该 404 为 DEC-015（契约 v0.6，TP-05 已入库）设计内的 NotFoundError（planService.ts L847-864），前端 feedback/index.tsx L73 catch 转 null 属既定容错；②与 T-P04 diff 零关联（本卡仅动 plan/index.tsx 与 e-final s7）；③浏览器网络层 404 原生日志无法被 JS 抑制，属设计内噪音而非缺陷。
- 挂账（卡外发现 #1）：后续任务将无反馈 GET 改为 200+null 消除噪音（涉契约评估，候选并入 T-P05 或独立小卡，届时报用户）。

## 6. teardown 对账（强制项）

基线（count-before.txt，2026-09-09T19:38:11Z）：**Plan=54（PROPOSED 29 / LOCKED 23 / COOKED 1 / SKIPPED 1），Event=93**。

| 轮次 | 清理对象 | 删除 | 终态 |
|---|---|---|---|
| r1（首轮 6 轮试跑产物） | 7 个 LOCKED Plan（每轮 recommend→GENERATE + lock→LOCK 各 1 Event） | Event 14 行 + Plan 7 行（先 Event 后 Plan，按 id 精确删） | Plan=54（构成同基线）/ Event=93，exit=0 |
| r2（第 7 轮正式运行产物） | Plan cmtv13xgb0000s8pgfq7kmoum（LOCKED）+ 绑定 Event | Event 2 行 + Plan 1 行 | Plan=54（构成同基线）/ Event=93，exit=0 |
| **合计** | — | **Event 16 行 + Plan 8 行** | **与 count-before 逐项一致 [OK]** |

进程与环境：两轮均 `taskkill /T /F` 杀净 3000/10086（先父后子）；终态复核 final-port-recheck.txt：api3000=0、h5_10086=0、pg54329=0；PG 经 pg_ctl stop -m fast 停止（两轮 pgctl exit=0），数据目录原样保留。

## 7. 卡外发现

1. **V5 根因**（见 §5 挂账）：GET feedback 无反馈 404→建议改 200+null。
2. **cookie 域细节**：ACCESS_TOKEN domain=127.0.0.1，浏览器地址必须用 127.0.0.1:10086，用 localhost 会全量 401（域不匹配）。已作为验收环境固定口径记录。
3. **GET /api/plans/:id** 在 Plan 不存在时同样抛 NotFoundError（与 V5 同族，暂不处置，随 #1 一并评估）。

## 8. 未验证项 / 边界说明

- 未做全量回归（tp01 17 断言未在本卡重跑）：本卡 diff 不触 API/引擎/契约，回归必要性低，如用户要求可补跑。
- 单道菜场景仅代码路径核实 + s3 详情截图间接覆盖，未单独截"单菜菜单 bought 态"专图。
- e-final s7 为静态注释，浏览器渲染两态以 H5 实测截图为准。

## 9. 结论

T-P04 交付**通过验收**（22/23 判定 PASS + 1 豁免，豁免理由与挂账已登记）。四角色闭环第四次跑通。git 提交与否由产品负责人裁决；待请示项：①提交 ②V5 豁免追认 ③内容轨道 3 决策点（见 CURRENT.md 下一步）。
