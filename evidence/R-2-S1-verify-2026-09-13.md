# R-2-S1 产品验收报告（fm-verify）

- 验收人：fm-verify（产品验收专家，独立验收）
- 验收执行日期：2026-09-14（文件名沿用卡号日期 2026-09-13）
- 验收对象：分支 `feat/tp-06-menu-expansion`，HEAD `6273ea8ec272d60070eb6a1ed3840c827aaec6fd`（与卡面 HEAD=6273ea8 一致，验收全程未产生新提交）
- 版本标识：feat/tp-06-menu-expansion @ 6273ea8
- 任务卡：evidence/R-2-S1-task-2026-09-13.md（AC1~AC5）
- dev 报告：evidence/R-2-S1-dev-2026-09-13.md（§四 tsx 实跑命令留存）
- review 报告：evidence/R-2-S1-review-2026-09-13.md（T-1 条件清单，本次验收即 T-1 闭合）
- 验收方式：全部命令真实执行、独立复跑，不信任 dev 报告数字；每项记录真实命令 + 关键输出摘录 + 退出码 + 数字结果
- 红线遵守：未删除 $env:TEMP\fm-rds.env；未停止本地 PG（54329）；未杀 9222 CDP 浏览器进程；未执行任何 DROP/清理类 SQL；未动 UAT 数据库活跃数据；除本报告落盘外零写入（未修改任何产品代码/测试代码/文档）

## 总体判定

**PASS（7/7 项通过：A~G 全 ✓）**

T-1 条件清单（review 报告第 1/2/5 项）全部独立复跑闭合，结果与 dev 报告一致；守卫真实口径 tsx 实跑 2 条复测逐字一致；断言文案抽查 8 处（超出 3 处下限）全部锚定源码实文。

---

## 版本确认

- 命令：`git branch --show-current; git rev-parse HEAD; git log --oneline -3`（cwd=d:\codex\family-menu）
- 结果：分支 `feat/tp-06-menu-expansion`；HEAD `6273ea8ec272d60070eb6a1ed3840c827aaec6fd`；EXIT=0
- 判定：[✓] 与卡面声明一致

---

## A. 根全量测试 pnpm test

- 操作（Operation）：在 d:\codex\family-menu 根执行 `pnpm test; Write-Output "EXIT=$LASTEXITCODE"`
- 预期（Expected）：Test Files 20 passed；Tests **454 passed（442 基线 + 12 新增 cli 用例）**；0 failed；EXIT=0；cli.spec.ts 行在列
- 实际（Actual）：
  - `✓ tools/content-pipeline/test/cli.spec.ts (12 tests) 33ms`（在列）
  - `Test Files  20 passed (20)` / `Tests  454 passed (454)`
  - `EXIT=0`
- 版本：feat/tp-06-menu-expansion @ 6273ea8
- 证据（Evidence）：终端输出留存（本报告执行时实时采集）；454 = 442 + 12 与 dev 报告 §四一致
- 覆盖边界：根 vitest 全量（20 文件）；api 测试内置 mock 服务器日志（req-* JSON 行）为测试框架正常输出，非异常
- 判定：[✓] 通过

## B. 禁忌集 pnpm test:taboo

- 操作：根目录执行 `pnpm test:taboo; Write-Output "EXIT=$LASTEXITCODE"`
- 预期：95/95 全绿；EXIT=0
- 实际：
  - `✓ packages/engine/test/safety-ingredient-realform.spec.ts (6 tests)` / `feasibility (7)` / `taboo (18)` / `swap-candidates (13)` / `score (51)`
  - `Test Files  5 passed (5)` / `Tests  95 passed (95)`
  - 性能锚点：`✓ 千套菜单 recommend 调用性能 < 50ms（AC12）  343ms`
  - `EXIT=0`
- 版本：feat/tp-06-menu-expansion @ 6273ea8
- 证据：终端输出实时采集；与 dev 报告「95/95（一票否决项全绿）」一致
- 覆盖边界：packages/engine 全部禁忌/安全测试；不含 content-pipeline 与 api
- 判定：[✓] 通过

## C. CLI 单测单跑 cli.spec.ts

- 操作：①按卡面字面口径在 tools/content-pipeline 执行 `npx vitest run test/cli.spec.ts`；②按 dev 报告 §二留存等价口径在根执行 `npx vitest run tools/content-pipeline/test/cli.spec.ts`
- 预期：12/12 passed；EXIT=0
- 实际：
  - 字面口径①：`No test files found, exiting with code 1`（filter: test/cli.spec.ts；include 模式为 `packages/*/test/**` 等相对仓库根）→ EXIT=1。**定性：命令执行口径问题，非产品缺陷**——本仓库 vitest 配置在仓库根、include 路径相对根目录解析，子目录字面 filter 必然失配；dev 报告留存口径即为根目录执行，与 dev 报告无冲突。
  - 等价口径②：`✓ tools/content-pipeline/test/cli.spec.ts (12 tests) 25ms` / `Test Files  1 passed (1)` / `Tests  12 passed (12)` / `EXIT=0`
- 版本：feat/tp-06-menu-expansion @ 6273ea8
- 证据：两条命令终端输出实时采集
- 覆盖边界：单文件 12 用例（PC-a~e + IMP-a~g）全部通过；未覆盖守卫 catch 块本体在 vitest 进程内的执行（review S-2 已备案已知限制，tsx 真实口径由 F 项覆盖）
- 判定：[✓] 通过（12/12）

## D. content-pipeline 构建 pnpm build

- 操作：在 tools/content-pipeline 执行 `pnpm build; Write-Output "EXIT=$LASTEXITCODE"`
- 预期：`tsc -p tsconfig.json` 无错误，EXIT=0
- 实际：输出仅 `$ tsc -p tsconfig.json`，无任何错误行；`EXIT=0`
- 版本：feat/tp-06-menu-expansion @ 6273ea8
- 证据：终端输出实时采集；与 dev 报告 AC3 一致
- 覆盖边界：TypeScript 编译检查；未含 dist 直跑口径（node dist/cli/*.js 属 R-2 卡外备案①已知限制，非本卡范围，未验证）
- 判定：[✓] 通过

## E. git 边界核对

- 操作（cwd=d:\codex\family-menu）：
  1. `git status --porcelain`
  2. `git diff --numstat`
  3. `git diff --name-only`
- 预期：M 恰 2 个 cli 文件；untracked 含本卡 4 文件（test/cli.spec.ts + evidence task/dev/review 3 份报告）；numstat 恰 2 文件各 13+/4-；禁区（packages/shared、packages/engine、packages/list-merger、apps/api、apps/h5）零 diff
- 实际：
  - status --porcelain 被跟踪改动恰 2 项：` M tools/content-pipeline/src/cli/import.ts`、` M tools/content-pipeline/src/cli/publish-check.ts`；untracked 本卡 4 文件全部在列：`?? tools/content-pipeline/test/cli.spec.ts`、`?? evidence/R-2-S1-task-2026-09-13.md`、`?? evidence/R-2-S1-dev-2026-09-13.md`、`?? evidence/R-2-S1-review-2026-09-13.md`
  - 其余 untracked（.pai/、docs/design/、docs/ui-redesign/、tests/engine-perf.mjs、tests/step08-deploy-validate.cjs、tools/append-log.mjs、tools/*.tar.gz、tools/preview-server.mjs、复盘报告.md）与本卡无关——dev 报告 §五已备案为开工前既有；其中 docs/design/、docs/ui-redesign/ 为新增 untracked 目录，不属 git diff（tracked 修改）范畴，未触碰禁区既有文件
  - `git diff --numstat`：`13  4  tools/content-pipeline/src/cli/import.ts`、`13  4  tools/content-pipeline/src/cli/publish-check.ts`——恰 2 文件各 13+/4-（numstat 口径，review S-1 建议已采纳）
  - `git diff --name-only`：仅上述 2 个 cli 文件；禁区目录零出现
  - 附注：numstat 输出带 `LF will be replaced by CRLF` warning，系 Git 换行提示，不影响数字
- 版本：feat/tp-06-menu-expansion @ 6273ea8
- 证据：三条命令终端输出实时采集
- 覆盖边界：工作区 diff（tracked 修改）；未含已提交历史核查（HEAD 即受测版本）
- 判定：[✓] 通过

## F. 守卫真实口径 tsx 实跑复测（以 dev 报告 §三 命令为准）

- 操作（cwd=tools/content-pipeline；坏 JSON 夹具落 $env:TEMP\fm-verify-s1-bad.json，内容 `{ broken`（8 字节 ASCII 无 BOM），未触碰 fm-rds.env）：
  - F-1（dev §三 #6）：`npx tsx src/cli/publish-check.ts --status PUBLISHED`
  - F-2（dev §三 #7）：`npx tsx src/cli/import.ts "$env:TEMP\fm-verify-s1-bad.json"`
  - F-3（卡面形态补充）：`npx tsx src/cli/publish-check.ts --help`
  - F-4（卡面形态补充）：`npx tsx src/cli/import.ts`（缺必填参数）
- 预期（按 dev 报告 §三）：
  - F-1：stderr `publish-check 执行失败： --status 非法值：PUBLISHED（仅允许 DRAFT/TESTED，大小写不敏感，逗号分隔）`，EXIT=1
  - F-2：stderr `import 执行失败： Expected property name or '}' in JSON at position 2 (line 1 column 3)`，EXIT=1
  - F-3：帮助文本（Usage/Options），EXIT=0
  - F-4：`error: missing required argument 'file'`，EXIT=1
- 实际（逐字记录）：
  - F-1 输出：`publish-check 执行失败： --status 非法值：PUBLISHED（仅允许 DRAFT/TESTED，大小写不敏感，逗号分隔）`，EXIT=1 → 与 dev 报告逐字一致
  - F-2 输出：`import 执行失败： Expected property name or '}' in JSON at position 2 (line 1 column 3)`，EXIT=1 → 与 dev 报告逐字一致
  - F-3 输出：Usage: publish-check [options] + Options（--status/--json/-h，文案与 dev 报告 §三留存帮助文本逐字一致），EXIT=0
  - F-4 输出：`error: missing required argument 'file'`（无「import 执行失败：」前缀，与 dev §六-5 备案一致：parse 阶段错误由 commander 直接 exit(1) 不走守卫 catch），EXIT=1
- 版本：feat/tp-06-menu-expansion @ 6273ea8
- 证据：4 条命令终端输出实时采集；isMain 守卫在 tsx 直跑下命中（parseAsync 正常驱动，catch 文案 + exit(1) 真实触达）
- 覆盖边界：tsx 直跑口径 4 条（含 2 条失败路径 + 1 条帮助 + 1 条缺参）；未覆盖 node dist 直跑（R-2 卡外备案①，非本卡范围）；F-1 在 createPrismaClient 建连前抛错（fail-fast），零 DB 触达
- 判定：[✓] 通过

## G. cli.spec.ts 断言文案与源码实文一致性抽查（grep 比对 8 处，超出 3 处下限）

- 操作：对 spec 断言文案逐条 grep 源码实文（tools/content-pipeline/src/）
- 比对结果（spec 行号 ← 源码锚点）：

| # | cli.spec.ts 断言 | 源码实文锚点 | 结论 |
|---|---|---|---|
| 1 | L232 `--status 非法值：PUBLISHED（仅允许 DRAFT/TESTED，大小写不敏感，逗号分隔）` | src/publish-check.ts L37 模板 `--status 非法值：${invalid.join(',')}（仅允许 DRAFT/TESTED，大小写不敏感，逗号分隔）`（PUBLISHED 为用例输入） | 吻合 |
| 2 | L244 `publish-check 执行失败：` | src/cli/publish-check.ts L79 守卫 catch（本次已读全文逐字确认） | 吻合 |
| 3 | L271 `=== fm-publish-check 存量过敏原盘点报告 ===` / L274 `结论：无缺口（exit 0）` | src/publish-check.ts L169 / L200 | 吻合 |
| 4 | L314 `结论：存在 1 道缺口菜品（exit 2；本工具只报告不处置）` | src/publish-check.ts L199 模板（gapDishCount=1 合成数据） | 吻合 |
| 5 | L409 `origin 授权值非法："WECHAT"（仅允许缺省=LLM_DRAFT 或显式授权 FETCHED）` | src/import.ts L130 模板 `${JSON.stringify(value)}`（序列化带双引号） | 吻合 |
| 6 | L435 `import 执行失败：` | src/cli/import.ts L180 守卫 catch（本次已读全文逐字确认） | 吻合 |
| 7 | L456 `[过敏原拦截] 花生拌饭：HARD 规则命中且…命中明细：` / L479 `校验通过（dry-run，未写入 DB）：` / L516 `导入成功：dishId=dish-new-77（status=DRAFT, origin=LLM_DRAFT` | src/cli/import.ts L127 / L151 / L160 模板（花生拌饭、dish-new-77 均为用例 fixture） | 吻合 |
| 8 | L544 `[警告] 1 个食材 category 不在标准六类` / L546 `仅警告，不阻断` | src/import.ts L30 / L32 模板 | 吻合 |

- 预期：断言文案全部锚定源码实文，无自创
- 实际：8/8 吻合，未发现自创文案或漂移
- 版本：feat/tp-06-menu-expansion @ 6273ea8
- 证据：grep 输出（行号+内容）实时采集；cli 两源文件全文人工核对（守卫块 L73-84 / L174-185 与任务卡定案方案逐字一致：isMain 判式、catch 回调文案、process.exit(1)、export { program }）
- 覆盖边界：抽查 8 处高频关键文案；cli.spec.ts 其余断言（如 statusFilter 白名单归一、exitCode 数值）随 C 项 12/12 实跑间接验证
- 判定：[✓] 通过

---

## 汇总

| 项 | 内容 | 结果 |
|---|---|---|
| A | pnpm test | [✓] 454 passed (454)，EXIT=0 |
| B | pnpm test:taboo | [✓] 95 passed (95)，EXIT=0 |
| C | cli.spec.ts 单跑 | [✓] 12 passed (12)，EXIT=0（等价口径；子包字面口径失配已记录，非产品缺陷） |
| D | pnpm build | [✓] tsc 无错，EXIT=0 |
| E | git 边界 | [✓] M 恰 2 cli 文件，numstat 各 13+/4-，untracked 本卡 4 文件在列，禁区零 diff |
| F | tsx 实跑守卫 | [✓] 4 条与 dev 报告逐字一致（含 EXIT） |
| G | 断言文案抽查 | [✓] 8/8 吻合，无自创 |

**总体判定：PASS**

## 未验证项 / 边界说明

1. node dist 直跑口径（bin shim → dist/cli/*.js）未实测——R-2 卡外备案①已知限制，非本卡范围。
2. 守卫 catch 块本体在 vitest 进程内不可达——review S-2 已备案已知限制，本次以 F 项 tsx 真实口径覆盖。
3. 真实 DB 链路盘点/导入不在本卡范围（本卡 12 用例全 mock 零 DB 触达；F 项失败路径亦未建连）。
4. C 项字面口径（在 tools/content-pipeline 内 `npx vitest run test/cli.spec.ts`）受 vitest 根配置 include 相对路径限制必然失配——建议主控知悉该执行口径（dev 报告留存口径为根目录执行，本次已按其复测通过），是否调整属主控裁量，verify 不修改任何配置。
