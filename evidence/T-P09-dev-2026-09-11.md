# T-P09 开发报告：契约 imageUrl 口径放宽（dev）

> 执行角色：fm-dev · 执行窗口：2026-09-11 · 环境：Windows / PowerShell / 便携 PG 17.5 @ 127.0.0.1:54329（本卡独占）
> 任务卡：docs/workflow/tasks/T-P09-契约imageUrl口径放宽.md
> 结论：**8/8 AC 全 [✓]**，管线/根回归全绿（361/361），端到端实证通过，teardown 七表精确还原、三端口归零、PG 已停、CDP 9222 存活。

## 1. AC 逐条自检

| # | AC | 结果 | 数字证据 |
|---|---|---|---|
| 1 | dish.ts imageUrl 放宽为「http(s) 绝对 URL 或 /images/ 相对路径」二选一；sourceUrl 不动；空串仍拒绝；版本注释按惯例记录 | [✓] | [dish.ts](file:///d:/codex/family-menu/packages/shared/src/schemas/dish.ts) L57-59 `z.union([z.string().url(), z.string().regex(/^\/images\//)])`；L60 sourceUrl `z.string().url()` 未动；空串两分支均不匹配仍拒绝（schemas.spec.ts L264 实证）；头部 L3-7 记录契约版本 v0.7（2026-09-11，T-P09），并注明历史 v0.2~v0.6 见 schemas/api.ts 头部（api.ts 本卡禁改，v0.7 落 dish.ts 头部） |
| 2 | 改前 grep packages/shared 确认 imageUrl zod 校验点仅 dish.ts 一处 | [✓] | 见 §2，全 shared 库仅 dish.ts L57 一处 zod 校验点，无其他校验点，无需停手 |
| 3 | import.spec.ts 补 4 类用例 | [✓] | +4 用例（相对路径通过 / 绝对 URL 通过 / foo/bar.jpg+缺前导斜杠拒绝 / 空串拒绝），25→29；vitest 实测 29 tests ✓ |
| 4 | fetch2dish.mjs 补 `--base-url=` 等号形式显式报错拦截 + spec 用例 | [✓] | parseArgs 拦截条件扩为 `k === '--base-url' \|\| k.startsWith('--base-url=')`（显式报错 process.exit(1)）；fetch2dish.spec.ts 子进程实测等号形式 status=1 且 stderr 含「已废弃」，33→35 |
| 5 | 回归全绿：shared 测试 + 管线 vitest + 根 pnpm test + h5 typecheck 0 错 + build EXIT=0 | [✓] | shared 直跑 63/63 EXIT=0；管线 vitest `Test Files 13 passed (13) / Tests 361 passed (361)`；根 pnpm test `Test Files 13 passed (13) / Tests 361 passed (361)`；typecheck EXIT=0（0 错）；build EXIT=0（webpack compiled，25 warnings 均 NutUI CSS calc/体积提示，0 error，与 T-P08 基线一致） |
| 6 | 端到端实证：有图新菜 draft（相对路径+静态文件真实存在）→ fm-import 真实入库 → DB 行相对路径 → 测试数据删净 | [✓] | 见 §4：IMPORT_EXIT=0，DB 行 `imageUrl=/images/dishes/tp09-verify/0.webp`（相对路径）、status=DRAFT、origin=FETCHED；Dish 行与静态目录均已删净（RESIDUE=0） |
| 7 | teardown：七表基线 + 29 道迁移数据不动 + 三端口=0 + PG 停 + CDP 9222 存活 + 报告落盘 | [✓] | 七表 7/7 PASS（Dish=48/Menu=13/MenuDish=42/Ingredient=79/Plan=54/Event=93/CookLog=6）；MIGRATION REL=29/ABS=0 不动；PG_STOP_EXIT=0；PORT 3000/10086/54329=0；CDP Browser=Edg/152.0.4191.66 存活；本报告即 AC7 落盘物 |
| 8 | 独占 PG，不并行 | [✓] | 全程唯一 PG=便携版 .pg/data @54329（本卡启动），无并行代码任务 |

## 2. AC2 影响面自查（grep 命中清单）

```
Grep "imageUrl" packages/shared →
  src/schemas/dish.ts:57        （唯一 zod 校验点：imageUrl 字段定义）← 本卡改动点
  src/types/index.ts            （仅 z.infer 类型导出，非校验）
  test/schemas.spec.ts          （测试用例，非校验点）

Grep "\.url\(\)|z\.union|DishSchema" packages/shared/src →
  src/schemas/dish.ts:42  DishSchema 定义
  src/schemas/dish.ts:58  z.union（本卡改）
  src/schemas/dish.ts:60  sourceUrl z.string().url()（保持不动）
  src/schemas/menu.ts:37  MenuDishSchema = { menuId, dishId, sort }，不内嵌 Dish，无 imageUrl
  src/types/index.ts      仅类型导出
```

结论：imageUrl 的 zod 校验点全 shared 库仅 dish.ts 一处；无其他校验点，未触发停手条件。既有用例 `not-a-url`/`http://` 拒绝行为在新口径下不变（两分支均不匹配），无需修改。

## 3. 改动文件清单（git diff --stat，本卡 5 文件 +94/-3）

| 文件 | ± | 说明 |
|---|---|---|
| packages/shared/src/schemas/dish.ts | +10/-1 | 契约对照：改前 `imageUrl: z.string().url().optional()`（仅绝对 URL）→ 改后 `z.union([z.string().url(), z.string().regex(/^\/images\//)]).optional()`（绝对 URL 或 /images/ 相对路径二选一），空串仍拒绝；头部补 v0.7 版本注释 |
| packages/shared/test/schemas.spec.ts | +24 | 2 个 T-P09 用例（相对路径通过 / 非法值+空串拒绝），61→63 |
| tools/content-pipeline/fetch2dish.mjs | +3/-1 | parseArgs 补 `--base-url=` 等号形式拦截（与空格形式同口径显式报错 exit 1，杜绝静默吞参） |
| tools/content-pipeline/test/fetch2dish.spec.ts | +28 | 新 describe 2 用例：空格形式回归 + 等号形式补漏（spawnSync 子进程实测 exit=1/stderr 含「已废弃」），33→35 |
| tools/content-pipeline/test/import.spec.ts | +30 | 4 个 T-P09 用例（相对路径✓/绝对URL✓/foo/bar.jpg+缺斜杠✗/空串✗），25→29 |

注：shared src 改动后已重建 dist（`pnpm --filter @family-menu/shared build` BUILD_EXIT=0）——管线与 h5 经 workspace 解析到 dist，不重建则回归读旧契约（首轮管线 1 failed 即此因，见 §7 遗留-2）。

## 4. AC6 端到端实证

1. **夹具**：`.workflow-verify/tp09/fixture-tp09.dish.json` —— 关键字段：`name=T-P09契约验证菜`、`mealRole=MAIN`、`imageUrl="/images/dishes/tp09-verify/0.webp"`（相对路径）、`sourceUrl=https://www.xiaohongshu.com/explore/tp09verify00000000000000000`、`sourceSite=xiaohongshu`、`status=DRAFT`、`origin=FETCHED`、`ingredients=[]`。
2. **静态文件真实存在**：`apps/api/static/images/dishes/tp09-verify/0.webp`，make-webp.cjs 生成 30 字节真实 WEBP 魔数（`RIFF....WEBP`），MAKE_WEBP_EXIT=0。
3. **真实入库**：`pnpm run import <fixture> --origin FETCHED`（cwd=tools/content-pipeline，tsx 直跑 src/cli/import.ts，dotenv 读根 .env 的 DATABASE_URL）
   → 输出「导入成功：dishId=cmtw5tkwm0000gopg7il5tba5（status=DRAFT, origin=FETCHED，显式授权 --origin FETCHED，食材 0 个）」，**IMPORT_EXIT=0**。
4. **DB 行核验**（verify-dish.cjs，pg 直查，VERIFY_EXIT=0）：
   `id=cmtw5tkwm0000gopg7il5tba5 / name=T-P09契约验证菜 / imageUrl=/images/dishes/tp09-verify/0.webp（相对路径入库 ✓）/ sourceUrl=https://...（绝对 URL 不动 ✓）/ sourceSite=xiaohongshu / status=DRAFT / origin=FETCHED` → VERIFY_E2E PASS。
5. **清理**：MenuDish 引用=0（无关联可挂）→ DELETE Dish×1（RETURNING id=cmtw5tkwm0000gopg7il5tba5）→ RESIDUE_T_P09_DISH=0；静态目录 tp09-verify 已删（Test-Path=False）。CLEANUP_DB_EXIT=0。

## 5. 测试数字（AC5 汇总）

| 套件 | 命令 | 结果 | 退出码 |
|---|---|---|---|
| shared 直跑 | `pnpm exec vitest run packages/shared/test/`（仓库根） | 63 tests passed（61+2） | 0 |
| 管线 vitest | `pnpm test`（cwd=tools/content-pipeline，即 vitest run --root ../..） | **Test Files 13 passed (13) / Tests 361 passed (361)**（353 基线 + 8 新增），Duration 1.64s | 0 |
| 根 pnpm test | `pnpm test`（仓库根，同一套件） | **Test Files 13 passed (13) / Tests 361 passed (361)**，Duration 1.22s | 0 |
| h5 typecheck | `pnpm --dir apps/h5 typecheck`（tsc --noEmit） | 0 错误输出 | **0** |
| h5 build | `pnpm --dir apps/h5 build`（taro build --type h5） | webpack compiled with 25 warnings in 12.6s（warnings=NutUI CSS calc 解析告警+体积提示，0 error，与 T-P08 基线一致） | **0** |

关键文件级明细：fetch2dish.spec.ts 35 ✓ / import.spec.ts 29 ✓ / schemas.spec.ts 63 ✓ / projection-order.spec.ts **5 tests 全过（PG 在位真实执行 DB 集成断言，无 skip）**。

## 6. AC7 teardown 对账

- **七表基线**（recon.cjs，清理后终态，EXIT=0）：Dish=48 / Menu=13 / MenuDish=42 / Ingredient=79 / Plan=54 / Event=93 / CookLog=6 —— 7/7 PASS。
- **迁移数据不动**：MIGRATION REL=29（FETCHED 且 imageUrl LIKE '/images/%'）/ ABS=0（imageUrl LIKE 'http%'）—— 29 道存量迁移数据原样保留。
- **残留**：tp09% 夹具三表（Dish/Menu/MenuDish）= 0。
- **PG**：`pg_ctl -D .pg/data -m fast stop` → 「服务器进程已经关闭」，PG_STOP_EXIT=0；54329 监听=0。
- **三端口**：PORT_3000_LISTEN=0 / PORT_10086_LISTEN=0 / PORT_54329_LISTEN=0。
  （注：teardown 复查时发现 :3000 有前序会话遗留的 apps/api dev server——`tsx watch src/server.ts`，PID 13524/父 26904，非本卡启动——按 AC7「三端口=0」taskkill /T /F 先子后父杀净后归零。）
- **CDP**：`GET http://127.0.0.1:9222/json/version` → Browser=Edg/152.0.4191.66，浏览器存活未杀，登录态未触碰。

## 7. 遗留 / 卡外问题清单（移交主控，本卡未顺手修改）

1. **projection-order.spec.ts 既有缺陷（卡外，apps/api/test 不在本卡允许修改范围）**：PG 不可达时 beforeAll 探测失败 → 3 条 DB 测试 skip，但 afterAll 的 cleanup()（deleteMany）无连接容错，抛 `PrismaClientKnownRequestError: Can't reach database server at 127.0.0.1:54329` → 整个测试文件被标 failed（pt2-err.txt 留证）。与 T-P08 挂账「建议-1 无 PG 静默 skip 假绿」为同一文件的关联缺陷。本卡处置：启动 PG 后重跑，5 条 DB 集成测试全部真实执行通过（非 skip 掩盖）。建议主控单开小卡为 afterAll cleanup 补 try/catch 容错。
2. **shared dist 时序教训**：改 shared src 后管线/根回归必须先 `pnpm --filter @family-menu/shared build` 重建 dist，否则 workspace 解析读旧契约（本卡首轮管线 1 failed 即此因，重建后全绿）。
3. **:3000 前序遗留进程**：本卡 teardown 发现并杀净前序会话遗留的 API dev server（tsx watch）。建议后续任务收尾检查项补上 3000 端口复核。
4. **工具链经验（非缺陷）**：a) PowerShell 5.1 `Start-Process -ArgumentList` 数组元素含空格不加引号，pg_ctl `-o "-p 54329"` 必须用单字符串传参，否则报「无效的操作模式 54329」；b) pnpm run 传参勿用 `--` 分隔符（会原样传入脚本导致 commander 报 too many arguments），直接 `pnpm run import <file> --origin FETCHED`。
5. 本卡无新增依赖；未改 prisma/**、apps/api/src、apps/h5/src、shared 其他文件及任何禁改文件；运行时无任何 LLM API 调用。

## 8. 证据文件索引（.workflow-verify/tp09/）

- 测试输出：pt.txt / pt2.txt（首轮，dist 陈旧 1 failed）/ pt2-err.txt（projection-order afterAll 报错留证）/ pt3.txt（全绿 361）/ rt.txt（根全绿 361）/ tc.txt+tc-err.txt（typecheck）/ build.txt+build-err.txt（build）
- 脚本：recon.cjs（七表对账）/ make-webp.cjs / verify-dish.cjs / cleanup-dish.cjs / fixture-tp09.dish.json
- PG 生命周期：pgctl-out.txt / pgctl-err.txt（启动）/ pgstop-out.txt / pgstop-err.txt（停止）/ log-tp09.txt（PG 运行日志）
