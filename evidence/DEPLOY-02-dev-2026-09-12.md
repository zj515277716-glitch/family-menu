# DEPLOY-02 开发完成报告（fm-dev）

- 日期：2026-09-12
- 角色：fm-dev（开发执行者，只执行不批准交付）
- 任务卡：DEPLOY-02（shared 单包 test 级联问题：基线记录 → 单文件验证 → 处置）
- 工作目录：d:\codex\family-menu（Windows / PowerShell）
- 分支：feat/tp-06-menu-expansion（HEAD 46dadbb）
- 报告人：开发代理（fm-dev）

---

## 0. 现状说明（必须先读）：卡面前言与仓库现状不符

卡2 前言称"DEPLOY-01（卡1）已另行完成并等待审核中"。**本会话开工时仓库实证与之不符**：

- `tools/content-pipeline/package.json` 的 test script 仍是 `vitest run --root ../..`（未含 `--no-file-parallelism`）；
- `evidence/` 目录中不存在 DEPLOY-01 任何报告。

依 DEC-004（仓库文件与测试结果优先于聊天上下文），本次按消息内两卡顺序**先执行 DEPLOY-01，再执行 DEPLOY-02**。DEPLOY-01 的修复与验证记录见 [evidence/DEPLOY-01-dev-2026-09-12.md](./DEPLOY-01-dev-2026-09-12.md)；其 shared 空跑根因定位（vitest 4.1.10 源码级）是本卡处置的直接依据。该冲突作为卡外发现同步移交主控。

## 1. 做了什么

1. **AC1 基线**：shared 单包 test 修复前连跑 2 次，记录退出码与输出数字。
2. **AC2 单文件验证**：从根目录 `pnpm vitest run packages/shared/test/schemas.spec.ts` 验证 spec 本身完好。
3. **AC3 处置**：选择**修复** `packages/shared/package.json` test script（最小 1 行，与 content-pipeline 既有模式同构），并给出理由与行为变化说明。
4. **修复后验证**：shared 单包 2 连跑 + 根 `pnpm test` 回归 + git diff 最小化核对。

## 2. 没做什么（边界遵守）

- 未改根 package.json、根 vitest.config.ts、任何 spec 文件、任何 src/ 业务代码。
- 未新增任何依赖（0 个）。
- 未执行 git commit（按卡面要求，提交由主控处理；待审版本=当前工作树改动 + 本报告）。
- 处置未选择"改根 vitest 配置 / projects 化"方案——如涉根配置结构调整须停下报主控，本次方案完全不触碰。

## 3. AC 逐条自检

### AC1：shared 单包 test 连跑 2 次基线记录 —— [✓]

命令（cwd=d:\codex\family-menu，修复前执行）：

```powershell
pnpm --filter @family-menu/shared test; Write-Output "RUN1_EXIT=$LASTEXITCODE"; pnpm --filter @family-menu/shared test; Write-Output "RUN2_EXIT=$LASTEXITCODE"
```

| 次数 | 退出码 | 输出关键数字 |
|---|---|---|
| 第 1 次 | **RUN1_EXIT=1** | `No test files found, exiting with code 1`；root=`D:/codex/family-menu/packages/shared`；include 打印为根配置三条 glob |
| 第 2 次 | **RUN2_EXIT=1** | 同上，完全一致 |

- 与卡面预期（No test files found → exit 1）一致。根因链（配置上溯 + include 相对子包 root 解析）见 DEPLOY-01 报告 §3/§4，此处不重复。

### AC2：根目录单文件验证 —— [✓]（数字与卡面乱码预期不同，以实际为准）

命令（cwd=d:\codex\family-menu）：

```powershell
pnpm vitest run packages/shared/test/schemas.spec.ts; Write-Output "EXIT=$LASTEXITCODE"
```

- 结果：**EXIT=0**；Test Files 1 passed (1)；**Tests 68 passed (68)**；Duration 436ms。
- 卡面乱码处预期为 "24 passed"，实测 **68 passed**，以实际为准。68 与 T-P12 verify 口径 67 + T-P15 新增 1 = 68 自洽（逐 it 实数：family 7 + dish 16 + menu 6 + plan 8 + api 27 + constants 4 = 68）。
- 结论：spec 本身完好，空跑纯属配置加载/解析路径问题（非测试文件损坏或命名不匹配默认 include）。

### AC3：处置落地或备案成文 —— [✓]（选择"修复"）

**修改内容**（唯一改动行，`packages/shared/package.json` scripts.test）：

- 改前：`"test": "vitest run"`
- 改后：`"test": "vitest run --root ../.. --no-file-parallelism"`

**选择修复而非备案的理由**：

1. 与仓库既有惯例同构：content-pipeline 等包的 test script 即 `vitest run --root ../..` 模式（本会话 DEPLOY-01 已为其补串行参数），shared 采用同构 script 不引入新模式。
2. 使单包 test 恢复真实输出：从"空跑 exit 1"变为"可执行、可观测、exit 0"，`pnpm --filter @family-menu/shared test` 从此可用。
3. 必须同时带 `--no-file-parallelism`：`--root ../..` 会跑全仓 15 个测试文件，若保留文件并行将重蹈 AC12 flaky（与 DEPLOY-01 修复前完全同根因：文件并行 CPU 竞争）。补串行参数是同一行内的必要组成，非扩大改动。
4. 最小化：仅 1 行、无新依赖、不触碰根配置结构，符合 AC5 预授权落点。

**行为变化说明（如实声明）**：修复后 `pnpm --filter @family-menu/shared test` 执行的是**全仓 391 用例（15 文件）串行**，而非"仅 shared 的 68 用例"。这与所有其他子包 `--root ../..` 模式的既有行为一致。若未来要求"单包只跑本包测试"，需 vitest projects 化改造（涉根配置结构调整），超出本卡边界，已列入遗留移交主控。

**修复后验证**（同 AC1 命令）：

| 次数 | 退出码 | Test Files | Tests | Duration |
|---|---|---|---|---|
| 第 1 次 | RUN1_EXIT=0 | 15 passed (15) | 391 passed (391) | 5.46s |
| 第 2 次 | RUN2_EXIT=0 | 15 passed (15) | 391 passed (391) | 5.40s |

### AC4：根 `pnpm test` 不回归 —— [✓]

命令（cwd=d:\codex\family-menu）：`pnpm test; Write-Output "EXIT=$LASTEXITCODE"`

- 结果：EXIT=0；Test Files 15 passed (15)；**Tests 391 passed (391)**；Duration 5.85s；AC12 无 FAIL。
- PG 常驻口径（0 skipped）；总数 391 与既有基线一致，无回归。

### AC5：diff 最小化 —— [✓]

- 本卡改动仅 `packages/shared/package.json` 1 行（§3 AC3）。
- 本次会话工作树合计 diff（git diff --stat 原样，含 DEPLOY-01 的 1 行）：

```text
 packages/shared/package.json        | 2 +-
 tools/content-pipeline/package.json | 2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)
```

- src/ 业务代码 0 改动；spec 文件 0 改动；根 package.json / vitest.config.ts 0 改动；新增依赖 **0 个**。
- 工作树中另有若干**本会话之前已存在**的未跟踪文件（.pai/、docs/design/preview.*、docs/ui-redesign/、tests/engine-perf.mjs、tests/step08-deploy-validate.cjs、tools/append-log.mjs、tools/fm-*.tar.gz、tools/preview-server.mjs、复盘报告.md），本次未触碰。

## 4. 备份结论（若主控否决修复方案的备案口径）

若主控认为"单包 test 跑全仓"语义不可接受而选择备案：仅需将 `packages/shared/package.json` 的 test script 回退为 `"test": "vitest run"`（或 `"test": "echo skip"` 之类显式声明），空跑机理与根因以 DEPLOY-01 报告 §4 为准成文即可，无其他连带影响。该决策权在主控，本卡默认交付"修复"方案。

## 5. 未验证项 / 遗留

1. **单包语义 vs 全仓行为**：修复后 shared 单包 test 跑全仓 391 用例（与其他包既有模式一致），"只跑本包"需 vitest projects 化改造，涉根配置结构，移交主控决策（同 DEPLOY-01 报告 §7-2）。
2. **PG 未启动口径**：本卡所有全量跑均为 PG 常驻口径（391/391），未实测 PG 停止时 388 passed | 3 skipped 口径（总数 391 不变）。
3. 退出码 1→0 的语义变化：CI 中若有"shared 单包 test 必须失败"之类假设（不存在已知依赖），已被本修复改变——主控审核时请注意该行为变化声明（§3 AC3-4）。

## 6. 卡外发现（移交主控，未自行处理）

1. **卡2 前言与仓库现状不符**（见 §0）：卡1 在本会话开工前并未执行；本次按序补做。建议主控核对任务派发链路，避免"已完成待审核"状态与仓库实际状态脱钩。
2. **vitest 配置上溯普适风险**：任何子包裸 `vitest run` 都会上溯继承根配置并可能空跑（详见 DEPLOY-01 报告 §7-2）。本次修复后仓库内已无裸 script，风险暂清零；projects 化可根治。
3. 工作树历史遗留未跟踪文件若干（§3 AC5 列出），与本卡无关，建议主控另行收口。

---

fm-dev 签署：以上命令均真实执行，退出码与数字为终端原样输出摘录，无 Mock。
