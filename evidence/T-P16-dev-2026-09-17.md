# T-P16 开发完成报告：推荐套餐按 MealRole 槽位动态组合（方案2 · PD-018）

> 报告人：fm-dev ｜ 日期：2026-09-17 ｜ 任务卡：docs/workflow/tasks/T-P16-推荐套餐MealRole槽位动态组合.md（唯一任务来源）
> 基线 git HEAD：`a095661`（feat/tp-06-menu-expansion HEAD，RIBS-MENU 方案1 落地后）
> 交付分支：`feat/tp-16-role-slot-composition`（自 a095661 切出，不合 main、不推送）
> 代码提交：`e2e8b92`（13 files, +1227/−61）；本报告随后续 [T-P16] 提交入库（提交号见分支 HEAD）。

## 一、改动摘要（git diff a095661 --stat 实测：8 tracked files +443/−61，加 5 个新增文件，合计 e2e8b92 = 13 files, +1227/−61）

| 文件 | 变化 | 内容 |
|---|---|---|
| packages/engine/src/composition.ts | 新增 | AC1 组合层主模块：composeMenusByRole（L132）/ mainSlotCount（L22）/ ComposeInput（L27）/ CompositionResult（L39）/ pickMustUseCoverDishes（L83）/ fitsSlots（L115）/ buildPrepSequence（L59） |
| packages/engine/src/index.ts | +8 | 导出 composeMenusByRole / mainSlotCount / ComposeInput / CompositionResult，并补导出 filterSafeDishes / DishSafetyResult |
| packages/engine/src/safety.ts | +88 | 新增 DishSafetyResult（L195）/ filterSafeDishes（L209）：dish 级三 scope（INGREDIENT/DISH/TAG）安全判定，**复用 safetyFilter 内部口径**；既有 safetyFilter 逻辑零行为变更（纯新增导出） |
| apps/api/src/services/planService.ts | +139/−61 | ①generateRecommendation 改走组合层（L435-461，library=虚拟菜单）②swapPlan 全换分支菜级排除（L547-556）+候选不足 3 套库内兜底/如实缺省（L600-621，修复原 excludeMenuIds 残留 TS2304）③listPlans 虚拟菜单菜名从 candidates 快照回退（L981+）④addFeedback CookLog.menuId 外键 null 适配（L899-905） |
| apps/api/prisma/seed-data.ts | +197 | AC8：固化 RIBS-MENU 方案1 手工数据终态（11 食材 cuid L91 / 4 排骨菜 PUBLISHED L189 / 3 FETCHED 排骨菜含 2 汤 mealRole=SOUP 纠偏 L315-316 / 用料 L431 / ribs-menu-01/02/03 L522-586 + 10 条 MenuDish L606+） |
| apps/api/prisma/seed.ts | +12 | 全 upsert 幂等重放；dish update 分支同步 status+mealRole 两个纠偏维度（L149-153） |
| apps/api/test/seed.spec.ts | +46 | 断言同步：13 dishes（10 seed+3 FETCHED）/ 7 menus / 4 排骨菜全 PUBLISHED / 2 汤 SOUP / FETCHED 来源字段（T-P16 AC8 用例） |
| packages/engine/test/composition.spec.ts | 新增 | 13 用例 8 组：C1 槽位映射 / C2 安全预过滤 / C3 缺槽降级 / C4 时长口径 / C5 mustUse / C6 覆盖式生成 / C7 换一批剔除 / C8 确定性 |
| tests/tp16-db-counts.cjs | 新增 | 七表计数 + AC8 纠偏字段抽查脚本（node-pg 只读 SELECT） |
| tests/tp16-seed-destroy.cjs | 新增 | AC8 破坏恢复验证第一步（人为破坏 status/mealRole 各 1 行） |
| docs/workflow/tasks/T-P16-推荐套餐MealRole槽位动态组合.md | 新增 | 任务卡随分支入库 |

**产品文档说明**：PRODUCT-CONFIRMATION.md（+8/−8）与 docs/ai-rebuild/PRODUCT-DECISIONS.md（+6）的改动为**主控派发 T-P16 前完成的 PD-018 产品登记**（变更记录行 + C-3「按主菜+配菜+汤骨架动态组合」修订 + PD-018 决策条目），非 dev 授权范围改动；随本提交一并固定待审版本，请主控复核确认。

- 禁区核对：apps/h5/src/**、prisma/schema.prisma、现有迁移（prisma/migrations/**）、tools/content-pipeline src/** 均零改动（git diff a095661 --stat 无这些文件）；**packages/shared 零改动（连注释级也未动）**；**新增依赖 0**。

## 二、逐条 AC 自检

### AC1 引擎组合层（mealRole 归池 + 槽位映射）— [✓]
- `composeMenusByRole(dishes, options)`（[composition.ts L132](../../packages/engine/src/composition.ts)）：options = { people, exclusions, mustUseIngredientIds?, excludeDishIds? }，菜池仅由第一参数传入（修正过早期草案中 ComposeInput 冗余 dishes 字段——与第一参数重复致 api tsc TS2345，已删）。
- 槽位映射：mainSlotCount（L22）= ⌈people/2⌉（0/1/2 人→1、3/4→2、5/6→3）；SIDE=1、SOUP=1；STAPLE 不设槽（挂账，PD-018 口径）。
- mealRole 归池：MAIN/SIDE/SOUP 三池（excludeDishIds 先剔除——供换一批）。
- **覆盖轮转生成**：MAIN 按 mainSlots 切片轮转、SIDE/SOUP 逐套轮转，套数 = max(⌈mainLen/mainSlots⌉, sideLen, soupLen)——每个合格菜至少出现在一个虚拟菜单，**feasibility 宽集语义由此在组合层保证（feasibility.ts 零改动）**。
- 虚拟菜单：id `virt-001`…、name `动态组合 #N`、scene WEEKDAY_FAST；结果含 virtualMenus + slotShortages + safetyExcluded。
- composition.spec C1（people=4/5 首套构成）/ C6（MAIN4+SIDE3+SOUP2→3 套全覆盖）/ C8（同输入 JSON.stringify 相等，确定性）。

### AC2 safety 零回归（禁菜绝不入槽）— [✓]
- 组合前 dish 级预过滤 filterSafeDishes（safety.ts L209）：逐菜三 scope 判定（INGREDIENT/DISH/TAG），HARD 禁菜 + 成分未确认菜直接排除并带原因（safetyExcluded）；**复用 safetyFilter 内部判定函数，口径单一**，禁菜在入池前即被拦截。
- safety.ts 既有 safetyFilter 行为零变更（仅新增导出），禁忌集 100%：`pnpm test:taboo` 6 files / 108 passed。
- composition.spec C2：HARD 三 scope 全排除 + 成分未确认排除 + safetyExcluded 带原因。

### AC3 mustUse 不回归（PD-001 硬要求）— [✓]
- mustUse 定向套：pickMustUseCoverDishes（L83）贪心选出覆盖全部必消食材的菜同桌；fitsSlots（L115）校验与槽位映射冲突则放弃定向套（退回覆盖轮转）；空槽从池头确定性填充。
- 空手判定仍由 recommend/feasibility 层原样负责（组合层只提供 library）：unmetMustUse + unmetReasons 侧车（TIME_BUDGET/NO_DISH）原样透传（planService L463-478，idToRaw 回译同键集一一对应）。
- composition.spec C5：覆盖菜全同桌 + 槽位冲突放弃。

### AC4 时长档 — [✓]
- 虚拟菜单 totalActiveMinutes = dishes.reduce(sum+activeMinutes)（composition.spec C4：50=15+20+5+10，修正过一次断言算术错误——实现正确、测试算错）；prepSequence 串行展开单调不减。
- 时长档过滤在 recommend 层原样生效（timeBudgetMin 透传 L447）；换一批候选不足 3 套时库内兜底补充含超时虚拟菜单（0.5 分，保留原「放宽时间限制」语义，planService L604-620）。

### AC5 缺槽降级（留空 + 如实标注、不凑数）— [✓]
- 某槽位池空 → 该槽留空 + slotShortages 标注（如「配菜缺位」）；主菜不足 ⌈people/2⌉ 道如实标注缺 N 道；**绝不用其他角色菜凑数**；全池被禁 → 零产出。
- slotShortages 并入候选 reasons 字符串数组透出（planService L458 主推荐 / L595 换批，同口径、零契约变更）。
- composition.spec C3：配菜/汤全空留空 + 主菜缺 N 道标注 + 全池被禁零产出。

### AC6 换一批 / 换一道不回归 — [✓]
- **换一批**（swapPlan 全换分支）：当前候选集用过的全部菜 → excludeDishIds（L547-556，从 candidates.menu 快照收集菜品级 id）→ 重新组合（L570-575）；虚拟菜单 id 每次动态生成，原 menuId 级排除已无意义，升级为菜品级排除。候选不足 3 套：组合库兜底补充（不重复 menuId），**库耗尽如实返回不足 3 套，不拿旧候选回填凑数**（L600-621；该段修复了原 excludeMenuIds 残留导致的 TS2304 与旧行为违背换批语义两问题）。
- **换一道**（单换分支，零改动）：hydrateLockedMenu **快照优先**（L208-218，DEC-013 方案 B——虚拟菜单必带 candidates.menu 快照，构造上必命中）→ runSwapFilter 五层过滤（safety/器具/时长/mustUse）→ 新菜继承被换菜槽位、prepSequence/totalActiveMinutes 重算 → candidates 原地重写快照（L691）。getSwapOptions 同走快照（L722-730）。
- 根 pnpm test 全绿，含既有 swap/contract 用例（真实 PG 53 用例）。

### AC7 契约与保存（响应结构不变；schema 改动=停手）— [✓]
- shared 契约零改动：CandidateSchema（menuId/score/reasons/breakdown?/menu?）原样，缺槽侧车并入既有 reasons 数组；PlanSchema 不动。git diff a095661 -- packages/shared 为空。
- 保存：plan.candidates Json 快照持久化（DEC-013 方案 B，零迁移）；listPlans 历史页虚拟菜单 id 查 MenuDish 必落空 → 从 candidates.menu.dishes[].name 回填菜名（L981+），快照缺失则如实缺省。
- **schema.prisma 与现有迁移零改动**（diff --stat 无 prisma 文件——未触发停手条件）。
- FK 矩阵核对：Plan.lockedMenuId 无外键（虚拟 id 直存无碍）；Event 无 menuId 列；**唯一冲突点 CookLog.menuId（FK→Menu）已适配**：virt-* 前缀置 null（L899-905），result/willRepeat/actualMinutes 照常落库，不伪造 menuId。

### AC8 方案1 数据固化为幂等 seed — [✓]
- seed-data.ts 固化方案1 手工数据**终态**：11 种食材（保留线上 cuid）；4 道排骨菜 PUBLISHED 升格（原 TESTED）；3 道 FETCHED 排骨菜（其中 2 汤 mealRole=SOUP 为方案1 纠偏终态，原入库误标 MAIN）；ribs-menu-01/02/03（prepSequence 原样固化 DB 现值）+ 10 条 MenuDish。
- seed.ts 全 upsert 幂等；dish update 分支同步 status+mealRole（防存量行漂移）。
- **破坏→重放→恢复闭环实测（2026-09-17 重跑取证，真实输出见 §三第 6 项）**：破坏 2 纠偏字段（destroyed rows: status=1, mealRole=1）→ `pnpm db:seed` 重放成功 → 全部恢复 PUBLISHED/SOUP，七表计数与基线一致。
- seed.spec.ts 断言同步 20/20（含 13 dishes/7 menus/4 排骨菜/2 汤/FETCHED 来源字段）。

### AC9 全量回归 + teardown 七表基线实测 — [✓]
- 四绿一记录（§三表）：pnpm test 471/471、test:taboo 108/108、api tsc EXIT=0、七表基线一致；h5 typecheck 6 错为 **pre-existing 已取证**（§五），h5 禁改移交主控。
- 七表基线实测（与基线**完全一致**）：Dish=49 / Menu=16 / MenuDish=52 / Ingredient=81 / Plan=59 / Event=98 / CookLog=6。
- AC8 抽查：4 排骨菜全 PUBLISHED（2 汤 SOUP）；ribs-menu-01/02/03 全 PUBLISHED（25/43/30min，3/4/3 道菜）；seed 标记 {"seedIngredients":17,"seedDishes":10,"fetchedDishes":30}。

## 三、测试命令与数字

| # | 命令 | cwd | 结果 | 退出码 |
|---|---|---|---|---|
| 1 | `pnpm --filter @family-menu/engine build` | 仓库根 | composition.ts 新增后重建 dist（engine main 指 dist，为 api tsc 前置） | 0 |
| 2 | `pnpm test`（便携 PG 常驻 54329） | 仓库根 | **21 files / 471 passed**（含 composition.spec 13/13、contract.spec 53 真实 PG、dishes.spec 10、seed.spec 20/20） | 0 |
| 3 | `pnpm test:taboo` | 仓库根 | **6 files / 108 passed**（AC2 禁忌零回归） | 0 |
| 4 | `pnpm --filter @family-menu/api exec tsc --noEmit` | 仓库根 | 0 错 | 0 |
| 5 | `pnpm --filter @family-menu/h5 typecheck` | 仓库根 | 6 errors（**pre-existing**，§五.1） | 1 |
| 6 | `node tests/tp16-seed-destroy.cjs` → `pnpm db:seed` → `node tests/tp16-db-counts.cjs` | 仓库根 | 破坏→重放→恢复闭环（真实输出见下） | 0（末位脚本） |
| 7 | `git diff a095661 --stat -- apps/h5 packages/shared` | 仓库根 | 空输出（两目录零改动取证） | 0 |

第 6 项真实输出摘录（2026-09-17）：

```
[before-destroy] cmtvkq3dl00009cpgaeksvumo | PUBLISHED | SOUP
[before-destroy] seed-dish-potato-ribs | PUBLISHED | MAIN
destroyed rows: status=1, mealRole=1
[after-destroy] cmtvkq3dl00009cpgaeksvumo | PUBLISHED | MAIN
[after-destroy] seed-dish-potato-ribs | TESTED | MAIN
Seed data validation passed (shared v0.1 zod schema).
Seed completed: 1 family + 1 rule + 3 exclusions + 28 ingredients + 13 dishes + 53 dish-ingredients + 7 menus + 23 menu-dishes.
=== seven-table counts ===
{ "Dish": 49, "Menu": 16, "MenuDish": 52, "Ingredient": 81, "Plan": 59, "Event": 98, "CookLog": 6 }
=== ribs dishes (AC8) ===
cmtvk2erk0000nopg3y2tqytq | PUBLISHED | MAIN | FETCHED | 零翻车！保姆级糖醋排骨！新手也能一次成功
cmtvkq3dl00009cpgaeksvumo | PUBLISHED | SOUP | FETCHED | 清热解暑冬瓜排骨汤，汤清肉烂全家爱喝
cmtvkqlfh000080pggqq7f7p0 | PUBLISHED | SOUP | FETCHED | ✔️巨简单、零翻车、超香甜的玉米排骨汤❗️
seed-dish-potato-ribs | PUBLISHED | MAIN | MANUAL | 土豆烧排骨
=== ribs menus (AC8) ===
ribs-menu-01 | PUBLISHED | 25min | dishes=3 | 糖醋排骨套餐
ribs-menu-02 | PUBLISHED | 43min | dishes=4 | 土豆烧排骨套餐
ribs-menu-03 | PUBLISHED | 30min | dishes=3 | 西兰花炒虾仁套餐
=== seed markers ===
{"seedIngredients":17,"seedDishes":10,"fetchedDishes":30}
```

- 说明：seed 写入数（13 dishes/7 menus/23 menu-dishes）是 seed 自有数据集 upsert 数；七表全量计数（Dish=49/Menu=16/MenuDish=52/Ingredient=81）含库内其他来源数据，两者口径不同，均与基线一致。

## 四、影响面核对

- **recommend（引擎评分层）零改动**：虚拟菜单与 MenuView 完全同构（menuId/name/scene/dishes/totalActiveMinutes/prepSequence），直接进评分管道。
- **feasibilityFilter 零改动**：宽集语义（∈宽集→TIME_BUDGET、∉→NO_DISH）由组合层覆盖式生成保证（每个合格菜至少出现在一个虚拟菜单）。
- **lockPlan/Event 零改动**：Plan.lockedMenuId 无外键、Event 无 menuId 列——虚拟 id 直存无碍；唯一 FK 点 CookLog.menuId 已 null 适配（AC7）。
- **h5 零改动**：dishes 列表渲染已泛化（PD-018「道数随槽位」预期零改动成立）；git diff 取证空。
- **shared 零改动**：契约 v0.11 不动，响应结构不变（AC7）。

## 五、卡外发现（不越界处理，移交主控）

1. **apps/h5 typecheck 6 错为 pre-existing（非本卡引入，已取证）**：`pnpm --filter @family-menu/h5 typecheck` 报 TS2322（targetName: string|undefined 不可赋给 string）×2 + TS2339（Property 'targetName' does not exist）×4。取证：`git diff a095661 --stat -- apps/h5 packages/shared` 空输出——本分支对 h5/shared 零改动，错误源自 shared ExclusionRule.targetName（optional）类型与 h5 既有渲染代码（今晚页忌口区 import ExclusionRule 直取 targetName）的既有不匹配，基线 a095661 时点即存在。h5 在本卡禁改清单，**未动**；建议主控评估是否派发 h5 侧修复任务（与 T-P16 无因果关系）。
2. **环境噪音**：`pnpm db:seed` 运行时沙箱拦截 prisma 对用户级遥测文件（prisma-nodejs/Config/commands.json）的写入并报 TRAE Sandbox Error——不影响 seed 本体执行（Seed completed 输出完整、恢复验证通过），仅记录。
3. 仓库存在历史遗留 untracked（.pai/、docs/design/、*.tar.gz、tools/*.mjs、tests/engine-perf.mjs、tests/step08-deploy-validate.cjs 等），非本卡产物，未纳入提交，维持原状。

## 六、未验证项

- 真实浏览器端到端实操（h5 屏②动态组合菜单渲染、换一批/换一道按钮实操、采购清单随虚拟菜单生成）未手测——本卡授权不含 h5，且 h5 typecheck 存在 pre-existing 6 错；UI 实操验收属 fm-verify 环节。
- 换一批/换一道在真实 PG 数据下的接口级路径由 contract.spec（53 用例）覆盖，未做人工浏览器实操。
- fm-reviewer 独立复审与 fm-verify 产品验收未执行（本报告仅为 dev 环节交付，待主控派发）。
- 公网 RDS 未涉及（本卡范围仅本机库）。
