# T-P11 产品验收报告（fm-verify · 独立验收）

- **验收卡**：T-P11 挂账清理——注释对齐与测试稳定性（队列②③⑨⑩，注释/测试级零行为变更卡）
- **验收角色**：fm-verify（产品验收，独立于 dev 与 review，只做验证不做修复）
- **验收日期**：2026-09-12
- **被测版本**：仓库工作区 @ HEAD `c4301e96e2f8367e20c87a60b67b97fe3d6dd294`（T-P11 改动为**未提交工作区改动**；验收全程未执行 git add/commit）
- **环境**：Windows + PowerShell 5.x；便携 PostgreSQL 17.5 @ 127.0.0.1:54329；pnpm monorepo
- **验收口径**：本卡无 UI/产品行为变更，无需 CDP/真机（未触碰 CDP 9222 与用户浏览器）；**不信任 dev 数字，全部回归由 fm-verify 本机独立复跑**；reviewer 口径「C-1/S-1 补正后即可转 VERIFY」，故本轮为最终验收轮
- **输入依据（DEC-004 完整阅读）**：任务卡 docs/workflow/tasks/T-P11-挂账清理注释对齐与测试稳定性.md（AC1-AC9+边界表+2026-09-12 审查补登）；evidence/T-P11-dev-2026-09-11.md；evidence/T-P11-review-2026-09-11.md；.workflow-verify/tp11/teardown.cjs（复用未改动）

---

## 结论：**PASS**

| # | 验收项 | 预期 | 实测 | 判定 |
|---|---|---|---|---|
| R1 | shared 重建 | EXIT=0 | EXIT=0 | PASS |
| R2 | pnpm test（根全量） | 389 passed | **389/389 首跑即绿**，EXIT=0 | PASS |
| R3 | pnpm test:taboo（一票否决） | 89/89 | **89/89**，EXIT=0 | PASS |
| R4 | tp01 must-use 回归（:3000 实跑） | 54 PASS / 0 FAIL | **54 PASS / 0 FAIL**，EXIT=0 | PASS |
| R5 | h5 typecheck | 0 错 | EXIT=0，0 错 | PASS |
| R6 | teardown 七表回基线 | Dish=48/Menu=13/MenuDish=42/Ingredient=79/Plan=54/Event=93/CookLog=6，REL=29 | 全部回基线，REL=29 | PASS |
| R7 | 零行为变更红线 | 改动全为注释/测试级，numstat 与 dev §2 一致 | 8 文件全对上，逐 hunk 全注释/测试级，零 schema/运行时变更 | PASS |
| R8 | 环境终态 | :3000=0、54329 监听=0 | 均为 0 | PASS |

任何一步均未出现 FAIL，未触发停手条款。

---

## 一、环境准备

### R0-1 PG 启动（127.0.0.1:54329）
- **Operation**：`.pg/bin/pg_ctl.exe -D .pg/data start` 启动便携 PG。
- **Expected**：PG 17.5 在 54329 端口 LISTENING。
- **Actual**：启动成功；netstat 复查确认 `127.0.0.1:54329` 与 `[::1]:54329` LISTENING，PID 44608。（备注：首次 netstat 查询在 PG 初始化完成前输出为空，换写法重查确认，非故障。）
- **Version**：工作区 @ `c4301e9`。
- **Evidence**：终端记录；后续 shared build/test/tp01/teardown 全部在该 PG 实例上真实执行。

### R1 shared 重建
- **Operation**：`pnpm --filter @family-menu/shared build`。
- **Expected**：EXIT=0，契约产物最新。
- **Actual**：EXIT=0。
- **Version**：同上。**Evidence**：终端记录（复跑前重建，保证回归基于当前工作区契约）。
- **Coverage**：仅构建产物重建，未修改任何契约源码。

---

## 二、独立回归复跑（不信任 dev 数字，全部本机实跑）

### R2 根全量 `pnpm test`
- **Operation**：PG 在线前提下 `pnpm test`（vitest run 全仓）。
- **Expected**（PRODUCT-CONFIRMATION 回归基线 + 任务卡）：389 passed；score.spec AC12 性能 <50ms 已知 flaky 允许重跑一次。
- **Actual**：**389/389 首跑即绿，EXIT=0**。AC12 性能断言本次未抖动，未动用重跑权。3 条 projection-order DB 用例在 PG 在线时为真实执行（非 skip）。
- **Version**：同上。
- **Evidence**：.workflow-verify/tp11v/verify-pnpm-test.log（40474 字节）。
- **Coverage 边界**：覆盖全部单测（shared/engine/list-merger/api 及跨包）；不含真机/H5 E2E（本卡无 UI 变更，任务卡明确豁免）。

### R3 `pnpm test:taboo`
- **Operation**：`pnpm test:taboo`。
- **Expected**：89/89（禁忌集 100%，一票否决项）。
- **Actual**：**89/89 全绿，EXIT=0**。
- **Evidence**：.workflow-verify/tp11v/verify-taboo.log（1094 字节）。
- **Coverage 边界**：仅禁忌集全量；备注：PS 5.x 对该命令 stderr 曾包装 NativeCommandError 误报，实际 EXIT=0，已核实非失败。

### R4 tp01 must-use 回归（真实页面链路后端侧）
- **Operation**：①非阻塞启动 API（dev 模式，日志 .workflow-verify/tp11v/verify-api-dev.log）；②轮询 :3000 就绪；③401 探测（无凭据请求受保护路由）；④`node tests/tp01-mustuse-regression.cjs`；⑤杀净 API 进程。
- **Expected**：探测 HTTP=401；tp01 **54 PASS / 0 FAIL**；终态 :3000 无监听。
- **Actual**：401 探测 **HTTP=401**（API 存活且鉴权生效）；tp01 **54 PASS / 0 FAIL，EXIT=0**；杀净后 `:3000` 监听数=**0**。
- **Version**：同上。
- **Evidence**：verify-tp01.log（9728 字节）、verify-api-dev.log（14086 字节）。
- **Coverage 边界**：tp01 覆盖必消匹配/空手/账本行为等后端行为面；本卡为注释级变更，预期行为零变化，实测 54/54 与基线一致。

### R5 h5 typecheck
- **Operation**：h5 工作区 tsc 类型检查。
- **Expected**：0 错，EXIT=0。
- **Actual**：EXIT=0，0 错。
- **Evidence**：verify-h5-typecheck.log（630 字节）。
- **Coverage 边界**：仅类型层（本卡 h5 改动仅 JSDoc 注释，预期零影响）。

---

## 三、teardown 闭环（复用 .workflow-verify/tp11/teardown.cjs，未改动）

### R6-1 dry-run（W1 防呆核对）
- **Operation**：`node .workflow-verify/tp11/teardown.cjs --dry-run`。
- **Expected**：CUTOFF=2026-09-11T16:05:00Z 窗口行 = Plan 增量与 Event 增量完全一致（W1），逐条 id 均为本次 tp01 夹具 `cmtx*` 前缀，不得混入旧基线 `cmtn*` 行。
- **Actual**：**W1 PASS**（窗口行 Plan 8/8、Event 10/10，行数与增量严格相等）；逐条 id 全为 `cmtx*`，与 tp01 输出的 planId 对应；`cmtn*` 旧基线行混入 **0**、夹具残留 **0/0/0**。
- **Evidence**：verify-teardown-dry.log（2636 字节）。

### R6-2 实删 + 七表基线核对
- **Operation**：dry-run 通过后去掉 `--dry-run` 实删，核对 AFTER 七表计数与 REL。
- **Expected**：基线 Dish=48 / Menu=13 / MenuDish=42 / Ingredient=79 / Plan=54 / Event=93 / CookLog=6，REL=29。
- **Actual**：删除 **14 PASS / 0 FAIL**（Event 10/10、Plan 8/8）；AFTER 七表 **全部回基线**（Dish=48、Menu=13、MenuDish=42、Ingredient=79、Plan=54、Event=93、CookLog=6），REL=**29**。
- **Evidence**：verify-teardown-real.log（3764 字节）、teardown-result.json（脚本落盘）。
- **Coverage 边界**：仅删除 CUTOFF 窗口内 tp11 夹具数据；W1 若失配脚本自动中止（本次未触发，未动用「停手报主控」条款）。

---

## 四、零行为变更 diff 审计（红线）

### R7-1 改动面清单核对
- **Operation**：`git status --short` + `git diff --numstat`，对照 dev 报告 §2 逐行核对。
- **Expected**：仅 dev §2 声明的文件有改动。
- **Actual**：改动面与 dev §2 **完全一致，无多余文件**：

| 文件 | numstat | 归属 |
|---|---|---|
| apps/api/test/projection-order.spec.ts | +31 / −10 | 本卡 |
| apps/h5/src/types/index.ts | +4 / −3 | 本卡 |
| packages/engine/src/recommend.ts | +6 / −1 | 本卡 |
| packages/shared/src/schemas/api.ts | +10 / −4 | 本卡（含 C-1 补正） |
| packages/shared/src/schemas/dish.ts | +1 / −5 | 本卡（含 S-1 补正） |
| apps/api/prisma/seed-data.ts | +1 / −1 | 本卡 |
| CURRENT.md | 2 / 2 | **主控遗留**（T-P05 结案条目，非本卡改动，仅记录） |
| docs/workflow/tasks/T-P10-….md | 1 / 1 | **主控遗留**（编号说明，非本卡改动，仅记录） |

### R7-2 逐 hunk 红线审计（6 个本卡文件）
- **Operation**：`git diff` 分三批终端直显，逐 hunk 检查每个 ± 行性质。
- **Expected（红线）**：不出现 schema 字段/类型/枚举定义变更、运行时代码逻辑变更；全部为注释或测试级改动。
- **Actual（逐文件结论，全部通过）**：
  1. **seed-data.ts（+1/−1）**：仅 L50 注释改写；`export const ingredients = [` 等数据行未动。
  2. **recommend.ts（+6/−1）**：仅头注释补 `unsatisfiableMustUseReasons` 说明（TIME_BUDGET/NO_DISH）；`import type` 与函数体未动。
  3. **dish.ts（+1/−5）**：旧 v0.7 注释段 5 行删除，换单行指针指向 api.ts 账本；`import { z } from 'zod';` 与 schema 定义未动。
  4. **api.ts（+10/−4）**：hunk1 头部账本（注释）v0.7 移入 + v0.8 新措辞；hunk2（`@@ -196,8 +201,9 @@`）unmetReasons 字段 JSDoc（C-1）；`unmetReasons: z.record(z.string(), z.enum(['TIME_BUDGET','NO_DISH'])).optional(),` 定义行未动。
  5. **h5 types/index.ts（+4/−3）**：DishSnapshot 的 imageUrl/sourceSite 两字段 JSDoc 注释更新；字段声明行未动。
  6. **projection-order.spec.ts（+31/−10）**：纯测试级——头注释 skip 可见性说明、beforeAll catch+console.warn 输出不可达原因与修复命令、afterAll 改为 `if (dbReady) await cleanup(); await prisma.$disconnect();`、hydrate 测试名更新并补 8 行投影断言（有图菜 imageUrl 含 tp07-spec.jpg / sourceSite='xiachufang' / sourceUrl 含 xiachufang.com；无图菜三字段 undefined）。无 PG 时跳过逻辑保持显式可见。
- **判定**：**零 schema 定义变更、零运行时代码变更**，全部为注释/测试级。红线通过。
- **Evidence**：diff 终端直显记录（导出文件受 PS 5.x 编码问题影响不可靠，verify-diff-masterleftover.txt 1313B 为部分导出，内容乱码但可辨认 CURRENT.md 的 T-P05 结案/`c4301e9`/T-P10 编号说明等主控遗留文字，与「仅记录不判定」口径一致）。

### R7-3 旧措辞零残留（Grep 佐证）
- `仅在空手且|组合凑不进` @ packages/shared/src → **0 命中**（v0.8 新措辞替换干净）。
- `连续记录` @ packages/shared/src/schemas → **0 命中**（dish.ts L3 失实表述已消除）。

---

## 五、AC 抽查（Operation / Expected / Actual / Version / Evidence / Coverage）

### AC1 seed-data.ts 分层匹配注释
- **Operation**：Read apps/api/prisma/seed-data.ts L48-67 + git diff 核对。
- **Expected**（任务卡 AC1）：注释与实际匹配行为一致，说明分层匹配与歧义不猜原则。
- **Actual**：L50 注释为「别名原则：覆盖用户日常说法（必消输入按 name+aliases 分层匹配：层 1 精确等值 + 层 2 双向子串唯一命中，命中 ≥2 歧义不猜、未映射原文透传，见 planService.resolveMustUseIds / utils/must-use-matcher.ts）」，与 planService 实际行为一致；`ingredients` 数据未动。
- **Version**：@ c4301e9 工作区。**Evidence**：本报告引用原文；diff 审计。**Coverage**：注释正确性走查；matcher 行为由 tp01 54 PASS 佐证。

### AC2 projection-order.spec 无 PG 时显式 skip 可见
- **Operation**：**PG 已停止**（54329 监听=0）状态下独立执行 `pnpm exec vitest run apps/api/test/projection-order.spec.ts`。
- **Expected**（任务卡 AC2 + 2026-09-12 审查补登）：DB 用例显式 skip 且计数可见（如 `2 passed | 3 skipped`），EXIT=0；beforeAll 输出不可达原因与修复命令。
- **Actual**：**`Test Files 1 passed (1)` / `Tests 2 passed | 3 skipped (5)`，EXIT=0**，skip 计数显式可见；hydrate 用例 2 条照常执行。
- **Version**：同上。**Evidence**：终端记录（vitest v4.1.10，Duration 830ms）。**Coverage**：同时覆盖「根 pnpm test 复跑时 PG 在线、3 条 DB 用例真实执行」与「无 PG 显式 skip」两侧。

### AC5 契约账本连续性（api.ts 单一入口）
- **Operation**：Read packages/shared/src/schemas/api.ts L1-40 + diff 审计。
- **Expected**（AC5/AC6 + reviewer C-1/S-1）：账本 v0.2~v0.8 连续收敛于 api.ts 头部单一路径；v0.3 无专行（记于 unmetMustUse 字段注）口径保持；v0.7 条目含来源标注；L17-20 头部措辞与 L204-205 字段 JSDoc 一致。
- **Actual**：L3 v0.2、L4-5 v0.4、L6-7 v0.5、L8-11 v0.6、L12-16 v0.7（含「原记于 dish.ts 头部，T-P11 账本收敛移入此单一入口」来源标注）、L17-20 v0.8（新措辞含「C-7a 组合必消空手时为空对象 {}；正常推荐时缺省」）；**v0.3 无专行**与 S-1 口径一致；头部与字段注措辞一致。
- **Version**：同上。**Evidence**：Read 原文；diff 审计。**Coverage**：账本文本走查；行为面由 389/389+54/54 佐证。

### AC6 旧措辞零残留（C-1/S-1 补正闭环）
- **Operation**：Grep 全库（packages/shared/src）检索旧措辞；dish.ts L1-5 走查。
- **Expected**：无「仅在空手且/组合凑不进」旧措辞残留；dish.ts 不再有「连续记录」失实表述。
- **Actual**：两处 Grep 均 **0 命中**；dish.ts L3 现为单行指针「契约版本账本见 schemas/api.ts 头部（v0.2 起；v0.3 无专行，记于 api.ts unmetMustUse 字段注。T-P11 账本收敛）。」。
- **Version**：同上。**Evidence**：Grep 结果。**Coverage**：全 shared 源码文本级；reviewer 判 [△] 的 AC6 经补正后本轮判 [✓]。

### AC7 recommend.ts 头注释补 unsatisfiableMustUseReasons
- **Operation**：git diff packages/engine/src/recommend.ts 走查。
- **Expected**（AC7）：头注释说明 unsatisfiableMustUseReasons 两种取值语义。
- **Actual**：头注释含 TIME_BUDGET / NO_DISH 说明；`import type` 与引擎函数体未动（纯注释 +6/−1）。
- **Version**：同上。**Evidence**：diff 审计。**Coverage**：注释走查；行为由 389/389（含 engine 套件）佐证。

### 其余 AC（AC3/AC4/AC8/AC9）核对口径
- AC3/AC4（h5 注释、spec 注释）：经 diff 逐 hunk 审计确认为纯注释/测试级改动，配合 h5 typecheck 0 错与 389/389 全绿佐证。
- AC8（边界约束遵守）：验收过程**零源代码/测试/文档/配置修改、零新增依赖、零 git add/commit**（见第六节终态）。
- AC9（证据落盘）：本报告 + .workflow-verify/tp11v/ 九份产物（脱敏，无真实用户数据；seed/夹具均为测试假数据）。

---

## 六、环境终态与边界遵守

| 项 | 终态 | 判定 |
|---|---|---|
| API 进程 | 已杀净，`:3000` 监听数 = **0** | 达标 |
| PG | `pg_ctl stop` EXIT=0，`LISTEN_54329_LINES` = **0** | 达标（用完即停） |
| 数据库 | teardown 后七表全回基线，REL=29 | 达标 |
| 代码改动 | 验收全程只读；未改任何源代码/测试/文档/配置 | 达标 |
| git | 未 add/commit（HEAD 仍为 `c4301e9`，T-P11 改动保持工作区待主控处置） | 达标 |
| 新增依赖 | 0 | 达标 |
| CDP/真机 | 未触碰 CDP 9222 与用户浏览器（本卡豁免） | 达标 |

## 七、遗留与备注（不影响 PASS 判定）

1. **主控遗留 2 文件**：CURRENT.md（2/2，T-P05 结案条目）与 T-P10 卡（1/1，编号说明）在工作区有未提交改动，dev 报告已声明，与 dev §2 一致，属主控轨道，非本卡越界；建议主控随合并一并处置。
2. **证据导出编码问题**：PS 5.x 下 `>`/Out-File 对长输出导出产生 UTF-16/乱码/截断（verify-diff-*.txt 导出基本失败），故 diff 审计改为终端直显逐批完成；verify-diff-masterleftover.txt（1313B）内容乱码但主控遗留文字可辨认。此为验收工具链问题，不影响被测代码与判定。
3. **PS 5.x NativeCommandError 误报**：taboo/typecheck/api-dev 处 stderr 包装误报，均以实际 EXIT=0 核实非失败。
4. **score.spec AC12 性能断言**：本次 389 首跑即绿未触发 flaky 重跑；其 <50ms 断言的固有抖动属性保留记录，不构成本卡问题。

## 八、验收材料清单

- 任务卡：docs/workflow/tasks/T-P11-挂账清理注释对齐与测试稳定性.md
- dev 证据：evidence/T-P11-dev-2026-09-11.md；review 证据：evidence/T-P11-review-2026-09-11.md
- 本报告：evidence/T-P11-verify-2026-09-12.md
- 验收产物（脱敏）：.workflow-verify/tp11v/{verify-pnpm-test.log, verify-taboo.log, verify-tp01.log, verify-h5-typecheck.log, verify-api-dev.log, verify-teardown-dry.log, verify-teardown-real.log, verify-pg-stop.log, verify-diff-masterleftover.txt} + .workflow-verify/tp11/teardown-result.json

**fm-verify 最终结论：T-P11 验收 PASS，无阻塞项、无越界项，建议主控按流程处置合并。**
