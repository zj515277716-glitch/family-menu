# T-P14 独立验收报告：时区口径统一——6 处 DateTime 列迁移 timestamptz（挂账④）

> 验收对象：T-P14 待审版本（**未 commit**）= 基线 git HEAD `7fe1fe2`（分支 feat/tp-06-menu-expansion）+ 工作区改动 `M apps/api/prisma/schema.prisma`（+6/−6）+ 新增 `apps/api/prisma/migrations/20260912000000_timezone_timestamptz/`；另含不入库环境级改动 `.pg/data/postgresql.conf`（方案 D，`timezone='UTC'`）。
> 验收人角色：fm-verify（独立验收，不信任 dev/reviewer 任何数字断言，全部亲自复跑取证）
> 验收日期：2026-09-12
> **总判定：PASS**（9 项复跑全命中 + 2 项静态确认通过；与 dev/review 报告零数字差异；无卡外改动）

---

## 一、环境记录

| 动作 | 命令原文 | 结果 |
|---|---|---|
| PG 启动前端口确认 | `Get-NetTCPConnection -LocalPort 54329` | `54329 NOT LISTENING`（另 3000 亦 NOT LISTENING，起点干净） |
| PG 启动 | `& "d:\codex\family-menu\.pg\bin\pg_ctl.exe" -D "d:/codex/family-menu/.pg/data" -o "-p 54329" -l "d:/codex/family-menu/.pg/pg-tp14verify.log" start` | 输出「服务器进程已经启动」；随后 `Get-NetTCPConnection` → `54329 LISTENING pid=26688` |
| dev server 启动 | apps/api 目录 `pnpm dev`（tsx watch src/server.ts，非阻塞） | 日志 `Server listening at http://127.0.0.1:3000`（pid 17984）；健康检查 `GET /health` → 200，body=`{"status":"ok","timestamp":"2026-09-12T04:36:47.170Z"}`（UTC Z 渲染） |
| dev server 停止 | StopCommand（job-71ca5942260b4213a2fcd06bec74b75c） | 执行成功 |
| PG 停止 | `& "d:\codex\family-menu\.pg\bin\pg_ctl.exe" -D "d:/codex/family-menu/.pg/data" -m fast stop` | EXIT=0，输出「服务器进程已经关闭」 |
| 终态端口确认 | `Get-NetTCPConnection -LocalPort 54329` / `-LocalPort 3000` | **`54329 CLOSED` + `3000 CLOSED`（双闭）** |

---

## 二、复跑结果表（9 项，命令原文 + 退出码 + 实测 vs 预期并列）

**执行顺序披露**：本验收先按清单顺序跑了 tp01（第 3 项，54 PASS/0 FAIL，证据在案），随即发现 tp01 残留（8 Plan + 10 Event）会把 Plan/Event 的 max 值顶到 2026-09-12，污染第 7/8 项断言。故调整为：**清理残留恢复基线 → 干净执行第 7/8 项 → 再跑第 4 项 tp12 → 终态计数复核**。所有断言数字均在基线库上取得，该顺序调整不影响任何结论；tp01 首跑与清理证据完整在案（本表 §3 与 §9）。

| # | 项目 | 命令原文 | 退出码 | 实测 | 预期 | 判定 |
|---|---|---|---|---|---|---|
| 1 | 根回归 | `pnpm test`（根目录） | 0 | Test Files **15 passed (15)**；Tests **390 passed (390)**；Duration 16.17s；输出经 `(skipped\|todo\|failed)` 过滤复查**零命中** | Test Files 15 passed；Tests 390 passed (390)；0 skipped | [✓] |
| 2 | 禁忌集 | `pnpm test:taboo` | 0 | Test Files 4 passed (4)；Tests **89 passed (89)** | 89/89 | [✓] |
| 3 | tp01 | `node tests/tp01-mustuse-regression.cjs`（dev server :3000 在跑） | 0 | **54 PASS / 0 FAIL**（末行 `== 结果: 54 PASS / 0 FAIL ==`） | 54 PASS / 0 FAIL | [✓] |
| 4 | tp12 | `node tests/tp12-feedback-null.cjs` | 0 | **18 PASS / 0 FAIL**；关键抽查：`submittedAt=2026-09-12T04:40:03.020Z`（**UTC Z 渲染**，与事件创建时刻一致）；teardown 自建 3 Plan+3 Event 白名单精确删除、残留=0、七表回基线 | 18 PASS / 0 FAIL；submittedAt 为 UTC 读回 | [✓] |
| 5 | h5 typecheck | apps/h5 目录 `pnpm typecheck`（package.json scripts 实名确认 = `tsc --noEmit`） | 0 | 输出仅命令行本身，**0 错** | 0 错 | [✓] |
| 6 | api tsc | apps/api 目录 `pnpm exec tsc -p tsconfig.json --noEmit` | 0 | 无输出，**0 错** | 0 错 | [✓] |
| 7 | AC3 SQL 侧读复 | `node .workflow-verify/tp14/sql-verify.cjs`（node pg 直查，默认连接，camelCase 列名双引号） | 0 | 见下方逐项对照，**6/6 精确命中 + 6 列类型全对 + timezone 双项确认** | 见下 | [✓] |
| 8 | AC3 Prisma 侧抽查 | apps/api 目录 `pnpm exec tsx ../../.workflow-verify/tp14/prisma-verify.mts`（生成客户端=应用同源 `src/generated/prisma/client.ts`；连接形态与应用 db.ts 同构：`new Pool({connectionString})` + `new PrismaPg(pool)` + `new PrismaClient({adapter})`，**无任何 timezone options 注入**） | 0 | `assertHits: 7`（**7/7 HIT**，全部 Z 渲染） | 7 项 Z 渲染且与 SQL 侧同绝对时刻 | [✓] |
| 9 | teardown 七表计数 + REL | `node .workflow-verify/tp14/teardown.cjs`（幂等 v2：探查 → 打印 → 断言 → 必要时删 → 计数） | 0 | `待删 Plan: 0 / 待删 Event: 0`（无残留）；计数终态 `{"Dish":48,"Menu":13,"MenuDish":42,"Ingredient":79,"Plan":54,"Event":93,"CookLog":6,"REL":29}` | Dish=48/Menu=13/MenuDish=42/Ingredient=79/Plan=54/Event=93/CookLog=6；REL=29 | [✓] |

### 第 7 项逐项对照（SQL 侧，::text 渲染 + ISO 双口径）

| 断言 | 实测（::text） | 预期 | 判定 |
|---|---|---|---|
| `SHOW timezone` | `UTC` | UTC | 命中 |
| `SELECT max("createdAt") FROM "Plan"` | `2026-09-06 09:04:09.747+00`（iso `2026-09-06T09:04:09.747Z`） | `2026-09-06 09:04:09.747+00` | 命中 |
| `SELECT max("createdAt") FROM "Event"` | `2026-09-06 09:04:09.754+00`（iso `…T09:04:09.754Z`） | `2026-09-06 09:04:09.754+00` | 命中 |
| `SELECT max("planDate") FROM "Plan"` | `2026-09-06 09:04:09.745+00`（iso `…T09:04:09.745Z`） | `2026-09-06 09:04:09.745+00` | 命中 |
| `SELECT max("cookedAt") FROM "CookLog"` | `2026-09-10 09:55:58.267+00`（iso `2026-09-10T09:55:58.267Z`） | `2026-09-10 09:55:58.267+00` | 命中 |
| `SELECT max("createdAt") FROM "Family"` | `2026-08-06 00:00:00+00`（iso `2026-08-06T00:00:00.000Z`） | `2026-08-06 00:00:00+00` | 命中 |
| `SELECT max("updatedAt") FROM "FamilyRule"` | `2026-08-06 00:00:00+00`（iso `2026-08-06T00:00:00.000Z`） | `2026-08-06 00:00:00+00` | 命中 |

6 列数据类型实测（information_schema，Plan/Event/CookLog/Family/FamilyRule 六列）**全部 `timestamp with time zone`**。迁移记录 `_prisma_migrations` 4 条全部 `finished=true`（含 `20260912000000_timezone_timestamptz`）。

### 第 8 项逐项对照（Prisma 侧，toISOString）

| # | 断言 | 实测 | 预期 | 判定 |
|---|---|---|---|---|
| 1 | Plan.max(createdAt) | `2026-09-06T09:04:09.747Z` | 同左 | HIT |
| 2 | Plan.max(planDate)（aggregate） | `2026-09-06T09:04:09.745Z` | 同左 | HIT |
| 3 | Plan.maxRow.planDate（最新行字段，双口径加固） | `2026-09-06T09:04:09.745Z` | 同左 | HIT |
| 4 | Event.max(createdAt) | `2026-09-06T09:04:09.754Z` | 同左 | HIT |
| 5 | CookLog.max(cookedAt) | `2026-09-10T09:55:58.267Z` | 同左 | HIT |
| 6 | Family.max(createdAt) | `2026-08-06T00:00:00.000Z` | 同左 | HIT |
| 7 | FamilyRule.max(updatedAt) | `2026-08-06T00:00:00.000Z` | 同左 | HIT |

Prisma 侧与 SQL 侧同值不同渲染（Z vs +00），绝对时刻完全一致 = 读回即写入真实绝对时刻，AC3 双向语义闭环成立。

### 第 9 项过程证据（清理窗口）

- tp01 首跑后残留探查（只读快照，污染态）：`Plan` 计数 62（=54 基线+8 残留）、`Event` 103（=93+10）；残留 id 全量打印在案（8 Plan + 10 Event，与 dev 报告 §5 #7「删 tp01 遗留 8 Plan + 10 Event」**数量与口径双一致**）。
- 清理动作：按 dev 同口径 `Plan/Event."createdAt" > '2026-09-07'`（timestamptz + UTC 会话下裸字符串按 UTC 解释）删除，先打印待删 id、断言数量（8/10）通过后执行，`已删 Event: 10 已删 Plan: 8`。
- 清理后基线复核 + tp12 自清后终态复核（两次）均为 `48/13/42/79/54/93/6 + REL=29`。

---

## 三、静态确认（2 项）

1. **待审版本范围**：`git status --short` 实测 = `M CURRENT.md`、`M apps/api/prisma/schema.prisma`、untracked：新迁移目录 `apps/api/prisma/migrations/20260912000000_timezone_timestamptz/`、任务卡、`evidence/T-P14-dev-2026-09-12.md`、`evidence/T-P14-review-2026-09-12.md`，以及历史遗留 untracked（`.pai/`、`docs/design/`、`docs/ui-redesign/`、`tests/engine-perf.mjs`、`tests/step08-deploy-validate.cjs`、`tools/append-log.mjs`、`tools/preview-server.mjs`、`tools/fm-*.tar.gz`、`复盘报告.md`）。上述历史遗留文件经 `Get-Item` 实测创建时间 **2026-08-07 ~ 2026-09-06，全部早于本卡开工日**，非 T-P14 引入。`git --no-pager diff --stat` 实测仅含两文件：`CURRENT.md | 2 +-`（+1/−1，主控维护内容）与 `apps/api/prisma/schema.prisma | 12 ++++++------`（**+6/−6**，与 dev §1 numstat 一致）。**无卡外代码改动**。
2. **`SHOW log_timezone`**：实测 `Asia/Shanghai`（在 sql-verify 快照内证）——dev §2-2 误改恢复的最终态经我方动态查询独立确认（与 reviewer 对 conf 文件的静态读核相互印证：L740 `timezone='UTC'`、L622 `log_timezone='Asia/Shanghai'`）。

另核对 [migration.sql](../../apps/api/prisma/migrations/20260912000000_timezone_timestamptz/migration.sql) 与 [schema.prisma](../../apps/api/prisma/schema.prisma)：6 条 `ALTER ... TYPE TIMESTAMPTZ(3)` 全带 USING（5×`AT TIME ZONE 'UTC'` + CookLog×`AT TIME ZONE 'Asia/Shanghai'`，方向与任务卡 AC2 零偏差）；头部含每列钟面归属依据注释；全文无本机路径/端口硬编码；schema 6 处 `@db.Timestamptz`（L29/L40/L178/L193/L200/L217）与卡面清单逐一对应。

---

## 四、与 dev/review 报告的差异清单

**数字差异：零。** 9 项复跑数字与 dev §4/§5、review「三、数字复核移交清单」全部一致。仅以下两项非差异披露：

1. tp12 `submittedAt` 数值不同：dev §5 #4 = `2026-09-12T03:34:59.631Z`，本验收 = `2026-09-12T04:40:03.020Z`——**形态一致（UTC Z 渲染）**，数值差异源于两次运行时刻不同，属预期。
2. 执行顺序调整（见第二节头部披露）：先 tp01 后清理再补第 7/8 项，纯属验收执行工程安排，未改变任何被测对象与结论；tp01 首跑 54 PASS/0 FAIL 与残留清理证据均完整在案。

S-1（迁移前读回模型叙事矛盾）已由主控裁决闭环（review 附录六：adapter-pg@7.9.1 `normalize_timestamp`/`normalize_timestamptz` 源码实证，dev §9 为真，历史「慢 8h」系 node-pg 直查路径行为），本验收无需再裁决；本方取得的 tp12 `submittedAt` UTC Z 读回、Prisma/SQL 双侧同绝对时刻等证据与该裁决口径完全相容，无新增矛盾。

---

## 五、验收覆盖边界

**已覆盖**：AC3 迁移后双向读回（SQL ::text 渲染 + Prisma toISOString 双口径 13 断言全命中）；AC4 全量回归六命令（含 387+3=390 口径自洽复核：本次根回归 0 skipped，3 条 projection-order DB 集成用例真实执行）；AC5 teardown 七表回基线 + REL=29；AC7 迁移文件静态复核；环境处置方案 D 生效态（`SHOW timezone`=UTC、`log_timezone` 不变）；待审版本范围与禁区零越界（git 层面）。

**未覆盖（照实转述，不新增义务）**：
1. **公网 RDS deploy 未验证**——本卡仅便携 PG 本地实证，RDS 迁移 deploy + 参数组 `timezone=UTC` 属部署对接（dev §13-1/§10-2，对接挂账①）。
2. **checksum 漂移挂账**——`20260904000000_rescale_event_type` 本机 `_prisma_migrations` checksum 与迁移目录文件不一致，主控已记挂账，本卡维持 deploy-only 未处置（dev §10-1）；本验收 `_prisma_migrations` 复核仅确认 4 条 `finished=true`，未做 checksum 比对（属挂账项非本卡 AC）。
3. **h5 真机回归未做**——本卡 h5 零改动，typecheck 0 错已实证；formatDate 日期提前一天问题的用户可见修复属部署后观察项（dev §13-3）。
4. **conf 改动不入库的部署映射**——方案 D 为本机 .pg/ 环境级，云上等价物=RDS 参数组，主控部署时按环境择一（dev §13-5）。
5. **3 条 DB 集成用例口径变化**——T-P11/12/13 根回归均无 PG（387+3skip），本会话首次真实执行（390/390 0 skipped），属任务卡 AC4 预告的合法口径变化，非异常。

**验收方过程披露（不影响结论）**：teardown 脚本 v1 存在取值笔误（`pool.query(...)[0].c` 未取 `.rows`）导致删除成功后计数段报错退出，删除与数量断言本身已成功执行，计数终态随即由只读 sql-verify 独立复核确认；v2 修正为幂等脚本并重跑落盘证据。验证脚本与结果 JSON 均落 `.workflow-verify/tp14/`（sql-verify.cjs / prisma-verify.mts / teardown.cjs / sql-verify-result.json / prisma-verify-result.json / teardown-result.json），未触碰任何业务代码/配置/schema/迁移/测试文件，未执行任何 git 写操作。

---

## 六、结语

9 项复跑全数命中预期，2 项静态确认通过，与 dev/review 报告零数字差异，待审版本范围干净（仅 schema.prisma +6/−6 + 新迁移目录，CURRENT.md 为主控维护内容），环境已完整还原（dev server 停、PG -m fast stop、端口 3000/54329 双闭）。

**建议主控进入提交环节。**
