# T-P03 dev API 跨域（CORS）处理 — 四角色闭环证据归档

> 日期：2026-09-06 ｜ 分支 feat/tp-06-menu-expansion ｜ 基线 HEAD `3656d65`（T-P02 提交）
> 任务卡：[T-P03-devAPI-CORS处理.md](./tasks/T-P03-devAPI-CORS处理.md) ｜ E1 来源：[evidence/T-P02-2026-09-06.md](./T-P02-2026-09-06.md) §5 卡外发现

## 1. 三方结论

| 角色 | 结论 | 要点 |
|---|---|---|
| 主控 PM（派发） | — | 依据 T-P02 evidence §5 E1 + 用户裁决「开卡」；AC1 预检放行 / AC2 鉴权语义不变 / AC3 无挂具真实浏览器直调 / AC4 证据链 |
| fm-dev（开发） | 交付 | `@fastify/cors` ^11.3.0 白名单方案（app.ts +19/-1）+ 18 断言真实 HTTP 回归脚本 + 自跑 teardown（Plan 58→54 / Event→93）；选型附插件源码行号自证 |
| fm-reviewer（复审） | **通过** | 无阻断；4 条建议级 B-1~B-4（见 §7）；注册顺序 / credentials 安全性 / 脚本真实性 / AC2-d 证明力逐项源码级证实 |
| fm-verify（验收） | **通过** | AC1 18 PASS / 0 FAIL；AC2 鉴权语义 8 条不变；AC3 无挂具两场景 5 PASS + consoleErrors=[]；tp01 回归 17 PASS；teardown 60→54 / 99→93 精确还原；端口监听杀净=0 |

## 2. fm-dev 交付内容

- **apps/api/src/app.ts**（+19/-1）：L7 `import cors from '@fastify/cors';`；L49-66 注册块，位于 `register(cookie)` 之后、`addHook('preHandler', authHook)` 之前——插件 onRequest 钩子短路预检，先于 authHook（插件源码 index.js:192-213 注释明示，fm-dev 附 4 处源码行号自证注释，fm-reviewer 逐条证实属实）。
- **白名单口径**：origin = `['http://127.0.0.1:10086', 'http://localhost:10086']`（数组=全等匹配）；`credentials: true`（cookie 鉴权必须）；`allowedHeaders = ['Content-Type', 'Authorization']`；methods 默认。
- **新增依赖**：`@fastify/cors` ^11.3.0（pnpm-lock.yaml +22/-5，+4 包；Fastify 官方插件，peer fastify 5.x）——**待产品负责人裁决**。
- **tests/tp03-cors-regression.cjs**（新增，未跟踪）：18 断言真实 HTTP 回归——2 源 × 5 预检断言（204 / ACAO 回显 / ACAM / ACAH / ACAC:true）+ AC2 8 条（无令牌 401 带头 / 带令牌 200 / 同源不变等）。无 Mock，无条件执行，失败分支有效。
- **禁改区零越界**：packages/shared、packages/engine、packages/list-merger、apps/h5/**、apps/api/prisma/** 均未触碰。

## 3. AC1 预检放行 — 基线对照

**修复前基线**（.workflow-verify/tp03-baseline-preflight.txt）：OPTIONS 自 :10086 → **401**，acao/acam/allowHeader 均 **null**（零 CORS 头）——即 T-P02 evidence §5 E1 原状。

**修复后**（fm-verify 独立启服自写探针 probe-ac1-ac2.cjs）：2 源（127.0.0.1 / localhost）× 5 断言全过——

- 状态 **204**；`Access-Control-Allow-Origin` 按请求源回显；ACAM / ACAH 正确；`Access-Control-Allow-Credentials: true`
- 结果：**18 PASS / 0 FAIL**，probe-exit.txt = **0**（.workflow-verify/tp03-verify/probe-result.txt、probe-exit.txt）

## 4. AC2 鉴权语义不变

8 条断言全过（同 probe）：无令牌跨源真实请求仍 **401**（且带 CORS 头）；带 ACCESS_TOKEN cookie 的跨源推荐请求 **200**；同源请求行为不变（由 tp01 回归 17 PASS 佐证）；生产同源部署零行为变更（代码 diff 仅 dev 白名单，无环境分支）。

## 5. AC3 无挂具真实浏览器直调（本卡核心）

fm-verify Playwright **不注册任何 route 拦截**（对照 T-P02 需 route 挂具代答预检的方法学升级）：

| 场景 | 路径 | 结果 |
|---|---|---|
| 西红柿（单必消） | 设置必消 → 点推荐 | **出候选页**，截图 AC3-01/02 |
| 苦瓜+西红柿组合 | 设置必消 → 点推荐 | **空手卡**，截图 AC3-03/04 |

- ac3-summary.json：**5 PASS / 0 FAIL**；**consoleErrors=[]、pageErrors=[]、failedRequests=[]**
- 两场景均从 :10086 页面真实跨源直调 :3000，预检由服务端 CORS 插件真实放行

## 6. 回归与 teardown 对账

- **tp01 回归**（fm-verify 独立复跑）：17 PASS，tp01-verify-exit.txt = **0**
- **teardown 强制项**：fm-verify 独立复核 COUNT 前后对照——
  - before：**Plan=60 / Event=99** → after：**Plan=54 / Event=93**（= 本地基线 54/93，RESIDUAL=0）
  - teardown-clean.log：**DELETED 6/6** 逐条对账（AC2-b mustUse=['西红柿'] Plan 1 + AC3 两场景各 1 + tp01 残留 3）
- **进程清理**：post-kill-listen-count.txt = **0**（:3000 / :10086 监听数归零）
- **git-status-final.txt**：工作区状态与预期一致（fm-dev 3 文件修改 + 回归脚本未跟踪）

## 7. fm-reviewer 建议级问题（4 条，无阻断）与主控处置

| 编号 | 内容 | 处置 |
|---|---|---|
| B-1 | AC4 证据链仓库内不可见（.workflow-verify/ac4-exit.txt 仅一行） | **已实质闭环**：fm-verify 独立复跑 probe（18 PASS EXIT=0）+ teardown 前后 COUNT 独立复核，本文件 §3/§6 即补齐证据 |
| B-2 | credentials:true 时非白名单源也无条件写 ACAC:true（浏览器因无 ACAO 不会采用，无实际风险） | 口径修正：非白名单为「**无 ACAO 头**」而非「无 CORS 头」；本文件按修正口径表述 |
| B-3 | strictPreflight 默认开 → 裸 OPTIONS（无 Origin 头）从 401 变 **400** | 行为变化已披露；仅影响 curl 裸 OPTIONS 类调试场景，业务无感 |
| B-4 | 缺非白名单 Origin 预检断言（应验证 204 + acao=null） | 后续排期加固（不阻断本卡） |

## 8. 卡外发现（fm-verify，均不顺手修改）

1. .workflow-verify 根目录残留既往会话日志未清理（建议后续统一归档策略）。
2. apps/h5 无 `dev` script，实为 `dev:h5`（派发指令口径已在过程中校正）。
3. 非白名单 Origin 预检（204 + acao=null）会进服务端日志（仅记录，暂不处理）。

## 9. 未验证项（如实披露）

- 真机 / 多浏览器（仅 Chromium）；
- HTTPS 生产域名 CORS（生产同源部署，理论不受影响，未实测）；
- 第三方 Origin 的浏览器级行为（仅 curl 级 + 代码级推断）；
- 其余路由（推荐之外）的预检行为（抽样未覆盖全路由）；
- h5 生产构建回归（本卡改动仅 api 侧，未重跑 h5 build）。

## 10. 待裁决

1. **T-P03 改动是否 git 提交**——拟提交范围：apps/api/src/app.ts、apps/api/package.json、pnpm-lock.yaml、tests/tp03-cors-regression.cjs、docs/workflow/tasks/T-P03-devAPI-CORS处理.md、evidence/T-P03-2026-09-06.md（CURRENT.md 台账随提交，沿用 T-P02 先例）。
2. **新增依赖 @fastify/cors ^11.3.0 披露确认**——Fastify 官方 CORS 插件，+4 包，仅 dev 白名单生效，生产同源零行为变更。
