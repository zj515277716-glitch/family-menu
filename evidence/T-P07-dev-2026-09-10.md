# T-P07 开发报告（fm-dev）

> 任务卡：[docs/workflow/tasks/T-P07-API投影亮图链路与orderBy挂账.md](../docs/workflow/tasks/T-P07-API投影亮图链路与orderBy挂账.md)
> 分支基线：HEAD `123195f`（CURRENT.md 口径）· 执行窗口：2026-09-10 晚 ~ 2026-09-11 凌晨 · 环境：Windows / PowerShell / PG 54329（本卡独占）
> 结论：**8/8 AC 全 [✓]**，teardown 七表逐项对账一致，三端口归零，PG 已停。

---

## 1. 改动摘要（diff 实测 `git diff --numstat`）

| 文件 | +行 | -行 | 内容 |
|---|---|---|---|
| apps/api/src/services/mappers.ts | 27 | 2 | ① `DishRow` 增 imageUrl/sourceUrl/sourceSite 三字段；② 新增交叉类型 `DishMediaView = DishView & {imageUrl?/sourceUrl?/sourceSite?}`（不动 engine 本体）；③ `toDishView` 投影三字段（null→undefined，缺省即前端降级，不编造）；④ `toMenuView` 防御性 `[...row.dishes].sort((a,b)=>a.sort-b.sort)` 后映射（R-4） |
| apps/api/src/services/planService.ts | 7 | 1 | `loadMenuViews` 与 `hydrateLockedMenu` 两处 dishes 关联查询补 `orderBy: { sort: 'asc' }`（R-2）；两函数 `export` 供集成测试 |
| apps/h5/src/pages/dish/index.tsx | 3 | 1 | R-1：useDidShow catch 空操作 → `setLogState((s)=>(s==='loading'?'idle':s))`（函数式更新防闭包旧态；done/idle 不动=不伪造已提交）。最小 diff |
| apps/api/test/projection-order.spec.ts | 175（新增） | 0 | 投影+排序测试：DB 集成 3 条（乱序写入读出按序×2 + 端到端投影）+ 纯函数 2 条（防御排序 / null→undefined 与序列化键消失）；PG 不可达时 DB 用例自动 skip |

**shared 改动：0 字节。** AC2 单列说明：`packages/shared/src/schemas/dish.ts` 的 imageUrl/sourceUrl/sourceSite 三个可选字段 T-C01 已合入（HEAD 既有），本卡无需任何契约追加；API 层以 `DishMediaView` 交叉类型在映射层透传，多出字段随 JSON 快照透传给前端，字段缺省序列化后不出现（非破坏性，语义/必填/删除均未触碰）。apps/h5 仅改 dish/index.tsx（卡内授权范围），schema/migrations/采集管线/产品文档零改动。

## 2. 证据（命令 + 退出码 + 数字）

### 2.1 改动前基线
- `node .workflow-verify/tp07/db-baseline.cjs` → `{"plan":54,"event":93,"menu":13,"menudish":42,"dish":48,"ingredient":79,"cooklog":6}`；29 道 FETCHED DRAFT 菜均有图且全部不在 PUBLISHED 菜单 → 需临时夹具承载亮图链路
- `node .workflow-verify/tp07/api-baseline.cjs` → recommend 响应 `dish0_imageUrl_present=false`（改动前无投影，存证 baseline-recommend.json）
- `node .workflow-verify/tp07/r1-prefix.cjs` → R-1 缺陷复现：反馈查询失败后 dock 卡「正在记…」loading 态，3 秒后仍卡死（CHECK R2/R3，存证 r1-prefix-result.json + shots/r1-p4-stuck-loading.png）

### 2.2 静态检查与测试（AC5）
- `pnpm test`（仓库根，vitest run）→ **13 文件 / 351 测试全过，EXIT=0**（含新增 projection-order.spec.ts 5 条；test:taboo 即 packages/engine/test 子集，随 351 一同全绿）
- `pnpm typecheck`（apps/h5，tsc --noEmit）→ **0 错误，EXIT=0**
- `pnpm build`（apps/h5，taro build --type h5）→ **EXIT=0**（webpack compiled with 25 warnings in 43774ms；25 条均为既有 NutUI postcss-calc/体积告警，与本卡无关）

### 2.3 API 投影直证（AC1）
- `node .workflow-verify/tp07/api-post-probe.cjs`（改动后，recommend 必消=五花肉）→ CANDIDATES_N=1（夹具菜单唯一入选），dishes[0] 键集含 `imageUrl,sourceSite,sourceUrl`，`imageUrl=http://127.0.0.1:3000/images/dishes/6a8db1e5000000002003a91e/0.webp`，`sourceSite=xiaohongshu`（存证 post-recommend.json）

### 2.4 单测断言明细（AC3，apps/api/test/projection-order.spec.ts 5/5 过）
1. loadMenuViews：夹具 MenuDish 按物理乱序插入（C→sort2, A→sort0, B→sort1），读出 `dishes id 序 = [a,b,c]` = sort 升序 ✓
2. hydrateLockedMenu 懒水合路径（candidates 快照缺省）：读出同序 ✓
3. toMenuView 纯函数：乱序 MenuDishRow 输入 → 有序输出，且不改输入数组（纯函数性）✓
4. toDishView：三字段有值透传 / null→undefined；JSON 序列化后 `imageUrl` 键消失（缺省即降级）✓
5. loadMenuViews 端到端：有图菜三字段齐全、无图菜序列化后三键均不出现 ✓

### 2.5 真机亮图验证（AC6，CDP 9222 复用不杀浏览器，375×812 移动仿真，零注入零改 DB）
`node .workflow-verify/tp07/e2e-imaged.cjs` → **CHECK 14/14 PASS，result exitCode=0**（存证 e2e-result.json）。链路：tonight 页真实键入必消「五花肉」（CDP Input.insertText）→ 推荐（引擎硬过滤）→ candidates → 锁定 → plan → dish 详情 → 换道。

| 检查 | 结果 | 数字 |
|---|---|---|
| F0 夹具 sort 序（DB 面） | PASS | sort 0/1/2 = 夹具有图菜/紫菜蛋花汤/蒜蓉青菜 |
| T0 必消输入 | PASS | inputVal=五花肉 |
| C1 夹具菜单入选 | PASS | 候选含「T-P07夹具套餐（临时）」 |
| C2/C3 candidates 菜序（UI 面） | PASS | dishes[0]=有图菜；三菜=sort 升序 |
| P1 plan 页菜卡序（UI 面） | PASS | 同上（store 快照口径） |
| D1 有图菜亮图 | PASS | src=…/images/dishes/6a8db1e5…/0.webp |
| D2 图片真实加载 | PASS | naturalWidth×Height=1080×1440 |
| D3 大图区尺寸 | PASS | rect 375×500（≥375×340 要求） |
| D4 图上叠信息 | PASS | 菜名全等 + chips=5 + 渐变遮罩存在 |
| D5 dock 态 | PASS | 「做完饭，记一笔」idle 可点 |
| E1 无图菜降级 | PASS | hasImg=false + 🍲 emoji + 降级提示文案 |
| E2 降级区尺寸 | PASS | rect 375×220（CSS 设计 440px·视口 0.5） |
| E3 换道到无图菜 | PASS | 菜名=紫菜蛋花汤（sort=1，序正确） |

截图（.workflow-verify/tp07/shots/）：
- `e2e-p0-candidates.png`（候选页，夹具菜单+排序）
- `e2e-p1-plan.png`（锁定后菜单页，菜卡序）
- `e2e-p2-dish-imaged.png`（**亮图态**：红烧肉实拍图全出血 + 图上叠菜名/时长/菜系/口味/器具 chips）
- `e2e-p3-dish-degrade.png`（**降级态**：紫菜蛋花汤 emoji 色块 + 契约缺省降级文案）

### 2.6 R-1 修复后回归（AC4）
`node .workflow-verify/tp07/r1-fixed.cjs` → **CHECK 4/4 PASS**（存证 r1-fixed-result.json + shots/r1fix-p4-back-idle.png）：
同改动前复现路径（真实链路 → 点「记一笔」→ feedback 页 → CDP Fetch 拦截断掉 GET feedback → 返回 dish 页）：
- R2-fixed-back-to-idle：查询失败后 dock=「做完饭，记一笔」可点（改动前：永久卡「正在记…」）✓
- R3-fixed-still-idle-3s：3 秒后仍稳定 idle，不反弹不卡死 ✓

### 2.7 teardown 对账（AC7）
`node .workflow-verify/tp07/teardown.cjs` → **TEARDOWN_OK，EXIT=0**
- 清理前：plan=66(+12) event=110(+17) menu=14(+1) menudish=45(+3) dish=49(+1)
- 窗口完整性校验：createdAt<2026-09-10 的 Plan=54 恰等基线（窗口切分准确，未误删真实数据）
- 清理内容：tp07-fixture-*（MenuDish3/DishIngredient1/Dish1/Menu1）+ tp07-spec-*（spec afterAll 已自清，脚本幂等复核）+ 验证期 Event17/Plan12
- 逐项对账：**plan=54 ✓ event=93 ✓ menu=13 ✓ menudish=42 ✓ dish=48 ✓ ingredient=79 ✓ cooklog=6 ✓（7/7 PASS）**
- 服务收敛：API PID 33660 / H5 PID 37860 已停，pg_ctl stop EXIT=0；`PORT_3000_LISTEN=0 / PORT_10086_LISTEN=0 / PORT_54329_LISTEN=0`

## 3. AC 自检表

| AC | 内容 | 判定 | 证据 |
|---|---|---|---|
| AC1 | 投影双链路（recommend 快照 + lockedMenu join），真机锁定后首屏亮图 | ✓ | 2.3 探针 + 2.5 D1-D4（首屏即亮图，store 快照来自 recommend 响应）+ 2.4 单测第 2 条（懒水合路径） |
| AC2 | 契约仅非破坏性可选字段追加（若需）并单列理由 | ✓ | 第 1 节单列说明：shared 零改动（三字段 T-C01 已有），映射层交叉类型透传 |
| AC3 | 三处补 orderBy sort asc + 单测断言乱序写入读出按序 | ✓ | planService 2 处 + mappers 防御排序；2.4 单测 1/2/3 条 + 真机 C3/P1/E3 三面 |
| AC4 | R-1 catch 中 loading 回退 idle，不伪造已提交，最小 diff | ✓ | +3/-1 行；done/idle 不动；2.6 回归 4/4 |
| AC5 | API 测试全绿 + h5 typecheck 0 + build EXIT=0 | ✓ | 351/351 EXIT=0；tsc 0 错误；build EXIT=0 |
| AC6 | 真机亮图验证全链路，有图亮图 375×340+叠信息 / 无图降级，截图留证 | ✓ | 2.5 表 14/14 + 4 张截图 |
| AC7 | teardown 恢复基线计数逐项一致 + 三端口=0 + PG 停 | ✓ | 2.7 七表 7/7 + 三端口 0 + PG_STOP_EXIT=0 |
| AC8 | 报告落盘 evidence/T-P07-dev-2026-09-10.md（含命令+退出码+数字） | ✓ | 即本文件 |

## 4. 未验证项与移交主控事项

1. **Node 22/Windows CDP 脚本退出崩溃**：脚本主体执行完毕并写入 result 后，进程在 uv 句柄清理阶段触发 `Assertion failed: !(handle->flags & UV_HANDLE_CLOSING)`（exit 3221226505）。Node 运行时已知问题，不影响验证结论（以 CHECK 输出与 result.json 为准）；该现象在改动前基线复现脚本中同样出现，非本卡改动引入。
2. **引擎候选数波动**：e2e 首轮 recommend 出现 2 张同名夹具候选卡、次轮 candN=1。属引擎推荐集行为（卡外），排序与亮图断言不受影响；如需归因请主控另立任务。
3. **feedback 提交主流程未回归**：本卡仅动 dish 页 catch 分支；R-3 三态 dock 经 r1fix 真机显示正常（idle/loading/done 渲染路径未动）。POST feedback 全流程属既有功能，未在本卡重复回归。
4. **工作区既有卡外文件（未动）**：`docs/workflow/tasks/T-C03-*.md` 的 1 行状态更新（主控已收敛标注）、`.pai/`、`docs/design/`、`docs/ui-redesign/`、`tools/*.tar.gz`、`tests/*.mjs`、`复盘报告.md` 等为工作区既有内容，本卡未触碰。
5. **验证脚本资产**：`.workflow-verify/tp07/`（夹具/e2e/teardown/截图/result.json）保留供 fm-reviewer / fm-verify 复核复跑；teardown 脚本幂等，可重复执行对账。

## 5. 新增依赖

无。测试与验证全部使用既有依赖（vitest 既有 devDep、pg 既有 API 依赖、Node 22 内置 WebSocket/fetch）。
