# T-P15 imageUrl regex 收紧（挂账⑧）——fm-reviewer 独立技术审查报告

> 审查人：fm-reviewer（工具级只读，未修改任何文件，未执行 shell/git/测试命令；本报告由主控代为落盘）
> 审查日期：2026-09-12 ｜ 审查对象：dev 报告 [T-P15-dev-2026-09-12.md](./T-P15-dev-2026-09-12.md) 自称的 3 文件 +33/−1 改动
> 审查依据：[任务卡 T-P15](../docs/workflow/tasks/T-P15-imageUrl-regex收紧.md)（AC1~AC6 与定稿口径）、[CURRENT.md L82 挂账⑧条目](../CURRENT.md)、流程文档 12.5 节（通过/需修改/无法判定三值口径）
> 方法声明：本代理无 shell/git 权限，凡涉及「diff 范围、运行时测试数字、DB 29 行」的断言均以读码佐证 + 移交 verify 复跑处理，不采信 dev 自述为最终证据。

---

## 一、逐审查点判定表

| # | 审查点 | 判定 | 证据与说明 |
|---|---|---|---|
| 1 | 定稿口径一致性 | **[✓]**（含 1 条记录级行号偏差） | [dish.ts:L55](../../packages/shared/src/schemas/dish.ts) 实际 regex 为 `/^\/images\/dishes\/[A-Za-z0-9]+\/\d+\.(webp|jpg|png|gif)$/`，与任务卡 L11、[CURRENT.md L82](../../CURRENT.md) 定稿值**逐字符一致**（`^\/images\/dishes\/` + `[A-Za-z0-9]+` + `\/` + `\d+` + `\.` + `(webp|jpg|png|gif)` + `$`）。union URL 分支（`z.string().url()`）与 `.optional()` 原样保留（dish.ts:L54-56）；L52 既有 v0.7 注释未动，v0.10 注释追加于 L53。**注意**：dev 报告与任务卡称 regex 在「L54」、注释追加于「L52」，实际分别为 L55/L53（L54 是 `imageUrl: z` 行），偏移 1 行但均在任务卡声明的 L52-55 改动区间内，见 S-1 |
| 2 | 契约账本 | **[✓]**（既有行零改动为读码旁证） | [api.ts:L24-27](../../packages/shared/src/schemas/api.ts) 恰新增 4 行，插入点在 v0.9 段（L21-23）之后、`import { z } from 'zod'`（L28）之前；格式与既有条目风格逐项吻合（`// v0.x（日期，任务，编号，用户已批准）：` + 缩进续行），含拍板差异一句备注与「29 行 100% 符合」说明。既有 v0.2~v0.9 行：v0.9 段恰 3 行，与 T-P12 报告「api.ts +6−2，账本 v0.9 段 3 行注释」记载吻合；v0.2~v0.8 内容与 T-P11 账本收敛记载吻合。逐字节零改动需 verify 以 `git diff --numstat`（预期 api.ts +4/−0）定案 |
| 3 | 边界纪律 | **[✓]**（读码层面；diff 范围本身移交 verify） | 逐一读码核实禁区现状无改动迹象：[schema.prisma:L103](../../apps/api/prisma/schema.prisma) `imageUrl String?` 无 zod；apps/api 全部 imageUrl 引用为透传/类型/注释/迁移 DDL（[mappers.ts:L171](../../apps/api/src/services/mappers.ts) `row.imageUrl ?? undefined` 仅透传），无新增校验点；apps/h5 仅类型与渲染（dish/index.tsx:L44 `toAbsoluteImageUrl`）；packages/engine 无 imageUrl 关联；[fetch2dish.mjs:L80](../../tools/content-pipeline/fetch2dish.mjs) `normalizeImageExt` 及 CT_EXT_MAP（L52-58）现状与拍板依据一致，未被改；content-pipeline src 的 import.ts L87 / draft.ts L81 均为既有消费点。全局搜索证实 DishSchema 校验消费点仅 content-pipeline 两处，「唯一校验点」口径成立 |
| 4 | 测试覆盖充分性 | **[✓]** | [schemas.spec.ts:L268-292](../../packages/shared/test/schemas.spec.ts) 新增 1 个 it、恰 11 组断言，AC3 六类覆盖项逐项对应且断言方向全部正确：四合法扩展名各通过（L272 webp / L273 jpg / L274 png 非零序号 12 / L275 gif）、泛前缀拒绝（L278 `/images/abc.jpg`）、`.jpeg` 拒绝（L281）、`.svg`/无扩展名拒绝（L283/L284）、目录结构不符拒绝（L287 `/images/dishes/`、L288 `/images/dishes/abc/`）、大写 `.WEBP` 拒绝（L291）。存量用例逐值核对与 dev 报告一致且行号引用准确：schemas.spec.ts:L247 0.webp 仍合规、L257/L261/L264 仍拒绝；import.spec.ts:L109/L117/L123/L126/L131 无冲突；fetch2dish.spec.ts:L171/L178/L184 产出值合规；[projection-order.spec.ts:L41](../../apps/api/test/projection-order.spec.ts) 绝对 URL 走 union URL 分支。零语义冲突，未触发「停下报主控」，未私改存量值 |
| 5 | 正则技术细节 | **[✓]**（含 2 条记录级观察） | 逐项核：`\.` 唯一一处已转义；无 `i` 标志、ext 硬编码小写与拍板一致；JS 无 `m` 标志下 `$` 严格锚串尾（JS `$` 不匹配尾随换行前，无绕过），zod `z.string().regex()` 直接使用 JS RegExp 语义一致；`^` 锚定使 `/images/dishes-evil/...` 类前缀绕过被拒；空串双分支均拒（存量 L264 断言在案）、undefined 由 optional 放行（存量 L228 断言在案），语义保持。`[A-Za-z0-9]+` 覆盖 hex24 noteId（0-9a-f 全在集内）。观察见 S-3/S-4：`\d+` 允许前导零、字符类宽于 hex24 均系定稿口径既定余量，非 dev 偏差；`[A-Za-z0-9]+` 不含 `-_` 与生产者 [sanitizePathSegment](../../tools/content-pipeline/fetch2dish.mjs) 允许 `-_` 存在理论边缘不一致，当前 DB 29 行 hex24 实证无影响，fail-fast 方向符合本卡目的 |
| 6 | dev 报告数字自洽性 | **[✓]**（含 1 条 ±1 疑点移交） | 算术链自洽：基线 387 passed + 3 skipped = 390 → 新增 1 it → 388+3 = 391；PG 常驻 3 条 projection-order 用例真实执行 → 391/391 0 skipped，与 T-P14 基线「390/390 0 skipped」+1 口径精确衔接。第 7/8/9 项命令链（原 script 退出码 1 → 挂串行参数等效命令 15 files/391 全绿 → score.spec 单跑 51/51）内部陈述一致。疑点：dev 称 schemas.spec.ts +27，读码估算新块 L267-292 为 26 行（±1 视空行计法），dish.ts +2/−1 与 api.ts +4 读码吻合，合计 +32 或 +33——git 原始数字无 shell 无法独立复验，移交 verify（S-2） |
| 7 | 卡外发现核实 | **[✓]** | 卡外发现 1 属实：[content-pipeline/package.json L23](../../tools/content-pipeline/package.json) test script 实为 `vitest run --root ../..` 无 `--no-file-parallelism`，而根 package.json L10 已是 `vitest run --no-file-parallelism`（T-P13 修复）——「未继承挂账⑥串行修复」定性准确；两命令经 `--root ../..` 加载同一根 vitest.config.ts L8 同一 include scope，「等效命令」主张脚本层面成立。卡外发现 2 定性合理：[shared/package.json L16](../../packages/shared/package.json) test = `vitest run`，include 模式 `packages/*/test/**` 以子包 cwd 解析无匹配，No test files found 机理读码成立（未复跑验证）。AC12 失败与 shared 纯函数改动的因果判断逻辑成立，但运行时现象（65ms>50ms 连续 3 次）未验证，移交 verify |
| 8 | 影响面复核 | **[✓]** | 与任务卡「影响面」节逐项一致（见审查点 3/4 证据）：唯一校验点（实际 L55，比卡面 L54 偏 1）、content-pipeline 消费两处（import.ts:L87 `DraftFileSchema=DishSchema.extend`、draft.ts:L81 `DishSchema.parse`）、mappers 透传、h5 仅渲染、schema.prisma String?、projection-order 绝对 URL。收紧后不合规路径在入库/起草 fail-fast，正是本卡目的，无静默写入脏数据路径 |
| 9 | 拍板差异依据核实 | **[✓]** | **不含 jpeg** 三条归一路径实证：fetch2dish.mjs:L54（image/jpeg Content-Type → .jpg）、L66（FFD8FF 魔数 → .jpg）、L85-86（URL 后缀 `jpe?g` 捕获后 `.replace('jpeg','jpg')`）——生产者恒不产出 .jpeg；测试 fetch2dish.spec.ts:L69/L91 在案（任务卡引用行号准确）。**保留 gif**：L57（image/gif → .gif）、L70（GIF8 魔数 → .gif）、L85（URL 后缀 gif 合法），测试 L83 在案——gif 是生产者合法输出，收掉会中断管线兜底路径。DB 29 行全 .webp 与「DB 无 .jpeg/.gif 实例」陈述一致（gif 保留系前瞻防中断，非存量需求），拍板依据充分 |

---

## 二、发现问题分级清单

- **S-1（建议级）行号引用偏移**：dev 报告与任务卡称 regex 在 dish.ts「L54」、v0.10 注释追加于「L52」，实际为 L55 / L53。不影响 AC 达成（均在任务卡声明区间 L52-55 内），建议主控归档时勘误，后续任务卡引用以实际行号为准。
- **S-2（建议级）+27/−1 与读码估算 26 行的 ±1 差异**：schemas.spec.ts 新增块读码估算 26 行（L267-292），dev 报 git diff --stat 为 +27，总数 +33/−1 相应为 +32/−1。属报告精度问题非代码问题，留 verify numstat 定案；若确为 +33/−1 则我方估算漏计一空行，无需动作。
- **S-3（记录级）regex 与 sanitizePathSegment 的理论边缘不一致**：生产者净化函数允许 `-_`（fetch2dish.mjs L94），定稿 regex `[A-Za-z0-9]+` 不含。若未来出现净化后含 `-_` 的 noteId，其目录名将在 fm-import 被拒（fail-fast，脏数据不落库）。当前 DB 29 行 hex24 实证零影响，属定稿口径既定设计，挂账观察即可，不构成本卡缺陷。
- **S-4（记录级）定稿余量**：`\d+` 允许前导零（`007.webp` 通过）、`[A-Za-z0-9]+` 宽于 hex24（如 `abc` 通过）。两者均为用户批准定稿值本身的设计余量，与「i=非负整数、noteId=[A-Za-z0-9]+」卡面口径一致，非偏差，无需处理。

无裁决级问题，无禁区越界迹象，无测试弱化/删除/跳过真实链路迹象（新增用例断言方向全部正确，存量用例零改动）。

---

## 三、数字复核移交清单（verify 复跑确认）

以下 dev 断言因本代理无 shell/DB 权限**未验证**，需 verify 独立复跑：

1. **diff 范围**：`git diff --numstat` 恰 3 文件——dish.ts +2/−1、api.ts +4/−0、schemas.spec.ts +27（或 26）/−0，总 +33/−1（或 +32/−1）；并确认 schema.prisma、apps/api/**、apps/h5/**、packages/engine/**、tools/content-pipeline src/**、fetch2dish.mjs、既有 v0.2~v0.9 账本行零改动。
2. **根回归**：PG 常驻下根 `pnpm test` 391/391 0 skipped、EXIT=0（算术 = T-P14 基线 390 + 1 新用例）。
3. **禁忌回归**：`pnpm test:taboo` 89/89、EXIT=0。
4. **content-pipeline 等效全绿**：`npx vitest run --root ../.. --no-file-parallelism` 15 files/391 全绿；并顺带观察原 script（并行模式）AC12 flaky 是否复现（佐证卡外发现 1 根因定性）。
5. **类型链**：shared build EXIT=0、h5 typecheck 0 错、api `tsc --noEmit` 0 错。
6. **AC5 DB 复核**：便携 PG（`-o "-p 54329"`）启动后 SQL 复验 origin='FETCHED' 且 imageUrl 非空 29 行 100% 匹配新 regex（29/29），用后 `-m fast stop`。
7. （可选佐证）`pnpm --filter @family-menu/shared test` 复现 No test files found（卡外发现 2）。

---

## 四、结语

**PASS-有条件。**

条件：verify 完成上述移交清单 1~6 的独立复跑且全部命中（尤其 git numstat 恰 3 文件、391/391、taboo 89/89、DB 29/29）。代码改动本体经读码审查**无阻断问题**：定稿 regex 逐字符一致、union/optional 结构未动、账本追加合规、AC3 测试覆盖逐项落实且断言方向正确、影响面与任务卡陈述逐项吻合、拍板差异（不含 jpeg/保留 gif）有生产者源码与测试双重支撑。S-1/S-2 为报告精度勘误项、S-3/S-4 为记录级观察，均不要求修改代码，由主控归档处置。按流程 12.5 口径，本审查结论等价于「通过（附 verify 复跑前置条件）」。
