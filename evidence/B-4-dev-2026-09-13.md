# B-4 开发完成报告：tp03 回归补非白名单 Origin 预检负路径断言

> 日期：2026-09-13 ｜ 执行角色：fm-dev ｜ 分支 feat/tp-06-menu-expansion ｜ 基线 HEAD `ee74afa`
> 任务卡：[evidence/B-4-task-2026-09-13.md](./B-4-task-2026-09-13.md)（T-P03 review 建议级 #4 核销）
> 本报告全部数字来自本机真实执行（命令+退出码逐条在案），无凭记忆转述。

## 0. 结论速览

| AC | 判定 | 关键数字 |
|---|---|---|
| AC1 新增恰 2 断言、既有 18 断言逐字不动 | [✓] | git diff 恰 1 文件 `tests/tp03-cors-regression.cjs` **+26/-0 纯追加**（0 删除行=既有断言零改动） |
| AC2 真实跑通 20 PASS / 0 FAIL EXIT=0 | [✓] | **20 PASS / 0 FAIL，EXIT=0**；修改前基线跑 **18 PASS / 0 FAIL，EXIT=0** |
| AC3 teardown 精确回基线 | [✓] | BEFORE Plan=56/Event=95 → **DELETED Events=2 Plans=2 → AFTER Plan=54/Event=93，RESIDUAL=0，EXIT=0**；七表前后逐项一致（见 §4） |
| AC4 零影响面 | [✓] | 根全量 **19 files / 442 passed (442)** EXIT=0 + **taboo 95 passed (95)** EXIT=0（与基线 442/95 零变化） |
| AC5 证据落盘 | [✓] | 本文件 + `.workflow-verify/b4/`（test-full.log / taboo-full.log / count-check.cjs 物证） |

新增依赖：**0**。禁区（packages/**、apps/api/src/**、apps/api/prisma/**、apps/h5/**、tools/**）**零改动**。红线全程遵守（未删 %TEMP%\fm-rds.env、未停 PG 54329、未杀 9222、无 DROP 类 SQL、未触碰生产 RDS）。

## 1. 环境与测试前基线

- API 启动：`pnpm --filter @family-menu/api dev`（= `tsx watch src/server.ts`）→ `Server listening at http://127.0.0.1:3000`（确认监听后才跑断言；结束后已停服，`PORT3000_LISTENING=False`）。
- PG 17.5 @ 127.0.0.1:54329 在线（`PORT54329_LISTENING=True`），全程未停。
- snapshot：`node tests/tp02-cleanup-test-plans.cjs snapshot` → `SNAPSHOT_OK: 已记录 54 个既有 Plan id -> .workflow-verify\tp02-planids-before.txt`（EXIT=0）。
- 测试前七表基线（只读 COUNT 脚本 `.workflow-verify/b4/count-check.cjs`，仅 SELECT）：

```
[2026-09-13T13:44:12.735Z] Dish=49 Menu=13 MenuDish=42 Ingredient=81 Plan=54 Event=93 CookLog=6 ExclusionRule=3
```

与任务卡 AC3 的 S-3 卡定谳基线 {Dish:49, Menu:13, MenuDish:42, Ingredient:81, Plan:54, Event:93, CookLog:6} + ExclusionRule=3 逐项精确一致。

## 2. 修改前基线跑（改造任务先例：先录既有行为）

命令：`node tests/tp03-cors-regression.cjs` → **EXIT=0**

```
BASE=http://127.0.0.1:3000
[PASS] AC1 预检 http://127.0.0.1:10086：2xx 放行（不再 401）: status=204（基线为 401）
[PASS] AC1 预检 http://127.0.0.1:10086：ACAO 白名单回显请求源: acao=http://127.0.0.1:10086
[PASS] AC1 预检 http://127.0.0.1:10086：ACAM 含 POST: acam=GET, HEAD, POST, PUT, PATCH, DELETE, OPTIONS
[PASS] AC1 预检 http://127.0.0.1:10086：ACAH 含 content-type: acah=Content-Type, Authorization
[PASS] AC1 预检 http://127.0.0.1:10086：ACAC=true（cookie 鉴权）: acac=true
[PASS] AC1 预检 http://localhost:10086：2xx 放行（不再 401）: status=204（基线为 401）
[PASS] AC1 预检 http://localhost:10086：ACAO 白名单回显请求源: acao=http://localhost:10086
[PASS] AC1 预检 http://localhost:10086：ACAM 含 POST: acam=GET, HEAD, POST, PUT, PATCH, DELETE, OPTIONS
[PASS] AC1 预检 http://localhost:10086：ACAH 含 content-type: acah=Content-Type, Authorization
[PASS] AC1 预检 http://localhost:10086：ACAC=true（cookie 鉴权）: acac=true
[PASS] AC2-a 无令牌跨源 GET rules：仍 401（鉴权语义不变）: status=401 body={"error":"Unauthorized"}
[PASS] AC2-a 无令牌跨源 GET rules：响应带 ACAO（浏览器可读到 401）: acao=http://127.0.0.1:10086
[PASS] AC2-b 带令牌跨源 POST recommend：200 且候选非空: status=200 candidates=3 planId=cmtzv8su80000uspg0r4etmyl
[PASS] AC2-b 带令牌跨源 POST recommend：响应带 ACAO: acao=http://localhost:10086
[PASS] AC2-c 同源（无 Origin）带令牌 GET rules：200（行为不变）: status=200
[PASS] AC2-c 同源（无 Origin）无令牌 GET rules：401（行为不变）: status=401
[PASS] AC2-d 生产 Origin 无令牌 GET rules：仍 401（请求未被 CORS 中断）: status=401 acao=null
[PASS] AC2-d 生产 Origin 带令牌 GET rules：200（同源部署行为不变）: status=200 acao=null

== 结果: 18 PASS / 0 FAIL ==
```

## 3. AC1 改动与 AC2 真实跑通

### 3.1 改动内容（[tests/tp03-cors-regression.cjs](../tests/tp03-cors-regression.cjs)）

- 头注释追加 2 行 B-4 描述（不修改任何既有行）。
- AC2-d 段之后新增「B-4（T-P03 review 建议级 #4 核销）：非白名单 Origin 预检负路径」段：
  - `OPTIONS /api/recommend` + `Origin: https://menu.jijingkongjian.xin`（复用 AC2-d 非白名单代表，口径一致）+ ACRM=POST + ACRH=content-type；
  - 断言 1：`status === 204`（预检仍被插件 onRequest 短路放行，不落鉴权 401）；
  - 断言 2：`acao === null`（白名单外不写 ACAO，浏览器将拦截）。
- 预期值出处（未自创）：[app.ts L60 注释](../apps/api/src/app.ts#L60)「origin 数组为全等白名单：不匹配时仅不写 ACAO 头、请求照常放行」+ fm-verify T-P03 实测（204 + acao=null，T-P03 报告 §8.3）。

git diff 实测（修改完成后）：

```
 tests/tp03-cors-regression.cjs | 26 ++++++++++++++++++++++++++
 1 file changed, 26 insertions(+)
```

`git diff --numstat`：`26  0  tests/tp03-cors-regression.cjs`——**0 删除行，纯追加**，既有 18 断言（名称、顺序、断言逻辑）逐字未动。

### 3.2 修改后真实跑（AC2）

命令：`node tests/tp03-cors-regression.cjs` → **EXIT=0**

```
BASE=http://127.0.0.1:3000
[PASS] AC1 预检 http://127.0.0.1:10086：2xx 放行（不再 401）: status=204（基线为 401）
[PASS] AC1 预检 http://127.0.0.1:10086：ACAO 白名单回显请求源: acao=http://127.0.0.1:10086
[PASS] AC1 预检 http://127.0.0.1:10086：ACAM 含 POST: acam=GET, HEAD, POST, PUT, PATCH, DELETE, OPTIONS
[PASS] AC1 预检 http://127.0.0.1:10086：ACAH 含 content-type: acah=Content-Type, Authorization
[PASS] AC1 预检 http://127.0.0.1:10086：ACAC=true（cookie 鉴权）: acac=true
[PASS] AC1 预检 http://localhost:10086：2xx 放行（不再 401）: status=204（基线为 401）
[PASS] AC1 预检 http://localhost:10086：ACAO 白名单回显请求源: acao=http://localhost:10086
[PASS] AC1 预检 http://localhost:10086：ACAM 含 POST: acam=GET, HEAD, POST, PUT, PATCH, DELETE, OPTIONS
[PASS] AC1 预检 http://localhost:10086：ACAH 含 content-type: acah=Content-Type, Authorization
[PASS] AC1 预检 http://localhost:10086：ACAC=true（cookie 鉴权）: acac=true
[PASS] AC2-a 无令牌跨源 GET rules：仍 401（鉴权语义不变）: status=401 body={"error":"Unauthorized"}
[PASS] AC2-a 无令牌跨源 GET rules：响应带 ACAO（浏览器可读到 401）: acao=http://127.0.0.1:10086
[PASS] AC2-b 带令牌跨源 POST recommend：200 且候选非空: status=200 candidates=3 planId=cmtzvabgp0002uspgq7v84lcf
[PASS] AC2-b 带令牌跨源 POST recommend：响应带 ACAO: acao=http://localhost:10086
[PASS] AC2-c 同源（无 Origin）带令牌 GET rules：200（行为不变）: status=200
[PASS] AC2-c 同源（无 Origin）无令牌 GET rules：401（行为不变）: status=401
[PASS] AC2-d 生产 Origin 无令牌 GET rules：仍 401（请求未被 CORS 中断）: status=401 acao=null
[PASS] AC2-d 生产 Origin 带令牌 GET rules：200（同源部署行为不变）: status=200 acao=null
[PASS] B-4 非白名单 Origin 预检：204 放行（预检仍被短路，不落鉴权 401）: status=204（预期 204）
[PASS] B-4 非白名单 Origin 预检：acao=null（白名单外不写 ACAO，浏览器将拦截）: acao=null

== 结果: 20 PASS / 0 FAIL ==
EXIT=0
```

2 条新断言实测与任务卡 §二 预期**完全一致**（status=204 放行 + acao=null），未触发异常升级路径。

## 4. AC3 teardown 精确回基线

命令：`node tests/tp02-cleanup-test-plans.cjs clean` → **EXIT=0**

```
BEFORE Plan=56 Event=95
CANDIDATES: 非快照 PROPOSED Plan=2，白名单命中=2，跳过=0
SAFETY_OK: 2/2 通过 status=PROPOSED + mustUse 精确匹配复核
DELETED Events=2 Plans=2
AFTER Plan=54 Event=93
RESIDUAL_CHECK: 目标 planId 残留=0
```

- Plan 56→54、Event 95→93：精确清除 §2/§3.2 两轮真实跑产生的 2 个测试 Plan（planId=cmtzv8su80000uspg0r4etmyl / cmtzvabgp0002uspgq7v84lcf，均 mustUse=['西红柿'] 白名单内）及关联 Event；快照差集 + 白名单双校验通过，RESIDUAL=0。
- 清理后七表终态（同一只读脚本）：

```
[2026-09-13T13:46:29.160Z] Dish=49 Menu=13 MenuDish=42 Ingredient=81 Plan=54 Event=93 CookLog=6 ExclusionRule=3
```

与测试前基线（§1）**逐项一致、零漂移**，ExclusionRule=3 未受影响。

## 5. AC4 零影响面证明

本机 PowerShell 对 pnpm shim stdout 捕获可能失效（LOGIN-DOM 卡先例），按既定等价口径 vitest 直跑（script 逐字符等价）：

| 命令 | 对应 script | 结果 | EXIT |
|---|---|---|---|
| `node node_modules/vitest/vitest.mjs run --no-file-parallelism` | 根 `test` | `Test Files 19 passed (19)` / `Tests 442 passed (442)` | **0** |
| `node node_modules/vitest/vitest.mjs run packages/engine/test/` | 根 `test:taboo` | `Test Files 5 passed (5)` / `Tests 95 passed (95)`（含 AC12 千套性能 345ms 全绿） | **0** |

与基线 442/442 + 95/95 **零变化**（改动仅 tests/*.cjs 独立脚本，不在 vitest include）。完整输出落盘：

- `.workflow-verify/b4/test-full.log`
- `.workflow-verify/b4/taboo-full.log`

## 6. git 终证与环境还原

```
git diff --stat:
 tests/tp03-cors-regression.cjs | 26 ++++++++++++++++++++++++++
 1 file changed, 26 insertions(+)

git status --porcelain（改动面）:
 M tests/tp03-cors-regression.cjs
（未跟踪项全部为开工前既有：.pai/、docs/design/*、docs/ui-redesign/、evidence/B-4-task-2026-09-13.md（任务卡）、
 tests/engine-perf.mjs、tests/step08-deploy-validate.cjs、tools/*、复盘报告.md——与本卡无关未触碰）
```

- 禁区七组路径（packages/**、apps/api/src/**、apps/api/prisma/**、apps/h5/**、tools/**）git diff 零出现。
- API dev 进程已停止：`PORT3000_LISTENING=False`；PG 54329 未动；9222 未触碰。
- 证据物证目录 `.workflow-verify/b4/`（gitignore L26 覆盖，不入库）：count-check.cjs / test-full.log / taboo-full.log。

## 7. 卡外发现

无。执行过程中脚本其他部分未发现新问题；异常升级路径（启服失败/断言与预期不符/teardown 未回基线/非 442）全程未触发。

## 8. 遗留

无本卡遗留。CORS 挂账队列中 B-2/B-3（见 CURRENT.md L84）不在本卡范围，保持待排期。
