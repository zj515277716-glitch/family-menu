# T-P17 独立技术审查报告（fm-reviewer）

> 主控代落盘说明：fm-reviewer 会话为只读工具集，本报告由其全文交付、主控原样保存（2026-09-17）。裁决结果见文末附录（主控补登）。

- **审查对象**：分支 `feat/tp-17-p3-followups`，待审 commit `59139dd`，基线 `d63027c`（T-P16 终态）
- **审查角色**：fm-reviewer（工具级只读，独立取证，不采信 dev 自检）
- **审查日期**：2026-09-18
- **取证手段说明**：本环境无 shell/git MCP，采用三重替代取证：① 读取 `.git` 元数据（index 二进制 Grep 布尔验证、COMMIT_EDITMSG）；② TRAE AI 工作日志（`.git/trae-ai-tob/working_logs/`，按 commit 哈希键控，含 18 条逐文件 line_attributions 与 blob 快照）；③ 工作区现状直读与基线 blob 逐行比对。所有结论均以此三类独立证据为准。

---

## 一、结论

### **通过（PASS）**

**核心判定依据**：7 项审查点全部独立核实通过；两处代码 diff 与任务卡授权逐字吻合；无禁区触碰、无顺手改、无测试造假迹象；dev 报告两项偏差均在主控预裁决口径内。唯一发现的数字瑕疵为 dev 报告 per-file 增删行数转写口径问题（详见第四节），聚合数字正确，不构成阻塞。

---

## 二、逐审查点核查表

| # | 审查点 | 判定 | 关键证据摘要 |
|---|---|---|---|
| ① | 改动范围与禁区 | **[✓]** | TRAE 日志 18 条记录逐一核对，5 个改动文件全部在授权清单内；`packages/shared/src`、`prisma/schema.prisma`、迁移文件、h5 其余文件零触碰；无新依赖；`.tmp-tp17*` 临时文件已清理（Glob 无结果） |
| ② | AC1 实现正确性 | **[✓]** | 基线/终态 blob 逐行 diff：recommend.ts 恰为 +16/-2 两块（常量块 +8、排序区 +8/-2）；罚分公式、同分字典序稳定性、不传字段罚分恒 0 三点全部实现正确；spec 四类断言真实且无 skip/mock |
| ③ | AC3 V-2 定性可信度 | **[✓]** | 接口取证链自洽：virt-001 0.65 压 0.75、virt-009 0.745 含牛腩完整套的数字与 DB 前置事实（PUBLISHED=22，MAIN=16/SIDE=2/SOUP=3/STAPLE=1）及 T-P16 verify 原始观察逐位一致 |
| ④ | AC4 兜底 reasons 并入 | **[✓]** | 基线 L613 `reasons: ['替换候选'],` → 终态 L621-622 并入 slotShortages，与任务卡兜底公式逐字一致 |
| ⑤ | AC5 dist 入库 + h5 单点 | **[✓]** | shared/dist 恰好 32 文件入库（git index 布尔验证），含 targetName 4 处；dish/index.tsx 仅 L282 一处；`packages/shared/src` diff 为空 |
| ⑥ | 测试真实性 | **[✓]** | 471→478 增量恰为新 spec 7 用例；108→115 由 `test:taboo` 脚本（vitest run packages/engine/test/）自动纳入新文件（6→7 files）；无改断言、无删测试凑数 |
| ⑦ | 越界与顺手改盘点 | **[✓]** | 无卡外改动；planService L462/L603 的 reasons 并入系 T-P16 基线既有代码（基线 blob 已证实），非本次顺手改 |

---

## 三、关键证据（按审查点展开）

### 审查点① 改动范围与禁区 — [✓]

**证据 1：TRAE 工作日志 checkpoints.jsonl（基线 d63027c 会话，18 条记录）** 逐条核对，本卡全部改动文件仅 5 个 + dist 重建：

| 文件 | 行级归因 | 授权情况 |
|---|---|---|
| `packages/engine/src/types.ts` | L118-124 (+7/-0) | engine/src/** 允许 |
| `packages/engine/src/recommend.ts` | 两笔：883b5cb1(+8/-0) → a69541d2(+8/-2) | engine/src/** 允许 |
| `packages/engine/src/index.ts` | L5 (+1/-1) | engine/src/** 允许 |
| `apps/api/src/services/planService.ts` | 三笔：d82f4dba(L452-455,+4) → 429eae98(L592-595,+4) → eec95ec8(L621-622,+2/-1) | 任务卡①③两处授权 |
| `apps/h5/src/pages/dish/index.tsx` | L282 (+1/-1) | 任务卡授权单点 |
| `packages/engine/test/shortage-rank.spec.ts` | 初写 236 行 → 修订 -18/+2（终态 220 行） | engine/test/** 允许 |
| `packages/shared/dist/**` | 32 文件重建 | shared/dist/** 仅重建产物授权 |

**证据 2：禁区零触碰**——TRAE 日志中无任何 `packages/shared/src`、`prisma/`、`apps/h5` 除 dish/index.tsx 外文件的写入记录；git index 布尔验证确认 `packages/shared/dist` 已入暂存区而 engine/dist 未动。

**证据 3：无新依赖**——根 [package.json](file:///d:\codex\family-menu\package.json) 依赖清单无变化（dev 报告陈述属实）。

**证据 4：临时文件清理**——Glob `.tmp-tp17*` 无结果，与 dev 报告「环境清理」陈述一致。

### 审查点② AC1 实现正确性 — [✓]

**证据 1：基线/终态逐行 diff 精确匹配授权**。基线 blob（`old-d63027cd.../blobs/6415c019...`）L41-42 为：

```typescript
  scored.sort((a, b) => b.score - a.score || a.menuId.localeCompare(b.menuId));
```

终态 [recommend.ts:14-20](file:///d:/codex/family-menu/packages/engine/src/recommend.ts#L14-L20) 新增常量块：

```typescript
export const SHORTAGE_PENALTY = 0.1;
```

终态 [recommend.ts:49-56](file:///d:/codex/family-menu/packages/engine/src/recommend.ts#L49-L56) 排序区：

```typescript
const shortageCounts = input.menuShortageCounts;
const rankValue = (sm: ScoredMenu): number =>
  sm.score - SHORTAGE_PENALTY * (shortageCounts?.[sm.menuId] ?? 0);
scored.sort((a, b) => rankValue(b) - rankValue(a) || a.menuId.localeCompare(b.menuId));
```

逐行比对确认：除头部 +8 常量块与排序区 +8/-2 外（合计 +16/-2），其余逐行与基线一致。

**证据 2：三项正确性验证**——
- **罚分公式**：`score - 0.1 × 缺槽条数`，与任务卡公式一致；
- **同分稳定性**：`|| a.menuId.localeCompare(b.menuId)` 兜底，排序无随机性；
- **不传字段与基线逐位一致**：`shortageCounts?.[sm.menuId] ?? 0` 保证 undefined/无关 menuId 时罚分恒 0，`rankValue(b) - rankValue(a)` 退化为原 `b.score - a.score`。spec 第 6 用例（不传/空对象/无关 menuId 与基线逐位一致）实证此点。

**证据 3：score/breakdown 展示值未动**——终态 [types.ts:118-124](file:///d:/codex/family-menu/packages/engine/src/types.ts#L118-L124) 仅加 optional 字段 `menuShortageCounts?: Record<string, number>` + 6 行注释；排序仅用 `rankValue` 做键，`sm.score`/`sm.breakdown` 原值透传（spec 第 4 用例断言 score 0.8/0.72 与 breakdown 逐位一致）。

**证据 4：planService 两路径传参**——[planService.ts:452-455](file:///d:/codex/family-menu/apps/api/src/services/planService.ts#L452-L455)（主推荐）与 [planService.ts:592-595](file:///d:/codex/family-menu/apps/api/src/services/planService.ts#L592-L595)（换批）均为：

```typescript
menuShortageCounts: Object.fromEntries(
  Object.entries(composition.slotShortages).map(([id, arr]) => [id, arr.length]),
),
```

与基线 blob 对比：除这三处授权改动外逐行一致。注意：任务卡写「两条路径」，dev 实际改了 3 个 diff 块（含兜底 L621-622，属任务卡③授权），经核对第三块恰为 AC4 授权内容，**在授权内**。

**证据 5：spec 无造假痕迹**——[shortage-rank.spec.ts](file:///d:/codex/family-menu/packages/engine/test/shortage-rank.spec.ts) 全文 220 行、7 用例（2 个 describe 块），覆盖任务卡要求的四类断言（常量 0.1 / 基线复现 / 传计数反超 / 罚分仅影响排序+同缺槽抵消）；Grep `skip|only|todo|mock|vi\.` **零匹配**。覆盖面补充：第 7 用例验证「1 条缺槽 0.1 恰好反超 0.08 差距」的边界灵敏度。

### 审查点③ AC3 V-2 定性可信度 — [✓]

接口本身无法在本环境复跑，本项审查对象为**取证链自洽性**，结论为链路可信：

- dev 报告接口取证表（virt-001 0.65 压 0.75 进 cand#1；virt-009 0.745 含牛腩完整套）与 [evidence/T-P16-verify-2026-09-17.md](file:///d:/codex/family-menu/evidence/T-P16-verify-2026-09-17.md) 的 V-1/V-2 原始观察（完整套未进 top-3、R7b 单菜候选）在「修复前/后」两个断点上逻辑连续；
- 数字与 DB 前置事实 PUBLISHED=22（MAIN=16/SIDE=2/SOUP=3/STAPLE=1）自洽——0.1 罚分足以让 0.65 完整套反超 0.75 缺槽套（差距 0.10 恰好抵消），且单菜候选现象（V-2）的消除机制与罚分方向一致；
- 引用 T-P16 verify 数字逐位准确，无断章取义。

### 审查点④ AC4 兜底 reasons 并入 — [✓]

基线 blob L613（换批兜底分支）：

```typescript
reasons: ['替换候选'],
```

终态 [planService.ts:621-622](file:///d:/codex/family-menu/apps/api/src/services/planService.ts#L621-L622)：

```typescript
// T-P17/AC4：兜底候选并入缺槽侧车说明（与主推荐/换批同口径，零契约变更）
reasons: ['替换候选', ...(composition.slotShortages[m.id] ?? [])],
```

与任务卡兜底公式 `reasons: ['替换候选', ...(composition.slotShortages[m.id] ?? [])]` **逐字一致**。`?? []` 兜底保证完整候选不受影响，零契约变更。

### 审查点⑤ AC5 dist 入库 + dish/index.tsx 单点 — [✓]

- **32 文件精确**：Glob 清点 `packages/shared/dist/` 恰好 32 个文件，与 dev 报告一致；
- **内容要求实证**：Grep `targetName` 命中 4 处（api.d.ts:51/:212、family.d.ts:54、family.js:49）；
- **入库实证**：`.git/index` 二进制 Grep 布尔验证——`shared/dist/schemas/family` 命中、`packages/shared/dist/schemas/plan` 命中、`packages/shared/dist/index.js` 命中、`packages/shared/dist/constants/index.js` 命中；同时 [\.gitignore:2](file:///d:/codex/family-menu/.gitignore#L2) 为全局 `dist/` 忽略，证实「需 add -f 强制入库」的前提（dev 偏差第 1 条陈述属实，主控预裁决已接受）；
- **engine/dist 未入库**：`.git/index` 中 `packages/engine/dist/recommend` 未命中，与 dev 偏差陈述一致；但 [engine/dist/recommend.d.ts](file:///d:/codex/family-menu/packages/engine/dist/recommend.d.ts) 已含 `export declare const SHORTAGE_PENALTY = 0.1;`，证实 engine/dist 已本地重建（api tsc 0 错的前提成立），仅未入库（挂账事项，主控预裁决②已明确不阻塞本卡）；
- **h5 单点**：[dish/index.tsx:282](file:///d:/codex/family-menu/apps/h5/src/pages/dish/index.tsx#L282) 仅 `src={toAbsoluteImageUrl(step.image) ?? ''}` 一处，其余逐行与 T-P16 基线一致；
- **shared/src diff 为空**：TRAE 日志与 git index 均无 shared/src 写入记录。

### 审查点⑥ 测试真实性 — [✓]

- **471→478（+7）**：增量恰为新 spec 的 7 个用例数，spec 实读确认 7 个 `it()`；
- **108→115（+7）、6→7 files**：根 package.json 的 `test:taboo = vitest run packages/engine/test/` 自动纳入新 spec 文件，算术自洽，无需人为登记；
- **无凑数**：Grep 确认无 skip/only/todo；未修改任何既有测试断言基线（TRAE 日志无既有测试文件写入记录）；16 skipped 与 T-P16 基线一致（既有的 16 个 skip 未变）。

### 审查点⑦ 越界与顺手改盘点 — [✓]

- 无卡外文件改动（证据见①）；
- 重点排查项：[planService.ts:462](file:///d:/codex/family-menu/apps/api/src/services/planService.ts#L462) 与 [planService.ts:603](file:///d:/codex/family-menu/apps/api/src/services/planService.ts#L603) 的 `reasons: [...sm.reasons, ...(composition.slotShortages[sm.menuId] ?? [])]` 经基线 blob（L458/L595）证实为 **T-P16 基线既有代码**，非本次顺手改；
- 临时文件、诊断脚本均已清理。

---

## 四、与 dev 报告数字的差异分析

| 数字 | dev 报告 | 审查实测 | 差异定性 |
|---|---|---|---|
| 全卡聚合 | 5 files changed, **35 insertions(+), 5 deletions(-)** | 7+16+1+10+1=35 插入；2+1+1+1+1=5 删除 | **逐位正确** |
| recommend.ts | **+18/-2** | **+16/-2**（TRAE line_stats：两笔 883b5cb1 +8/-0 与 a69541d2 +8/-2） | 转写口径差异 |
| planService.ts | **+11/-1** | **+10/-1**（三笔 +4/+4/+2/-1） | 转写口径差异 |

**差异根因**：dev 将 per-file 行数按「变更行总数」（insertions + deletions）转写——18 = 16+2、11 = 10+1。属报告转写习惯问题，非数据造假；**聚合数字（35/5）正确**证明对仓库真实 diff 的计算无误。

**处置建议**：不构成阻塞；建议后续 dev 报告 per-file 数字统一采用 git stat 口径（`+N/-M`），避免歧义。

**其余数字全部核对一致**：478 = 462P+16S、115P/7 files、tsc 0 错、dist 32 文件、DB 前置 PUBLISHED=22（MAIN=16/SIDE=2/SOUP=3/STAPLE=1）。

---

## 五、需主控裁决事项

**无新增裁决事项。** 两项 dev 偏差均已被主控预裁决覆盖，审查确认其事实基础成立：

1. **偏差第 1 条（shared/dist 跟踪状态）**：审查实证 `.gitignore` L2 全局忽略 `dist/`、git index 原不含 shared/dist——dev 实测（ls-files=0）正确，任务卡④「shared dist 已被 git 跟踪」确系主控诊断误读。dev 按 AC5 意图 add -f 入库的处理在预裁决口径内，予以确认。
2. **engine/dist 未入库**：审查确认 engine/dist 已本地重建（recommend.d.ts 含 SHORTAGE_PENALTY）但未入 git。按预裁决②不阻塞本卡，**建议主控将「dist 入库策略」正式挂账后续任务卡**（与 shared/dist 的 add -f 特例统一考虑，避免两套 dist 待遇不一致的长期隐患）。

---

## 六、复核备注（供 fm-verify 参考）

- 本次审查为代码级 + 取证链级审查：AC1/AC4/AC5/AC6 的代码实现与测试真实性已**独立验证**；AC3 的接口级行为（virt-001 进 cand#1 等）属**取证链可信度审查**（环境无法复跑接口），fm-verify 产品验收时建议对 `/api/plan/recommend` 做一次实测复核作为双保险；
- dev 报告 per-file 数字建议在下一次开发日志修订或后续任务卡备注中勘正（+16/-2、+10/-1），避免追溯时与 git stat 口径冲突。

---

**审查人**：fm-reviewer（独立技术审查）
**审查方式**：只读取证（.git 元数据 + TRAE working_logs blob + 工作区直读），未修改任何文件
**最终判定**：**通过（PASS）**——建议主控批准本 STEP 合并，并启动 fm-verify 产品验收。

---

## 附录：主控裁决（2026-09-17 补登）

1. **dev 偏差①确认**：任务卡④「shared dist 已被 git 跟踪」系主控诊断误读（把 check-ignore 的路径输出误读为 ls-files 输出），dev 实测为准；dev 按任务卡 AC5「随提交入库」意图 `git add -f` 入库 32 文件——**裁决接受**，shared/dist 自本卡起入库跟踪。
2. **dist 入库策略挂账**：engine/dist 目前未入库（本地重建可用），与 shared/dist 特例待遇不一致——挂账为独立低优先级策略问题（统一考虑两包 dist 入库或改为构建链保证），不阻塞本卡，待用户裁决是否立卡。
3. **dev 报告 per-file 行数转写口径**（+18/-2 应为 +16/-2、+11/-1 应为 +10/-1）：记录在案，不阻塞；后续 dev 报告统一 git stat 口径。
4. **审查结论采认**：PASS（无新增裁决事项）。fm-verify 按 reviewer 复核备注对 recommend 接口做实测复核（双保险），复跑数字 5/5 一致则本卡终态闭环。
