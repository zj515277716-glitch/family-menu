# T-P10 产品验收报告（fm-verify）

> 独立产品验收报告 · 依据流程文档 §12.8 格式
> 验证角色：fm-verify（独立于 fm-dev / fm-reviewer）
- 验证日期：2026-09-11
- 被测版本：本地工作区（基线 HEAD=`3426a2d`，T-P10 改动未提交，`git status --porcelain` 36 条；分支 `feat/tp-06-menu-expansion`，apps/api + apps/h5 + packages 均为工作区实时代码。验收全程未切分支、未提交 git、未修改任何业务代码/文档）
- 结论：**通过**（A 场景 33/33 + 混合 API 佐证 10/10 全 PASS、console 错误全场景为零；B 回归链独立复跑全绿；C teardown 七表精确回基线；AC1-AC7 逐条 [✓]）

---

## 1. 验证环境

| 项 | 值 |
|---|---|
| 数据库 | 便携 PostgreSQL，`127.0.0.1:54329/family_menu`（开工复核在位，收尾已 `pg_ctl stop`） |
| API | `pnpm --filter @family-menu/api dev`，`http://127.0.0.1:3000` |
| H5 | Taro4 dev server（dev:h5，port=10086），`http://127.0.0.1:10086`（全程 127.0.0.1，保证 cookie domain 一致） |
| 浏览器 | Edge 独立实例，CDP 端口 :9222（页面操作全部经 CDP 真实驱动，无接口调用替代点击） |
| 登录态 | cookie `access_token=family-menu-local-2026`（本地开发固定值，非真实用户凭据） |
| 视口 | 移动 375×667 dpr2（V1/V4/V2/V3/V5 全部同视口） |

## 2. 基线确认（开工核对 + 收尾复核）

- 开工直查 PG（`db-count.cjs` 只读快照）：Dish=48 / Menu=13 / MenuDish=42 / Ingredient=79 / Plan=54 / Event=93 / CookLog=6 —— 与派发基线一致，未偏离。
- 迁移数据 REL=29（`SELECT COUNT(*) FROM "Dish" WHERE origin='FETCHED'`），收尾复核仍 29，未动。
- B 复跑 tp01 期间 DB 增量：Plan 54→62（+8）、Event 93→103（+10），与 dev 报告 teardown 量吻合；UTC 阈值 `2026-09-11T00:00:00Z` 精确圈定验收窗口（基线最新记录为 2026-09-06 UTC，无重叠风险）。
- 收尾 teardown 后复核七表全部回基线（见 §7）。

## 3. 执行方法

- 工具（隔离目录 `.workflow-verify/tp10v/`，均为 CommonJS）：`cdp.cjs`（CDP over WebSocket：newTab/navigate/evalJs/waitFor/shot/CLICK/CLICK_TEXT）+ `verify-tp10v.cjs`（场景编排）+ `api-mixed.cjs`（混合型 API 佐证）+ `db-count.cjs`（七表快照）+ `teardown.cjs`（白名单清理）。
- 页面交互全部模拟真实用户：输入三级降级（insertText → 逐字符可信键 → React 原生 setter，本次全部命中 `cdp-trusted-enter` 一级可信键盘路径）、回车三级降级（可信键 → 合成 KeyboardEvent → confirm 事件）、点击按类名+文本匹配。判定事实源：页面 DOM + zustand 持久化 store（localStorage `family-menu-session`）。
- console 错误收集：`Runtime.enable` 后监听 `Runtime.consoleAPICalled`（type=error）+ `Runtime.exceptionThrown`，每场景边界断言零新增。
- 纪律：`waitFor` 中只做无副作用被动探测；有副作用的点击只在断言点显式执行恰一次；文本断言全部逐字对照（方案 §3.5 文案表 / 确认书 C-7/C-7a 原文）。
- 场景顺序：V1 → V4（同会话出路链路）→ V2 → V3 → V5（混合），与方案 §6.4 口径一致。

## 4. 逐项验证总表（AC 对照）

| AC | 内容（任务卡原文摘要） | 验证方法 | 结果 | 关键证据 |
|---|---|---|---|---|
| AC1 | 引擎宽集+归类 TIME_BUDGET/NO_DISH（缺器具归 NO_DISH），`unsatisfiable` 值/形状零变化，组合凑不进仍走 C-7a | `pnpm test` 含 feasibility.spec E1-E7（含 E6 混合两型、E7 集成透传）+ tp01 全链路 R1/R2 实测归类 + V3 C-7a 不混淆 | [✓] | §6（389/389）；§5 V3、V5；tp01 R1={苦瓜:NO_DISH}、R2={土豆丝:TIME_BUDGET} |
| AC2 | 契约 v0.8：RecommendResponseSchema 增 optional `unmetReasons`（record，key=用户原文，值枚举） | `pnpm test` 含 schemas.spec S1-S4 + 混合 API 实测响应形状（键集/逐键/全量三重断言） | [✓] | §6（389/389）；§5 M3-M6 |
| AC3 | API 透传：unmetMustUse 语义不变（tp01 既有 35 断言零改动），unmetReasons 经 idToRaw 同一映射回译，routes 显式透传 | tp01 独立复跑 54/54（既有断言零改动全绿）+ R1-R4 新断言 + R2「土豆丝」原文回译实测 | [✓] | §6 tp01 54 PASS/0 FAIL；§5 M2 |
| AC4 | 屏⑥三态渲染：时间型新文案（标题含当次所选档位分钟数+出路暗示）、无菜型逐字不变、C-7a 逐字不变、混合两型分句、unmetReasons 缺省防御按无菜型渲染 | CDP 真机 V1/V2/V3/V5 逐字断言 + V4 出路全链路；缺省防御分支（`reasons?.[raw] ?? 'NO_DISH'`）真机不可构造，见 §5 各场景 Coverage boundaries | [✓] | §5 V1-V5；shots 13 张；verify-result.json 33/33 |
| AC5 | 确认书先行：C-7 修订（PD-017）落 PRODUCT-CONFIRMATION.md 并登记 PRODUCT-DECISIONS.md | 开工必读核对：C-7 节（PD-017 修订版）+ C-7a 节在案（主控 2026-09-11 完成） | [✓] | PRODUCT-CONFIRMATION.md C-7/C-7a |
| AC6 | 全套回归绿 + teardown 七表回基线（48/13/42/79/54/93/6，REL=29 不动） | B 独立复跑全链 + C teardown 实测 | [✓] | §6、§7 |
| AC7 | 收尾复核：:3000/:54329 零遗留进程 | `Get-NetTCPConnection` 四端口收尾复核（:3000/:54329/:10086/:9222） | [✓] | §7（全部 CLEAN） |

## 5. 场景明细（V1–V5 + 混合 API 佐证）

> 被测版本均同 §0 头部（HEAD=`3426a2d` + 未提交 T-P10 改动）。Evidence 根目录 `.workflow-verify/tp10v/`。逐场景 console 错误全部为 0（`consoleErrorsTotal=0`，verify-result.json `consoleErrorsByScene` 五场景空数组）。

### V1（移动 375×667）：时间型空手卡（核心新增）— PASS
- Operation：清 localStorage 会话重进 tonight 页 → 点击输入框 → 输入「土豆丝」→ 回车成 chip → 确认默认时长档 30 分钟 → 点击「推荐今晚吃什么」→ 等待空手卡。
- Expected（方案 §3.5/§6.4 + 确认书 C-7 PD-017）：chip「土豆丝 ×」上屏且 `store.mustUse=['土豆丝']`、`timeBudgetMin=30`；空手卡标题=「能用到「土豆丝」的菜，今晚 30 分钟内排不下」（含当次所选档位分钟数，D-6）；正文=「菜里有能用到它的做法，但整套工时超过了 30 分钟。可以把时长换长一点再试，或去掉「土豆丝」按剩下的必消推荐。必消食材是硬要求，我们不会随便凑数。」；按钮 primary「去掉「土豆丝」再试」+ ghost「返回修改必消食材」；空手不建 Plan/不写 Event。
- Actual：全部逐字一致。V1-01-chip / V1-01b-default-budget-30 / V1-02-empty-title / V1-03-empty-text-timeout / V1-04-empty-buttons 全 PASS；teardown 窗口在该时段仅 V4 产物 1 Plan（V1 空手时段零写入）。
- Version：HEAD=`3426a2d` + 未提交 T-P10 工作区改动。
- Evidence：shots/v1-01-tonight-chip.png、v1-02-empty-timeout.png（已目检：时间型空手卡渲染正确）；verify-result.json checks `V1-*`。
- Coverage boundaries：覆盖 30min 默认档单必消时间型全文案；未覆盖 15min 档（同属 30 档以下必然空手，无信息增量）；未覆盖多必消全 TIME_BUDGET 组合（API 侧 tp01 R2 已覆盖同构路径）。

### V4（移动 375×667）：出路全链路（同会话换 60 分钟档）— PASS
- Operation：V1 空手卡上点击「返回修改必消食材」→ 确认 chip 保留 → 真实点击「60 分钟」时长档 chip → 确认 `store.timeBudgetMin=60` → 点击推荐 → 候选页 → 点击锁定 → plan 页 → 采购清单滚动至土豆行。
- Expected：返回后 chip「土豆丝 ×」保留；换档后推荐成功出候选且顶部横幅「✓ 必消食材已用上：土豆丝」；锁定后 plan 页横幅保持；清单土豆行标「已有 · 必消」；本次成功链路建 1 Plan + 2 Event（GENERATE/LOCK）。
- Actual：全部一致。候选 2 套=「番茄土豆炖牛腩套餐」「土豆炖鸡块套餐」；横幅逐字一致（候选页与 plan 页均为「✓ 必消食材已用上：土豆丝」）；清单土豆 `have="已有 · 必消"`；planId=`cmtx56vc9000idspgwcxl0521`；Z1 窗口统计 newPlans=1 / newEvents=2。
- Version：同上。
- Evidence：shots/v4-01-back-edit.png、v4-02-budget-60.png、v4-03-candidates-60min.png（已目检：候选 2 套+必消横幅）、v4-04-plan-60min.png、v4-05-list-have.png；verify-result.json checks `V4-*`、`Z1-window`。
- Coverage boundaries：覆盖「时间型空手 → 换长时长 → 出路可达」完整闭环；未覆盖空手卡 primary 按钮「去掉「土豆丝」再试」分支（tp01/既有回归覆盖去词语义，与换档出路互斥且无耦合）。

### V2（移动 375×667）：无菜型文案逐字不变（回归）— PASS
- Operation：清会话重进 → 输入「苦瓜」回车成 chip（默认 30 分钟档）→ 点击推荐 → 空手卡。
- Expected（确认书 C-7 既有文案，本次零改动）：标题=「今晚没有能用上「苦瓜」的做法」；正文=「必消食材是硬要求，用不上的方案不会推荐。现在的菜库里暂时没有用上苦瓜的菜，我们不会随便给你一套凑数的菜单。」；按钮 primary「去掉「苦瓜」再试」+ ghost「返回修改必消食材」。
- Actual：标题/正文/按钮全部逐字一致（V2-02-empty-title / V2-03-empty-text-verbatim / V2-04-empty-buttons 全 PASS）。
- Version：同上。
- Evidence：shots/v2-01-tonight-chip.png、v2-02-empty-nodish.png；verify-result.json checks `V2-*`。
- Coverage boundaries：覆盖库外词无菜型；15/60 分钟档下苦瓜同样 NO_DISH（归因与档位无关，API 侧 tp01 R1 已固定 30min 口径），未重复验证。

### V3（移动 375×667）：C-7a 组合凑不进文案逐字不变（回归）— PASS
- Operation：清会话重进 → 依次输入「西红柿」「西蓝花」两枚 chip → 点击推荐 → 空手卡。
- Expected（C-7a 既有文案，本次零改动）：标题=「西红柿、西蓝花这两样今晚凑不进同一桌菜」；正文=「每样单独都能做，但没有一桌能同时用上它们。」；仅 ghost「返回修改必消食材」，无 primary 重试按钮（`hasRetryPrimary=false`）；不与 C-7 指名型混淆（emptyReason.length===0 分支原样）。
- Actual：全部逐字一致；按钮恰为 `['返回修改必消食材']` 且无 primary（V3-02-title-C7a / V3-03-text-C7a / V3-04-no-retry-btn 全 PASS）。
- Version：同上。
- Evidence：shots/v3-01-tonight-chips.png、v3-02-empty-c7a.png（已目检：C-7a 文案+仅 ghost 按钮）；verify-result.json checks `V3-*`。
- Coverage boundaries：覆盖两必消 C-7a 渲染分支；「去掉…再试」按钮在该分支本就不存在（既有口径），未验证点击 ghost 后行为（与 T-P05 验收时口径一致，非本卡改动面）。

### V5（移动 375×667）：混合型空手卡（时间型+无菜型分句，D-4）— PASS
- Operation：清会话重进 → 依次输入「土豆丝」「苦瓜」两枚 chip（默认 30 分钟档）→ 点击推荐 → 空手卡。
- Expected（方案 §3.5 混合分句 D-4 + 确认书 C-7 PD-017）：标题含两词=「今晚没有能用上「土豆丝、苦瓜」的做法」；正文=「能用到「土豆丝」的菜今晚 30 分钟内排不下，换个更长的时长也许能排上；「苦瓜」暂时没有能用到它的菜。必消食材是硬要求，我们不会随便凑数。」；按钮 primary「去掉「土豆丝、苦瓜」再试」+ ghost「返回修改必消食材」。
- Actual：全部逐字一致；时间型分句/出路分句/无菜型分句三段断言（V5-03/04/05）均逐字命中。
- Version：同上。
- Evidence：shots/v5-01-tonight-chips.png、v5-02-empty-mixed.png（已目检：混合分句渲染正确）；verify-result.json checks `V5-*`。
- Coverage boundaries：覆盖「1 TIME_BUDGET + 1 NO_DISH」混合型；两词顺序颠倒（苦瓜在前）未重复验证（渲染按 unmetReasons 分组拼句，与输入顺序无关，API 侧 M3 键集断言已固定）。

### M 混合型 API 佐证（补充证据，非页面路径）— PASS
- Operation：`api-mixed.cjs` 经真实 HTTP `POST /api/recommend`（body `{people:4, timeBudgetMin:30, mustUse:['土豆丝','苦瓜']}`，cookie 鉴权）+ PG 直查 Plan/Event 前后计数。
- Expected（派发口径）：status 200；candidates=[]；unmetMustUse 含两词原文；unmetReasons 恰为 `{"土豆丝":"TIME_BUDGET","苦瓜":"NO_DISH"}`（key=用户原文，值=枚举）；响应无 planId；Plan/Event 零增量（空手不建）。
- Actual：10/10 PASS（EXIT=0）。status=200、candidates=0、`unmetMustUse=["土豆丝","苦瓜"]`、`unmetReasons={"土豆丝":"TIME_BUDGET","苦瓜":"NO_DISH"}`（M3 键集 / M4·M5 逐键 / M6 全量逐字三重断言）、planId=undefined、Plan 63→63、Event 105→105。
- Version：同上。
- Evidence：`.workflow-verify/tp10v/api-mixed.cjs` 运行日志（10 PASS / 0 FAIL）。
- Coverage boundaries：佐证 h5 三态渲染所依赖的 API 侧车字段真实形状；不替代页面操作验证（页面路径已由 V5 覆盖）。

## 6. 独立复跑回归链（B，fm-verify 独立执行，全绿）

| # | 命令 | 结果 | 退出码 |
|---|---|---|---|
| 1 | `pnpm --filter @family-menu/shared build` | 构建成功（契约 v0.8 dist 重建） | 0 |
| 2 | `pnpm --filter @family-menu/engine build` | 构建成功 | 0 |
| 3 | `pnpm test` | **15 files / 389 tests 全过**（= 既有 378 + feasibility.spec E1-E7 7 断言 + schemas.spec S1-S4 4 断言，增量与 dev 报告口径吻合） | 0 |
| 4 | `pnpm test:taboo` | **4 files / 89 tests 全过**（禁忌集 100%，一票否决项通过） | 0 |
| 5 | `node tests/tp01-mustuse-regression.cjs` | **54 PASS / 0 FAIL**（真实 HTTP + 真实 PG，禁 Mock；既有 35 断言零改动全绿 + T-P05 增量 + T-P10 R1-R4：R1 苦瓜→`unmetReasons={苦瓜:NO_DISH}`、R2 土豆丝@30min→`{土豆丝:TIME_BUDGET}`、R3 土豆丝@60min 候选非空（实测 2 套）且响应无 unmetReasons 键、R4 西红柿正常推荐无 unmetReasons 键） | 0 |
| 6 | h5 typecheck（`pnpm --filter h5 exec tsc --noEmit` 口径） | 0 error | 0 |
| 7 | h5 build（`pnpm --filter h5 build:h5` 口径） | webpack 25 warnings / **0 error**（与 dev/review 基线一致） | 0 |

- 备注 1：PowerShell 5.1 下 `2>&1` 重定向 vitest/tsc 的 stderr 输出会显示 `NativeCommandError` 噪音，属已知重定向表现，不影响判定（以退出码与测试计数为准）。
- 备注 2：tp01 复跑为验收前置步骤，其 DB 产物（+8 Plan / +10 Event）已并入验收窗口，由 §7 teardown 一并清理。
- 备注 3：h5 build 与 dev server watch 存在 Windows 文件锁冲突风险，回归期间已先停 dev server 再 build，build 后重启供 CDP 场景使用（收尾已停）。

## 7. Teardown 与收尾（C）

- 白名单来源：`teardown-window.json`（UTC 阈值 `2026-09-11T00:00:00Z` 圈定全部验收产物：Plan=9 / Event=12，其中 tp01 复跑产物 8/10 + 本验收 V4 产物 1/2；空手场景 V1/V2/V3/V5 与混合 API 零写入，已由窗口统计证实）。
- `teardown.cjs` 执行结果 **11/11 PASS（EXIT=0）**：
  - T0 删除前白名单命中核对：Plan 9/9、Event 12/12（无漂移方可删除）；
  - T1 先删 Event：deleted=12/12；T2 后删 Plan：deleted=9/9（外键顺序正确）；
  - T3 删除后七表复核：**Dish=48 / Menu=13 / MenuDish=42 / Ingredient=79 / Plan=54 / Event=93 / CookLog=6 —— 全部回基线；REL=29 不动**。
- 服务与端口收尾：
  - h5 dev server（:10086）：StopCommand 停止 → 零监听；
  - PG（:54329）：`pg_ctl stop -D .pg/data -m fast` → EXIT=0「服务器进程已经关闭」→ 零监听；
  - API（:3000，PID 35488）与 Edge CDP（:9222，PID 34364 及子进程树）：均为验收期间启动的独立进程，按 PID 结束 → 零监听；
  - 最终复核：`PORT3000=CLEAN / PORT10086=CLEAN / PORT9222=CLEAN / PORT54329=CLEAN`（AC7 达成，含派发要求的 :3000/:54329）。
- 验收产物清单（隔离目录，未触碰业务基线）：`.workflow-verify/tp10v/` 下 db-count.cjs、cdp.cjs、verify-tp10v.cjs、api-mixed.cjs、teardown.cjs、verify-result.json、teardown-window.json、teardown-result.json、before-window.json、shots/*.png ×13。

## 8. 环境噪音与执行说明（不影响结论）

- h5 dev server 在验收前一段会话中进程死亡（:10086 无监听），已按环境口径重启（编译成功后 PID 840 监听确认）再执行场景；收尾已停。
- 终端中文输出在 GBK 控制台显示为乱码，仅为显示问题；全部中文断言值以 UTF-8 产物文件（verify-result.json 等）核验为准。
- 脚本初版一处 ROOT 路径层级错误（api-mixed.cjs ENOENT）与一处冗余断言已当场修正，均不涉及业务代码。

## 9. 最终结论

- **A 真机/CDP 场景**：V1 时间型（核心新增）、V2 无菜型逐字不变、V3 C-7a 逐字不变、V4 出路全链路、V5 混合分句——33/33 checks 全 PASS，console 错误全场景为零，输入全部走 CDP 可信键盘路径（cdp-trusted-enter）。
- **B 独立复跑回归链**：shared/engine build、pnpm test 389/389、test:taboo 89/89、tp01 54/54、h5 typecheck+build 0 error——全绿。
- **C teardown**：白名单精确删除 Plan 9 / Event 12，七表回基线 48/13/42/79/54/93/6、REL=29 不动，四端口零遗留。
- **AC 对照**：AC1-AC7 全部 [✓]（明细见 §4）。
- **T-P10 验收结论：PASS（通过）**。未发现实现问题，无 FAIL 项、无越界修改。建议主控按流程进入用户审核/合并环节。
