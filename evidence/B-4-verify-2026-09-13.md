# B-4 独立验收复跑报告（fm-verify）

> 日期：2026-09-13 ｜ 执行角色：fm-verify（产品验收，独立于 dev/review）
> 验收对象：任务卡 B-4（T-P03 review 建议级 #4 核销）——[evidence/B-4-task-2026-09-13.md](./B-4-task-2026-09-13.md) AC1~AC5
> 输入：dev 报告 [evidence/B-4-dev-2026-09-13.md](./B-4-dev-2026-09-13.md)、review 报告 [evidence/B-4-review-2026-09-13.md](./B-4-review-2026-09-13.md)（PASS WITH NOTES 0C/0T/2S，移交清单 V-1~V-5）
> **本报告全部数字来自 fm-verify 本机独立真实执行（命令+退出码逐条在案），未采信 dev/review 数字作为判定依据。**

## 0. 总判定：**PASS**

V-1~V-5 全部通过，AC1~AC5 独立复证全部成立；未发现任何 C/T/S 级新问题；红线全程遵守；异常升级路径全程未触发。

| 验证项 | 判定 | 关键实测数字 |
|---|---|---|
| V-1 复跑回归脚本 | [✓] | **20 PASS / 0 FAIL，EXIT=0**；末 2 条即 B-4 断言（status=204、acao=null） |
| V-2 复证改动面 | [✓] | `git diff --numstat` = `26 0 tests/tp03-cors-regression.cjs` 恰 1 文件、纯追加 |
| V-3 复证 teardown | [✓] | 中间态 55/94 → DELETED 1/1 → **AFTER 54/93，RESIDUAL=0，EXIT=0**；七表 before/after 逐项相等 |
| V-4 复证全量 | [✓] | `pnpm test` **442/442** EXIT=0 + `pnpm test:taboo` **95/95** EXIT=0（**原命令口径**，基线零变化） |
| V-5 物证一致性抽查 | [✓] | dev 物证 log 三件与 dev 报告 §5 逐项吻合；:3000 验收后清零、PG 54329 未动、9222 未触碰（HTTP 200 在线） |

## 版本与环境

- 分支：`feat/tp-06-menu-expansion`；HEAD：`ee74afacf0b528a13b06780348f28ab5d275d300`（与任务卡基线 `ee74afa` 一致）
- API：`pnpm --filter @family-menu/api dev`（= `tsx watch src/server.ts`，pid 22080）→ `Server listening at http://127.0.0.1:3000`，`PORT3000_LISTENING=True` 探活确认后才执行断言
- PG：127.0.0.1:54329 在线（`PORT54329_LISTENING=True`），全程未停
- 测试账号/令牌：使用仓库 .env 既有 `ACCESS_TOKEN`（与 dev 同一授权口径，实值未读取落盘——脱敏）

---

## V-1 复跑回归脚本 [✓]

- **Operation**：①`pnpm --filter @family-menu/api dev` 非阻塞启动 → 轮询至 `Server listening at http://127.0.0.1:3000`；②`Test-NetConnection 127.0.0.1 -Port 3000` → True 留证；③`node tests/tp03-cors-regression.cjs`，输出 Tee 落盘。
- **Expected**（任务卡 AC2 + review 移交清单 V-1）：20 PASS / 0 FAIL、EXIT=0；末 2 条 PASS 为 B-4 断言（status=204、acao=null）；既有 18 断言全绿。AC2-b 产生 1 Plan+1 Event 属既有机制（任务卡注明）。
- **Actual**：`== 结果: 20 PASS / 0 FAIL ==`，`REGRESSION_EXIT=0`。末 2 条实测：
  - `[PASS] B-4 非白名单 Origin 预检：204 放行（预检仍被短路，不落鉴权 401）: status=204（预期 204）`
  - `[PASS] B-4 非白名单 Origin 预检：acao=null（白名单外不写 ACAO，浏览器将拦截）: acao=null`
  - 既有 18 条 PASS 与 dev 报告 §3.2 逐行同构；AC2-b 实测 `planId=cmtzvvrj200001cpgwi8zvpay`（与 dev 两轮 planId 均不同，独立真实执行第三值）。
- **Version**：feat/tp-06-menu-expansion @ ee74afa
- **Evidence**：`.workflow-verify/b4-verify/tp03-regression-verify.txt`（全量输出）、`api-dev.log`、`port3000-listening.txt`
- **Coverage boundaries**：覆盖 API 实跑下 tp03 全部 20 断言（真实 HTTP fetch，无 Mock）。未覆盖：H5 页面端浏览器真实预检行为（本卡为 API 回归脚本核销，页面端已由 T-P03 验收覆盖）；未做 review 清单中标注"可选"的 git stash 前后对比（既有 18 断言零改动已由 V-2 的 0 删除行数学保证）。

## V-2 复证改动面 [✓]

- **Operation**：`git branch --show-current`；`git rev-parse HEAD`；`git diff --numstat`；`git diff --stat`；`git status --porcelain`（全部只读）。
- **Expected**（review 移交清单 V-2）：numstat = 26/0 恰 1 文件 `tests/tp03-cors-regression.cjs`；porcelain 改动面 = `M` 该文件 + untracked 证据类。
- **Actual**：
  - numstat：`26      0       tests/tp03-cors-regression.cjs`（0 删除行 = 既有断言纯追加、逐字未动的数学保证）
  - `git diff --stat`：`tests/tp03-cors-regression.cjs | 26 +++... / 1 file changed, 26 insertions(+)`
  - porcelain：` M tests/tp03-cors-regression.cjs` + untracked 15 项，全部为证据类/开工前既有（`.pai/`、`docs/design/*`、`docs/ui-redesign/`、`evidence/B-4-{task,dev,review}-2026-09-13.md`、`tests/engine-perf.mjs`、`tests/step08-deploy-validate.cjs`、`tools/*`、`复盘报告.md`），与 dev 报告 §6 声明一致（`evidence/B-4-review-2026-09-13.md` 系 dev 之后主控落盘 review 报告新增，属证据类）。禁区路径（packages/**、apps/api/src/**、apps/api/prisma/**、apps/h5/**、tools/** 的 tracked 文件）零修改。
- **Version**：同上
- **Evidence**：本节数字直接引自 git 实测输出（未截留文件，输出全文见 git 终端；关键数字已复录）
- **Coverage boundaries**：覆盖 tracked 改动与 untracked 清单；未覆盖：git 历史提交比对（非本卡范围）。

## V-3 复证 teardown [✓]

- **Operation**：①V-1 前录 before：`node .workflow-verify/b4/count-check.cjs`（只读 COUNT）→ `.workflow-verify/b4-verify/before-count.txt`；②备份 dev 快照 → 重跑 `node tests/tp02-cleanup-test-plans.cjs snapshot`（teardown 既有机制，刷新快照保证独立复跑一致性，EXIT=0，`SNAPSHOT_OK: 已记录 54 个既有 Plan id`）；③V-1 后录中间态 count；④`node tests/tp02-cleanup-test-plans.cjs clean`；⑤录 after count。
- **Expected**（任务卡 AC3 + review 移交清单 V-3）：clean 后 Plan/Event 回 54/93、RESIDUAL=0；七表基线 {Dish:49, Menu:13, MenuDish:42, Ingredient:81, Plan:54, Event:93, CookLog:6} + ExclusionRule=3 逐项一致（S-3 卡定谳口径）；before/after 逐项相等。
- **Actual**（时间序完整算术链）：
  - before：`[2026-09-13T14:01:26.681Z] Dish=49 Menu=13 MenuDish=42 Ingredient=81 Plan=54 Event=93 CookLog=6 ExclusionRule=3`（与 S-3 定谳口径逐项精确一致）
  - 中间态（V-1 后）：`[2026-09-13T14:03:00.865Z] ... Plan=55 Event=94 ...`（恰 +1/+1，为本轮 AC2-b 所产生；同时反向佐证 B-4 的 OPTIONS 预检请求零数据副作用）
  - clean：`BEFORE Plan=55 Event=94` → `CANDIDATES: 非快照 PROPOSED Plan=1，白名单命中=1，跳过=0` → `SAFETY_OK: 1/1 通过 status=PROPOSED + mustUse 精确匹配复核` → `DELETED Events=1 Plans=1` → `AFTER Plan=54 Event=93` → `RESIDUAL_CHECK: 目标 planId 残留=0`，`TEARDOWN_EXIT=0`
  - after：`[2026-09-13T14:03:01.129Z] Dish=49 Menu=13 MenuDish=42 Ingredient=81 Plan=54 Event=93 CookLog=6 ExclusionRule=3` —— **与 before 逐项相等，零漂移**
- **Version**：同上
- **Evidence**：`before-count.txt`、`mid-count.txt`、`teardown-clean.txt`、`after-count.txt`、`snapshot-result.txt`、`tp02-planids-before-dev.txt`（dev 快照备份）
- **Coverage boundaries**：覆盖 Plan/Event 清理闭环与八表 COUNT 级基线比对；未覆盖：表内容级/字段级 diff（S-3 定谳口径即 COUNT 级，本卡未改任何数据写入逻辑）。

## V-4 复证全量 [✓]（执行口径：pnpm 原命令）

- **Operation**：按 review S-1 建议优先实跑任务卡字面原命令：`pnpm test` → `pnpm test:taboo`（输出 Tee 落盘 + `$LASTEXITCODE` 留证）。本机 pnpm shim stdout 捕获正常，**未降级**至 vitest 直跑等价口径。
- **Expected**（任务卡 AC4）：442/442 + 95/95 EXIT=0，与基线零变化。
- **Actual**：
  - `pnpm test`（= `vitest run --no-file-parallelism`）：`Test Files 19 passed (19)` / `Tests 442 passed (442)`，`PNPM_TEST_EXIT=0`
  - `pnpm test:taboo`（= `vitest run packages/engine/test/`）：`Test Files 5 passed (5)` / `Tests 95 passed (95)`（含 `千套菜单 recommend 调用性能 < 50ms（AC12） 388ms` 全绿），`PNPM_TABOO_EXIT=0`
  - 与基线 442/442 + 95/95 **零变化**（改动仅 tests/*.cjs 独立脚本，不在 vitest include，实测印证）。
- **Version**：同上
- **Evidence**：`pnpm-test-stdout.txt`、`pnpm-taboo-stdout.txt`
- **Coverage boundaries**：覆盖根全量 vitest + engine taboo 全量；未覆盖：lint/build（非本卡 AC）；AC12 耗时为性能自然波动量（388ms vs dev 345ms），断言全绿不受影响，不构成差异项。

## V-5 物证一致性抽查 [✓]

- **Operation**：①实读 dev 物证 `.workflow-verify/b4/test-full.log`（offset 168-182）与 `.workflow-verify/b4/taboo-full.log`（全文）；②实读 `.workflow-verify/b4/count-check.cjs` 全文；③与 dev 报告 §5 逐项比对；④现场清理确认：`:3000` 复探、`:54329` 探活、`http://127.0.0.1:9222/json/version` 只读探活。
- **Expected**（review 移交清单 V-5 + 本轮红线）：dev 物证 log 与 dev 报告 §5 数字吻合；count-check.cjs 仅 SELECT；:3000 验收后清零；PG 54329 未动；9222 未触碰。
- **Actual**：
  - `test-full.log` L179-180：`Test Files 19 passed (19)` / `Tests 442 passed (442)` —— 与 dev 报告 §5 一致
  - `taboo-full.log` L9-12：`千套菜单 recommend 调用性能 < 50ms（AC12） 345ms` + `Test Files 5 passed (5)` / `Tests 95 passed (95)` —— 与 dev 报告 §5（含 AC12 345ms）一致
  - `count-check.cjs` L28 仅 `SELECT COUNT(*)::int AS n FROM "<表>"`，零写入零删除，与 review S-2 描述一致
  - 现场清理：停服后 `PORT3000_LISTENING=False`（本验收启动的 API 已自停）；`PORT54329_LISTENING=True`（本地 PG 在线未动）；`CDP9222_ALIVE=True HTTP=200`（9222 CDP 浏览器未触碰、仍在线）
- **Version**：同上
- **Evidence**：`port3000-after-stop.txt`、`port54329-check.txt`、`cdp9222-check.txt`；dev 物证 log 实读行号已在案
- **Coverage boundaries**：覆盖 dev 三件物证与本报告现场清理；未覆盖：dev 物证与 db 逐行内容级比对（COUNT 级口径已闭环）。

---

## 红线遵守声明（本轮实测）

1. 未删 `$env:TEMP\fm-rds.env`（全程无任何针对该文件的操作）
2. 未停止本地 PG 54329（探活 True 始终在线，无 stop/kill 类命令）
3. 未杀 9222 CDP 浏览器（仅只读 GET /json/version 探活留证，HTTP 200）
4. 未执行 DROP/清理类 SQL（本轮唯一数据删除为 `tests/tp02-cleanup-test-plans.cjs clean`，任务卡红线明示按既有机制除外；count-check.cjs 实读仅 SELECT）
5. 未触碰生产 RDS（本轮全部连接均指向本地 PG，经 .env `DATABASE_URL` 同一口径）

## 备案披露

1. **V-4 口径升级**：dev 因 shim 捕获疑虑采用 vitest 直跑等价口径（review S-1 备注）；本轮实跑确认本机 shim 可捕获，**已按任务卡字面 `pnpm test` / `pnpm test:taboo` 原命令完成复证**，S-1 建议落实，无口径残留。
2. **teardown 快照刷新**：为独立复跑闭环，先备份 dev 快照（`tp02-planids-before-dev.txt`）再重跑 snapshot（54 id，EXIT=0）。快照文件属 teardown 既有机制的运行时输入，位于 gitignore L26 覆盖目录，不入库。
3. **AC12 耗时波动**：本轮 388ms vs dev 345ms，同断言全绿，性能自然波动，非差异项。
4. **PowerShell 噪音**：pnpm stderr 经 2>&1 重定向会显示 NativeCommandError 样式噪音（`$ tsx watch ...` 回显行），不影响退出码与测试结果判定，如实披露。
5. **物证脱敏**：本轮 log 中 planId（`cmtzvvrj200001cpgwi8zvpay`）为回归脚本自产生的测试数据 id（非真实用户数据），与 dev/review 报告先例一致予以留证；.env 实值（DATABASE_URL/ACCESS_TOKEN）未读取落盘。
6. **review 未验证项闭环情况**：review 报告 §五 1/2/4（实跑数字、git 数字、令牌实跑有效性）已由本轮 V-1/V-2/V-4 独立复证覆盖；§五 3（T-P03 报告无 AC2-d 断言名）维持 review 备案结论，不影响判定。

## 物证清单（.workflow-verify/b4-verify/，gitignore 内不入库）

| 文件 | 内容 |
|---|---|
| `before-count.txt` / `mid-count.txt` / `after-count.txt` | 七表+ExclusionRule 计数三时点 |
| `snapshot-result.txt` / `tp02-planids-before-dev.txt` | 快照刷新 EXIT=0 / dev 快照备份 |
| `api-dev.log` | API 启动日志（Server listening :3000） |
| `port3000-listening.txt` / `port3000-after-stop.txt` | :3000 探活 True / 停服后 False |
| `tp03-regression-verify.txt` | V-1 全量输出（20 PASS / 0 FAIL） |
| `teardown-clean.txt` | V-3 clean 全量输出（RESIDUAL=0） |
| `pnpm-test-stdout.txt` / `pnpm-taboo-stdout.txt` | V-4 两命令全量输出 |
| `port54329-check.txt` / `cdp9222-check.txt` | PG 在线 / CDP 未触碰探活 |

## 结论

B-4 改动（tests/tp03-cors-regression.cjs +26/-0 纯追加 2 条 B-4 断言）经独立复跑全部验证通过：回归 20 PASS/0 FAIL、teardown 精确回基线零漂移、全量 442+95 与基线零变化、改动面恰 1 文件、证据链自洽。**判定 PASS，无遗留问题**；CORS 挂账 B-2/B-3 不在本卡范围，维持待排期。
