# B-4 任务卡：T-P03 review 建议级 #4 核销——tp03 回归补非白名单 Origin 预检负路径断言

> 日期：2026-09-13 ｜ 分支 feat/tp-06-menu-expansion ｜ 基线 HEAD `ee74afa`（LOGIN-DOM 提交后）
> 来源：[evidence/T-P03-2026-09-06.md](./T-P03-2026-09-06.md) §7 B-4 原文「缺非白名单 Origin 预检断言（应验证 204 + acao=null）」；队列挂账见 CURRENT.md L84「fm-reviewer 建议级 B-2/B-3/B-4 排期」。

## 一、目标

tests/tp03-cors-regression.cjs 现有 18 断言仅覆盖白名单源预检正路径与生产域名 Origin 的**非预检**请求（AC2-d），缺「非白名单 Origin 预检」负路径。本卡补齐该断言，固化 @fastify/cors 白名单语义的防回归锚点（未来若有人误改 origin 白名单逻辑/strictPreflight 等配置，负路径断言即失败暴露）。

## 二、预期行为依据（断言预期值不许自创）

1. 代码级：apps/api/src/app.ts L60 注释「origin 数组为全等白名单：不匹配时仅不写 ACAO 头、请求照常放行」——预检仍被插件短路 204，但不写 ACAO。
2. 实测级：fm-verify 于 T-P03 验收时实测「非白名单 Origin 预检（204 + acao=null）」（T-P03 报告 §8.3）。
3. 白名单现值（app.ts L65）：`['http://127.0.0.1:10086', 'http://localhost:10086']`。

## 三、AC（逐条 [✓]/[✗] 自检）

- **AC1** tests/tp03-cors-regression.cjs 新增一段（建议位于 AC2-d 之后，段名/注释须标注 B-4 来源），恰 2 条新断言：
  - 非白名单 Origin 预检（OPTIONS /api/recommend + Origin + ACRM=POST + ACRH）→ status=204（放行）；
  - 同请求 → acao=null（白名单外不写 ACAO，浏览器将拦截）。
  - 预检 Origin 复用脚本既有非白名单代表 `https://menu.jijingkongjian.xin`（与 AC2-d 口径一致，注释呼应生产同源部署不产生预检的语义）。
  - **既有 18 断言逐字不变**（名称、顺序、断言逻辑全部不动）。
- **AC2** 真实跑通：API 服务运行后 `node tests/tp03-cors-regression.cjs` → **20 PASS / 0 FAIL，EXIT=0**（18 旧全绿 + 2 新全绿）。
- **AC3** teardown 还原：跑 tests/tp02-cleanup-test-plans.cjs 后 Plan=54 / Event=93 精确回基线；七表基线 **{Dish:49, Menu:13, MenuDish:42, Ingredient:81, Plan:54, Event:93, CookLog:6} + ExclusionRule=3** 零漂移（S-3 卡定谳口径）。
- **AC4** 零影响面证明：`pnpm test` 442/442 + `pnpm test:taboo` 95/95 EXIT=0（改动仅 tests/*.cjs 独立脚本，不在 vitest include，全量数必须与基线零变化）。
- **AC5** 证据落盘：完成报告写 evidence/B-4-dev-2026-09-13.md，含真实执行输出（回归脚本全量输出、teardown 前后 COUNT、git diff --stat 实测数字；禁止凭记忆转述数字）。

## 四、边界约束

1. **只许改** tests/tp03-cors-regression.cjs（+ 新增证据报告）。其余一切文件禁改。
2. 禁改禁区：packages/shared、packages/engine、packages/list-merger、apps/api/src/**、apps/api/prisma/**、apps/h5/**、tools/**。
3. 不新增任何依赖；不改动 apps/api 运行时代码与 CORS 配置。
4. 红线：不删 $env:TEMP\fm-rds.env；不停止本地 PG 54329；不杀 9222 CDP 浏览器；不执行 DROP/清理类 SQL；不触碰生产 RDS。
5. 不做卡外顺手修改（发现脚本其他问题→写进完成报告）。

## 五、异常升级路径

启服失败 / 任一新断言实测与 §二 预期不符 / teardown 未回基线 / pnpm test 非 442 → **停止实施**，完整记录现场（命令+输出+退出码）报主控，不自行扩大改动、不多次盲试。

## 六、派发

- 执行角色：fm-dev ｜ 完成后返回逐条 AC 自检 + 证据文件路径。
