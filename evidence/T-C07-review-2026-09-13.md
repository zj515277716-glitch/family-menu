# T-C07 技术审查报告（fm-reviewer → 主控代落盘）

> 审查：fm-reviewer｜日期：2026-09-13｜总判定：**PASS WITH NOTES**（0 C / 0 T / 5 S）
> **声明：本审查方未修改任何文件、未执行任何写库命令；本次审查全部操作为只读实读（Read/Glob/Grep/LS）。**

> 主控落盘注记：本卡此前已完成一轮审查（总判定 PASS，6 条 S 级），因主控侧上下文压缩丢失报告原文，故重派 fm-reviewer 基于当前文件独立复审，**以本报告为准**。两轮口径差异如实记录：本轮复审新增 S-4（putExclusions 全量替换语义，实现卡必备设计项）；原 ExclusionRule 基线预声明 / upsert 重放禁清库 / 端到端拦截验证三项要求并入本轮移交清单 2.a/2.b/2.c。

## 一、审查范围与方法

- **审查对象**：任务卡 evidence/T-C07-task-2026-09-13.md、dev 评估报告 evidence/T-C07-dev-2026-09-13.md、评估产物 11 个文件（6 脚本 + 2 快照 + 3 结果 JSON，.workflow-verify/t-c07/）、上游 evidence/T-C06-review-2026-09-13.md（S-3 原始观察）。
- **方法**：①源码逐行实读（packages/engine/src/safety.ts、apps/api/prisma/seed-data.ts、apps/api/prisma/seed.ts、apps/api/src/services/planService.ts、apps/api/src/services/mappers.ts、packages/engine/src/types.ts、共享 schema/constants、taboo/score/seed 测试规格、swap-candidates/score/recommend）；②产物直接实读（v0~v5 脚本逐行审读确认 SELECT-only、3 个 result JSON 与 2 个快照逐项比对）；③packages/engine/dist/safety.js 与 src 逐函数对照（v2 运行时所 import 的生产代码与源码逻辑一致，已实读确认）。
- **方法学限制（如实声明）**：本审查环境无 shell，`pnpm test` / `node v*.cjs` / `git status` 均未独立重跑。测试数字（391/391、89/89）与 git 状态采信 dev 运行时留痕（其中 git status 留痕固化于 .workflow-verify/t-c07/result-v2.json Part D）；HEAD=805814f / 分支名未独立核验（无 git 访问，标注未验证），但所有被审文件内容与任务卡描述基线一致，构成间接佐证。凡属此类均在下文标注。

## 二、逐 AC 复核结论

| AC | 判定 | 审查依据（独立复核） |
|---|---|---|
| AC1 全景数据面 | **[✓]** | result-v1.json 直接实读：规则 2 行原文（HARD+TAG 花生 / SOFT+TAG 内脏）、花生族食材恰 1 行（花生米 + 2 别名 + category=调料）、含花生菜 2 道含 optional（false/true）与全表行数（14/10）、category 7 类分布无「花生」、flavorTags 8 个分布无「花生」、Dish=49（DRAFT 30/PUBLISHED 18/TESTED 1）。与 dev 报告 §2 及 seed 源码（seed-data.ts L27-46）逐项一致。 |
| AC2 缺口定性 | **[✓]** | ①源码级：本人实读 safety.ts L104-127 独立确认（见三.a）；②运行时级：v2-runtime-repro.mjs 逐行审读——真实 PG 连接 + `import` dist 生产代码 + 真实 mappers，非 Mock；result-v2.json Part B：total=49/blocked=0/passed=49，含花生 2/2 暴露。满足「禁只靠静态推理」要求。 |
| AC3 暴露面量化 | **[✓]** | 全集=2 道（均 DRAFT，含 status/花生行/两消费口径），由 result-v1/v2 交叉印证；「全库 0/49 被拦」意味着花生规则对任意一道菜均无拦截力，dev 表述准确。反向路径三通道逐一证伪（v1 花生 tag=0、category 无花生品类、TAG 通道源码不读食材名），「花生反向有效路径=空集」结论成立；SOFT 内脏不走安全层（score.ts SOFT 分支）故安全层实际拦截清单为空，与 Part A 12/12 通过自洽。 |
| AC4 处置方案 | **[✓]** | A/B/C 三案齐备，每案含改动位置形态/影响面/风险副作用/推荐序；aliases 泛匹配误伤讨论在案（方案 A 精确等值不误伤 vs 方案 B contains 误伤推演）；三约束（零 LLM、shared 零改动、RDS 另立卡）经源码核验成立：ExclusionRuleSchema（packages/shared/src/schemas/family.ts L48-57）的 scope 枚举含 INGREDIENT、targetId/targetTag 均 optional 且无 superRefine → 方案 A 新行零契约改动即可通过 zod。发现一处方案 A 影响面遗漏（putExclusions 全量替换语义，见 S-4），不构成 AC4 不达标，列为移交项。 |
| AC5 环境与回归 | **[✓*]** | 快照前后 13 表逐字段一致（snapshot-before.json / snapshot-after.json，与任务卡基线数字完全吻合）+ v0~v5 全部脚本经我逐行审读确认 SELECT-only（无任何 INSERT/UPDATE/DELETE/DDL）→ 零写库红线成立（方法学注记见 S-3）。`pnpm test` 391/391 与 `test:taboo` 89/89：dev 报告留有命令+退出码+数字，本审查无 shell 未独立复跑，**采信留痕**（[✓*] 部分独立）。证据落盘真实（命令+退出码+数字齐备）。 |
| 升级1 同族扫描 | **[✓]** | §五单列；result-v3.json 实读：花生=口径缺口（通道①②全 0、③命中 1 食材/2 菜引用），内脏=完全死规则（三口径全 0），2/2 规则全覆盖（v1/v2/v3 三方行数一致均为 2）。 |
| 升级2 S-3 证伪可能 | **[✓]** | S-3 观察成立非证伪：T-C06 静态推断「可能不拦」升级为运行时实证「不拦」。§3.4 的偏差解释方向正确但措辞失准（见 S-1）。 |
| 边界约束 | **[✓]** | 产物仅落 .workflow-verify/t-c07/ 与 evidence 报告（LS 实证恰 11 个产物文件）；脚本零依赖新增（node 内置 + 既有 pg）；未改 shared（constants/schema 实读为基线原样）；未碰 RDS（dev 声明 + 脚本仅连本地 127.0.0.1）；零代码改动佐证见三.e。 |

**AC 复核结论：8/8 达成（AC5 为部分独立验证 [✓*]），0 项不达成。**

## 三、关键技术结论独立复核（a~e）

**a) TAG 通道不读食材名 → 对花生米拦截力为零 —— 成立（源码级独立确认）。**
seed-data.ts L28-36 确有 `{scope:'TAG', targetTag:'花生', severity:'HARD'}` 规则；checkTagScope L104-127 全函数仅两处命中判定：L111 `dish.flavorTags.includes(tag)`（数组等值包含）与 L118 `ing.category === tag`（等值比较），`ingredientName`/`aliases` 二字段在全函数任何位置都不出现。花生米 category=调料（v1 实证）→ 两通道对「花生」字符串的命中面均为空集。dist 产物逐函数对照与 src 逻辑一致（packages/engine/dist/safety.js L75-97），v2 运行时 import 的即此逻辑。

**b) 运行时复现 0/49 拦截、含花生 2/2 暴露 —— 成立（产物级独立确认）。**
v2-runtime-repro.mjs 逐行审读：真实 `.env` DATABASE_URL 连 PG、`import()` dist 生产 safetyFilter 与 api dist mappers、装配复刻 loadExclusionViews（planService.ts L104-118）与 loadMenuViews（L122-139）、Part B 复刻 toSyntheticMenu（packages/engine/src/swap-candidates.ts L33-44）。result-v2.json：Part A 12 PUBLISHED 菜单 12/12 通过 0 被拦；Part B total=49/blockedCount=0/passedCount=49；peanutExposed 恰为宫保鸡丁（花生米 50g optional=false）与蚝油生菜（花生米 10g optional=true）两行，dishId 与 T-C06 verify 记录一致。数字全部对得上。

**c) 反证链 C1/C2/C3 支撑方案 A 通道有效性 —— 成立（附一处方法学注记）。**
- C1（result-v2.json partC.c1）：内存追加 HARD+INGREDIENT 后 2/2 blocked，reason 含 `命中 HARD 食材禁忌`，optional=true 行同拦——与 checkIngredientScope L59-84 逐行吻合（L67 内层 for 遍历全部食材行，无 optional 判断）；
- 生产链路四环节逐一源码确认：buildHardIngredientNameSets（safety.ts L7-21，targetId+targetName+targetAliases 入集）、loadExclusionViews（planService.ts L107-109，仅 INGREDIENT 行 join）、toExclusionView（mappers.ts L125-126，注入 targetName/targetAliases）、ExclusionView（packages/engine/src/types.ts L48-57，契约承载）；
- C2（flavorTags 补「花生」即拦）、C3（targetTag=调料拦 46/49，与含调料品类菜数 46 精确相等——按 partB 49 行名单逐一比对，未被拦的 3 道恰为无调料品类食材的番茄炒蛋/蒜蓉青菜/西兰花炒虾仁，内部自洽）均成立；
- **注记**：C1 探针是在内存直接构造 ExclusionView（携带 targetName/aliases），未运行时走「DB 规则行 → loadExclusionViews join → view」全链（零写库红线下的合理替代）；该全链的 INGREDIENT 分支为源码级确认 + 各环节分别运行时实证。因此**端到端拦截验证须列为实现卡验收项**（移交清单 2.c）。

**d) 方案 A 副作用=0 与 seed 全 upsert 幂等 —— 基本成立（两处补强/收紧）。**
- seed.ts L85-219 逐段实读：Family/FamilyRule/ExclusionRule/Ingredient/Dish/DishIngredient/Menu/MenuDish 八段全部 upsert，无任何 deleteMany/delete，重放安全（dev 引「L83-207」与实际范围略有出入，无实质影响）。新增规则行走 `where:{id}` upsert 的 create 分支即可落库。
- **补强（对实现卡有利）**：dev §六方案 A 风险 3「seed.spec.ts 若断言规则数量需同步调整」可收紧——seed.spec.ts L36-52 实为「schema parse 全通过 + 至少 1 HARD + 至少 1 SOFT」三类**最小值/遍历断言**，叠加 ExclusionRuleSchema 无 superRefine，方案 A 新增一行**现有测试零改动即通过**。
- **收紧（见 S-4）**：方案 A 影响面遗漏了 putExclusions（planService.ts L352-370）全量替换语义（事务内 deleteMany+createMany），seed 重放落地的规则可被一次前端保存整体清除。
- 副作用=0 的「0 食材行菜=0 道」依赖 v5（stdout 未落盘，见 S-2），方向为安全侧保守过滤，风险等级低。

**e) 红线核查 —— 成立（部分独立验证）。**
- 零写库：13 表 before/after 计数逐字段一致（两快照实读比对）+ v0~v5 六脚本逐行审读均 SELECT-only（含 v4/v5）→ 判定成立；窗口方法学注记见 S-3。
- 零代码改动：result-v2.json Part D gitStatusPorcelain 仅 `??` untracked 行、无任何 `M` 行（dev 运行时留痕）；本审查另以直接实读佐证——safety.ts/seed-data.ts/seed.ts/planService.ts/mappers.ts/types.ts 内容均与「未做方案改动」的基线预期逐行一致；dist/safety.js 与 src 逻辑一致。本方无 shell 未复跑 git status（采信留痕）。untracked 清单内无本卡相关源码修改。
- 产物落位：LS 实证 .workflow-verify/t-c07/ 恰 11 个文件，与本审查所读一一对应。

## 四、分级问题清单

**C 级（阻塞）：无。**
**T 级（必须修复才可过）：无。**

**S 级（建议级，不阻塞通过）：**

| 编号 | 位置 | 问题 | 处置要求 |
|---|---|---|---|
| S-1 | dev 报告 §3.4 | 用例形态表述失准：「既有 TAG 用例全部是 flavorTags 恰含目标串」不成立——taboo.spec.ts L86-101（L94 targetTag=水产）标题即「按食材 category 过滤（非 flavorTags）」的 **category 通道**用例，score.spec.ts L199-209（L203 水产）同为 category 通道；仅 L262-272（麻辣）与 taboo L77-84（内脏）是 flavorTags 通道。**核心论点不受影响**：既有用例确无「食材名/别名含目标串而 flavorTags/category 均不含」的真实形态用例（fixtures 的 EXCLUSION_HARD_OFFAL_TAG 配 MENU_OFFAL 走 flavorTags 通道），391/391 与缺口共存的原因成立 | 实现卡补真实形态用例时一并按此口径修正表述；dev 报告是否回改由主控裁定（本审查不改文件） |
| S-2 | dev 报告 §七 #8/#9 | v4/v5 证据未落盘：.workflow-verify/t-c07/ 仅有 v4/v5 脚本，无对应 result JSON（stdout-only）；「嫩豆腐 category=豆制品」「0 食材行菜=0 道」两个数字不可独立追溯（本审查无 shell 无法重跑；v1 的 categoryDistinct 豆制品=1 可部分交叉印证豆制品行存在） | 实现卡落地方案 A 时重跑 v4/v5 并落盘结果；或主控要求 dev 补落 stdout 留痕 |
| S-3 | 快照方法学 | 零写库证明的窗口注记：before 02:06:05Z / after 02:08:29Z（约 143 秒窗口），v4/v5 在窗口外执行（其只读性由我逐行审读脚本代码确认）；且**计数快照证明「行数不变」，是「数据未变」的必要非充分证据**，二者叠加 SELECT-only 代码审读后零写库结论方成立 | 在 evidence 报告或实现卡中保留此方法学注记即可，无需重做 |
| S-4 | planService.ts L352-370（dev 报告 §六 未覆盖） | **审查方新增观察**：`putExclusions`（F1，v0.2）为事务内 `deleteMany + createMany` 全量替换。方案 A 经 seed 重放落库的 HARD+INGREDIENT 规则，一旦用户经该接口保存一次禁忌列表即被整体清除（连同原 TAG 规则）；反之它也是不经 seed 的备选落地路径（运行时 API 写入）。这是方案 A 影响面/落地路径的真实缺口 | 列入实现卡设计项：①评估 putExclusions 全量替换与 seed 层规则的关系（客户端感知/合并策略/文档口径）；②或改走 API 写入路径；③端到端验收须覆盖「保存后规则仍按预期生效」场景 |
| S-5 | dev 报告 §3.1/§七 | 引用勘误（纯文档，零逻辑影响）：①§3.1「apps/api recommend.ts 调 safetyFilter」路径归属错误，实为 packages/engine/src/recommend.ts L21；②§七零代码改动 bullet「8 个脚本+快照+结果 JSON」与实际 11 个产物（6 脚本+2 快照+3 结果）不符（§九清单正确）；③seed.ts 行号范围 L83-207 与实际 L85-219 略有出入 | 随下次报告修订或由主控注记，不单独返工 |

## 五、总判定

**PASS WITH NOTES**（0 C / 0 T / 5 S）。

理由：任务卡 8 项 AC/升级/边界要求全部真实达成且证据可追溯（AC5 测试复跑部分采信留痕）；五项关键技术结论 a~e 经本审查独立复核全部成立；S-3 观察从静态推断升级为运行时实证的定案链完整（源码精读 + dist 生产代码真实库复现 + 三通道反证）。S-1~S-5 均为建议级：S-4（putExclusions 全量替换语义）是唯一可能影响后续实现卡设计正确性的实质项，必须进入移交清单；其余为文档精度与证据落盘完备性问题。

## 六、移交清单（主控代落盘事项）

1. **本报告全文**代落盘至 `evidence/T-C07-review-2026-09-13.md`（本审查方未写任何文件）。
2. **花生缺口实现卡（方案 A 推荐落地）必备验收/设计项**：
   - a. seed-data.ts 新增 `{scope:'INGREDIENT', targetId:'cmtvnuvxvc2oy5fdrzhpxxe69', severity:'HARD'}` 规则行，**保留**原 TAG 规则；**ExclusionRule 基线 2→3 须在卡内预声明**（v1/v3 类全表扫描脚本重跑时行数口径随之变化）；
   - b. 重放纪律：seed.ts 全 upsert 已核实无 deleteMany，实现卡**禁引入 deleteMany/清库式迁移**；
   - c. **端到端拦截验证列验收项**：真实 INGREDIENT 规则行经 loadExclusionViews join → toExclusionView → safetyFilter 全链，宫保鸡丁（optional=false）与蚝油生菜（optional=true）2/2 拦截；并重跑 v5 留痕确认 checkUnconfirmedIngredients 影响面仍为 0；
   - d. **S-4（putExclusions 全量替换语义）处理方案**须在卡内显式交代；
   - e. 补「食材名≠品类」真实形态 engine 用例（S-1 口径一并修正）；
   - f. RDS 侧落地另立卡（任务卡约束），落地前先做同款三口径扫描。
3. **卡外发现转挂账（本卡未处置，任何人不得顺手修改）**：
   - R-1：category 取值域与 shared 常量漂移（库内存在 category=豆制品 食材，不在 packages/shared/src/constants/index.ts L5 六类内；涉 packages/shared，须主控批准路径）；
   - R-2：内容管线无过敏原校验环节（方案 C 纵深校验可另立管线卡）；
   - R-3：SOFT+TAG 与 checkTagScope 同构口径死（packages/engine/src/score.ts L83-92），未来内脏族食材入库降权同样失效——内脏规则的死是口径性死而非数据巧合。
4. S-2（v4/v5 结果补落）如主控认为必要可单独要求 dev 执行，非阻塞项。

（报告完）
