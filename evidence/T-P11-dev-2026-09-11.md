# T-P11 开发完成报告（fm-dev）

- **任务卡**：[docs/workflow/tasks/T-P11-挂账清理注释对齐与测试稳定性.md](../docs/workflow/tasks/T-P11-挂账清理注释对齐与测试稳定性.md)（队列②③⑨⑩，零行为变更）
- **角色/日期**：fm-dev / 2026-09-11~12
- **性质**：注释级 + 测试级清理，全程零运行时行为变更（AC9 diff 审计见 §3）

---

## §1 AC 逐条自检

| AC | 结果 | 说明与证据 |
|---|---|---|
| AC1 seed-data.ts L50 注释对齐（2026-09-12 补做闭环） | **[✓]** | **补做段（主控勘误补登边界后执行，仅 seed-data.ts L50 一行注释）**：[seed-data.ts:50](file:///d:/codex/family-menu/apps/api/prisma/seed-data.ts#L50)「必消输入按 name+aliases **精确匹配**」→「分层匹配：层 1 精确等值 + 层 2 双向子串唯一命中，命中 ≥2 歧义不猜、未映射原文透传，见 planService.resolveMustUseIds / utils/must-use-matcher.ts」——与 [planService.ts:156-159](file:///d:/codex/family-menu/apps/api/src/services/planService.ts#L156-L159) resolveMustUseIds 注释口径一致（T-P05 方案 E 分层匹配）。首轮核实（2026-09-11）：planService.ts（原边界表内）无过时注释可修，真实位置 seed-data.ts:50 越界，已停手移交主控；主控补登边界后本段补做完成 |
| AC2 projection-order.spec 假绿+容错 | **[✓]** | 选择「显式 skip 且原因/计数可见」方案（未采用无 PG 直接 fail）：无 PG 时 3 条 DB 用例经 `context.skip('PG 不可达…')` 显式跳过（计数 "N passed | M skipped" 始终可见），`beforeAll` 增加 `console.warn` 输出不可达原因与修复命令；`afterAll` 加 `dbReady` 守卫（无 PG 不执行 `deleteMany`，杜绝连接错误成为未处理异常），`$disconnect` 始终执行。文件头注释同步说明可见性依据 |
| AC3 投影断言固化 vitest | **[✓]** | 覆盖度核查结论：loadMenuViews 路径投影/排序断言、toMenuView 纯函数乱序断言**原已存在**；唯一缺口是 **hydrateLockedMenu 懒水合路径无投影断言**——已补 8 行：有图菜 `imageUrl` 含 tp07-spec.jpg / `sourceSite`='xiachufang' / `sourceUrl` 含 xiachufang.com，无图菜三字段 `undefined`（与 toDishView 映射口径一致）。测试名更新为 `...with media projection` |
| AC4 h5 types/index.ts L50 注释 | **[✓]** | `DishSnapshot.imageUrl` 注释块从「当前 API 菜单快照未投影该字段（恒 undefined）」过时描述改为：T-P07 起已投影、T-P08 起 DB 只存 `/images/` 相对路径、做法页经 `toAbsoluteImageUrl` 按 `TARO_APP_API_BASE_URL` 拼基址（历史绝对 URL 原样直用）、失败走 onError 降级；`sourceSite` 行改为「API 已投影，h5 当前未渲染来源徽标，保留待用」。纯注释，类型字段零变更 |
| AC5 契约账本收敛 api.ts | **[✓]** | dish.ts 头部 v0.7 段（5 行）**内容原样**移入 api.ts 头部账本段（v0.2~v0.8 连续单一入口），dish.ts 头部留单行指针「契约版本账本（v0.2~v0.8 连续记录）见 schemas/api.ts 头部（T-P11 账本收敛）」；`DishSchema` 等 schema 定义零变更。移动附来源标注见 §5 披露 4 |
| AC6 api.ts v0.8 措辞如实化 | **[✓]** | 「仅空手且 unmetMustUse 非空时由服务端携带；正常推荐与 C-7a 时缺省。」→「空手时由服务端携带：存在不可消耗必消时为非空分类对象（与 unmetMustUse 同键集）；**C-7a 组合必消空手时为空对象 `{}`**；正常推荐时缺省。」采用方案 a，未动 routes 三元携带，零行为变更（C-1 已补正闭环 2026-09-12，见 §7） |
| AC7 recommend.ts L2 头注释 | **[✓]** | 单行扩为 6 行：输出签名补 `unsatisfiableMustUseReasons`，key=ingredientId（与 unsatisfiableMustUse 同键集）、value='TIME_BUDGET'（器具齐全含该食材的菜单存在但当次时长排不下）/'NO_DISH'（经安全/器具过滤后暂无可用菜单）、空手必非空/正常推荐为空对象——与 [engine/types.ts](file:///d:/codex/family-menu/packages/engine/src/types.ts) `MustUseUnmetReason` 定义及空手注释一致 |
| AC8 回归全绿 | **[✓]** | 全链数字见 §4；shared 改 src 后已先 `pnpm --filter @family-menu/shared build` 重建 dist 再跑回归；teardown 七表回基线 REL=29 |
| AC9 零行为变更审计 | **[✓]** | `git --no-pager diff --numstat`（EXIT=0）+ 5 文件逐文件全量 diff（EXIT=0）逐一核对，结论见 §3；本报告即落盘证据 |

**汇总：9/9 [✓]（AC1 于 2026-09-12 主控补登边界后补做闭环，原 8/9 中唯一 [✗] 已消除）。**

## §2 修改文件清单（本会话）

| 文件 | numstat(+/-) | 改动性质 |
|---|---|---|
| [apps/api/test/projection-order.spec.ts](file:///d:/codex/family-menu/apps/api/test/projection-order.spec.ts) | +31 −10 | 测试级：头注释、beforeAll 容错+warn、afterAll dbReady 守卫、hydrate 投影断言固化（AC2/AC3） |
| [apps/h5/src/types/index.ts](file:///d:/codex/family-menu/apps/h5/src/types/index.ts) | +4 −3 | 仅注释（AC4） |
| [packages/engine/src/recommend.ts](file:///d:/codex/family-menu/packages/engine/src/recommend.ts) | +6 −1 | 仅 L2 文件头注释（AC7） |
| [packages/shared/src/schemas/api.ts](file:///d:/codex/family-menu/packages/shared/src/schemas/api.ts) | +7 −2 | 仅头部注释：v0.7 段移入 + v0.8 措辞（AC5/AC6） |
| [packages/shared/src/schemas/dish.ts](file:///d:/codex/family-menu/packages/shared/src/schemas/dish.ts) | +1 −5 | 仅头部注释：账本单行指针（AC5） |
| [apps/api/prisma/seed-data.ts](file:///d:/codex/family-menu/apps/api/prisma/seed-data.ts) | +1 −1 | 仅 L50 注释（AC1 补做段，2026-09-12） |

- **新增依赖：0**；禁入项（Redis/MQ/微服务/K8s）：未触碰。
- git status 中另有两个 M 文件为**前会话/主控遗留，非本会话改动**：CURRENT.md（+2−2，T-C05 审核通过定稿状态 + 下一步/T-P11 执行中队列更新）、docs/workflow/tasks/T-P10-….md（+1−1，状态行改「已完成…提交 c4301e9」）。

## §3 AC9 零行为变更 diff 审计结论

逐文件全量 diff 核对（`git --no-pager diff -- <file>`，两条命令均 EXIT=0）：

1. **recommend.ts**：全部 −/+ 行均为 `//` 注释行，`import type …` 行仅在上下文出现未变更 → 零行为变更；
2. **api.ts**：全部 −/+ 行均为 `//` 注释行（v0.7 段与 v0.8 段），`import { z } from 'zod'` 及所有 Schema 定义未出现在改动行 → **schema 定义零变更**；
3. **dish.ts**：全部 −/+ 行均为 `//` 注释行 → **schema 定义零变更**；
4. **h5 types/index.ts**：−/+ 行均为 `/** */` 注释内容行，`imageUrl?: string` / `sourceSite?: string` 字段行未变更 → 纯注释；
5. **projection-order.spec.ts**：改动全部位于测试文件（注释、beforeAll/afterAll 容错逻辑、测试标题与断言）→ 测试级，且全链回归（§4）证明不改行为。

**总结论：本会话所有改动仅注释/测试级，无任何运行时行为变更。**

**补做段（2026-09-12）diff 审计**：`git --no-pager diff -- apps/api/prisma/seed-data.ts`（EXIT=0）——仅 1 行 `-`（旧「精确匹配」注释）+ 1 行 `+`（新「分层匹配」注释），`export const ingredients = [` 及其余行均在上下文未变更；`//` 行注释不在任何模板字符串内，不影响 seed 执行。另以 Grep 核对 seed-data.ts 全文：「精确/匹配」仅 L50 一处，无其他过时注释需上报。

## §4 测试与回归证据（命令 + 退出码 + 数字）

| 步骤 | 命令 | 退出码 | 数字 |
|---|---|---|---|
| shared 重建 dist | `pnpm --filter @family-menu/shared build` | 0 | tsc 无错 |
| 根全量测试 | `pnpm test` | 0 | **389/389**（首跑 1 failed 388 passed 为性能抖动，见 §5 披露 1，重跑通过） |
| 禁忌集 | `pnpm test:taboo` | 0 | **89/89（100%）** |
| 端到端 tp01 | `node tools/verify/tp01-*.cjs`（:3000 探测 401 确认 API 存活） | 0 | **54 PASS / 0 FAIL** |
| h5 类型检查 | `pnpm --filter @family-menu/h5 typecheck` | 0 | 0 错 |
| h5 构建 | `pnpm --filter @family-menu/h5 build` | 0 | 构建成功（25 个体积类 warning 为 NutUI 既有库警告，非错误，挂账⑦已有条目） |
| teardown | `node .workflow-verify/tp11/teardown.cjs` | 0 | **14 PASS / 0 FAIL**；删除 Event 10/10、Plan 8/8（窗口行全部 cmtx* 新 cuid）；AFTER：Dish=48 / Menu=13 / MenuDish=42 / Ingredient=79 / Plan=54 / Event=93 / CookLog=6，**REL=29**；tp07-spec 夹具残留 0/0/0 |

**补做段（2026-09-12）验证记录**：

| 步骤 | 命令 | 退出码 | 数字 | 说明 |
|---|---|---|---|---|
| seed 解析验证 | `pnpm exec vitest run apps/api/test/seed.spec.ts`（根目录运行） | 0 | **16/16 passed (1 file)** | seed.spec 直接 import `../prisma/seed-data.js` 做纯 zod 校验（无需 PG），证明 seed-data.ts 可被正常解析导入，注释改动零影响 |
| 仅注释证明 | `git --no-pager diff -- apps/api/prisma/seed-data.ts` | 0 | 1 行 +/1 行 −，均为注释 | 见 §3 补做段 diff 审计 |

- 验证方式说明：apps/api tsconfig `include: ["src"]` 不含 prisma 目录，seed-data.ts 不在 `tsc --noEmit` 范围（首次尝试 `pnpm --filter @family-menu/api exec vitest run test/seed.spec.ts` EXIT=1 系 vitest workspace 根配置 include `apps/*/test/**` 下相对 filter 落空「No test files found」，非测试失败），故按任务卡预案选用「仓库现有 seed 相关测试」作为最小充分验证；纯注释改动不重跑全量回归。

## §5 披露事项与移交

1. **~~【移交主控】AC1 真实位置在边界表外~~（已闭环 2026-09-12）**：过时注释在 [seed-data.ts:50](file:///d:/codex/family-menu/apps/api/prisma/seed-data.ts#L50)「必消输入按 name+aliases 精确匹配，见 planService.resolveMustUseIds」。建议改法（供主控派卡参考）：「必消输入按 name+aliases 分层匹配（层 1 精确等值 + 层 2 双向子串唯一命中，见 planService.resolveMustUseIds / utils/must-use-matcher.ts）」。→ 主控 2026-09-12 勘误补登边界（AC1 补登段 + 边界表新增行）派回补做，已按建议措辞（并补「命中 ≥2 歧义不猜、未映射原文透传」口径）完成，验证见 §3 补做段 diff 审计与 §4 补做段验证记录。
2. **性能测试抖动（与本会话改动无关）**：`score.spec.ts` 「千套菜单 recommend 调用性能 < 50ms（AC12）」首跑 53.639ms 超阈值失败（1 failed | 388 passed），该测试本会话未改动，直接重跑即 389/389——机器负载抖动；挂账队列已有「⑧AC12 并行模式 flaky」条目，维持挂账。
3. **teardown 窗口圈定缺陷被 dry-run 拦截**：初版「createdAt DESC OFFSET=基线数」圈窗会误圈最旧基线行（cmtn*）而留存会话新行（dry-run 实证）；修正为「createdAt > T-P10 teardown 完成时刻阈值（2026-09-11T16:05:00Z，由 tp10v/teardown-result.json mtime 反推）」+ W1 防呆（窗口行数必须等于总数−基线，不等即中止），二次 dry-run 确认 Plan 8/8、Event 10/10 全为 cmtx* 后才实删。
4. **AC5 移动带来源标注**：api.ts v0.7 段内容原样移入，段末追加了来源标注行「（原记于 dish.ts 头部，T-P11 账本收敛移入此单一入口。）」——历史口径文字未改写，仅附加出处说明，如主控认为不宜可在审阅时删除该括注。
5. **PG 服务器时钟偏移 8 小时（非阻塞，披露）**：本机 2026-09-12 01:21 写入的行 createdAt 存为 2026-09-11T09:21:20Z（慢 8h）；窗口判定因此不纯依赖时钟，靠 W1 行数核对 + cuid 前缀交叉验证（新 cmtx* vs 旧基线 cmtn*）。与挂账「CookLog 与 Plan/Event 时区口径不一致（timestamptz 评估）」相关，建议主控并轨评估。
6. **前会话事件存档**：tp01 首跑曾因 PG 未就绪失败一次（重启后通过）；vitest 4 非 TTY 输出可见性实测矩阵（console.* 被抑制、skip 计数始终可见）为 AC2 设计依据，已写入 spec 文件头注释。
7. **teardown 产物**：`.workflow-verify/tp11/teardown.cjs` + `teardown-result.json` 为本次 teardown 执行产物（沿用 tp10v 先例，目录被 .gitignore 忽略，不入库）。
8. **提交**：按卡口径 fm-dev 不执行 git commit，待审版本已固定在工作区，交主控/reviewer 处置。

## §6 未验证项

- CDP/真机侧 UI 回归不在本卡范围（AC2/AC3 的浏览器侧验证属 cjs 回归链，本卡固化为 vitest 后由 tp01/常规 vitest 覆盖）。
- ~~seed-data.ts L50 修正未执行（越界，待主控派卡）~~（已闭环：2026-09-12 主控补登边界后补做完成，见 §1 AC1 与 §4 补做段验证记录）。

## §7 补正记录（2026-09-12，C-1/S-1）

> 主控派发单点补正：fm-reviewer 审查「有条件通过」（0 阻塞/1 条件 C-1/3 建议），S-1 经主控裁量采纳随 C-1 顺手修正。两处均为**纯注释修改，零行为变更**；边界锁定：api.ts 仅 unmetReasons 字段 JSDoc 注释行、dish.ts 仅 L3 指针注释行，其余文件不动。

### 7.1 改动内容与 diff 摘要

| 文件 | 位置 | 改动 | 性质 |
|---|---|---|---|
| [api.ts:200-207](../packages/shared/src/schemas/api.ts)（C-1） | unmetReasons 字段级 JSDoc | −2/+3 | 仅注释：旧措辞「仅在空手且 unmetMustUse 非空时由服务端携带；正常推荐与组合凑不进（C-7a）时缺省。」改为与头部账本段（L19-20）一致的「空手时由服务端携带：存在不可消耗必消时为非空分类对象（与 unmetMustUse 同键集）；C-7a 组合必消空手时为空对象 `{}`；正常推荐时缺省。」；「向后兼容：optional 增量（先例同 v0.3 unmetMustUse）。」一句保留（拆行排版） |
| [dish.ts:3](../packages/shared/src/schemas/dish.ts)（S-1） | 头部指针注释 | −1/+1（行内替换） | 仅注释：删除失实声称「契约版本账本（v0.2~v0.8 连续记录）见 schemas/api.ts 头部」，改为「契约版本账本见 schemas/api.ts 头部（v0.2 起；v0.3 无专行，记于 api.ts unmetMustUse 字段注。T-P11 账本收敛）。」——与 api.ts 账本实况一致（头部仅 v0.2/v0.4/v0.5/v0.6/v0.7/v0.8；v0.3 记于 api.ts L193 `RecommendResponseSchema` JSDoc 的 unmetMustUse 字段注 + L199 行尾注），保持单行、保持指针语义 |

### 7.2 验证记录（命令 + 退出码 + 数字）

| AC | 命令/方法 | 退出码 | 数字/结论 |
|---|---|---|---|
| AC-a | Grep `仅在空手且\|组合凑不进` @ packages/shared/src | — | **0 命中**＝旧措辞零残留；新措辞 Grep 确认落位：api.ts L19-20（头部账本段）+ L204-205（字段 JSDoc），两处句式一致 |
| AC-b | Grep `契约版本账本见\|连续记录` @ packages/shared/src/schemas | — | dish.ts L3 新措辞命中；「连续记录」**0 命中**＝失实声称消除 |
| AC-c | `pnpm --filter @family-menu/shared build` | **0** | `tsc -p tsconfig.json` 无错，dist 已重建 |
| AC-d | `pnpm exec vitest run apps/api/test/projection-order.spec.ts`（根目录） | **0** | **2 passed \| 3 skipped (5)**，Test Files 1 passed (1)；3 条 DB 用例显式 skip（PG 未启动属预期，skip 计数可见，AC2 机制生效） |
| AC-e | `git --no-pager diff -- packages/shared/src/schemas/api.ts packages/shared/src/schemas/dish.ts` + `--numstat` | **0 / 0** | numstat：api.ts **+10/−4**、dish.ts **+1/−5**；逐行核对全部 ± 行均为注释行（`//` 或 JSDoc ` * ` 行），`unmetReasons: z.record(...)`、`import { z } from 'zod';` 等代码行均在上下文未变更——**零 schema/代码变更**。git 输出的 LF→CRLF 行尾 warning 为本机常规提示，不影响 diff 内容与退出码 |

**与 T-P11 原有改动的区分**（叠加态核对）：api.ts 原改 numstat 为 +7 −2（§2），现为 +10/−4，差额 +3/−2 恰为本次补正 hunk（diff `@@ -196,8 +201,9 @@`，即 unmetReasons 字段 JSDoc）；头部账本段 hunk（`@@ -9,10 +9,15 @@`）为 T-P11 原改动（AC5/AC6），本次未触碰。dish.ts 原 +1 −5，现仍 +1/−5，本次为该指针行的行内内容替换（行数不变、内容更新）。

### 7.3 补正段 AC 自检

- **AC-a：[✓]** 字段 JSDoc 新措辞与头部账本段一致，旧措辞零残留（Grep 0 命中）
- **AC-b：[✓]** dish.ts L3 不再声称「连续记录」覆盖 v0.2~v0.8，措辞与 api.ts 账本实况一致
- **AC-c：[✓]** shared build EXIT=0，dist 已重建
- **AC-d：[✓]** 冒烟 2 passed / 3 skipped、EXIT=0（skip 属预期，照实记录）
- **AC-e：[✓]** diff 审计：本次新增改动行全部为注释行，零 schema/代码变更
- **AC-f：[✓]** 本节即补正记录；§1 AC6 行尾已追加「（C-1 已补正闭环 2026-09-12，见 §7）」

- 新增依赖：0；禁入项（Redis/MQ/微服务/K8s）：未触碰；未执行 git commit/git add（待审版本已固定在工作区，提交由主控负责）。
