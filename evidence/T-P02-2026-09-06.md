# T-P02 组合必消凑不进一桌提示 闭环证据（2026-09-06）

> 四角色闭环第二次完整跑通：主控 PM 派发 → fm-dev 开发 → fm-reviewer 静态复审 → fm-verify 真实浏览器验收（Playwright 首次启用）→ 主控收敛。
> 任务卡：[docs/workflow/tasks/T-P02-组合必消空手提示.md](../docs/workflow/tasks/T-P02-组合必消空手提示.md)
> 基线：HEAD `be4f9a3`（分支 feat/tp-06-menu-expansion）；工作区 diff：7 文件 +58/-12（原文见 §6），禁区（packages/shared、packages/engine、packages/list-merger、apps/api、apps/api/prisma）零改动——AC3 ✓

## 1. 闭环记录（三方独立结论）

| 角色 | 结论 | 要点 |
|---|---|---|
| 开发 fm-dev | 交付 | h5 `composeEmptyTitle` 动态量词（这一样/这两样/这三样，mustUse 上限 3）+ 空手卡三分支（C-7 指名 → C-7a 组合 → 通用兜底），tonight/index.tsx +20/-3 行；UI 基准先行：e-final.html 屏⑥变体 B + design-spec.md 屏⑥说明与关键跳转行；契约 v0.6 判定语义 JSDoc 1 行；新增 tests/tp02-cleanup-test-plans.cjs（teardown 快照/清理双模式）；自测 typecheck EXIT=0 + tp01 回归 17 PASS + HTTP 复测 + 测试数据清理（Plan 57→54） |
| 技术审查 fm-reviewer（工具级只读） | **通过** | 逐 AC 4/4 ✓；引擎语义证明：单必消 + unmet 空 不可达（单食材即全集必走 C-7 指名分支），C-7a 分支实际只接 mustUse=2/3；量词「这样」口径无权威出处不构成缺陷；禁区零越界与 e-final 15 屏结构完整性取证；无阻断，5 条建议级（见 §4） |
| 产品验收 fm-verify（本地，Playwright 真实浏览器） | **通过** | 四场景 6 项判定全 PASS（含 S2/S3 追加回屏验证），断言文本逐字命中；consoleErrors=[]；tp01 回归 17 PASS/0 FAIL；h5 build EXIT=0；teardown Plan 61→54 RESIDUAL=0；服务收尾双端口复测关闭；仓库零新增依赖（Playwright 装在 .workflow-verify/ 内） |

## 2. 浏览器级验收证据（fm-verify 实测，Playwright chromium，截图落 `.workflow-verify/tp02-verify/`，判定原文 tp02-verify-result.json 六项 pass=true）

- **S1** 必消「西红柿」→ 进入候选页（hasMenuHeading=true）：S1-before-recommend.png / S1-candidates.png
- **S2** 必消「西红柿 西蓝花」→ C-7a 组合变体逐字命中：
  - 标题「西红柿、西蓝花这两样今晚凑不进同一桌菜」／正文「每样单独都能做，但没有一桌能同时用上它们。」／回改按钮可见／无 C-7 文案（noC7Text=true）：S2-emptycard.png
  - 点「返回修改必消食材」回屏①，chips=西红柿+西蓝花：S2-backtoedit.png
- **S3** 必消「苦瓜」→ C-7 单食材文案不变：「今晚没有能用上「苦瓜」的做法」+ 重试/回改按钮 + 无 C-7a 文案（noC7aText=true）：S3-emptycard.png
- **S4** 无必消 → 正常出菜单（chipsBefore=0，进入候选页）：S4-candidates.png
- 全程 consoleErrors=[]（零报错）

## 3. 构建、回归与 teardown 证据（原文落盘 `.workflow-verify/`）

```
h5-build-exit.txt: H5_BUILD_EXIT=0            # pnpm build，Compiled successfully in 31.10s，25 warnings 非致命
tp01 回归: node tests/tp01-mustuse-regression.cjs → 17 PASS / 0 FAIL，EXIT=0
teardown-clean.log（fm-verify 验收收尾）:
BEFORE Plan=61 Event=100
SAFETY_OK: 7/7 通过 status=PROPOSED + mustUse 精确匹配复核
DELETED Events=7 Plans=7
AFTER Plan=54 Event=93
RESIDUAL_CHECK: 目标 planId 残留=0
```

验收基线 Plan=54 / Event=93（T-P01 清理后的干净态），teardown 后精确回到基线。

## 4. 复审建议级问题（fm-reviewer，无阻断，记录在案）

1. index.tsx:16 注释可补一句引擎语义说明「单必消 + unmet 空 不可达，C-7a 分支实际只接 2/3 样」（无需改运行时）
2. 文档引号小不一致：S3 页面用「苦瓜」，确认书 C-7 原文用『苦瓜』（直角引号），建议文档侧对齐
3. 引擎语义证明：单样 + unmet 空 不可达（正向确认，非缺陷）
4. 量词「这样」（单样）口径无权威出处，复审认定不构成缺陷
5. 第 5 条为低优先级提示，完整清单以 fm-reviewer 复审回报为准

**主控裁决**：本卡不返工；第 1、2 条留待后续文档/注释类小任务或随 E1 卡顺带处理。

## 5. 卡外发现 E1：dev API 无 CORS 处理（未修复，待独立新卡）

- **现象**：OPTIONS 预检落 authHook 返回 401，响应零 CORS 头（acao/acam/allowHeader 均 null）。探针原文（e1-preflight-result.txt，3 条全部 401 + 三头 null）：跨域浏览器（127.0.0.1:10086 / localhost:10086）直调 :3000 被预检阻断
- **影响面**：dev 模式真实浏览器不可用；生产同源部署（menu.jijingkongjian.xin）**不受影响**
- **定性**：T-P02 首次浏览器级验收暴露的**既有缺陷**，非本卡引入；apps/api 属 T-P02 禁改范围，本卡未修复（如实披露）
- **验收方法学披露**：fm-verify 用 Playwright route 拦截代答预检（204 + CORS 头），真实请求经 route.fetch() 原样透传（stats: preflight=0 / proxied=12 / errors=0）；业务语义断言不受挂具影响——S2 走 C-7a 分支、S3 走 C-7 分支的差异化证明成立
- **处置**：待产品负责人裁决开独立新卡（候选改动点：apps/api/src/app.ts 加 @fastify/cors 或预检豁免；属新卡允许范围）

## 6. 环境异常记录：会话中途工作区回滚（已恢复，零丢失）

- **现象**：本卡执行中途，未提交的文档编辑与未跟踪新文件（T-P02 任务卡、C-7a 条目、PD-014 等）从磁盘消失；已提交内容（be4f9a3）完好，fm-dev 自身代码改动保留。fm-dev 开工核验时发现并如实上报，与主控早前 grep 结果矛盾——按 DEC-004「仓库文件优先」以磁盘为准。
- **主控处置**：亲自核验磁盘属实后，以 HEAD 态锚点全量确定性重放（T-P02 任务卡、PRODUCT-CONFIRMATION.md 5 处、PRODUCT-DECISIONS.md PD-014、CURRENT.md 3 处、.gitignore 3 行），逐一 grep 复核恢复成功；本次收敛前再次全量核验（任务卡/代码改动/证据文件/git 状态均在盘）。
- **根因**：未定位（疑 IDE checkpoint / 沙箱层回滚机制）；列为待观察异常，主控此后每轮收敛前加做磁盘核验。
- **git diff --stat 原文**（vs HEAD be4f9a3）：

```
 CURRENT.md                           |  7 ++++---
 PRODUCT-CONFIRMATION.md              | 15 ++++++++++++---
 apps/h5/src/pages/tonight/index.tsx  | 20 +++++++++++++++++---
 apps/h5/src/types/index.ts           |  1 +
 docs/ai-rebuild/PRODUCT-DECISIONS.md |  6 ++++++
 docs/ai-rebuild/ui/design-spec.md    |  4 ++--
 docs/ai-rebuild/ui/e-final.html      | 17 ++++++++++++++++-
 7 files changed, 58 insertions(+), 12 deletions(-)
```

## 7. 未验证项（必填）

- 真机 / 多浏览器（本轮仅 chromium）：未验证
- 生产构建产物（dist）实际运行：未验证（仅 build EXIT=0）
- C-7a「保存/刷新重进」持久化链路（确认书 C-7a 条目内）：未验证
- 脱离 CORS 挂具的真实浏览器直调：不支持（E1 修复前不可行）
- 生产环境：未触碰（本地验收）

## 8. 待产品负责人裁决

1. **T-P02 改动 git 提交与否**（先例：T-P01 用户裁决「提交」→ be4f9a3）
2. **E1 是否开独立新卡修复**（改动点 apps/api/src/app.ts，属 T-P02 禁改范围之外的新卡）
