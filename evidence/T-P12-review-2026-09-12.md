# T-P12 技术审查报告（fm-reviewer 回报 · 主控落盘）

> **落盘说明**：本文档由主控依据 fm-reviewer 2026-09-12 回报整理落盘（reviewer 为工具级只读角色，无写入权限，报告由主控代落盘，先例同 T-P11）。落盘于回报当次会话完成，内容为结构化忠实记录，未经主控改写判定结论。
> 审查对象：T-P12 GET feedback 空态语义修订（404→200+null，契约 v0.9）；待审版本：工作区未提交态，HEAD=`033af65`。

## 一、总判定：通过

7 项审查点全部通过；无阻塞项、无 C 级条件项。S-1~S-4 均为建议级，不构成拒收理由。fm-dev 自检 7/7 AC 与 reviewer 独立静态核实结论一致。行级 diff 与测试数字未由 reviewer 独立复跑（其环境无命令执行工具，采用静态代码审查 + mtime 排序旁证 + 基线连续性核对替代），移交 fm-verify 复跑补证闭环。

## 二、逐审查点结论（7/7 通过）

1. **契约 v0.9 语义正确性**：账本 v0.9 行（api.ts L21-23）措辞与挂账⑤授权及实现逐字对应；FeedbackResponseSchema JSDoc（L169-176）准确；schema 定义零变更核实成立（L177-183 与 T-P11 基线形状一致，dist 产物已随源码重建）；账本单一入口口径延续 T-P11 收敛裁决。
2. **API 行为**：planService.getFeedback 返回类型 `Promise<FeedbackResponse | null>` 与 JSDoc、实现三态一致（plan 不存在 throw L866-868 / 无事件 return null L874-876 / 有事件完整对象 L884-890）；routes/plans.ts null 分支 `reply.code(200).send(null)`（L151-152）序列化精确符合「响应体即 null 字面量」；app.ts L81-101 NotFoundError→404、ZodError→400 映射链未触碰；PlanIdParamsSchema 400 先行。
3. **contract.spec.ts 改写质量**：空态用例（L780-791）三层断言钉死 200 / `body==='null'` 字面量 / `JSON.parse` 为 null，杜绝「200+空对象」弱断言；plan 不存在用例（L793-801）经真实 setErrorHandler 映射 404，本地 NotFoundError class 同源无 instanceof 断裂；全文件 `it(` 计数 53 与 dev 报告一致，旧语义断言零残留。
4. **H5 零行为变更**：client.ts getFeedback 返回类型/第三参 notFoundAsNull=true 均不变，仅 L152 注释更新；request 封装 L34-59 逻辑零变化；h5 其余消费文件正确未触碰（禁改清单）。行级 diff 未独立验证归入 S-1。
5. **tp12-feedback-null.cjs 脚本质量**：真实 HTTP 非 Mock（fetch + access_token cookie），三态断言完整（态1 body 严格 `==='null'`、态2 五键集锁定「有反馈形状零变化」、态3 404）；PG 直查复用 apps/api 既有 pg 依赖非新增；tp12- 前缀 cuid 白名单自建自清 + 七表 before===after 绝对基线断言；DATABASE_URL 日志脱敏、无硬编码凭据。
6. **边界合规**：改动集恰为任务卡允许清单（5 文件 + tp12 脚本 + evidence 报告 + shared dist 重建产物），engine/prisma/h5 其余文件/产品文档无越界迹象；新增依赖 0。
7. **回归真实性抽查**：根 vitest 串行 390/390 = T-P11 基线 389 + 净增 1 用例，算术自洽；contract 53/53、taboo 89/89、tp01 54 PASS、七表终态 48/13/42/79/54/93/6 与 CURRENT.md T-P11 基线连续一致；dev 报告披露诚实（AC12 并行 flaky 如实标注）。测试数字未独立复跑（系统性未验证边界，移交 verify）。

## 三、建议级问题（S-1~S-4）

- **S-1 行级 diff 与测试数字未独立复跑**（reviewer 工具限制）：由 fm-verify 复跑补证（清单见移交建议）。
- **S-2 .workflow-verify/tp12/ 缺清理脚本运行输出日志**：verify 复跑时留存输出即可；七表绝对基线断言可复现，不构成事实疑点。
- **S-3 挂账编号口径冲突**：T-P11/T-P12 dev 报告把 AC12 flaky 称「挂账⑧」，CURRENT.md 权威队列 ⑥=AC12 flaky、⑧=imageUrl regex 收紧。
  - **主控裁量（2026-09-12）**：权威编号以 CURRENT.md 为准（⑥=AC12 flaky，⑧=imageUrl regex 收紧）；dev 报告历史文件不改，此处勘误记录在案，后续任务按 CURRENT.md 编号摘取。
- **S-4 h5 页面注释过时**：feedback/index.tsx L73、history/index.tsx L71 注释仍述 404 语义（行为无影响；该两文件本卡禁改，dev 未改是正确遵守边界）：挂账后续允许触碰 h5 的任务顺带更新，不单开卡。

## 四、条目级结论（C 项）

无。契约授权链完整（CURRENT.md 挂账⑤ + 2026-09-11 用户总授权），无需回溯批准。

## 五、reviewer 移交建议（原话口径）

**可直接转 fm-verify**，无需先补正。建议 fm-verify 复跑清单（对应 S-1/S-2）：

1. `git diff` 行级核对：改动恰为 api.ts、planService.ts、plans.ts、contract.spec.ts、client.ts（仅注释）+ 新增 tp12 脚本与 evidence 报告；
2. shared build EXIT=0；contract.spec 53/53；tests/tp12-feedback-null.cjs 18 PASS（留存输出日志补 S-2）；tp01 54 PASS；
3. 根 vitest 串行 390/390（按 dev 披露以串行模式跑，规避已挂账的 AC12 并行 flaky）；`pnpm test:taboo` 89/89；
4. h5 typecheck 0 错；终态七表基线 48/13/42/79/54/93/6 与 CURRENT.md T-P11 记录核对；
5. 54329 端口=0 残留监听。

复核结论若与上述一致，本卡可判定通过并按流程停「待用户审核」。

## 六、主控裁量（2026-09-12）

- reviewer 移交口径「可直接转 fm-verify，无需先补正」**采纳**，省一轮 dev 补正。
- S-3 编号勘误按第三节主控裁量执行；S-2 由 verify 复跑时留存日志；S-4 挂账随 ⑥⑦ 卡顺带（记入 CURRENT.md 下一步段）。
