# R-9「DRAFT 内容 HTTP 只读口」独立验收报告（fm-verify）

> **主控落盘注记（2026-09-13）**：本报告由主控代落盘（fm-verify 报告全文文本返回，先例延续）。S-V1 计数口径更正：dev/review 报告「16 字段」实为 17 项，实质三方逐字段对齐成立，本报告第③节偏差 1 为准；不影响验收结论。

---

- 验收对象：分支 feat/tp-06-menu-expansion，HEAD 实测 `1403b8ee903a91489896df8249bab2b22577df93`（工作区未提交改动即验收对象）
- 验收方式：真实命令复跑（退出码+数字实录）+ 逐文件读码对证据链；未修改任何仓库文件、未执行任何写库操作（临时只读脚本除外，已删）
- 必读材料：任务卡/dev 报告（含 T1 返工记录）/review 报告（含主控补记 4 条）开工前已完整读取

---

## ① 总体判定：**PASS**

| AC | 判定 | 核验要点 |
|---|---|---|
| AC1 列表端点 | [✓] | 白名单/多值去重保序 trim/非法 400/缺省全量/z.array(DishSchema)/orderBy id asc/无分页，全部代码与运行时双证实 |
| AC2 详情端点 | [✓] | join 8 字段 view/DishSchema.extend/404/T1 licenseNote 三处投影+断言，运行时实录命中 |
| AC3 origin 投影 | [✓] | FETCHED 实值直达 zod 不被 default 吞，实测 30 行逐行与 DB 一致 |
| AC4 鉴权只读 | [✓] | 全局 authHook 覆盖 + 401 实测 ×2；dishesService 零写库 |
| AC5 测试回归 | [✓] | 442/442、95/95、build EXIT=0、dishes 10/10，均为本次独立实跑 |
| AC6 约束零侵入 | [✓] | 禁区零 diff、zod ^4.4.3 三处同版本 lock 单实例、零 LLM API |

问题分级：**C=0 / T=0 / S=1**（见第④节，不阻塞合并）。

---

## ② A~E 逐项验收记录

### A. 运行时复跑（全部真实执行）

| # | 命令 | 结果 | 退出码 |
|---|---|---|---|
| A1 | 根目录 `pnpm test` | **Test Files 19 passed (19) / Tests 442 passed (442)**，Duration 7.13s | **EXIT=0** |
| A2 | 根目录 `pnpm test:taboo` | **Test Files 5 passed (5) / Tests 95 passed (95)**，Duration 763ms | **EXIT=0** |
| A3 | apps/api `pnpm build` | `prisma generate` → ✔ Generated Prisma Client (7.9.1) + `tsc -p tsconfig.json` 无错误 | **EXIT=0** |
| A4 | dishes.spec 单文件复跑 | **Test Files 1 passed (1) / Tests 10 passed (10)**（376ms），用例 a~j 全绿 | **EXIT=0** |

A4 口径说明：先按验收指令在 **apps/api 目录**执行 `pnpm vitest run dishes` → **EXIT=1（No test files found）**——根 vitest 配置 include 为 workspace 相对模式（`apps/*/test/**/*.spec.ts`），root 设为 apps/api 时无法匹配，属命令执行目录口径问题而非产品缺陷；改用与 dev T1 复验一致的**根目录等价口径** `pnpm vitest run dishes` → EXIT=0。

### A5 项：dishes.spec 运行输出关键实录（逐项确认实际生效）

- **FETCHED 行数**：`[dishes] origin=FETCHED 实测行数：30（基线卡口径 29，以 DB 实测为准）`——实测 30，非空且 >0 断言生效（dishes.spec.ts L302-305）
- **licenseNote 非空硬断言（T1）**：`[dishes] FETCHED 行 licenseNote 实值抽查：cmtvk1w3v0000rwpgkoxz982k -> FETCHED 自xiaohongshu笔记 6a8db1e5000000002`——null 跳过分支未触发，硬断言实际生效，抽查值与 dev T1 报告自述一致
- **幂等验证**：`[dishes] 存量全量 DishSchema.parse 幂等验证：49/49 全过`——49/49 零畸形
- **rm9- 清理**：afterAll 双重断言（rm9- 残留 0 + Dish 总数回基线）随 10 用例全绿生效，C 项独立复查进一步实锤（见下）
- **HTTP 状态码日志**：401×2（列表/详情无 cookie）、200×多、400×4（FOO / DRAFT,FOO / 空串 / 尾随逗号）、404（rm9-not-exist）全部实测在案

### B. 边界核验

- `git rev-parse HEAD` → `1403b8ee903a91489896df8249bab2b22577df93` ✓
- `git diff --stat 1403b8e` → **4 files changed, 35 insertions(+), 3 deletions(-)**：apps/api/package.json（3 行）/ apps/api/src/app.ts（7 行）/ apps/api/src/services/mappers.ts（23 行）/ pnpm-lock.yaml（5 行）——与主控补记第 1 条逐字节一致
- `git status --short`：M 为上述 3 文件 + pnpm-lock；未跟踪中 R-9 产物仅 `routes/dishes.ts`、`services/dishesService.ts`、`test/dishes.spec.ts`、`evidence/R-9-*.md`×3；其余未跟踪（.pai/、docs/design/、docs/ui-redesign/、tests/*.mjs/cjs、tools/*.mjs/*.tar.gz、复盘报告.md）与 dev 报告「卡外既有未跟踪文件，非本卡产物」一致
- **禁区零改动**：packages/shared、apps/h5、tools/、packages/engine、packages/list-merger、apps/api/prisma 在 diff 与 status 中均零出现 ✓

### C. rm9- 零残留复查（临时只读脚本，用完已删）

- 脚本链路与 db.ts L20-31 一致（根 .env dotenv + @prisma/adapter-pg + pg Pool），仅执行 count 只读查询
- 实测输出：`{"rm9Dish":0,"dishTotal":49,"fetchedTotal":30,"exclusionRules":3,"ingredients":81}`，**EXIT=0**——rm9- 零残留实锤，Dish 总数回基线 49、FETCHED=30、ExclusionRule 三行与 seed 数据零触碰，与主控补记第 3 条完全一致
- 临时脚本 `fm-verify-tmp-r9check.mts` 已删除（Test-Path=False），apps/api 目录 `git status --short` 终态复查：仅 3 M + 3 新增，**零验收残留**

### D. AC1~AC6 逐条独立核验（读码对证据链，不轻信自述）

- **AC1** [✓]：dishes.ts L20 `z.array(DishSchema)` 本地组合不改 shared；L51-78 查询 transform——`split(',')` 逐段 `trim`（L62）、`Set` 去重保序（L59-75）、逐段 `ContentStatusSchema.safeParse`（shared dish.ts L15 值域 DRAFT/TESTED/PUBLISHED），非法段 `ctx.addIssue` → 路由层 400（L84-87）；缺省 undefined 直通 service 无过滤；dishesService.ts L23 `orderBy id asc`（schema.prisma Dish 无 createdAt/updatedAt，口径成立）；app.ts L126 注册 + L119-122 注释更新为 17 条实数口径并注明 R-9；无分页 ✓
- **AC2** [✓]：dishesService.ts L34-41 findUnique + include ingredients.ingredient，无行抛 NotFoundError → app.ts L84-86 映射 404（实测命中）；dishes.ts L27-36 8 字段 view；L39-41 `DishSchema.extend` 本地组合。**T1 专项**：mappers.ts L62-64（DishRow.licenseNote `string | null`）/ L165（DishMediaView `licenseNote?: string` 连带声明）/ L195-196（`row.licenseNote ?? undefined`，null→undefined 对齐 imageUrl 口径）；spec L216 兼容断言 + L219-242 FETCHED 非空硬断言（含库内全 null 时如实报告分支）实测生效。全字段三方对照见第③节偏差 1（实数 17 项，逐一对齐无遗漏）
- **AC3** [✓]：mappers.ts L57 DishRow.origin 非空 `string`（schema.prisma L102 `origin ContentOrigin @default(LLM_DRAFT)` 非空列）→ L190 无条件投影 → DishMediaView.origin 必填（L171）→ 响应对象内 origin 恒有实值，shared dish.ts L51 `.default('LLM_DRAFT')` 仅字段缺失时兜底，**FETCHED 实值不可能被吞**——spec 用例 i 全列表逐行 API origin === DB 实值 + FETCHED id 集合相等 + 动态 baselineFetched 断言，实测 30 行通过
- **AC4** [✓]：app.ts L72 实例级 `addHook('preHandler', authHook)`，注册于 /images 静态（L79）与全部路由（L123-126）之前，Fastify 封装模型下子插件继承 → /api/dishes 覆盖；L29 `/health` 前缀豁免未动；spec 用例 a 401×2 实测（HTTP 日志 req-1/req-2 statusCode 401）。零写库：dishesService 全文 43 行仅 findMany/findUnique，routes 层无写库路径
- **AC5** [✓]：见 A 项实测数字（442/442、95/95、build EXIT=0、dishes 10/10）
- **AC6** [✓]：禁区零 diff（B 项）；不做 H5 UI/写口/分页（dishes.ts 仅两个 GET，无 PATCH/POST）；零 LLM API（4 个改动源文件 + spec 逐行读过，无任何网络调用）；**T2 留痕独立复核**：zod `^4.4.3` 三处同版本（apps/api package.json L26 / packages/shared package.json L20 / tools/content-pipeline L39），pnpm-lock packages 区唯一真实包 `zod@4.4.3`（L8317）、snapshots 区唯一 `zod@4.4.3: {}`（L17030），无 3.x 或其他 4.x 实例，openai peer 已解析 4.4.3——单实例无漂移

### E. 测试真实性

[✓] 无 Mock 冒充：spec 走 `buildApp() + inject`（真实 Fastify 生命周期）+ 真实 Prisma 客户端（db.ts L20-21 dotenv 从根 .env 加载 → 本地 PG 54329/family_menu，本次 A1/A4/C 三轮真实连接成功即为 PG 在线证据）；dishes.spec.ts L39-51 `SELECT 1` gate，PG 不可达时逐用例显式 skip 计数可见、不假绿；断言强度充分（DB 实值逐字段对照、集合相等、零残留硬断言）；唯一写库语句为 rm9- 造行 createMany 与 startsWith('rm9-') 清理，物理隔离不可能波及存量（腊肉用户行 ex-1788658316681 全程未触碰）。

---

## ③ 与 dev/review 报告数字的偏差

1. **[S-V1，数字口径微差] 「16 字段」实为 17 项**：dev/review 报告写「DishSchema 16 数据字段 = schema.prisma Dish 16 数据列 = toDishView 16 项投影」，逐一点名实测：shared dish.ts L38-60 为 17 键（id/name/mealRole/cuisine/flavorTags/spicyLevel/splitFlavor/activeMinutes/totalMinutes/equipment/steps/status/origin/imageUrl/sourceUrl/sourceSite/licenseNote），schema.prisma Dish L89-110 同为 17 数据列，toDishView 为 17 数据字段投影 + ingredients（join 项）。**两份报告自身的枚举列表也是 17 项但自称 16**——实质三方逐字段一一对应、无遗漏无多余成立，纯计数口径笔误，不阻塞合并（主控已在落盘注记中更正）。
2. **[留痕] A4 命令目录口径**：验收指令口径「apps/api 目录 `pnpm vitest run dishes`」实测 EXIT=1（No test files found）；根目录等价口径 EXIT=0。与 dev T1 复验口径（根目录）一致，不构成偏差立案，如实留痕。
3. **其余全部数字与 dev/review/主控补记一致**：442/442、95/95、build EXIT=0、dishes 10/10、diff 35+/3- 四文件、`{rm9Dish:0, dishTotal:49, fetchedTotal:30, exclusionRules:3, ingredients:81}`、licenseNote 抽查行与文案（cmtvk1w3v0000rwpgkoxz982k → 前缀 40 字符脱敏输出一致）、FETCHED=30。

## ④ 发现问题分级

- **C 级：0 条**
- **T 级：0 条**
- **S 级：1 条新增**——S-V1（即第③节偏差 1）：dev/review 报告「16 字段」计数口径与实数 17 不符，实质对齐成立，不阻塞合并，主控落盘注记已更正。既有备案（test 不入 typecheck、卡面 29/30 口径）本轮不复述、不重复计数。

## ⑤ 未验证项声明

1. 未做浏览器/人工 HTTP 手测（未启动服务手点页面）——沿用本卡与 dev/review 口径，端点行为由 inject 集成测试全量覆盖 + HTTP 状态码日志实录佐证；属范围口径内，非缺陷。
2. engine `DishIngredientView` 类型文件未逐行直读——以 spec L248-259 的 8 键集精确断言 + DB join 实值逐字段对照（实测通过）作为运行时证据，review 已静态核过与 dishes.ts L27-36 一致。
3. pnpm-lock snapshots 区 @types/node peer 元数据逐字节 diff 未重复复核——主控补记第 2 条已定谳（纯类型定义，apps/api 自身仍绑 22.20.1，无运行时影响）。
4. H5/前端消费侧不在本卡范围（卡面明确不做 UI），未验证。

## ⑥ 环境状态确认

- 本地 PG 17.5 @ 127.0.0.1:54329/family_menu **在线**（A1/A4/C 三轮真实连接成功）；未停止、未重启
- 临时只读脚本已删除，`git status --short -- apps/api/` 终态仅 3 M + 3 新增，**工作区零验收残留**；`git diff --stat` 验收前后一致（35+/3-）
- 全程未执行任何写库/DROP/清理类命令；未触碰 UAT/生产数据与 RDS；未动 `$env:TEMP\fm-rds.env`；未杀 9222 CDP 浏览器；腊肉用户行 ex-1788658316681 零触碰
- 未修改任何仓库文件（验收期间创建的唯一文件为临时脚本，已删；本报告全文文本返回，未落盘）

**结论：R-9 全部 AC 验收通过（PASS，S 备注一条不阻塞），四角色闭环（dev→review→verify）的运行时证据链已由本轮独立复跑补齐，可交主控进入合并流程。**
