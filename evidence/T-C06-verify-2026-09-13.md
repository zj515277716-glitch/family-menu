# T-C06 产品验收报告（fm-verify → 主控代落盘）

> 验收：fm-verify｜日期：2026-09-13｜总判定：**PASS**（6/6 项通过，0 [✗]，附 2 条 S 级观察与移交意见）
> 被测版本：工作区当前态（含 batch-fetch.mjs 1 行、apply-ingredients.mjs 4 行未提交改动）+ 本地 PG 17.5 @ 127.0.0.1:54329/family_menu。本卡为内容数据入库卡（无 UI 改动），无独立 UI 版本号。
> 方法学声明：全部命令真实执行，退出码与数字如实记录。API 服务未运行（连接被拒，实测），按边界约束采用只读脚本直查库方式完成业务可用性核验，未启动任何服务、未写库、未改任何仓库文件（验证脚本仅存于 $env:TEMP，用后删除）。

## 1. 数据终态核验 — [✓]

- 操作：`node tools/content-pipeline/out/xhs/tc06-snapshot.mjs`（变量捕获落盘 220 行至 $env:TEMP\tc06-verify-snapshot.json 留证）。
- 预期：Dish=49 / Ingredient=81 / DishIngredient=375 / Plan=54 / Event=93 / CookLog=6 / Menu=13 / MenuDish=42；FETCHED/DRAFT 30 道 268 行；蚝油菜 rows=10。
- 实际（exit 0）：dishTotal=49、ingredient=81、dishIngredient=375、plan=54、event=93、cookLog=6、menu=13、menuDish=42 逐字一致；dishByOriginStatus FETCHED/DRAFT n=30；fetchedDishCount=30、fetchedDishIngredientRows=268；蚝油菜 cmtz3kdhp000070pga77gl5wt rows=10；allRowsMd5=`85afe82dc24336a5eb7a54376ec58a94`（与开发报告 §9/§15 终态逐字一致）。
- 覆盖边界：只核计数与行集 md5；md5 行集口径已覆盖 ingredientId|qty|unit|optional|ing_name。

## 2. 业务可用性核验 — [✓]

- 操作：隔离只读脚本 PG SELECT（TEMP 内，用后删除）+ 知识库文件静态核读。
- Dish 记录：name=「蚝油生菜原来不炒更好吃，脆嫩还少油」、mealRole=MAIN、cuisine=家常、flavorTags=[家常]、spicyLevel=0、activeMinutes=15、totalMinutes=30、equipment=[wok]、steps=17 步、status=DRAFT、origin=FETCHED、sourceSite=xiaohongshu——全部完整非空。口径澄清：契约 dish.ts 无 difficulty 字段，以 activeMinutes/totalMinutes/spicyLevel 等契约字段替代核验。
- imageUrl=`/images/dishes/6a4a209d000000002101b02a/0.webp`，regex 校验 imageUrlRegexPass=true（v0.10 站内相对路径分支）。
- DishIngredient 关联 10 条，与知识库逐项一致（生菜 400g 主料、大蒜 20g、蚝油 30ml、生抽 8ml、玉米淀粉 12g、清水 125ml、盐 2g、食用油 10ml、小米辣 10g 可选、花生米 10g 可选）；新建食材「生菜」（cmtz3tf4q31th5sycx87vtkra）、「小米辣」（cmtz3tf4zcm7y4dkaypoi9nz9）均在关联中；Ingredient 总数=81。
- 数据链形态：allQtyPositive=true、allUnitNonEmpty=true、categories∈{蔬菜,调料}（合法 CATEGORIES 集合）、optionalCount=2；ingredients-draft.json L515-530 role 全部∈{主料,调料}、uncertain=false。
- 覆盖边界：API/H5 页面级真实交互未做（服务未运行且本卡无 UI 改动）；imageUrl 仅核 regex 与文件存在性，未做 HTTP 图片渲染验证。

## 3. 既有 29 道零漂移 — [✓]

- 操作：实读 tc06-drift-check.json；重跑 `node tools/content-pipeline/out/xhs/tc06-drift-check.mjs`（exit 0，输出 `DRIFT CHECK PASSED: 29/29 unchanged, 1 added`）；程序化比对 drift 基线 29 项与实时查库快照逐菜 md5/rows。
- 实际：unchanged=29 / changed=0 / added=1 / missing=0；unchangedDetail 29 项与实时快照零失配（MISMATCH_COUNT=0），29 道行数合计=258；蚝油菜 live 与 drift 记录一致（rows=10 / md5=1743849ad21ed4c0d66b852b088db1e6）。抽查三道：红烧肉合集 rows=8/md5=bc0b074afad282cf3afc0942fd2bff6c、6道巨下饭家常菜 rows=14/md5=ccdbd54e1d59d82a8c5f85a94e55682e（dev 报告已更正首次笔误「鱼香肉丝」）、茄子 6 种做法 rows=10/md5=a0c7390460d8faead7e4f6eb8a088259——与 §4 基线逐一一致。
- 覆盖边界：md5 口径为 ingredientId|qty|unit|optional|ing_name 排序行集（不含 di.id）；按脚本设计此口径下任何内容级改动均会失配。

## 4. 基线不回滚 — [✓]

- 操作：drift-check.json countsPre（卡前基线）与实时快照 counts（终态）比对 + 新蚝油菜残留查询。
- 实际：卡前 Plan=54/Event=93/CookLog=6/Menu=13/MenuDish=42 与实时终态逐字一致（五项零漂移）；Dish 48→49、Ingredient 79→81、DishIngredient 365→375（+10）为本卡预期终态未回滚；蚝油菜 MenuDish=0、CookLog=0（无 Plan/Event 残留渠道，Plan 经 candidates JSON 快照引用无外键表）。
- 覆盖边界：Plan.candidates JSON 内可能存在的菜名快照未逐条解析（表计数不变 + MenuDish/CookLog 零关联已构成无新残留的充分证据）。

## 5. 回归测试 — [✓]

- 操作：`pnpm --filter @family-menu/content-pipeline test 2>&1 | Select-Object -Last 25`（真实执行）。
- 实际：exit 0，Test Files 15 passed (15)，Tests 391 passed (391)，Duration 5.09s。覆盖面如实说明：实际 vitest 按 --root 仓库根执行，范围超出 content-pipeline 单包，含 shared/schemas 68、engine/taboo 18、engine 全部 38、list-merger 38、api/contract 53、api/seed 16、api/matcher 17 及内容管线 fetch2dish 35/import 29/draft 14/coverage 17/menu-assemble 10。套件不含 batch-fetch.mjs（CDP 编排）与 apply-ingredients.mjs（真实 PG 写入）直接单测（与开发报告披露一致，两脚本本卡以真实数据终态验证替代）。

## 6. 图片资产核验 — [✓]

- 操作：PowerShell 实测 static 目录 + 解析 fetch.json 三方比对。
- 实际（exit 0）：DIR_EXISTS=True；0.webp=140120、1.webp=153824、2.webp=89836 字节——与预期逐字节精确一致；fetch.json downloadedImages 3 条 bytes 与磁盘三方吻合；imageUrl 目录名与 noteId=6a4a209d000000002101b02a 一致；fetch.json title 与入库 name 一致、desc 642 字符非空、imageUrls=6（imageCount 口径）。

## 7. 补充复核：AC1 代码改动形态 — [✓]

- 操作：`git diff --numstat` + `git status --short`（只读，exit 0）。
- 实际：apply-ingredients.mjs 4/4 行（L69/L119 判断值 + L70/L120 文案成对，符合主控修订后「恰 4 行」约束）、batch-fetch.mjs 1/1 行；工作区仅此 2 个 M 文件，untracked 均为仓库既有；out/xhs/ 证据产物不入 git（.gitignore 内）。

## 总判定：PASS

6/6 验收项全部通过，AC1~AC6 交叉印证成立，数据账目三方闭环（258+10=268、79+2=81、365+10=375），T-C04 挂账「29/30 唯一失败项」补采闭环确认达成。边界约束全程遵守：零写库、零代码修改、未停 PG 54329、未杀 9222、$env:TEMP\fm-rds.env 未触碰（Test-Path=True）。

## 移交意见（2 条，均不阻断）

1. [S 级观察] 证据文件瑕疵：tc06-drift-check.json 末尾混入一行非 JSON 文本「DRIFT CHECK PASSED: ...」（落盘时含 stdout 尾行），标准 JSON 解析器直接读取会报错（脚本自身判定逻辑不受影响）。建议随下卡顺手剥离该行，或忽略。
2. [支持技术审查 S-3 立项] VERIFY 侧实证：蚝油菜含「花生米」DishIngredient 行（ingredientId=cmtvnuvxvc2oy5fdrzhpxxe69、category=调料、optional=true、qty=10g），与宫保鸡丁共同构成花生过敏暴露面，印证技术审查 S-3（TAG 过滤口径缺口）——涉「安全过滤>一切评分」铁律，建议主控优先单独立项评估。

## 覆盖边界总说明

本验收未做 H5/API 页面级真实交互（服务未运行，本卡无 UI 改动）；蚝油菜 status=DRAFT，按契约仅 PUBLISHED 进入推荐池，故其推荐可见性属后续人工审改升 TESTED/PUBLISHED 后的范畴，不在本卡验收内；imageUrl 的 http(s) 远程分支与图片浏览器渲染未验证（落库为站内相对路径分支）。
