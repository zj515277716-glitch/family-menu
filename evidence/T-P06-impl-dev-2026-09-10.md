# T-P06 做法详情页图文改版实施——开发完成报告（方向 A 沉浸大图流）

> 执行角色：fm-dev ｜ 日期：2026-09-10 ｜ 任务卡：docs/workflow/tasks/T-P06-做法详情页改版实施.md
> UI 基准：docs/ai-rebuild/ui/dish-detail-vA.html ｜ 验收口径：PRODUCT-CONFIRMATION.md C-4（PD-016 修订版）
> 约束遵守：packages/shared 与 apps/api 零改动；运行时零 LLM API；零新增依赖；无 Redis/消息队列/微服务/K8s。

---

## 1. 结论一句话

vA 方向稿已完整落进真实 dish 详情页并全部真实浏览器实证：全出血大图开场（图上叠菜名+时长/菜系，本环境 API 未投影 imageUrl 故全部按契约降级为「实拍位」色块）+「‹ 上一道｜下一道 ›」双通道换道导航（图侧箭头+页尾双卡，按备菜顺序，无上下文自动隐藏）+ 主料/调料分组用料（沿用人数换算）+ 笔记式步骤流 + 底部悬浮胶囊「做完饭，记一笔」三态防抖（idle/loading/done，R-3）。typecheck 0 错、h5 build EXIT=0；CDP 真机判定 flow 27/29（2 个 FAIL 经复现实验证实为测试前提错误而非页面缺陷，见 §5）+ 直进场景 6/6 + hero 渲染路径 7/7 + dock 恢复时间线闭环。teardown 精确还原，报告落盘即本文件。

## 2. 改动文件清单（git diff --stat，相对 HEAD，共 5 文件 704+/101-）

| 文件 | 变更量 | 说明 |
|---|---|---|
| apps/h5/src/pages/dish/index.tsx | 264 ± | 按 vA 重写：hero 大图/无图降级/图侧箭头/页尾换道/分组用料/待补齐态/笔记步骤/悬浮胶囊三态（redirectTo 换道防栈增长；useDidShow 回查反馈回显 done） |
| apps/h5/src/pages/dish/index.css | 378 ± | vA 全量样式（750 稿口径与 app.css token 对齐；hero 680 CSS=340 视觉；胶囊 safe-area） |
| apps/h5/src/types/index.ts | 18 ± | **前端类型增量单列**：DishSnapshot 追加 imageUrl?/sourceSite?/ingredients?（category/optional）/steps?。理由：对齐 shared 契约字段与 API 菜单快照实际返回，全部可选、字段缺省自动走既有分支；非契约变更（packages/shared 未动） |
| docs/ai-rebuild/ui/e-final.html | 137 ± | 仅屏⑪区块按 vA 重写（方向 A 专属样式 + 三态演示 JS + TabBar 隐藏），其余屏未触碰 |
| docs/ai-rebuild/ui/design-spec.md | 8 ± | 组件表追加沉浸大图区/无图降级/换道导航/分组用料/悬浮胶囊；s11 页面状态与图像规则同步 |

git status 对账：本卡改动恰为上述 5 文件；`packages/**`、`apps/api/**` 零改动；其余工作区 M/?? 条目（PRODUCT-CONFIRMATION.md、T-C03/T-C05 任务文档、tests/*、tools/*.tar.gz 等）均为本任务开始前既有，未触碰。

## 3. AC 自检表

| AC | 内容 | 自检 | 证据 |
|---|---|---|---|
| AC1 | e-final 屏⑪+design-spec 按 vA 更新 | [✓] | §2 后两行；屏⑪为唯一改动区块 |
| AC2 | 首屏大图区（含无图降级） | [✓] | 实测降级：A1 heroEmpty=true/heroImg=false、A2 降级块菜名=当前菜、A3「试做实拍位」角标注明契约缺省降级；**有图渲染路径**以披露的 CDP Fetch 注入法实证（§5.4）：hero 渲染真 `<img>` 且 complete=true、全出血 375×340、图上叠菜名+6 chips+渐变遮罩（I1–I7，7/7） |
| AC3 | 换道导航（上下文探测，直进隐藏） | [✓] | 菜单上下文内：A9–A11（第一道无上一道、下一道=凉拌黄瓜·5分）、B1–B5（页尾卡+图侧箭头双向换道，菜名/时长随动）；直进：G1（同会话直进→persist 注水（C-3a 设计）正常渲染）、H1–H5（全新会话直进→空态页、switcher/dock/hero 全不渲染、有「回今晚」出口） |
| AC4 | 分组用料+「用料待补齐」态 | [✓] | A5/A6：4 人份换算、主料/调料双分组（5 项，groupLabels=["主料","调料"]）、可选标代码就绪；**待补齐态如实说明**：本轮推荐菜单三道菜 ingredients 均非空，真实数据未触发该态——态渲染代码就绪且在 e-final 屏⑪静态演示，专拍需构造 0 用料菜入菜单（§7.4） |
| AC5 | 笔记式图文步骤 | [✓] | A7：steps=5、序号从 1；第二道 steps=2；步骤契约无图字段→本版纯文字流（卡内既定），嵌图路径留待 API 投影后扩展 |
| AC6 | 操作条三态防抖（R-3） | [✓] | idle：A8（做完饭，记一笔，无 done/loading 类）；loading：C1（点击后 cls=`dish-dock-btn loading`、文案「正在记…」，截图 s5a）；done：E1（返回本页自动回查 getFeedback→「记好了 ✓」cls 含 done）+ flow6 时间线（0.5s 翻转 done 并稳定 6.5s）；防重复：E2 done 态再点击 hash 不变、态不变 |
| AC7 | 契约与 API 零改动 | [✓] | §2 git 对账；types 增量仅前端 |
| AC8 | build EXIT=0+真实浏览器验证+teardown+报告落盘 | [✓] | typecheck 0 错、`pnpm --filter h5 build` EXIT=0（t8）；浏览器验证 §4–§5；teardown 对账 §6；即本报告 |

## 4. 真实浏览器验证（CDP 9222 Edge 复用，自建 tab 用毕即关，全程未杀浏览器）

判定脚本四份（已按 teardown 约定删除，判定原文存 `.workflow-verify/t-p06/verify-result*.json`）：

| 脚本 | 退出码 | 判定 | 覆盖 |
|---|---|---|---|
| flow.cjs（主流程） | 2 | 27/29 PASS | tonight→推荐→候选→锁定→菜单→做法页全链路 + 降级 hero/换道/分组用料/三态/防重复 |
| flow2.cjs（直进两态） | 0 | 6/6 PASS | 同会话直进（persist 注水）+ 清 storage 全新会话直进（空态） |
| flow6.cjs（dock 恢复时间线） | 0 | 时间线闭环 | 冷启动直进 0.0s idle→0.5s done 稳定至 6.5s，页内 fetch 反馈接口=200 |
| heroimg.cjs（hero 渲染路径） | 0 | 7/7 PASS | Fetch 拦截注入 imageUrl→hero 真图渲染（I1–I7） |

关键判定摘录（CHECK|id|结果|实际值，全文在 verify-result.json）：

```
CHECK|A1-hero-degrade|PASS|heroEmpty=true heroImg=false（API 菜单快照未投影 imageUrl，按契约缺省降级）
CHECK|A6-ing-groups|PASS|groups=["主料","调料"] ingCount=5 optTags=0
CHECK|A9-switcher-visible|PASS|swPrev=— swNext=凉拌黄瓜 · 5 分 sidePrev=false sideNext=true
CHECK|B2-prev-appears|PASS|sidePrev=true swPrev=家常蒸蛋羹 · 5 分
CHECK|C1-dock-loading-state|PASS|dish-dock-btn loading|正在记…
CHECK|E1-dock-done|PASS|dock=记好了 ✓ cls=dish-dock-btn done
CHECK|E2-done-anti-reclick|PASS|hash unchanged=true cls=dish-dock-btn done
CHECK|H1-fresh-empty-state|PASS|emptyState=true    （清 storage 后直进）
CHECK|H2-fresh-no-switcher|PASS|switchVisible=false
CHECK|I3-img-loaded|PASS|naturalWidth=1 complete=true
CHECK|I4-fullbleed|PASS|rect=375x340（340px 视觉=680 CSS/2 缩放）
```

flow.cjs 唯二 FAIL 为 F1/F2「直进即空态」——测试前提错误：h5 store 带 zustand persist(localStorage)（C-3a「刷新/重进=看到最后那套」既定设计），同会话直进必然注水菜单上下文。以 flow2（清 storage=全新会话）+ flow6（注入真实会话）复现实验改判，非页面缺陷。

## 5. 环境事件与根因闭环（如实记录）

1. **PG 中途死亡**：任务开始前已存在的 PG 实例（PID 276，非本会话所启）约 22:47 意外停止（该实例日志不在本任务日志内，死因无可查记录；`.pg/pg-t-p06.log` 仅存 22:44 一次「lock file already exists」撞锁 FATAL——当时 276 尚存活，属正常撞锁拒绝）。
2. **API 500 群的直接原因**：`.pg/api-t-p06.log` req-v 起 500 全部为 `PrismaClientKnownRequestError: Can't reach database server at 127.0.0.1:54329`——纯 DB 不可达，**未发现 API 代码缺陷**（早前「listPlans 500」疑点即此，撤回）。
3. **对验证的影响**：flow2 G1「直进后 dock=idle」当时疑似页面缺陷；重启 PG 后以 flow6 时间线闭环：真实原因是 getFeedback 请求 500 → catch 保持 idle——正是页面注释「查询失败不动当前态（如实，不假装已提交）」的设计行为。
4. **有图路径的披露式验证**：API 菜单快照恒不投影 imageUrl（t1 基线实锤，缺口移交主控 §7.1），任务卡预案的「有图 PUBLISHED/FETCHED 替代」均不可行。改用 CDP Fetch 域仅拦截 `/api/recommend` 响应，为每 dish 注入 1×1 红 PNG 的 data-URL（无外网依赖），实证 hero 前端渲染路径 7/7。**该注入仅存在于测试 harness 内存响应中**：不改业务代码、不写库、不掩盖缺口（降级态另以真实数据完整验证）。

## 6. Teardown 对账

| 项 | 动作 | 结果 |
|---|---|---|
| 临时脚本 | 删除 `.pg/qa-t-p06*.cjs`×3 + `.workflow-verify/t-p06/*.cjs`×7 | 全部删除（DeleteFile 回执） |
| 运行时证据 | 保留 `.workflow-verify/t-p06/`（gitignore 沙箱） | 13 张截图 + verify-result×3（§4 清单） |
| CDP tab | 每脚本自关 | /json/list 仅剩任务前既有「小红书」tab，零泄漏 |
| 服务 | 停 API(PID 36768)、H5 dev(PID 3044 树)、PG(pg_ctl stop -m fast) | `ALL_PORTS_CLOSED`（3000/10086/54329 全空） |
| git | status 对账 | 本卡 5 文件；packages/api 零改动；既有未提交条目未触碰 |
| DB | 计数对账（真实验证足迹，如实保留） | 见下表 |

DB 足迹（recommend/lock/feedback 均为真实写库，与前几轮验证惯例一致，不做删除）：

| 时点 | Plan | Event | 差额来源 |
|---|---|---|---|
| 任务卡基线 | 54 | 93 | — |
| 浏览器阶段起点 | 55 | 94 | 本会话早前 t1 基线脚本 recommend（+1/+1） |
| flow 主流程后 | 56 | 97 | recommend/lock/feedback 各 +1 event |
| flow6 后 | 57 | 100 | recommend+feedback |
| heroimg 后（final） | 58 | 101 | recommend+lock |

（+3 Plan / +7 Event，逐笔闭合；Dish/Menu/MenuDish/Ingredient 全程 48/13/42/45 不变。）

## 7. 未验证项 / 遗留 / 卡外发现（移交主控）

1. **API 菜单快照不投影 imageUrl/sourceSite**（做法页全部降级的根因）：Dish 表已有图（T-C04 入库），仅快照投影缺失。属 apps/api 范围（本卡禁改）。前端类型已先行对齐，**API 补投影后前端零改动自动亮图**。
2. **「用料待补齐」态未获真实数据触发**：当前推荐菜单三菜用料齐全。态代码就绪 + e-final 屏⑪静态演示覆盖；若需真机截图需先构造 0 用料 FETCHED 菜并被引擎选入菜单。
3. **gen-index.cjs 既有路径缺陷**（`pnpm build:h5` 在 apps/h5 下执行时拼出错误 dist 路径，`pnpm build` 不受影响）：早前已记录，维持移交主控。
4. **PG PID 276（任务前既有实例）中途死亡，原因不明**：已以任务卡命令重启完成验证、teardown 停止。若复现建议查 Windows 事件查看器与该实例启动方式。
5. **视频位、步骤嵌图、多图**：按卡内既定不在本期范围（DishStep 契约无图字段；imageUrl 契约单图）。
6. 工作区既有未提交条目（PRODUCT-CONFIRMATION.md、T-C03/T-C05、tests/* 等）：非本轮产生，未触碰。

## 8. 截图清单（.workflow-verify/t-p06/shots/，375×812@2x）

| 文件 | 场景 |
|---|---|
| s0_tonight.png | 今晚页（链路起点） |
| s1_candidates.png | 候选菜单页 |
| s2_plan.png | 今晚菜单页（3 菜卡：家常蒸蛋羹/凉拌黄瓜/紫菜蛋花汤） |
| s3_dish_first.png | 做法页首屏：降级 hero（实拍位角标+菜名）+返回浮钮 |
| s3b_dish_ing_steps.png | 主料/调料分组用料（4 人份）+笔记式步骤 |
| s3c_dish_switch_dock.png | 页尾换道双卡（上一道—/下一道）+悬浮胶囊 idle |
| s4_dish_switched.png | 换到第二道后：双向换道（prev=蒸蛋羹/next=紫菜蛋花汤） |
| s5a_dock_loading.png | 三态之 loading：「正在记…」禁点 |
| s5_feedback_form.png | 反馈三问页 |
| s6_feedback_done.png | 提交成功态（记好了） |
| s7_dish_dock_done.png | 返回做法页：胶囊 done「记好了 ✓」绿底 |
| s8_direct_entry_empty.png | 全新会话直进：空态页、导航/胶囊不渲染 |
| s9_hero_injected.png | 注入 imageUrl 后：全出血大图+叠名+chips+渐变（渲染路径实证，披露见 §5.4） |

---

*本轮遵守：只做卡内事；packages/shared 未动；运行时零 LLM API；零新增依赖（CDP 驱动复用依赖树既有 ws@8.21.2）；未引入 Redis/消息队列/微服务/K8s；测试真实执行（命令+退出码+数字见 §3–§6）。*
