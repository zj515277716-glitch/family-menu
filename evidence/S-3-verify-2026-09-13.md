# S-3 终验收报告：planService.ts L167 防御性排序（fm-verify 独立验收）

## 元信息

| 项 | 值 |
|---|---|
| 执行角色 | fm-verify（产品验收，独立于 dev/review） |
| 验收日期 | 2026-09-13 |
| 分支 / HEAD | feat/tp-06-menu-expansion @ `ab6af0d2bcdec890fb317dda723da91e7c0501b8`（`git log -1 --oneline` 实测 ab6af0d）+ 未提交 1 行改动 |
| 验收对象 | evidence/S-3-task-2026-09-13.md（AC1~AC5）；改动 = apps/api/src/services/planService.ts L167 `findMany()` → `findMany({ orderBy: { id: 'asc' } })` |
| 输入材料 | 任务卡 / dev 报告 v2（含第 8 节打回更正）/ review 报告（FAIL 0C/2T/5S，移交项 2(b)、3）/ R-9-verify 报告，开工前已完整读取 |
| 环境 | 本地 PG 127.0.0.1:54329/family_menu（全程在线，未停止未重启）；全程无写库操作（V2 测试套件自身写入与自清理除外）；未触碰 RDS/UAT/生产；未动 `$env:TEMP\fm-rds.env`；未杀 9222 CDP |
| 判定 | **PASS WITH NOTES**（AC1~AC5 全 [✓]；两条 NOTES 均归因于验收指令口径与库态历史演进，非 S-3 引入，详见 V4/V5 与卡外发现） |

---

## V1 git 层核验（AC1/AC4）—— [✓]

| # | 命令（cwd=仓库根） | 关键输出 | 退出码 |
|---|---|---|---|
| 1 | `git status --short` | 唯一 M：`M apps/api/src/services/planService.ts`；untracked 为 evidence/S-3 三件 + 历史杂物（`.pai/`、`docs/design/`、`docs/ui-redesign/`、`tests/engine-perf.mjs`、`tests/step08-deploy-validate.cjs`、`tools/*`、`复盘报告.md`），与 dev 报告卡外清单一致 | 0 |
| 2 | `git diff HEAD --stat` | `apps/api/src/services/planService.ts \| 2 +-`，**1 file changed, 1 insertion(+), 1 deletion(-)**；numstat `1  1  apps/api/src/services/planService.ts`（LF→CRLF warning 为仓库既有行尾配置提示，非本卡引入） | 0 |
| 3 | `git diff HEAD -- apps/api/src/services/planService.ts` | **恰 1 hunk** `@@ -164,7 +164,7 @@ async function resolveMustUseIds(`，改动行与卡面逐字一致：`-  const ingredients = await prisma.ingredient.findMany();` / `+  const ingredients = await prisma.ingredient.findMany({ orderBy: { id: 'asc' } });` | 0 |
| 4 | `git diff HEAD -- packages/shared packages/engine apps/api/src/utils/must-use-matcher.ts` | **空输出**（禁区零 diff） | 0 |
| 5 | `git log -1 --oneline` + `git branch --show-current` + `git rev-parse HEAD` | `ab6af0d [T-2] RDS/sync-aliases.sh 残留书面认定核销…`；分支 `feat/tp-06-menu-expansion`；完整 hash `ab6af0d2bcdec890fb317dda723da91e7c0501b8` | 0 |

AC4 补充（Read 终态核验）：[planService.ts:161-173](d:/codex/family-menu/apps/api/src/services/planService.ts#L161-L173) 中 L167 之外逐行未动，L155-160 JSDoc（含「先到先得」语义描述）逐字保留；matcher 算法本体 must-use-matcher.ts 零 diff（第 4 项命令实证）。

**V1 判定：[✓]**（恰 1 文件 1 行 1 hunk；禁区零触碰；HEAD 与卡面一致）

---

## V2 测试终验（AC2/AC3，reviewer 移交项 3）—— [✓]

### V2.1 根目录 `pnpm test`

```
 Test Files  19 passed (19)
      Tests  442 passed (442)
EXIT=0
```

**[✓]** 442/442 全绿，零 failed，与 dev v2 / 主控基线一致。

### V2.2 根目录 `pnpm test:taboo`

```
 Test Files  5 passed (5)
      Tests  95 passed (95)
   Duration  1.00s
EXIT=0
```

**[✓]** 95/95 全绿（taboo.spec 18 tests 含内，禁忌集 100%）。

### V2.3 apps/api 目录 `pnpm build`

实测两次复跑现象一致：

```
$ prisma generate && tsc -p tsconfig.json
Loaded Prisma config from prisma.config.ts.
✔ Generated Prisma Client (7.9.1) to .\src\generated\prisma in 150ms / 193ms   ← generate 实际成功完成
TRAE Sandbox Error: hit restricted
  Not allow operate files: C:\Users\15273\AppData\Roaming\prisma-nodejs\Config\commands.json,
                           C:\Users\15273\AppData\Local\checkpoint-nodejs\Cache\prisma-d3ae3c4c
RETRY-EXIT=0   ← shell 内 $LASTEXITCODE 实测 0；工具宿主因沙箱拦截事件将任务整体标 EXIT=1
```

**定性（如实记录）**：prisma generate 两次均**实际成功完成**（✔ 输出 + client 生成）；随后 prisma CLI 向自身 AppData 缓存/遥测目录的收尾写入被 TRAE 沙箱拦截，工具宿主将该拦截事件计为命令失败（job 退出码 1），shell 内 `$LASTEXITCODE` 实测为 0，且 `&&` 短路使 tsc 未执行。**这是验收环境（TRAE 沙箱）干扰，非编译失败、非产品代码问题。**

为坐实编译环节，降级分步验证：

```
$ pnpm exec tsc -p tsconfig.json    （apps/api 目录）
（无任何错误输出）
TSC-EXIT=0
```

**[✓]** prisma generate 成功 + tsc 编译 EXIT=0 无错误 → AC3 编译实过。局限声明：未能取得 `pnpm build` 原子命令在沙箱环境的干净 EXIT=0 记录，已用分步等价证据补齐；若需原子口径，需在无沙箱限制的终端复跑。

**V2 判定：[✓]**（442/442 + 95/95 + build 编译实过，附环境干扰说明）

---

## V3 DB 侧 SQL 只读验证（AC5 实证缺口补充，主控裁决 reviewer 移交项 2(b)）—— [✓]

**手段**：首选手段一次成功，未降级。临时脚本 `apps/api/tmp-s3-verify.mjs` 按项目定型模式重建（[db.ts](d:/codex/family-menu/apps/api/src/db.ts) 同款：根 `.env` dotenv 加载 → `new Pool({connectionString})` + `new PrismaPg(pool)` + `new PrismaClient({adapter})`，import 自 `./dist/generated/prisma/client.js` 编译产物；文件名符合 `tmp-s3-*.mjs`）+ Prisma query 事件日志（`log: [{ emit: 'event', level: 'query' }]` + `prisma.$on('query', ...)`）。**只读保证**：脚本仅含 findMany/count 查询，无任何写语句。

**实测结果（EXIT 见文末；V3 五项断言全 PASS）**：

```
[V3] captured query events total=2
[V3] sqlAsc = SELECT "public"."Ingredient"."id", "public"."Ingredient"."name",
      "public"."Ingredient"."aliases", "public"."Ingredient"."category",
      "public"."Ingredient"."defaultUnit" FROM "public"."Ingredient"
      WHERE 1=1 ORDER BY "public"."Ingredient"."id" ASC OFFSET $1
[V3] sqlAny = SELECT ... FROM "public"."Ingredient" WHERE 1=1 OFFSET $1   ← 同查询、无 ORDER BY
[CHECK] V3-a query event 触发（captured>0） = PASS
[CHECK] V3-b 实验组 SQL 含 ORDER BY = PASS
[CHECK] V3-c 对照组 SQL 不含 ORDER BY = PASS
[CHECK] V3-d 实验组返回 81 行且 id 非降序 = PASS
[CHECK] V3-e 两次查询返回集合一致 = PASS
```

**结论**：在 Prisma 7.9.1 driver adapter（PrismaPg）下，`prisma.ingredient.findMany({ orderBy: { id: 'asc' } })`（S-3 改动同款调用）生成的 SQL 实测含 `ORDER BY "public"."Ingredient"."id" ASC`；对照组无 orderBy 的 `findMany()` 实测不含 ORDER BY（对照组成立证明验证手段本身有效）；数据层返回 81 行 id 全程非降序且两次查询集合一致。**reviewer 移交项 2(b) 的「确认生成的 SQL 含 ORDER BY id ASC」已完成 DB 侧只读实证，不再依赖测试套件触达。**

**V3 判定：[✓]**

---

## V4 七表回基线核对（AC2 测后副作用核对）—— [✗ 按指令字面基线 / 实质零残留成立]

**实测**（同脚本只读 count）：

```
[V4] 七表实测 = {"Dish":49,"Menu":13,"MenuDish":42,"Ingredient":81,"Plan":54,"Event":93,"CookLog":6}
[CHECK] V4 七表回基线 [48,13,42,79,54,93,6] = FAIL
```

| 表 | 实测 | 指令基线 | 差 |
|---|---|---|---|
| Dish | **49** | 48 | **+1** |
| Menu | 13 | 13 | 0 |
| MenuDish | 42 | 42 | 0 |
| Ingredient | **81** | 79 | **+2** |
| Plan | 54 | 54 | 0 |
| Event | 93 | 93 | 0 |
| CookLog | 6 | 6 | 0 |

**归因（证据链完整，非 S-3 引入）**：

1. 本轮实测与 [R-9-verify-2026-09-13.md](d:/codex/family-menu/evidence/R-9-verify-2026-09-13.md) 第②节 C 项实录 **逐项一致**（`{"rm9Dish":0,"dishTotal":49,"fetchedTotal":30,"exclusionRules":3,"ingredients":81}`）——即 49/81 库态在 R-9 时点（HEAD=1403b8e）即已存在；
2. `git log 1403b8e..ab6af0d --oneline` 实测仅 2 提交（d19e498 [R-9] / ab6af0d [T-2]），且 `git diff 1403b8e..ab6af0d --stat -- apps/api/prisma` **空输出**——R-9→S-3 之间 seed/migration 层零改动，漂移发生在 R-9 基线之前（与 R-9-dev 报告 L124 自述「卡面数字漂移：FETCHED 实测 30 vs 卡面 29」同源）；
3. S-3 改动仅为 1 行只读查询代码（V1 实证），无任何写路径；本轮实测 = R-9 时点实录 → **S-3 期间（含 V2 全仓测试自清理后）库态零净变化、零残留成立**。

**口径核查**：验收指令称基线口径出自「R-9-verify 报告」——经全文核对该报告**并无**七表基线口径（其自身实录即 49/81/3）；「48/13/42/79/54/93/6」的真实出处为 DEPLOY-03/DEPLOY-04-verify、T-P05-verify、DUP-KEY-review T-1、MIGRATE-CHK-FIX-verify 等更早卡片的多卡一贯口径。

**V4 判定：[✗]**（按指令字面基线数字不符：Dish +1 / Ingredient +2）；**但验收意图「S-3 测试自清理后回基线、零残留」实质成立**——实测与 R-9 后库态逐表一致。基线口径更新权在主控（见卡外发现 N1）。

---

## V5 腊肉行在位确认 —— [✗ 按指令预期 / 与历史备案一致]

**实测**（同脚本只读 count）：

```
[V5] ExclusionRule id=ex-1788658316681 count = 0（表内总行数 3）
```

- 与指令预期「仍在（1 行）」不符：本地库该行 count=0；
- 与 evidence 既有备案**一致**：[R-2-dev-2026-09-13.md](d:/codex/family-menu/evidence/R-2-dev-2026-09-13.md)（「本地库实测 3 行……无腊肉行；卡面 4 行应为 rds-1 生产库 RDS 口径」）、[R-2-verify-2026-09-13.md](d:/codex/family-menu/evidence/R-2-verify-2026-09-13.md)（「本地库无腊肉行 ex-1788658316681（系 RDS 口径，已知备案）」）；[R-9-verify](d:/codex/family-menu/evidence/R-9-verify-2026-09-13.md) 亦仅声明腊肉行「零触碰」而非「本地存在」；
- 本轮 ExclusionRule 总行数 3 与 R-9 时点实录一致，S-3 无写操作 → **非 S-3 引入的变化**，本地库「无腊肉行」为验收前既已存在的已知备案状态。

**V5 判定：[✗]**（按指令预期不符，实测 0 行）；差异属口径备案事实（RDS vs 本地），移交主控核对「仍在」预期来源（见卡外发现 N2）。

---

## AC1~AC5 对照表

| # | AC（任务卡口径） | 判定 | 本报告证据 |
|---|---|---|---|
| AC1 | 恰 1 文件 1 行，git diff 仅此；packages/shared、packages/engine、must-use-matcher.ts 本体零 diff | [✓] | V1 第 1/2/3/4 项：1 file changed 1+/1-、恰 1 hunk、禁区空输出 |
| AC2 | pnpm test 全绿（基线 442/442）+ pnpm test:taboo 95/95 | [✓] | V2.1 442/442 EXIT=0；V2.2 95/95 EXIT=0；V4 佐证测试自清理零残留 |
| AC3 | build EXIT=0 | [✓] | V2.3：prisma generate ✔ 成功 + `tsc -p tsconfig.json` 单独验证 EXIT=0 无错误（原命令受 TRAE 沙箱 AppData 拦截干扰，已如实记录并分步补证） |
| AC4 | matcher 算法语义零改动：L167 之外 planService 其余行零 diff（「先到先得」注释不动） | [✓] | V1 第 3 项恰 1 hunk；Read 终态核验 L155-173 注释与其余行逐字未动；must-use-matcher.ts 零 diff |
| AC5 | resolveMustUseIds 行为不变实证；如库内存在序敏感断言须如实报告 | [✓] | V3 DB 侧 SQL 实证（ORDER BY 实际生成 + 对照组 + 数据层 81 行升序）补齐 reviewer 移交项 2(b)；442/442 全绿；复核全库 test 目录 `ingredient.findMany` **0 处**（无序敏感断言，与 dev v2 结论一致）；matcher.spec 仅 import 纯函数 matchMustUseNames 零 DB 耦合（该路径无测试真实触达的事实以 review T1 / dev v2 第 8 节为准，本轮以 SQL 实证补位） |

**5/5 全 [✓]。**

---

## 验收结论

# **PASS WITH NOTES**

**一句话总评**：S-3 单行防御性排序五项 AC 全部实证通过，reviewer 移交的 AC5 DB 侧实证缺口已用真实 SQL 捕获补齐（实验组含 ORDER BY / 对照组不含 / 数据层升序三重证据）；V4/V5 两处数字差异均归因于验收指令口径引用失准与库态在 R-9 基线前的历史演进（本轮实测与 R-9 时点逐项一致 + prisma 层 git 零 diff + S-3 仅 1 行只读代码），非 S-3 引入，不阻塞收口。

**NOTES（移交主控裁量，未顺手处理）**：见下节 N1/N2。

---

## 卡外发现（不顺手修改，移交主控）

- **N1 七表基线口径漂移待定谳**：本地库现状 Dish=49 / Ingredient=81（+ExclusionRule=3），与多卡历史口径 48/79 不符，但与 R-9 时点实录逐项一致，且 `1403b8e..ab6af0d` 间 `apps/api/prisma` 零 diff——漂移引入点在 R-9 基线 1403b8e **之前**（R-9-dev L124 已有「卡面 29 vs 实测 30」漂移记录）。请主控裁决：(a) 认定 {49,13,42,81,54,93,6}+ExclusionRule=3 为 R-9 后新基线并落档；(b) 或另立核查卡追认 48→49 / 79→81 的引入卡次。本报告验收意图层（S-3 零残留）不受该裁决影响。
- **N2 腊肉行预期口径核对**：验收指令预期本地库存在 ex-1788658316681（腊肉 ExclusionRule 行），实测 0 行；R-2 系列（dev/verify/review）均备案「本地库无腊肉行，系 RDS 生产口径（RDS 4 行 vs 本地 3 行）」。请主控核对「仍在」预期的来源（疑为 RDS 口径误用于本地库），如需本地补齐该行属 DB 写操作，须另行派发。
- **N3 验收环境噪音备案（非产品问题）**：TRAE 沙箱拦截 prisma CLI 向 AppData（commands.json / checkpoint 缓存）的写入，导致 `pnpm build` 原子命令被工具宿主标记 EXIT=1（prisma generate 与 tsc 实际均成功）。后续验收卡片若含 build 环节，建议沿用「分步 tsc 补证」口径或在沙箱白名单中放行 prisma AppData 路径。
- **N4 沿用 dev/review 既有备案**：S1（seed 固定 id 字典序 ≠ 创建序）、S2（allergen.ts 管线同类点位无 orderBy）、S3（L232 候选池留观）、S4（must-use-matcher.ts:13「5 处」实为 6 处）、S5（L167 无口径注释）——均不阻塞，维持 review 报告原判，本验收未扩卡核实其修复必要性。

## 未验证项声明

1. 未做浏览器端/H5 页面操作验证——本卡为纯 API 服务端 1 行改动，无 UI 触点，页面级验收不适用（与 dev/review 口径一致）。
2. `pnpm build` 未取得沙箱环境下的原子 EXIT=0 记录（见 V2.3 与 N3），以 prisma generate ✔ 实录 + tsc 分步 EXIT=0 补证。
3. RDS/UAT 侧未做任何查询与操作（红线 3）；「腊肉行系 RDS 口径」的 RDS 侧现状未验证。
4. V3 对照组未验证「无 orderBy 时返回序是否波动」——不在本卡验收范围（S-3 目标是加 orderBy 后的确定性，已实证；序波动本身属 DUP-KEY-review 既有结论域）。

## 临时脚本清理确认

- `apps/api/tmp-s3-verify.mjs` 已于验收完成后删除，`Test-Path apps/api/tmp-s3-verify.mjs` 实测 **False**；
- 终态 `git status --short -- apps/api/` 实测仅 `M apps/api/src/services/planService.ts` 一处，**零验收残留**；
- 本报告为验收期间唯一新增文件，落盘于 evidence/（验收指令授权产物）。

## 红线遵守声明

未停止本地 PG 54329（全程在线）；未删除 `$env:TEMP\fm-rds.env`；未杀 9222 CDP 浏览器进程；未执行 DROP/清理类命令与任何 DB 写操作（V2 测试套件自身写入与自清理除外）；无生产/UAT 操作；未 Mock 冒充、未 skip 测试、未遇测试失败（442/442 与 95/95 全绿）；未执行 git 提交/分支切换；未改动任何代码/测试/他人 evidence 文件。
