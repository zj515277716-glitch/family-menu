# DEPLOY-04 任务卡归档（主控）

> 本文件为主控派发卡原文归档（响应 fm-reviewer S-3：任务卡字面落盘以闭合证据链）。附主控裁决记录。

## 任务卡 DEPLOY-04：AC12 门禁噪声处置实施（C2b+C3 组合，用户已拍板）

### 背景
评估卡 AC12-PERF-EVAL 已定案：AC12（packages/engine/test/score.spec.ts 千套菜单 recommend <50ms）阈值恰在实测单次中位 50.040ms 上，min-of-3 只能把 51.7% 单次失败率压到 20% 每跑假红率，失败模式为秒级环境劣化。用户于 2026-09-12 拍板实施 C2b+C3 组合（详见 evidence/AC12-PERF-dev-2026-09-12.md §4，含 diff 预览与修订记录 REV1）。本卡实施该组合，50ms 语义保留、不偏离 AC12 字面。

### 目标与改动（恰 2 文件）

**改动 1（C3，spec 侧 min-of-7）**：packages/engine/test/score.spec.ts
- L784 `for (let i = 0; i < 3; i++)` → `for (let i = 0; i < 7; i++)`
- L782 注释「多次运行取最小值（减少机器负载波动干扰，反映代码真实性能）」→「多次（7 次）运行取最小值（减少机器负载波动干扰，反映代码真实性能）」
- L790-792 threshold 逻辑与断言（<50 / coverage <200）**一字不动**

**改动 2（C2b，门禁层 best-of-3 OR 链）**：根 package.json
- scripts 区新增一行：`"test:gate": "pnpm test || pnpm test || pnpm test"`
- 现有 `"test": "vitest run --no-file-parallelism"` **不动**
- 注意：`||` 由 pnpm 的 script-shell 执行（Windows 上默认 cmd，支持条件执行），须实际执行验证而非假设

### 验收标准（AC）

- **AC1**：两文件 diff 恰如上述（`git diff --stat` 应为 2 文件，score.spec.ts 改动 2 行、根 package.json 净增 1 行），其余零改动
- **AC2**：C3 效果验证——受控环境（须核查并声明 :3000 无监听、无 dev server）下，AC12 所在 spec 单文件跑 10 次独立进程（如于 packages/engine 下 `npx vitest run --root ../.. --no-file-parallelism test/score.spec.ts`），记录每次 exit 与断言数字；**≤1 次 FAIL 可继续**（min-of-7 残余概率预期 1-4%，属设计内容忍），≥2 次 FAIL → 停止落盘报告并升级
- **AC3**：C2b 语义验证——根目录实际执行 `pnpm test:gate` 恰 1 次，EXIT=0，3 跑各 391/391 0 skipped，记录三跑 Duration；若 `||` 语义在当前 script-shell 下不生效（如三跑未串行执行或首轮失败未继续）→ 停止报告
- **AC4**：test:taboo 不受影响——跑 `pnpm test:taboo` 1 次确认绿（EXIT=0）
- **AC5**：报告落盘 evidence/DEPLOY-04-dev-2026-09-12.md（含环境声明、全部命令+退出码+数字、diff 摘要、AC 自检、卡外发现）
- **AC6**：新增依赖 0；不改 shared 契约；不动 CI（无 CI 现状）

### 输入资源
- evidence/AC12-PERF-dev-2026-09-12.md §4（C2b diff 预览 L199-206、C3 diff 预览 L214 起、REV1 修订记录——数学口径以该修订版为准：残余点估计 0.8%，CI 情景 0.05%-7.2%）
- packages/engine/test/score.spec.ts L771-794 现状
- 根 package.json 现状（先读再改）

### 边界约束
- 恰 2 文件：根 package.json（仅 scripts 加 1 行）、packages/engine/test/score.spec.ts（仅 2 行）
- AC12 阈值数值、断言语义、coverage 双轨逻辑零改动
- 测量命令真实执行并记录退出码与数字，禁止 Mock；单文件采样 10 次约 1-2 分钟，不得并发起跑
- test:gate 执行总耗时约 30s（3×10s），属预期

### 异常升级路径
- 采样/门禁跑中遇到**非 AC12** 的既有测试失败 → 停止，落盘后报告，不裁决
- AC2 出现 ≥2 次 FAIL → 停止落盘报告
- AC3 若 cmd/pnpm 的 `||` 语义异常（如命令不存在、首轮失败但后续跑未执行）→ 停止报告
- 环境异常（:3000 被占/PG 未起）→ 本卡采样不依赖 PG，但须如实声明环境状态

## 主控裁决记录（2026-09-12，响应 fm-reviewer 报告与 fm-dev 卡外发现）

1. **AC3 卡面失实勘误（主控责任，与 DEPLOY-03 C-2 同类）**：AC3 写「3 跑各 391/391」与 C2b best-of-3 OR 链设计内在矛盾——OR 链设计本为首轮绿即短路，「三跑 Duration」在首轮绿场景按设计不可观测。fm-dev 观测 1 跑 391/391 EXIT=0（15.20s）属设计内正确行为；`||` 语义经 T1/T3（失败→继续）与 T2/T4（成功→短路）四组受控实验验证生效。**裁决：接受评估报告 REV1 的 C2b 权威语义（best-of-3 OR 链，非多数决），当前实施即为定稿。**
2. **C-1 裁决路径**：选「dev 报告补转述」——C2b 漏报危险点（持续轻微回归如 45→52ms 稳定劣化会被 OR 链救活，仅 >55-60ms 重大回归可检出）须在 dev 报告专节转述，权威记录 evidence/AC12-PERF-dev-2026-09-12.md L210；使用约定：test:gate 为批跑噪声兜底入口，test 仍是单跑权威门禁，不得以 test:gate 替代 test 作唯一门禁（未来 CI 若引入须重估该取舍）。
3. **C-2 移交 fm-verify**：or-check 四组实验（T1-T4）由 verify 独立复跑并落盘输出与退出码，闭环「失败→继续」路径证明。
4. **S-1 移交 fm-verify**：git 层核验（git status --porcelain / git diff --stat / packages/shared 零改动确认）。
5. **S-2 勘误**：dev 报告「与评估卡 §4 预览逐字符一致」措辞修正为「一致（C3 另含任务卡明确定义的注释行改动）」——任务卡原文即含 L782 注释改动，非越界，系评估预览未含注释行。
6. **S-3 闭环**：本文件即任务卡归档。
