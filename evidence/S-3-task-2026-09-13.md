# S-3 任务卡：planService.ts L167 防御性排序（DUP-KEY-review S-3 挂账核销）

- 派发：主控 PM（2026-09-13，队列按总授权依次执行）
- 执行：fm-dev
- 性质：轻量实现卡（评估结论=改，1 行改动）

## 背景

DUP-KEY 卡消歧后，must-use-matcher 层 1「先到先得」在当前库态与遍历序无关（review R5 实证 matchResultStableAcross3Runs=true）。但 [planService.ts:167](../../apps/api/src/services/planService.ts) `prisma.ingredient.findMany()` 无 orderBy——PG 无 ORDER BY 时返回序不保证（跨会话/执行计划/表状态可变），构成两个真实代价：

1. **排查成本**：DUP-KEY 调查实际遭遇序波动（indices 6/31/15/20 vs probe run1 6/28/15/5），靠消歧才消除；未来任何数据漂移（如管线/人工入库再引入重复 key）会重启「序依赖选边」且不可复现。
2. **不确定性敞口**：matcher 层 1 语义=先到先得，DB 任意序使「先到」无定义。

同类盘点（本卡不改，备案）：L106 exclusions（集合语义序无关）、L113 ingredientMap（Map 键序无关）、L342 getExclusions（仅回传展示序）、L232 PUBLISHED 候选池（引擎侧同分行为未评估，留观不扩卡）。

## 目标 / AC

**改动**：[planService.ts:167](../../apps/api/src/services/planService.ts) `findMany()` → `findMany({ orderBy: { id: 'asc' } })`（恰 1 文件 1 行；id=cuid 创建序，语义最贴近「先到」，与 R-9 dishesService orderBy id asc 先例一致）。

| # | AC | 验证方式 |
|---|---|---|
| AC1 | 恰 1 文件 1 行，git diff 仅此；packages/shared、packages/engine、must-use-matcher.ts 本体零 diff | `git diff --stat HEAD` + numstat |
| AC2 | pnpm test 全绿（基线 442/442）+ pnpm test:taboo 95/95 | 真实执行，复制命令+退出码+数字 |
| AC3 | build EXIT=0 | apps/api build |
| AC4 | matcher 算法语义零改动：planService.ts L167 之外 planService 其余行零 diff（注释「先到先得」不动，DB 侧确定性化不改变算法） | Read 核验 |
| AC5 | resolveMustUseIds 行为不变实证：根 pnpm test 含 mustUse 相关 spec（matcher.spec 纯内存零耦合）全绿即覆盖；如库内存在序敏感断言（消歧后不应存在）须如实报告 | AC2 复跑覆盖+如实报告 |

## 边界约束

1. 仅改 planService.ts L167 一行，禁碰其他 findMany（上方盘点备案不改）。
2. 禁改 packages/shared（契约）、packages/engine、apps/api/src/utils/must-use-matcher.ts。
3. 不做任何生产操作；不做 DB 写操作。
4. 测试防假绿：不得 Mock、不得跳过；如遇失败立即停止报主控，不得自行改测试或扩改动面。

## 异常升级路径

测试失败 / AC1 边界守不住 / 发现本卡外问题 → 停止，写进完成报告报主控，不顺手修改。

## 产出

- 代码：planService.ts 1 行
- dev 报告：evidence/S-3-dev-2026-09-13.md（含 AC 逐条 [✓]/[✗] 自检）
