# AI 开发与改造优化操作流程

> 副标题：面向非程序员产品负责人的四角色协作与证据验收工作流  
> 版本：V1.0｜日期：2026-09-06  
> 适用：TRAE CN SOLO 主导的新应用开发、存量项目改造、功能迭代与缺陷修复。  
> 核心原则：你确认需求、UI 和功能结果；AI 负责实现、技术审查和独立验证。必要的账号、费用、生产数据与上线操作仍须获得授权。  
> 文件性质：可采用的操作规范，不代表角色、权限、测试和发布检查已经配置完成。本次只生成文档，未改动两个项目或其线上服务。

## 阅读与采用方式

- **你第一次只读第 1、2、11 章**：了解自己的操作、开始方法和现有项目的落地顺序。
- **主控 Agent 初次建立流程时读全文**；日常只读取现行规则、当前状态、当次任务和必要的相关章节，不要每次重读全部历史。
- **开发、技术审查、产品验收 Agent** 读取自己的角色规则、任务包、对应产品条目及相关检查章节，不继承全部聊天记录。
- 第 12 章是可复制的工作模板，由 AI 填写维护，不要求你手工制作表格。
- 本文建议替代旧方案中“主控同时承担业务开发”和“只凭自检报告放行”的安排。**只有落实到项目现行入口后才算切换成功**，不能让新旧规范同时指挥开发。
- 已有的产品决定、已完成代码和历史记录不因采用本文而自动作废；未确认的新建议不得自动变成开发需求。

## 1. 你的操作保持简单

### 1.1 你只需要完成这些事情

| 时点 | 你做什么 | AI 应交给你什么 |
|---|---|---|
| 需求对齐 | 讲清要解决的问题，回答关键问题，确认做什么与不做什么 | 一份普通中文的需求确认稿 |
| UI 对齐 | 选择方向，体验原型，确认布局、风格和操作是否顺手 | 2–3 套核心方向稿；选中后的一套完善稿 |
| 功能定义 | 确认操作后应该看到什么、保存什么、失败如何处理 | 可观察的功能行为表 |
| 功能体验 | 在 AI 内部验证通过后，实际完成几个任务并反馈 | 固定版本预览、操作步骤、预期结果和已知限制 |
| 必要授权 | 决定账号、费用、发布对象、上线时间与生产变更范围 | 清楚的影响、风险、恢复办法和建议 |

“三类对齐”不是只能交谈三次。需求有变化可以重新确认，但不把技术步骤逐项转交给你。

你不负责看代码、解释报错、选择数据库结构、判定测试是否充分，也不负责在不同 Agent 之间搬运消息。

### 1.2 对你展示的状态

- **未准备好**：AI 仍在开发或内部验证，你不用帮它调试。
- **可体验**：指定范围已通过内部检查，请你确认产品体验；不等于允许公开上线。
- **可上线**：体验已确认且发布条件具备，请你决定是否按指定范围发布。
- 发布后明确报告“已发布的版本和范围”，不把“可上线”说成“已经上线”。

未完成一周真实使用等效果验证时，另写“使用效果观察中”，不把它混进代码完成状态。

### 1.3 AI 只因四类事项请求你决策

1. 产品含义有两种合理解释，且会影响结果。
2. UI 或操作体验需要选择。
3. 需要账号、购买、外部平台权限或外部协调。
4. 涉及生产数据、对外发布、删除、不可逆操作或超出已有授权的风险。

普通技术失败在 AI 内部排查。确实无法完成时，应报告缺少的能力和可选路径，不能无限循环，也不能让你替技术风险签字。

## 2. 第一次落地：从一个项目、一条流程开始

推荐先在家庭菜谱 APP 试运行，再迁移到农产品信息交易平台。不要同时改造两个项目的全部工作流。

### 第一步：打开正确的项目，不要先移动代码

**你做：**在 TRAE 中打开目标项目根目录。家庭菜谱项目为 `D:\codex\family-menu`。把本文件提供给主控；若 TRAE 不能读取文档所在位置，再复制一份到项目文档目录，不要搬动整个仓库。

**AI 做：**只读确认项目路径、当前分支与改动、现行规则入口、产品基准、现有测试和部署方式。保留未提交和未跟踪文件，不擅自清理或覆盖。

**交付：**一页“流程接管检查”，只说明当前依据、冲突、缺少的能力、试点范围。此时不修业务代码、不部署。

### 第二步：确认采用新流程，统一规则入口

**你做：**确认“采用四角色工作流，先改流程配置，不动生产服务”。只需一次范围授权。

**AI 做：**在授权范围内修改项目入口，让 TRAE 实际加载的规则、仓库入口和现状文件指向同一套现行规范。先检查工具实际加载哪些文件，不假定某个文件名一定自动生效。

**交付：**新的统一入口；历史资料仍保留，但不能继续派发当前任务。用新会话验证是否能恢复正确目标。

### 第三步：验证四角色确实能运行

**你做：**必要时完成工具授权或界面中必须人工操作的设置；不手工调度成员。

**AI 做：**执行第 3.3 节的无害能力检查。确认是真正独立会话，还是只有提示词分角色；确认哪些限制可由权限执行，哪些只能软约束。

**交付：**能力状态“已验证／不支持／未验证”，以及可运行的替代方案。未验证项不能标成已配置。

### 第四步：选一条用户流程作为试点

**你做：**确认先做“必消食材输入到推荐结果”，不扩大到整个 APP 重做。

**AI 做：**按当前定稿检查这条流程，建立任务包、独立审查、页面验收和回归测试。已正确的部分保留，只修复可复现的问题。

**交付：**经过内部验证的体验版本，而不是一份“已完成”的长报告。

### 第五步：你体验，AI 收尾，再扩大范围

**你做：**按简明任务卡使用，说明哪里不符合预期。

**AI 做：**将反馈区分为缺陷、新需求、设计取舍或尚未复现的现象；补回归测试，核对最终版本，更新唯一状态。

**扩展条件：**新会话能接续、失败能被拦截、关键测试能重跑、你收到的版本与证据一致。满足后再迁移其他流程和另一个项目。

## 3. 四角色分工：分开责任，不追求数量

### 3.1 固定角色

| 角色 | 负责什么 | 允许的工作 | 不允许的工作 |
|---|---|---|---|
| 主控 PM | 产品方向、计划、任务分配、依赖、状态与证据核对 | 读项目与证据，维护协调文档，派发任务，汇总给用户 | 编写业务代码；为赶进度降低验收要求；批准自己的技术实现 |
| 开发 DEV | 一个有边界的开发或修复任务 | 修改被授权代码，增加测试，在隔离环境运行验证 | 自己批准交付；顺手扩功能；用删除测试换通过 |
| 技术审查 REVIEW | 实现合理性、安全、数据一致性、测试充分性 | 独立读需求和代码，在安全环境复跑检查，提出可复现问题 | 直接改业务代码后自审；只复述开发报告；替用户决定产品 |
| 产品验收 VERIFY | 页面、操作、状态、数据保留、设备表现和发布后体验 | 在测试账户与授权环境操作，编写验收测试，留截图与运行记录 | 把接口返回当作页面结果；在生产中随意造数据；悄悄修改 UI 基准 |

说明：

- 主控“不写业务代码”不等于“不读代码、不理解证据”。必要时可以定位证据，也可以派发技术调查。
- 技术选择通常由开发提出、技术审查核对；主控协调。只有影响产品、费用、授权或生产风险时才请用户决定。
- 验收 Agent 可以在独立测试目录或隔离工作区编写测试，不因此获得任意修改业务代码的权限。
- 设计、架构、安全等专家按需调用，不默认增加一套长期驻留团队。
- 发布是一个受授权的执行任务，可交给指定执行 Agent；发布者仍不能兼任唯一放行者。

### 3.2 调度与写入纪律

1. 一个项目只有一个主控维护当前队列。
2. 默认只有一个活动业务代码任务。审查可并行，但必须面对同一个固定版本。
3. 两个角色不得同时修改同一工作区的同一批文件。测试编写如有重叠，由主控串行安排或隔离后合并。
4. 成员向主控返回发现和建议，不自行给其他成员增派工作。
5. 成员完成一个切片后结束；下个切片优先使用干净会话。必要知识通过任务包和证据移交，不通过继承全部推理移交。
6. 审查后又修改代码，受影响的审查与测试结论必须更新；旧版本通过不自动覆盖新版本。

### 3.3 编排能力必须实测

在测试目录、临时样例和测试账户中完成以下检查，不触碰生产数据：

- 主控是否能够派发独立成员任务，并接收结果？
- 成员是否可只获得自己的任务包，而非默认携带全部历史？
- 主控和审查者能否被工具权限限制为不写业务代码？只在提示词中约束时，要标明“软约束”。
- 验收者能否打开应用、执行操作并保存真实截图或轨迹？
- 放入一个预先声明的失败样例，检查失败是否能阻止“验证通过”，随后移除该样例并保留流程测试结果。
- 换一个新主控会话，仅凭现行入口能否说清目标、版本、当前任务和未完成项？

若当前 TRAE 版本不支持某项，不得宣称已经实现。可采用独立会话、受限目录和可重复运行的检查脚本作为替代；如果仍需人工搬运任务，应如实说明未实现自动编排，并先解决能力缺口。不要为追求自动化未经批准更换整套工具。

## 4. 文件与记忆：一个入口，分层读取

### 4.1 最小文件集合

下列是建议布局，不是要求给已有项目重复创建同义文件。AI 应优先复用现有对应资料。

```text
项目根目录/
├─ PRODUCT-CONFIRMATION.md     产品要求与已确认行为；用户唯一日常确认稿
├─ CURRENT.md                  唯一当前进度摘要；不复制整段历史
├─ docs/workflow/
│  ├─ 00-START-HERE.md          现行入口、权威顺序、角色与授权边界
│  └─ tasks/T-xxx.md            当前任务的范围、验收与交接
├─ docs/design/                被选定的 UI 基准；旧方向稿明确存档
├─ evidence/T-xxx/              执行记录、截图、轨迹和结论
└─ 原有测试目录/               保留可重复运行的测试代码
```

项目已存在 `docs/ai-rebuild/` 时可以直接复用，不必另造一个并列的权威中心。

### 4.2 权威与冲突处理

1. 工具权限、安全约束和用户明确授权不可被项目文档绕过。
2. 用户新确认的产品变更，由主控先更新确认稿及版本，再派发受影响工作。尚未记录的明确新决定先同步，不能假装不存在。
3. 项目内的产品含义以已确认产品稿为准；UI 按其引用的定稿版本执行。
4. 技术任务不得缩小产品验收要求。计划中的遗漏要补计划，不能用遗漏证明“不必实现”。
5. `CURRENT.md` 是当前进度摘要；各任务卡保存验收条件与证据引用。不要另维护一份竞争性的“当前状态”。
6. 历史状态、旧总方案、旧 UI 与长日志只作追溯；实际运行证据决定“实现到哪里”，但不能自动改写“用户要什么”。

### 4.3 每次开工与上下文恢复

主控或成员开工时先核对：

- 我是谁，负责哪项任务？
- 产品与 UI 依据是哪个版本？
- 目标、禁止范围和完成条件是什么？
- 当前代码、数据环境和上次检查结果是什么？
- 哪些仍是推测或未验证项？

恢复时不能只读日志最后一句“继续”。发现入口互相矛盾时先整理指向，不沿用最方便的一份。

主控常驻上下文只保留目标、版本、当前任务、阻塞和证据索引；按需读取详细日志。成员回传简要事实和证据路径，不粘贴全部终端输出。

外部文件同样可能过时：每次更新附时间与版本，恢复时检查一致性。上下文摘要不能替代原始产品基准和运行证据。

## 5. 从需求到功能定稿：保持三对齐

### 阶段 A：需求对齐

**AI 操作：**通过讨论或反问确认目标用户、最重要的问题、三个核心任务、必要业务规则、使用设备、成功标准与第一版不做的事。一次优先问一个最影响决策的问题。

**用户操作：**用生活语言回答和确认，不填写技术规格。

**产物：**产品确认书 A。已有项目先识别哪些需求仍有效，不因换流程重新推翻全部决定。

**通过条件：**用户明确确认。未确认补充列为建议，不加入实施范围。

### 阶段 B：UI 对齐

**AI 操作：**用真实中文内容和有代表性的数据生成 2–3 套核心方向稿。先比较信息组织和操作方式，不能只换三种颜色；选中后只完善一套。

完善稿覆盖相关页面的正常、加载、空结果、错误、成功和必要权限状态。固定颜色、字体、间距、按钮、图标与组件；保存定稿版本。涉及输入法、移动端底栏和长内容时提前验证可用性。

**用户操作：**选择并体验完善稿，确认好看、看得懂、用得顺。

**通过条件：**UI 定稿获得确认。若有重大技术可行性疑问，AI 可先做隔离的小实验，不把实验擅自发展成正式产品。已有项目以对比和保留为主，不要求无理由重画三套。

### 阶段 C：功能行为对齐

每条功能按六项描述：

1. 什么场景下使用；
2. 用户做什么；
3. 屏幕应该出现什么；
4. 哪些结果要保存；
5. 刷新、重进或跨天以后怎样；
6. 失败时怎样提示、怎样恢复。

**AI 操作：**核对 UI 与需求是否一致，补充遗漏的异常路径，生成可观察的行为表和初步验收任务。

**用户操作：**确认这些结果就是自己想要的。无需判断接口与数据库。

**通过条件：**确认书 A/B/C 的有效版本明确。关键安全要求不能因用户未提出就省略；应由 AI 技术审查补齐安全实现，不擅自扩大可见功能。

### 定稿后的变更

新想法先分类：原需求缺陷、新需求、UI 选择或技术内部调整。产品范围改变时说明影响，由用户确认后更新产品稿、UI、任务与测试；保留旧版以便追溯。正常需求变更不应在复盘中被误算为开发遗漏。

## 6. AI 内部执行：一个切片的完整闭环

一个切片是“用户完成一件事”，不是单独一层代码。例如“输入必消食材，得到符合要求的推荐或明确无结果”，而非“新增推荐接口”。

### 步骤 1：主控派发任务

- 引用确定的产品条目、UI 状态、代码基线和授权环境。
- 明确允许修改的范围、依赖、禁止项与通过条件。
- 把验收要求在开发前交给验收者检查，避免开发结束后再按已有实现编考试题。
- 指定需要保留的测试、证据位置及角色分工。
- 如果任务无法在一个清楚的用户结果内描述，继续拆小；但不把前端、后端、数据库割裂后分别宣称用户功能完成。

### 步骤 2：开发复现、实现、自测

- 改造任务先运行相关现有行为，记录基线；缺陷任务先复现，无法复现就标明条件未知。
- 增加能捕捉目标行为的测试，再实现；现有缺陷的测试应能在修复前失败、修复后通过。
- 运行代码检查、相关单测与真实集成测试；修改共享包后验证实际运行产物已更新。
- 不用演示数据掩盖服务错误；允许单测和设计原型使用明确标注的模拟数据。
- 开发完成后固定待审版本，提交自检结果、变更摘要、未验证项和证据。状态只能进入“待独立验证”。

### 步骤 3：技术审查独立检查

- 先读产品要求、任务边界和实际改动，再读开发者解释。
- 检查数据是否真实保存、错误是否正确处理、权限是否合理、迁移与旧数据是否兼容。
- 检查测试是否绕过关键链路：测试夹具是否提前构造了本应由真实代码生成的数据？接口测试是否被标成页面测试？
- 检查是否删除、跳过或弱化测试；若测试本身错误，需要解释并独立确认，不能只为变绿而改预期。
- 核查可重复运行的证据，而非只认可报告里的勾选。
- 输出通过、需修改或因缺环境无法判定。发现问题交回主控，不直接改业务代码。

### 步骤 4：产品验收操作真实页面

- 使用指定版本、测试账户和受授权的数据环境。
- 按产品确认书从页面入口完成操作，不用直接调用接口替代用户点击。
- 检查可见结果，以及刷新、重新进入后的结果；必要时查询数据库作补充证据。
- 对照定稿检查布局、组件、文案和异常状态；不能只搜索源码颜色值。
- 保存实际截图、操作轨迹或可重跑的页面测试。截图证明外观，不独自证明持久化和所有交互。
- 移动端模拟与实际设备验证分开标注。微信小程序不能把 H5 浏览器通过当作小程序真机通过。
- 检查失败则回到开发；缺工具或设备则说明覆盖缺口，不标为通过。

### 步骤 5：主控核对放行

主控核对五个问题：

1. 每条产品验收要求有没有对应证据，而不是只有测试数量？
2. 技术审查与产品验收是否实际执行，结论是否有来源？
3. 证据是否属于当前待交付版本、环境和数据条件？
4. 是否还有失败、跳过、未执行或仅靠推测的必需检查？
5. 合并后或发布配置改变后，是否需要重新验证？

全部满足才可以标为“内部验证通过”。验证通过不代替用户确认，也不代替上线授权。

### 步骤 6：交给用户体验并收尾

给用户固定版本、3–5 个有代表性的任务、每步预期和已知限制。用户只反馈实际体验，不阅读技术报告。

缺陷由 AI 复现修复并重跑检查；新需求单独确认。所有最终修复合在一起后，再跑核心旅程，不能只把十份不同版本的通过报告相加当作最终版本通过。

## 7. 验收证据与状态：不允许“说完成”代替完成

### 7.1 证据能证明什么

| 证据类型 | 能支持的结论 | 不能单独支持的结论 |
|---|---|---|
| 源码检查、类型检查、单元测试 | 局部实现和规则满足对应检查 | 用户已能完成整个功能 |
| 真实 API 与数据库测试 | 接口与实际数据链路在该条件下通过 | 页面呈现、键盘输入和移动端布局正确 |
| 真实浏览器操作与断言 | 指定版本、页面和场景下的交互结果 | 全部真机与全部异常都已覆盖 |
| 截图与视觉比对 | 指定页面状态的可见效果 | 数据保存、权限隔离和所有按钮行为正确 |
| 发布后核心流程验证 | 指定线上版本的核心路径可用 | 所有用户条件与长期效果都已达标 |
| 用户体验与实际使用记录 | 体验偏好、任务价值和约定效果 | 代码安全、并发一致性或技术质量全面合格 |

### 7.2 每次验证的最低记录

- 对应产品条目与任务编号。
- 验证者角色与会话或运行标识，区分自检和独立检查。
- 产品稿、UI、代码及产物版本；有未提交改动时记录差异快照，不能仅用旧提交号代表新内容。
- 环境、设备或浏览器、测试账户、数据基线与迁移状态；不记录真实口令或敏感个人数据。
- 实际执行方法、退出结果、通过／失败／跳过／未执行情况。
- 原始输出、截图或轨迹位置；只保存脱敏材料，按项目规则控制访问和保存期限。
- 结论适用范围、未覆盖条件和已知限制。

关键验收测试入项目版本管理，运行输出作为证据保存。可以清理无用临时排查文件，不得删除唯一可复跑的关键测试后只留下“PASS”文字。

### 7.3 必须阻止放行的情况

- 必需检查失败、未运行、被跳过或环境缺失。
- 用接口返回冒充页面结果，用模拟数据冒充真实持久化。
- 未经确认改变 UI 基准、产品行为或验收预期。
- 验证版本与交付版本不同，且没有补充验证。
- 核心按钮假成功、输入静默丢失、越权、敏感口令下发等阻断问题。
- 在构建产物中发现面向用户浏览器的错误本机地址，或关键配置缺失。
- 关键数据库或数据升级没有验证，发布恢复办法不具备可执行条件。

尽量让确定性检查由工具返回结果，并把通过条件接入自动执行流程。模型生成的“通过”字符串不是门禁。若平台没有硬约束能力，明确保留软约束风险，不能宣称系统已经能强制阻止一切违规。

性能波动、偶发失败要保留首次结果并调查条件，不能反复运行直到偶然全绿、只保留最后一次。非阻断检查允许经技术审查说明范围后延期，但不能延期核心行为或安全要求来换取“可上线”。

### 7.4 AI 内部状态与用户状态的映射

| AI 内部状态 | 进入条件 | 用户看到的状态 |
|---|---|---|
| 待定稿／待开发／开发中 | 尚未通过相应阶段 | 未准备好 |
| 已实现、待独立验证 | 开发自检已交付 | 未准备好 |
| 内部验证通过、待用户体验 | 必需技术检查与页面验收通过 | 可体验 |
| 用户体验通过、发布准备中 | 用户接受该版本体验 | 未准备好发布；体验结论保留 |
| 发布条件具备、待授权 | 发布、升级、恢复和风险条件已核对 | 可上线 |
| 已发布 | 授权范围内发布并完成发布后核验 | 已发布版本与范围 |

“真实使用一周”之类的任务另设结果：观察中／达标／未达标及原因。发送网址和使用说明只能完成“交付体验”，不能完成“一周效果验证”。

## 8. 缺陷修复：把一次修好变成不容易再坏

### 8.1 固定处理顺序

1. **记录现象**：用户在哪个版本、设备、页面做了什么，看见什么，原本期待什么。优先由 AI 获取已有信息，必要时只问缺失的一项。
2. **在安全环境复现**：匹配实际输入方式、旧数据、已有缓存与操作顺序；不得为复现随意修改生产数据。
3. **区分事实与假设**：“代码看起来正确”只能排除有限可能；“疑似缓存”不能写成已确认根因。
4. **沿真实链路定位**：页面输入 → 请求内容 → 服务处理 → 数据 → 响应 → 页面展示 → 交付版本。按证据缩小范围，不固定先怪某一层。
5. **增加回归检查**：尽量先证明旧行为失败，再修复。只能在真机复现的，保留完整步骤和设备条件，配合能自动化的部分检查。
6. **最小范围修复**：一个逻辑问题一项任务，不意味着只能改一个文件；避免无关重构。
7. **独立复验与相关回归**：不只检查报错消失，还检查用户结果、保存、刷新和邻近流程。
8. **验证实际交付版本**：对外报告“已修复”必须说明验证的版本和范围；只有本地通过时应写“本地已修复，待交付核验”。

### 8.2 同一问题反复出现时

默认在同一用户现象连续两次修复交付仍未解决时，主控停止继续叠补丁，交给干净上下文的审查者做根因复查。重点检查：

- 是否一直没有在同样条件下复现？
- 是原问题未修好、多个原因并存、旧产物未更新，还是后续回归？
- 代码、旧数据、构建配置与实际页面是否对应？
- 现有测试是否跳过了真实用户操作？

这一步不是默认推翻所有代码，也不是自动全量重写。技术无法判定时报告能力缺口，不让用户反复盲测。

### 8.3 家庭菜谱案例应沉淀的检查

| 曾经的问题 | 应保留的检查 | 需要覆盖的条件 |
|---|---|---|
| 中文名称与 ID、别名不一致 | 名称归一和真实数据集成测试 | 标准名、别名、未知词、已有数据库升级 |
| 输入后未确认就点击推荐，内容丢失 | 真实页面操作测试 | 输入后直接点按钮；已确认后点按钮；重复与数量上限 |
| 换菜看似成功但结果不一致 | 页面操作、刷新与数据核对 | 菜卡、清单、备菜信息以及不允许换菜的情况 |
| 跨天临时设置没有按约定重置 | 时间边界测试 | 同日保留、次日重置、时区依据，以当前定稿为准 |
| 三套主题和组件配置冲突 | 定稿视觉比对与组件检查 | 主要页面、空／错／加载状态及小屏幕 |
| 本地地址进入线上产物 | 生产构建静态检查和发布后页面冒烟 | 实际交付包、允许的服务地址与缺配置失败提示 |
| 用户加载旧页面 | 升级场景测试与版本识别 | 新访问者、已访问旧版的浏览器；不要只测清缓存后的访问 |
| 数据库升级遗漏 | 真实数据库升级测试 | 新建数据库和从旧版本升级两条路径 |

上表是待建立或核查的测试清单，不表示它们已运行通过。涉及真实设备、输入法和浏览器缓存的覆盖仍有边界，应明确记录。

## 9. 发布与安全：把“部署成功”变成“交付版本可用”

### 9.1 发布前

- 先确认具体项目、目标环境、发布对象和授权范围。公开上线与内部预览是不同动作。
- 固定构建入口，检查依赖、配置、共享包构建顺序和最终产物。不临时切换构建路线后沿用旧证据。
- 对生产环境采用所需配置，不把本机地址、测试账户或共享秘密写入公开页面文件。
- 环境变量注入前端后仍然对访问者可见；环境变量不是保密机制。身份认证须按技术安全要求实现。
- 准备并验证已有数据的升级路径。初始化数据、测试数据和生产业务数据分开，不把测试种子当作生产初始化方案。
- 明确迁移与应用启动顺序，以及旧版／新版兼容策略；不能依赖尚未迁移的新应用先健康才能执行迁移。
- 数据变更前按授权备份并核查恢复条件。代码回退不自动恢复数据库、服务器配置或已发送的通知。
- 在接近生产的测试环境验证候选产物。无需为小项目搭建庞大平台，但必须有隔离的验证条件。

### 9.2 发布时与发布后

- 发布已验证的同一份产物；记录代码、构建、配置和迁移版本，不记录秘密值。
- 在授权的测试账户或安全操作范围内，打开实际地址走核心流程。
- 检查版本、新旧访问路径、关键服务连接、登录或访问控制、实际页面和数据结果。
- 出现阻断问题时先停止扩大影响，再按已授权预案恢复；恢复本身如果超出授权范围，须先说明并申请。
- 发布后检查通过才报告已发布；仅上传文件或首页返回正常不能作为全部完成证明。

### 9.3 能力与风险边界

AI 内部技术审查不能保证零缺陷。涉及支付、复杂权限、敏感个人信息、医疗决策等高风险应用，若 AI 无法给出充分验证，应限制上线范围或建议有资质的技术人员独立评估。用户决定是否购买服务、缩小范围或暂停，不负责判定代码安全。

## 10. 工具与 Skills：补能力，不叠加流程

### 10.1 先保留、再补齐

| 能力 | 起步选择 | AI 负责 | 用户负责 |
|---|---|---|---|
| 编排与开发 | 继续使用当前 TRAE CN SOLO | 核实独立会话、权限与派发能力，建立四角色边界 | 必要的软件界面设置、授权 |
| 版本与状态 | 复用项目现有版本管理和 Markdown 文件 | 保护现有改动，记录版本，维护一个当前状态 | 不需要手工维护日志 |
| 代码与规则检查 | 复用现有类型检查、单测和代码规范工具 | 核对实际可运行命令，保留与维护测试 | 需要安装权限时确认 |
| 真实数据验证 | 与项目同类的隔离测试数据库 | 准备测试数据，验证新库和升级路径 | 必要的环境或账号授权 |
| Web 页面验收 | 缺少时考虑 Playwright 等浏览器测试工具 | 编写真实操作、截图、轨迹与回归测试 | 必要的下载授权和少量设备体验 |
| 小程序验收 | 对应平台开发者工具与目标设备 | 核对平台能力并执行可自动化检查 | 登录、设备授权和不可自动化的真实体验 |
| CI／发布检查 | 先做项目内可重复检查，再按托管环境接入 | 固定检查与发布入口、保留证据 | 平台权限、费用和上线决定 |

Playwright 可用于 Web 页面操作与浏览器测试，并提供移动端模拟；轨迹工具能帮助回看操作过程。它不等同于所有手机真机，也不直接证明微信小程序运行正确。[官方介绍](https://playwright.dev/docs/intro)、[轨迹查看](https://playwright.dev/docs/trace-viewer-intro)

### 10.2 Superpowers 等技能如何使用

Superpowers 可作为测试驱动、系统化调试、验证和审查等方法的来源，但不是必须先安装的前置条件。[项目官方仓库](https://github.com/obra/superpowers)

采用前由 AI 核查当前版本的具体技能内容、安装方式、许可证、工具要求及与当前 TRAE 的兼容性，不承诺可以直接一键接入。

原则：

- 一套项目只有一个有效的主流程，不同时让多个框架接管规划与状态。
- 技能只在需要的阶段加载，不把整套技能和全部历史预先塞进每个 Agent。
- 技能不能覆盖已确认产品目标、权限规则和证据标准。
- 安装技能不是完成验证；必须用一个小任务确认它确实触发并产生有效结果。
- 不为了工具而迁移已有 UI 组件库或技术栈；有明确收益和验证依据再考虑。

### 10.3 安装与环境约束

先检查已有依赖和平台，再由 AI 给出最小安装清单与用途。在项目锁定兼容版本，不在改造过程中无理由全部升级到最新版。

系统级安装、外部账号连接、付费或权限扩大需要授权。禁止把绕过沙箱、关闭安全限制或隐蔽启动服务写成通用“效率技巧”。

## 11. 两个现有项目与未来项目的具体落地

### 11.1 家庭菜谱 APP：补齐交付防线，不再次全量重做

按下列顺序，每步通过后再扩大范围：

1. **保留现场、重新定级。**记录最新代码与部署版本，区分内部验证、用户体验、一周使用效果。不能照抄旧审查的问题清单作为当前仍存在的事实。
2. **统一规则。**核对 `.trae/rules/project_rules.md`、`AGENTS.md`、旧 `STATUS.md` 与新入口的指向；以现行产品确认稿和流程为准，历史继续保留。
3. **先核查安全与交付风险。**重点检查前端令牌暴露、生产地址、数据库升级和构建路径。如果仍有阻断项，先在隔离环境修复，不以 UI 满意代替技术通过。
4. **用必消食材试点四角色。**按第 8.3 节覆盖标准名、别名、未知词、直接提交、同日与跨天行为；已通过内容不重复开发。
5. **补关键持久化与页面回归。**换菜后刷新、清单联动、反馈修改、历史回显、错误恢复，全部以当前确认稿为准。已明确延期的学习功能不自动纳入本轮。
6. **核对选定 UI。**使用当前被确认的方向与版本逐页核对，不再把旧主题、旧占位图规则混进新验收。
7. **验证同一份候选产物。**内部检查通过后，经授权发布或提供预览；检查手机实际访问的版本与行为。
8. **用户体验与效果观察分开收尾。**你先体验核心任务。原计划若保留一周真实使用，就继续记录约定指标；若要调整周期或指标，明确确认后更新，不把“已发网址”当成完成。

本次产物以“一条稳定发布路径＋一套关键回归测试＋准确的当前状态”为主，不以再增加一部长篇总方案为目标。

### 11.2 农产品信息交易平台：先打通一条真实任务

1. **读取最新现场。**此前审查只作为历史线索，重新核对现行产品要求、代码、运行条件与遗留项。
2. **复用验证过的四角色流程。**迁移角色边界和证据规则，不复制家庭菜谱的产品内容与验收结果。
3. **重新确认当前角色与最短路径。**以实际产品定义中的农户、商户、运营等角色为准，不先假定它必须具备普通电商订单流程。
4. **选定一条纵向任务。**例如“发布信息→符合条件者查看／报名→另一角色确认→双方看到状态”，具体动作先由产品负责人确认。
5. **让 UI 面向普通用户。**若仍要求输入 UUID、ISO 时间或英文枚举，改为选择、搜索、日期控件与中文表达；同时补齐实际主数据来源，不能只换标签。
6. **边做边接真实服务。**从页面走到真实数据，再回到另一角色页面验证；不同角色分别使用测试账户，重点检查权限和状态一致性。
7. **检查历史上线阻断线索。**上传方式与大小限制、环境变量名称、生产初始化数据、对象存储、数据库集成测试是否实际执行；确认现状后再修，不盲目按旧报告重做。
8. **按下一条用户任务扩展。**消息、交付、投诉与运营工作台只按确认范围逐步补齐；不为了页面数量扩张后台功能。
9. **再进入目标设备和小范围试用。**小程序真机与运营后台分别验收，经授权使用限定范围和可恢复的数据操作。

### 11.3 未来新项目

启动时只建立最小入口、产品确认稿和当前状态。按 A 需求、B UI、C 功能定义推进；关键技术风险先做隔离验证。

随后先完成一条从页面到真实数据的完整流程，让用户确认方向与真实可用性，再扩展剩余功能。不再先铺满后端和表结构、最后集中补前端体验。

### 11.4 小改动可以简化，但不能取消独立验证

- 文案、间距等低风险小改：一页任务、前后截图、相关检查、独立复核即可；角色可按需启动，不重走全套需求设计。
- 跨页面功能、数据写入、权限或发布改动：执行完整切片闭环。
- 高风险变更：增加专项技术检查与恢复验证，不能让“小改动”标签绕过安全要求。

## 12. 可直接复制的工作模板

模板中的占位内容由 AI 按实际项目填写。不得保留占位符就开始执行，也不得预填“通过”。这些是角色与任务提示模板，不是已经安装的 Skill 或已验证的 TRAE 配置。

### 12.1 用户第一次发给 TRAE SOLO：只读接管

```text
请先作为项目经理接管流程，不编写业务代码。

我是一名非程序员，只负责需求、UI 和功能结果的确认，以及必要授权。
请读取我提供的《AI开发与改造优化操作流程》并按其中的只读接管步骤工作。

先核对当前项目目录、实际改动、现行产品确认书、UI 定稿、任务状态、
项目自动加载规则、已有测试与部署记录。历史报告只作线索，不认定问题仍存在。

本轮只读：不安装依赖、不修改文件、不部署、不操作生产数据、不对外发送内容。
不要要求我看代码或测试日志，不要把不明确的事项脑补为已完成。

最后只给我一页：
1. 当前真正生效的需求与流程依据；
2. 新旧规则冲突及最小调整范围；
3. 四角色编排的已验证、未验证和不支持能力；
4. 建议先试点的一个用户任务；
5. 下一步需要我提供的必要授权。
```

### 12.2 用户确认采用后：授权建立工作流

```text
我确认采用四角色工作流：主控不写业务代码，开发、技术审查和产品验收分离。

请在刚才确认的项目范围内建立或更新流程入口、角色规则、任务与证据模板，
统一新旧规则的指向，并用无害测试验证派发、独立上下文、权限和失败拦截。
保留历史资料和现有未提交改动，不删除或覆盖不属于本次范围的内容。

本轮只授权流程文件、角色配置和隔离的流程验证样例；不授权业务功能修改、
生产数据操作、公开发布、对外推送或付费。新增权限和安装要求须先说明用途。

请报告实际生效的能力。平台不能硬性限制的部分标明软约束，不能写成已经强制落实。
完成后给我下一步试点操作，不要要求我审核代码。
```

### 12.3 主控角色规则

```text
你是唯一主控。负责目标、版本、范围、派发、证据、依赖和当前状态，不写业务代码。
产品稿决定用户要什么；实际运行证据决定做到哪里。二者不一致时，不得修改标准凑通过。

开工先读取有效入口、CURRENT 和当次任务。按需加载历史，不重复吞入全部日志。
一次派发一个有完整用户结果的开发任务，给成员最小充分任务包。
开发自检后安排独立技术审查与产品验收；验证期间固定代码版本，避免共享写入冲突。
成员回报必须带版本、实际执行证据、未通过与未执行项。只有自检勾选不得放行。
修改后重新检查受影响项，最终组合版本仍需核心回归。

普通技术问题内部协调。仅产品含义、UI 取舍、账号费用、生产或超出授权风险向用户提问。
缺少工具、权限或验证环境时保持未验证并报告缺口，不模拟成功。
用户只收到产品体验任务和必要决策，不承担成员调度、代码审核或技术风险判定。
```

### 12.4 开发角色规则

```text
你是开发成员，只执行主控提供的一个任务包，不负责批准自己的交付。
先核对需求与 UI 版本、允许范围、验证条件和环境。信息冲突先报告主控。
缺陷先复现并补回归检查，再做最小必要实现；保留已正确部分和用户现有改动。
不得静默增加功能、改变定稿、删除失败测试或用模拟数据掩盖真实服务失败。
在授权的隔离环境运行相关检查，固定待审版本后返回改动、证据和未验证项。
看到任务外问题记录移交，不自行扩展；阻断本任务时说明依赖，由主控调整范围。
```

### 12.5 技术审查角色规则

```text
你是独立技术审查成员，不修改业务代码，不接受“开发说通过”作为证据。
先读产品要求、任务边界和实际改动，再读开发解释，避免只沿开发者思路自检。
检查数据保存、边界与错误处理、权限安全、兼容升级、回归影响及测试真实性。
检查证据对应版本与环境，关键测试是否保留、是否跳过真实链路或弱化预期。
必要时在安全环境独立复跑；不能执行的明确写未验证。
返回通过、需修改或无法判定，附具体证据、影响和最小修复建议。不直接修完再自审。
```

### 12.6 产品验收角色规则

```text
你是独立产品验收成员。依据产品确认书和选定 UI，通过实际页面验证用户结果。
先执行产品任务，再阅读开发者对实现的解释；使用明确版本、测试账户与授权环境。
从入口点击、输入、提交，检查屏幕结果、刷新重进、异常恢复及目标设备表现。
接口和数据库检查只能补充，不得代替页面操作。原型图不得充当实际运行截图。
可以在隔离测试范围编写验收脚本并保存脱敏证据，不修改业务代码或 UI 基准。
没有实际运行页面就不能称页面验收通过；没有真机就不称真机通过。
发现问题给出操作、预期、实际、版本、证据与覆盖边界，交回主控安排修复。
```

### 12.7 单任务交接卡

```markdown
# T-编号：用户任务名称

## 依据与环境
- 产品条目及版本：
- UI 状态及版本：
- 代码基线／待审版本：
- 测试环境、测试账户、数据基线：
- 已有授权范围：

## 目标与边界
- 用户最终能完成的一件事：
- 允许修改范围：
- 不做什么：
- 前置依赖和任务外发现的处理方式：

## 验收要求
| 编号 | 用户操作 | 预期屏幕结果 | 保存／重进／失败恢复 | 验证方法 |
|---|---|---|---|---|
| AC-01 | 待填写 | 待填写 | 待填写 | 待填写 |

## 执行与交接
- 开发成员：
- 技术审查成员：
- 产品验收成员：
- 必须运行并保留的测试：
- 证据保存位置：
- 未覆盖条件及处理：
- 不得自行更改的产品预期：
```

### 12.8 独立验证回报

```markdown
# T-编号 验证回报

- 验证角色、会话／运行标识：
- 日期：
- 产品、UI、代码、产物版本：
- 环境、设备、测试数据条件：

| 验收项 | 实际方法与执行记录 | 结果 | 证据位置 |
|---|---|---|---|
| 待填写 | 待填写 | 未执行／通过／失败／受阻／不适用及理由 | 待填写 |

- 首次失败及重跑说明：
- 未覆盖条件：
- 已知用户影响：
- 建议：通过／需修改／无法判定（依据）：

说明：不得预填通过；不适用需解释；必需项未执行、失败或受阻不得放行。
```

### 12.9 用户体验交付卡

```markdown
# 本次可体验：任务名称

- 体验地址与版本：
- 本次改了什么：
- 本次未改变什么：
- 测试账户的安全获取方式：

| 请你做 | 应该看到 |
|---|---|
| 任务 1 | 预期结果 |
| 任务 2 | 预期结果 |
| 任务 3 | 预期结果 |

- 仍存在的限制及影响：
- AI 已检查范围／未检查范围：
- 需要你确认：结果是否符合要求；哪里不好理解或不好操作。
- 当前只是体验交付，不代表已经获准公开发布。
```

### 12.10 用户遇到问题时的简短反馈

```text
我在【设备／页面】做了【操作】，原本希望【结果】，实际看到【现象】。
这是截图或录屏：【如有】。
请先核对我看到的版本和真实操作路径，再复现、定位、修复并独立复验。
不要只看代码后告诉我已正常；只在缺少关键复现信息时问我一个问题。
```

以上信息不要求每项都由用户手填；AI 能从现有页面、证据和记录获取的自行获取。不要让用户提交口令、敏感家庭资料或完整生产数据作为诊断素材。

## 13. 怎么判断流程真的变好了

### 13.1 每完成一个切片，由 AI 自动记录

只在任务回报末尾增加简短统计，不要求用户另做管理报表：

- 内部验证拦截的问题数与用户体验才发现的问题数；按影响分类，不用数量掩盖严重缺陷。
- 同类问题复发情况，以及对应回归测试是否能捕捉。
- 从接任务到内部验证通过的耗时；拆分真实开发、等待与返工。
- 因真实产品决定请求用户的次数，以及本可由 AI 内部解决的无效打断。
- 新会话能否恢复正确任务，证据是否对应实际交付版本。

先积累几个切片的基线，再比较趋势。不以测试总数、Agent 总数、提交总数或日志长度作为成熟度证明。

### 13.2 复盘必须区分四件事

1. **事实**：什么版本、什么操作、什么结果，有何证据。
2. **原因假设**：哪些已排除，哪些仍需验证；不凭现象直接确定根因。
3. **修复**：代码、配置、数据或流程改了什么，以及适用范围。
4. **防复发机制**：保留了什么测试、自动检查或权限限制，后续如何运行。

测试未覆盖不等于问题只能由用户发现；合理模拟测试不等于造假，但不能扩大它的证明范围。没有压缩前后会话证据，不把所有偏移都归因于上下文丢失。

### 13.3 本版流程试运行通过条件

- [ ] 现行规则入口唯一，旧状态不再派发当前任务。
- [ ] 主控、开发、技术审查、产品验收的实际运行边界有证据。
- [ ] 一个故意失败的流程样例能够阻止“通过”结论。
- [ ] 一个干净新会话能够恢复正确任务，不依赖用户重讲背景。
- [ ] 一条真实用户流程通过页面、数据与相关异常检查。
- [ ] 至少一项典型缺陷已经变成可重复运行的回归测试。
- [ ] 最终交付版本与证据一致，用户体验卡准确。
- [ ] 没有把交付说明、代码完成或上线动作当成长期使用效果达标。

这些勾选由实际验证决定。本文件生成时全部未勾选，不代表配置已经完成。

## 14. 设计依据与适用边界

### 14.1 来自本次项目的关键证据

- [复盘报告](D:/codex/family-menu/复盘报告.md)：问题时间线与执行者总结，需与原始记录交叉核对。
- [原角色决策](D:/codex/family-menu/DECISIONS.md)：已存在角色分离设计，说明不能简单把问题解释为“从未使用多智能体”。
- [TRAE 项目规则](D:/codex/family-menu/.trae/rules/project_rules.md)与[重建入口](D:/codex/family-menu/docs/ai-rebuild/00-START-HERE.md)：新旧指挥入口存在冲突的线索。
- [技术计划](D:/codex/family-menu/docs/ai-rebuild/TECHNICAL-PLAN.md)与[开发日志](D:/codex/family-menu/开发日志.md)：TP-08 从一周使用效果验证被转述为体验交付，促成本版明确区分状态。
- [换菜验证证据](D:/codex/family-menu/evidence/TP-03-2026-09-04.md)：接口／数据库验证被写成界面一致，以及一次性测试脚本删除，促成本版明确证据范围与持续回归要求。
- [产品决定](D:/codex/family-menu/docs/ai-rebuild/PRODUCT-DECISIONS.md)：整套换等存在已确认的范围变更，复盘不能把合法变更都计作遗漏。

以上是 2026-09-06 读取时的过程证据，不是对未来版本的缺陷判定，也不是本次新一轮线上验收结论。

### 14.2 方法参考

Anthropic 的上下文工程说明讨论了精简相关上下文、外部笔记和专职子 Agent 等方法；本流程据此采用任务包、短状态与按需读取，但不将多 Agent 当作可靠性保证。[上下文工程](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)

Web 验收工具与技能参考见第 10 章。工具能力随版本变化，具体安装与配置需在实施时按当前环境复核。

### 14.3 最终原则

> 人工审核产品是否正确、清楚、好用；AI 内部分工验证实现是否真实、可靠、可交付。  
> 主控管理方向，不代替开发；技术审查查实现，产品验收查真实使用。  
> 文档保存目标，测试防止回归，版本与证据支撑结论。  
> 不靠增加角色和日志制造完成感，只凭与约定范围相匹配的结果放行。
