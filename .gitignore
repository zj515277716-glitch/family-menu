node_modules/
dist/
coverage/
.env
.env.local
.env.*.local
# 生产构建注入配置（含线上地址与访问口令，只留在本机构建机，绝不入库）
.env.production
.turbo/
*.log
.DS_Store
Thumbs.db
.taro/
.DS_Store

# Prisma 7 生成产物（通过 prisma generate 重新生成）
apps/api/src/generated/

# SWC 编译器插件（平台特定二进制，Taro 编译时自动下载）
.swc/

# 本机便携版 PostgreSQL（binaries + 数据目录，不入库）
.pg/

# 四角色工作流验收沙箱（临时验证脚本与运行时证据落盘，不入库）
.workflow-verify/

# 内容管线抓取产物（含站点 token 与抓取内容/图片，只留本机，绝不入库；C-1 裁决 2026-09-10）
tools/content-pipeline/out/

# API 运行时图片目录（T-C03 静态服务落盘内容，不入库；R-11 裁决 2026-09-10）
apps/api/static/
