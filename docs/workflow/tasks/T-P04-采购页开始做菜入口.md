# T-P04 采购页「开始做菜」入口（PD-015）

> 状态：**待用户审核**（fm-dev 交付（7/7 AC 自检，typecheck=0）→ fm-reviewer 复审**通过**（3 建议级 R-1~R-3 无阻断，AC2 五处独立核实无反例）→ fm-verify 验收**通过**（第 7 轮正式 22 PASS/1 FAIL，唯一 FAIL V5=设计内 404 console 噪音，主控裁定豁免并挂账；teardown 54/93 精确还原）→ 主控收敛完毕；证据 [evidence/T-P04-2026-09-10.md](../../../evidence/T-P04-2026-09-10.md)；git 提交与否由产品负责人裁决）

## 背景

用户 UAT 反馈（2026-09-06）：采购页确认购买后按钮只是静止文案「✓ 已按这个买」，看完做法要绕回顶部点菜卡；应直接变「开始做菜」跳转详细做法页。已按流程先行改确认书：C-8 修订 + PD-015 登记（PRODUCT-CONFIRMATION.md / docs/ai-rebuild/PRODUCT-DECISIONS.md 均已更新，fm-dev 不再改这两个文件）。

## 现状（主控已核实，fm-dev 自行复核）

- [apps/h5/src/pages/plan/index.tsx#L447-L452](../../../apps/h5/src/pages/plan/index.tsx) 底栏：`onClick={() => !bought && setBought(true)}`，`bought ? '✓ 已按这个买' : '就按这个买'`——bought 后按钮无动作。
- 同页 L247-249 已有 `goDish(dish)` → `Taro.navigateTo({ url: '/pages/dish/index?dishId=${dish.id}' })`（顶部菜卡 L322-327 使用中）。
- L287 副标题明示「点菜卡可看做法」；顶部菜卡按 `lockedMenu.dishes` 渲染，首张强调底。
- dish 详情页已有（`/pages/dish`，dishId 入参），无上一道/下一道导航（改版属 UAT 反馈 3b，不在本卡）。
- UI 基准 [docs/ai-rebuild/ui/e-final.html#L573](../ai-rebuild/ui/e-final.html) 屏⑦按钮为静态「就按这个买」。

## AC（逐条 [✓]/[✗] 自检）

- **AC1 按钮状态机**：未确认时按钮=「就按这个买」（可点击，一次性确认，重复点击无副作用）；点击后按钮文案变「**开始做菜**」；不再出现「✓ 已按这个买」文案。
- **AC2 跳转目标**：bought 态下点「开始做菜」→ 跳转 `/pages/dish/index?dishId=<当晚备菜顺序第一道菜的 dishId>`。fm-dev 须核实 `lockedMenu.dishes` 数组顺序与备菜顺序（prepSequence）的关系：若已按备菜顺序排列取 `dishes[0]`；否则取 prepSequence 最小者。核实结论写进完成报告。
- **AC3 行为不回退**：「做完饭回来记录一下 →」按钮不变；顶部菜卡点按进详情不变；清单勾选（Checkbox）行为不变；bought 状态刷新/重进后按现状保持（不在本卡扩展持久化）。
- **AC4 场景覆盖**：多道菜（≥2）跳第一道；单道菜正常；无锁定菜单空态页（L252-265）不受影响。
- **AC5 范围纪律**：后端零改动、契约零改动、`apps/api/**` 与 packages/** 禁改。
- **AC6 UI 基准同步**：e-final.html 屏⑦（s7 区块）在按钮处加 PD-015 两态说明注释（仅限该区块，不动其他屏）。
- **AC7 证据与清理**：build 或 dev 冒烟 + 真实浏览器截图（未确认态/已确认态/详情页落点三张）；teardown 强制——测试产生的 Plan/Event 清零，恢复基线 **Plan=54 / Event=93**，COUNT 前后对照数字入报告；退出码原文。

## 边界

- **允许**：`apps/h5/src/pages/plan/index.tsx`（+index.css 如需）、`docs/ai-rebuild/ui/e-final.html` 仅 s7 区块。
- **禁改**：packages/shared、packages/engine、packages/list-merger、apps/api/**、apps/h5 其余页面与全局样式、PRODUCT-CONFIRMATION.md、PRODUCT-DECISIONS.md。
- h5 dev 启动命令为 `pnpm --filter h5 dev:h5`（:10086）；>30s 命令用 Start-Process 脱离包装器+轮询日志；退出码用 $LASTEXITCODE；停 dev server 先杀 tsx watch 父进程再杀子。

## 异常升级

缺信息或发现卡外问题 → 停手写进完成报告，不顺手修改。
