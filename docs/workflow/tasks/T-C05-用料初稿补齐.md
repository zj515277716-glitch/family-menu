# T-C05 29 道 FETCHED 菜谱用料初稿补齐（常见做法知识库）

> 状态：**已完成**（fm-dev 交付：知识库 29 道 258 条用料（65 唯一食材、11 道 uncertain 标记）+ 幂等入库脚本 apply-ingredients.mjs（单事务/只动 FETCHED/DRAFT，重跑×2 零副作用）+ 19 道既有菜 DishIngredient 107 行逐行零变化 + 报告 [evidence/T-C05-dev-2026-09-10.md](../../../evidence/T-C05-dev-2026-09-10.md)（§8 打印友好审改清单）；AC4 的 Plan/Event +1/+1 判 [✗] 经主控复核为 T-P06 并行会话测试产物，已由主控删净恢复 54/93，本卡数据面 Dish=48/Menu=13 零问题；**2026-09-11 用户审核通过，29 道用料初稿定稿**——11 道 uncertain 标记维持原样在案，后续蚝油菜补采等任务如需调整可走幂等回写）

## 背景

T-C04 采集的 29 道菜 origin=FETCHED/status=DRAFT，ingredients 均为空（抓取页正文无文字用料，未伪造）。用户 2026-09-10 指令：**按常见做法补齐 ingredients 初稿供人工审改**。

## AC

- **AC1 知识库文件**：tools/content-pipeline/out/xhs/ingredients-draft.json（gitignore 内）：29 道菜（按 dishId+菜名对照）→ ingredients 数组（name/amount/role 主料|调料，家常做法常见用量，2 人份基准）。
- **AC2 数据真实性口径**：用量为常见家常做法经验值（如红烧肉：五花肉 500g、冰糖 20g…），报告中明确声明「初稿为常见做法经验值，需人工审改」；拿不准的菜（如地方变体）标注 `uncertain: true`。
- **AC3 入库脚本**：tools/content-pipeline/apply-ingredients.mjs：读知识库 → 按 dishId 更新 PG（建缺失 Ingredient、写 DishIngredient、role/amount；只动 FETCHED/DRAFT 菜，**不得触碰既有 19 道菜**）；幂等（重跑先清该菜旧 DishIngredient 再写）。
- **AC4 对账**：29 道 FETCHED 菜全部 ingredients 非空（uncertain 者也算补齐但标记）；既有 18 PUBLISHED+1 TESTED 菜的 DishIngredient 数量与内容零变化；总对账 Dish=48/Menu=13/Ingredient≥45/Plan=54/Event=93。
- **AC5 证据落盘**：evidence/T-C05-dev-2026-09-10.md（含 29 道菜名与用料清单摘要，供用户审改的打印友好格式）。
- **AC6 teardown 纪律**：本卡产出为真实数据**不删除**；PG 用完 stop（54329=0）。

## 边界

- 允许：tools/content-pipeline/（新增 apply-ingredients.mjs 与知识库文件）、out/xhs/**。
- 禁改：apps/h5/**、packages/**、apps/api/src/**、prisma、xhs-fetch.mjs/fetch2dish.mjs/import.ts/batch-fetch.mjs、两个产品文档、CDP 浏览器。
- 不用 LLM API（DEC-006 精神：本卡走人工知识库文件，不引入管线 LLM 调用）。
- PG：pg_ctl start/stop（.pg/bin 无 psql，用 node pg 包 .cjs 直查）。

## 异常升级

缺信息或卡外问题 → 停手写进完成报告。
