# T-P10 空手文案区分时长原因

> 状态：**进行中——STEP-2 实现**（2026-09-11 方案经用户批准（D-1~D-8 按推荐）；确认书 C-7 修订（PD-017）已由主控落 PRODUCT-CONFIRMATION.md 并登记 PRODUCT-DECISIONS.md；AC 与允许修改范围已按方案 §6/§3.2 锁定，fm-dev 开始实现）
> 依据：
> - 产品：T-P05 fm-verify 验收报告 §6「给主控的产品观察」（[evidence/T-P05-verify-2026-09-11.md](../../evidence/T-P05-verify-2026-09-11.md) L131-133）：30min 档输入「土豆丝」，matcher 命中土豆后引擎仍空手——空手卡文案「没用上『土豆丝』」让用户误以为库里没有能做土豆丝的菜，实际是有菜但 30 分钟档排不下；用户从文案无法区分「没这个食材」与「有但今晚时间不够做」
> - 编号说明：用户口径「T-P06」，因 T-P06 已被做法详情页改版占用（提交 `123195f`），按仓库台账顺延编号 **T-P10**
> - 代码基线：`3426a2d`（T-P05 提交后 HEAD，分支 feat/tp-06-menu-expansion）
> - 核心现状：packages/engine/src/feasibility.ts（第一轮时长/器具硬过滤→可达菜单，第二轮 mustUse 覆盖统计→unsatisfiable，L37-106）；apps/api/src/routes/recommend.ts L4-27 + planService.ts L405-412（空手返回 candidates=[] + unmetMustUse 原文数组）；apps/h5/src/pages/tonight/index.tsx L204-215 + types/index.ts L98-100（前端纯判定两变体：unmetMustUse 非空=C-7 指名型／空=C-7a 组合凑不进型）

## 目标（用户能完成的一件事）

必消空手时，屏⑥文案能说清「为什么空手」：是库里根本没有能用到该食材的菜，还是能用到但今晚时长档排不下（后者同时给出「换长时长重试」的出路暗示），用户不再被误导。

## 验收条件（AC，已锁定 2026-09-11，按方案 §6 细化）

| # | 内容 | 验证方法 |
|---|---|---|
| AC1 | 原因分类落地：引擎对 unsatisfiable 必消给出 TIME_BUDGET/NO_DISH 归类（宽集=通过器具过滤且不限时长的菜单食材并集；缺器具归 NO_DISH（D-2）；空手判定 `unsatisfiable: string[]` 值/形状零变化；组合凑不进仍走既有 C-7a 通道不混淆） | 引擎单测 feasibility.spec E1-E7 |
| AC2 | 契约 v0.8：RecommendResponseSchema 增 optional `unmetReasons`（record，key=用户原文，值枚举 TIME_BUDGET/NO_DISH），已获用户批准 | shared schemas.spec S1-S4 |
| AC3 | API 透传：unmetMustUse 语义不变（tp01 既有 35 断言零改动），unmetReasons 经 idToRaw 同一映射回译，routes 显式透传（zod strip 规避） | tp01 回归断言扩展 R1-R4 |
| AC4 | 屏⑥三态渲染：时间型新文案（标题含当次所选档位分钟数+正文出路暗示，D-6）、无菜型现状文案逐字不变、C-7a 文案逐字不变；混合两型分句（D-4）；unmetReasons 缺省防御分支按无菜型渲染 | 真机/CDP 截图 V1-V4 |
| AC5 | 确认书先行：C-7 修订（PD-017）经用户批准后落 PRODUCT-CONFIRMATION.md 并登记 PRODUCT-DECISIONS.md | 确认书 C-7 节 + PD-017 明细（已完成 ✓ 2026-09-11 主控） |
| AC6 | 全套回归绿：shared build→engine build→pnpm test→pnpm test:taboo→tp01 全绿；teardown 七表回基线（Dish=48/Menu=13/MenuDish=42/Ingredient=79/Plan=54/Event=93/CookLog=6，REL=29 不动） | 回归链按方案 §6.5 + teardown |
| AC7 | 收尾复核：:3000/:54329 零遗留进程 | netstat 记录 |

## 允许修改范围（已锁定 2026-09-11）

- packages/engine/src/feasibility.ts + src/types.ts（MustUseUnmetReason 等类型）+ src/recommend.ts（仅 return 一行透传，方案 §3.2 已披露）+ src/index.ts（+1 行 MustUseUnmetReason 伴随导出，fm-reviewer P1 建议主控补登，2026-09-11 补登）+ test/feasibility.spec.ts（新增）
- packages/shared/src/schemas/api.ts（契约 v0.8 unmetReasons + 账本 v0.8 记 api.ts 头部，D-7）
- apps/api/src/services/planService.ts（空手分支 L405-414 回译透传）+ apps/api/src/routes/recommend.ts（显式透传）+ tests/tp01-mustuse-regression.cjs（追加 R1-R4）
- apps/h5/src/pages/tonight/index.tsx + src/types/index.ts（屏⑥三态细分渲染）
- docs/ai-rebuild/ui/e-final.html 屏⑥（时间型/混合型两帧）+ design-spec.md（确认书驱动，随实施同步）
- PRODUCT-CONFIRMATION.md C-7 节 + docs/ai-rebuild/PRODUCT-DECISIONS.md PD-017（确认书修订，主控已完成 2026-09-11）
- 本任务卡状态头 + evidence/T-P10-dev-*.md

## 禁改区（草案）

- prisma/schema.prisma 及任何 migration（空手原因不入库，Plan 快照不回填）
- packages/engine 的评分/多样化/换菜逻辑（只动可行性判定相关）
- 屏⑥既有 C-7/C-7a 文案措辞（除非确认书修订明确改写）
- 既有测试预期不得为变绿修改（新增用例除外）
- migrate 脚本、抓取脚本、UI 基准文件（e-final.html/design-spec.md 由确认书修订驱动）

## 边界约束

- **确认书先行**：屏⑥为 UI 文案，须先改 PRODUCT-CONFIRMATION.md C-7（PD 登记）并经用户批准，再动 h5 代码
- **契约变更**：packages/shared 改动属契约变更（AGENTS.md 铁律 5），方案中须单列契约 diff 并经用户批准
- 契约改动后 shared 须先 build 重建 dist 再跑回归（环境口径）
- 方案应给出向后兼容策略：旧 Plan 行 Json 快照无新字段属预期（用户重新推荐即得新响应），不做迁移回填
- 运行时禁止调用 LLM API（DEC-006）；禁止引入 Redis/消息队列/微服务/K8s（DEC-008）

## 异常升级路径

- 引擎复算成本/口径争议（如「放宽时长」的精确定义）→ fm-dev 写进方案「待裁决项」报主控，不自行定夺
- 方案与确认书现状冲突 → 停下报主控，先修确认书再出方案
- 发现卡外问题（如 C-7a 判定链路 bug）→ 写进完成报告，不顺手修改

## 执行与交接

- 技术方案：fm-dev（2026-09-11 派发，仅方案不写业务码，产物落 docs/workflow/T-P10-技术方案-空手文案区分时长原因.md；2026-09-11 用户批准，D-1~D-8 按推荐）
- 进度：主控已落确认书 C-7 修订（PD-017，2026-09-11）→ AC/范围锁定 → fm-dev 实现（STEP-2）→ fm-reviewer → fm-verify（真机/CDP：V1 时间型空手、V2 无菜型逐字不变、V3 C-7a 逐字不变、V4 出路暗示换档全链路）
- 证据保存：evidence/T-P10-dev-2026-09-1x.md（dev）；evidence/T-P10-verify-2026-09-1x.md（verify）
- 不得自行更改的产品预期：空手不建 Plan/不写 Event（PD-001）；组合凑不进走 C-7a；必消语义=「家里有这食材」非「想吃这道菜」
