# T-P05 必消食材模糊/别名匹配

> 状态：**已完结**（2026-09-11 用户批准提交：fm-dev 交付 9/9 AC + 修复轮 7/7 AC；fm-reviewer「需修改」唯一阻塞 B-1 已修复——seed 虾仁双归属删 2 词（git diff 证实为基线既有问题随本卡一并修复）后通过；fm-verify 通过 45/45。证据：[evidence/T-P05-dev-2026-09-11.md](../../evidence/T-P05-dev-2026-09-11.md)（§1-9 主轮、§10 修复轮）、[evidence/T-P05-verify-2026-09-11.md](../../evidence/T-P05-verify-2026-09-11.md)）
> 依据：
> - 产品：CURRENT.md 挂账队列②；UAT 事故链（复盘报告.md L132-167：西蓝花/别名缺失/库外词三轮事故；TP-01/TP-02 既有必消链路）
> - 技术方案：[docs/workflow/T-P05-技术方案-必消模糊别名匹配.md](../T-P05-技术方案-必消模糊别名匹配.md)（**已批准 2026-09-11**：方案 E 分层匹配 + D1 歧义透传 + D2 捎带修 L508 + D3 seed 别名扩充）
> - 代码基线：`56ca378`（T-P09 提交后 HEAD，分支 feat/tp-06-menu-expansion）
> - 核心现状：apps/api/src/services/planService.ts L159-184（resolveMustUseIds：trim+小写精确等值）与 L508（全换分支原文直传遗留）

## 目标（用户能完成的一件事）

输入日常写法的必消食材（「小番茄」「土豆丝」等子串写法），推荐正确命中食材库并产出候选；命不中的词继续走既有空手提示（「去掉『X』再试」），不静默丢失、不误命中。

## 验收条件（AC）

| # | 内容 | 验证方法 |
|---|---|---|
| AC1 | 精确层零回归：西红柿/西蓝花/番茄等既有命中与苦瓜空手路径不变；tp01 17 断言全绿 | tests/tp01-mustuse-regression.cjs（真实 HTTP+PG） |
| AC2 | 子串唯一命中：「小番茄」→番茄、「土豆丝」→土豆走**层 2 子串路径**（seed 故意不补「小番茄」别名作对照） | 新增回归断言+PG 直查 |
| AC3 | 歧义透传：多命中词（如「鸡」→鸡蛋/鸡腿/鸡胸）空手且 unmetMustUse 回传原文，行为与现状一致 | 新增回归断言 |
| AC4 | 库外不可满足词不回归：苦瓜→200+candidates:[]+原文回传+不建 Plan/不写 Event | tp01 既有断言保持 |
| AC5 | matcher 纯函数单测：归一化/层 1 精确/层 2 唯一命中/歧义≥2 透传/空串边界 | apps/api/test/matcher.spec.ts（新增） |
| AC6 | D2：全换分支 L508 改走映射（mustUseIngredients 由 resolveMustUseIds 输出），补断言证明原文不再直传 | 单测/回归断言 |
| AC7 | D3：seed-data.ts aliases 扩充常见日常写法一批（清单列入 evidence；重跑 seed 幂等验证；「小番茄」故意不补） | seed 重跑+PG 直查 aliases |
| AC8 | 全套回归绿：管线测试+根回归+pnpm test:taboo 100%（引擎零改动仍须跑禁忌一票否决口径）；teardown 七表回基线（Dish=48/Menu=13/MenuDish=42/Ingredient=79/Plan=54/Event=93/CookLog=6）且迁移数据 REL=29 不动 | pnpm test / test:taboo + teardown 脚本 |
| AC9 | 收尾复核：:3000 端口无本卡遗留进程（T-P09 教训：前卡 tsx watch dev server 遗留） | netstat/taskkill 记录 |

## 允许修改范围

- apps/api/src/utils/must-use-matcher.ts（**新增**：匹配纯函数，零 IO）
- apps/api/src/services/planService.ts（resolveMustUseIds 改调纯函数 + L508 全换分支修复）
- apps/api/test/matcher.spec.ts（**新增**）
- apps/api/prisma/seed-data.ts（仅 aliases 扩充）
- tests/tp01-mustuse-regression.cjs（扩展断言，既有 17 断言不得删改预期）
- 本任务卡状态头 + evidence/T-P05-dev-2026-09-11.md

## 禁改区

- packages/shared/**（契约零变更）、packages/engine/**（方案 E 零改动）、apps/h5/**（零改动）
- prisma/schema.prisma 及任何 migration
- apps/api/src 其他文件（路由/routes 层不需要动）
- 既有测试预期不得为变绿修改（新增用例除外）
- 产品文档、docs/ai-rebuild/ui/e-final.html、design-spec.md、migrate 脚本、抓取脚本

## 环境与纪律

- 便携 PostgreSQL 54329：PowerShell 5.1 启动用 `Start-Process cmd /c "pg_ctl -D <datadir> -l <logfile> start"` 脱离终端；pg_ctl 挂住可 StopCommand 后另开终端验证（PG 实际已起）
- 开工先复核 :3000 端口遗留进程（T-P09 教训），有则 taskkill 清理并记录
- .cjs 临时脚本 `require('pg')` 用绝对路径 `require('d:/codex/family-menu/apps/api/node_modules/pg')`；根 `"type":"module"`
- 测试必须真实执行：命令+退出码+数字结果，禁止 Mock 冒充；禁止调用 LLM API（DEC-006）
- 卡外发现问题→写进完成报告，不顺手修改；新增依赖须列明理由
- 交付物：代码+测试+evidence/T-P05-dev-2026-09-11.md（含 seed 别名扩充清单、teardown 证明、AC 逐条 [✓]/[✗] 自检）

## 执行与交接

- 开发：fm-dev（2026-09-11 派发）
- 技术审查：fm-reviewer（交付后并行派发）
- 产品验收：fm-verify（交付后并行派发；真机抽查子串命中/歧义空手/去词重试交互）
- 证据保存：evidence/T-P05-dev-2026-09-11.md（dev）；evidence/T-P05-verify-2026-09-11.md（verify）
- 不得自行更改的产品预期：必消语义=「家里有这食材」非「想吃这道菜」；歧义词不猜、空手透传（方案 E §3.1）
