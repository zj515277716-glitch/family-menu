# T-P06 做法详情页图文改版实施（方向 A，PD-016）

> 状态：**待用户审核**（fm-dev 交付 8/8 AC → fm-reviewer 复审**有条件通过**（无阻断；R-1 loading 卡死小缺陷建议下批修复、R-2 teardown 转嫁主控纪律观察、R-3 flow6 证据缺口、R-4 mappers orderBy 归 API 小卡）→ fm-verify 验收**通过**（V1~V9 全 PASS：降级 7/7+有图 9/9、换道 5/5、用料与 DB 逐项一致、三态防抖 8/8、待补齐态 10/10、console 全为设计内 404 豁免、teardown 删 7 Plan+15 Event+1 临时菜恢复基线 Dish=48/Menu=13/Plan=54/Event=93 逐项一致、三端口清零）→ 主控收敛完毕。卡外发现移交：cookie 实名 access_token（非 ACCESS_TOKEN）；lock 响应水合路径不投影问题（candidates 页用 recommend 快照注 store）——均为 API 投影小卡设计输入。git 提交与否由产品负责人裁决）

## 背景

用户 2026-09-10 选定方向 A（沉浸大图流）。确认书 C-4 已按 vA 口径修订、PD-016 已登记。本卡把 vA 静稿落进真实 dish 详情页（apps/h5）。

## AC

- **AC1 UI 基准先行落盘**：e-final.html 屏⑪（dish 详情）按 vA 更新（大图开场/图上叠信息/换道导航/用料分组/步骤流/悬浮胶囊操作条），design-spec.md 同步相应规格描述；其余屏不动。
- **AC2 首屏大图区**：340px 全出血菜图（Dish.imageUrl），图上叠菜名+时长/菜系；无图降级色块+菜名占位；**本版单图**（契约仅 imageUrl 单字段；若有多图来源可扩展显示，但不得为此改 packages/shared 契约——不做多图切换或仅以"仅一张图"呈现）。
- **AC3 换道导航**：「‹ 上一道｜下一道 ›」按今晚菜单备菜顺序巡览；先读现有数据流（dish 页入参与菜单数据获取方式）确定实现：有菜单上下文时显示，直进无上下文时隐藏导航（不自作主张加 API）。
- **AC4 用料清单**：主料/调料分组（沿用现有人数换算逻辑）；ingredients 为空的 FETCHED 草稿菜显示「用料待补齐」态，不虚构、不留破版。
- **AC5 步骤区**：笔记式图文交错 1-2-3（沿用 steps 数据，图位逻辑：有图嵌图无图纯文字）。
- **AC6 操作条三态（R-3 归并）**：底部悬浮胶囊「做完饭，记一笔」——可点/loading（提交中禁点防重复）/已提交反馈态；对接现有反馈提交逻辑。
- **AC7 契约与 API 零改动**：禁改 packages/**、apps/api/**；仅 apps/h5 内 dish 详情页相关文件 + e-final.html + design-spec.md。
- **AC8 测试与证据**：h5 build EXIT=0；真实浏览器验证（起 API+H5，选一道有图 PUBLISHED 菜与一道无图 FETCHED DRAFT 菜各走一遍 C-4 场景；换道导航/三态防抖/空态截图）；teardown 精确还原（Plan=54/Event=93 含构成、端口清零、PG 停）；报告落盘 evidence/T-P06-impl-dev-2026-09-10.md。

## 边界

- 允许：apps/h5/src/pages/dish/**（及实现所需的最小共享样式/组件文件，diff 单列）、docs/ai-rebuild/ui/e-final.html（仅屏⑪区块）、docs/ai-rebuild/ui/design-spec.md。
- 禁改：packages/**、apps/api/**、apps/h5 其他页面（plan/index 等只在确需传参时最小改动并单列 diff 说明理由）、PRODUCT-CONFIRMATION.md、PRODUCT-DECISIONS.md、xhs-fetch.mjs/batch-fetch.mjs、CDP 浏览器。
- 沙箱纪律：>30s 脱离+轮询；退出码 $LASTEXITCODE；teardown 基线 Dish=48/Menu=13/Ingredient=45/MenuDish=42/Plan=54/Event=93（注意 Dish 基线已因 T-C04 变为 48）。

## 异常升级

缺信息或卡外问题 → 停手写进完成报告。
