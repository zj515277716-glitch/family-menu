# T-P11 挂账清理：注释对齐与测试稳定性（队列②③⑨⑩，零行为变更）

> 状态：**已完成**（2026-09-12 C-1/S-1 补正闭环 → fm-verify 独立验收 **PASS**：389/389 首跑即绿 + taboo 89/89 + tp01 54 + teardown 七表回基线 REL=29 + diff 红线审计零行为变更；按用户 2026-09-11 总授权「按挂账队列依次执行、卡完成即提交」随 T-P11 提交）

## 背景

CURRENT.md 挂账队列 ②③⑨⑩ 四项均为注释级或测试级、**零运行时行为变更**，按队列原文「随下卡顺手修」的处置意图，打包为本卡一次清理：

- **②**：apps/api/src/services/planService.ts 种子方法约 L50 注释过时（T-P05 卡外发现，注释仍述「精确匹配」，实际 T-P05 已改分层匹配层 1 精确 + 层 2 双向子串唯一命中）
- **③建议-1**：projection-order.spec（T-P07 产物，T-P09 fm-dev 遗留披露）无 PG 环境静默 skip 形成假绿 + afterAll cleanup 无 PG 时无容错
- **③建议-1b**：recommend 投影断言（imageUrl 投影 + orderBy sort asc）固化进 vitest
- **③建议-2**：apps/h5/src/types/index.ts 约 L50 旧注释漂移
- **⑨**：契约版本账本分裂——v0.2~v0.6 与 v0.8 记 api.ts、v0.7 记 dish.ts 头部，收敛回 api.ts 单一入口
- **⑩P2**：api.ts v0.8 注释「C-7a 时缺省」与实际行为「空对象」不符（T-P10 复审 P2，方案 §3.3/§3.4 自矛盾所致，无行为缺陷——h5 C-7a 分支先判定不读该字段）
- **⑩P3**：packages/engine/src/recommend.ts L2 文件头注释未同步 unsatisfiableMustUseReasons 输出

## AC

- **AC1（②）**：~~planService.ts~~ **seed-data.ts** 种子方法过时注释更新为与 T-P05 分层匹配实际逻辑一致的描述；纯注释，零代码变更。（**2026-09-11 主控勘误补登**：dev 核实 planService.ts L50 附近无过时注释（resolveMustUseIds 注释已是分层描述），挂账②真实位置为 apps/api/prisma/seed-data.ts:50「精确匹配」——T-P05 卡外发现时位置误记；seed-data.ts 系 T-P05 D3 已改过的种子文件，改动纯注释，主控补登边界派回补做）
- **AC2（③建议-1）**：projection-order.spec 无 PG 时**不再假绿**——显式 skip 且 skip 原因/计数在输出可见（或无 PG 直接 fail，二选一并在报告说明选择）；afterAll cleanup 增加「无 PG 不抛未处理错误」容错。
- **AC3（③建议-1b）**：recommend 投影断言固化进 vitest——确认 projection-order.spec 对「imageUrl/sourceUrl/sourceSite 三字段投影 + orderBy sort asc 三处统一」的覆盖度，缺什么补什么（若 cjs 回归里存在仅 HTTP 层验证的投影断言，固化为 vitest 可重复执行的单测；已有则验证并说明）。
- **AC4（③建议-2）**：apps/h5/src/types/index.ts 约 L50 漂移注释修正为与当前类型实际结构一致；纯注释。
- **AC5（⑨）**：契约版本账本收敛——v0.7 段落从 dish.ts 头部移入 api.ts 账本段（v0.2~v0.8 连续单一入口，内容原样保留不得改写历史口径），dish.ts 头部留单行指针「契约版本账本见 schemas/api.ts」；**schema 定义零变更**。
- **AC6（⑩P2 方案 a）**：api.ts v0.8 注释措辞修正——「C-7a 时缺省」改为如实描述（如「C-7a 组合必消空手时为空对象 `{}`」）；**不采用方案 b（routes 空对象不携带）**，零行为变更。
- **AC7（⑩P3）**：recommend.ts L2 文件头注释补 unsatisfiableMustUseReasons 输出说明（与 types.ts 定义一致）；纯注释。
- **AC8 回归全绿**：根 `pnpm test` 全绿（记录数字，T-P10 后基线 389/389）+ taboo 100% + tp01 回归 PASS（54 断言）+ h5 typecheck 0 错 + build EXIT=0；shared 改 src 后必须 `pnpm --filter @family-menu/shared build` 重建 dist 再跑回归；PG 相关测试跑完后 teardown 七表回基线（Dish=48/Menu=13/MenuDish=42/Ingredient=79/Plan=54/Event=93/CookLog=6，REL=29 不动）。
- **AC9 零行为变更审计**：完成报告含全量 diff 自查结论——所有改动仅注释/测试/文档，无任何运行时行为变更；完成报告落盘 evidence/T-P11-dev-2026-09-11.md（R-1 纪律）。

## 边界（允许修改范围锁定）

| 文件 | 限制 |
|---|---|
| apps/api/src/services/planService.ts | **仅注释** |
| apps/h5/src/types/index.ts | **仅注释** |
| packages/shared/src/schemas/api.ts | **仅注释**（账本收敛 + v0.8 措辞） |
| packages/shared/src/schemas/dish.ts | **仅注释**（账本指针） |
| packages/engine/src/recommend.ts | **仅 L2 文件头注释** |
| apps/api/prisma/seed-data.ts | **仅 L50 附近注释**（2026-09-11 主控补登，AC1 勘误后真实位置） |
| projection-order.spec 所在测试文件（dev 定位） | 测试容错/断言固化 |

- **红线**：禁止任何 schema 定义变更、任何运行时代码行为变更、禁止动 routes/h5 页面逻辑。shared 注释级改动授权来源：用户 2026-09-11「按挂账队列依次执行」总授权 + 本卡 AC5/AC6 限定注释。
- **异常升级**：发现注释与实际代码不符且需改代码才能对齐 → 停手写进报告报主控，不得顺手改代码；测试基线数字与 T-P10 后基线（389/389）不符 → 如实报告差异不隐瞒。

## 环境口径

- 便携 PG 127.0.0.1:54329；tests/*.cjs 用绝对路径 require pg；根 package.json ESM。
- teardown 基线：Dish=48 / Menu=13 / MenuDish=42 / Ingredient=79 / Plan=54 / Event=93 / CookLog=6，REL=29 不动。
- 完成报告强制落盘 evidence/T-P11-dev-2026-09-11.md；逐条 [✓]/[✗] 对照 AC。
- 提交由主控负责（fm-dev 不执行 git commit）。

## 2026-09-12 技术审查补登（fm-reviewer：有条件通过，0 阻塞/1 条件/3 建议）

- 结论：AC1~AC5、AC7~AC9 全 [✓]，**AC6 判 [△]**。审查证据已落盘 [evidence/T-P11-review-2026-09-11.md](../../evidence/T-P11-review-2026-09-11.md)（主控代落盘）。
- **C-1（条件项，必须补正）**：dev 的 AC6 仅修正 api.ts 头部账本段（L17-20）v0.8 措辞，L201-206 unmetReasons 字段级 JSDoc 残留旧误导措辞「仅在空手且 unmetMustUse 非空时由服务端携带；正常推荐与组合凑不进（C-7a）时缺省」，与头部新措辞自相矛盾。属 AC6 原文「v0.8 措辞修正」注释级遗漏，无需扩卡，派回 fm-dev 单点补正。
- **S-1（建议，主控采纳修正）**：dish.ts L3「契约版本账本（v0.2~v0.8 连续记录）见 schemas/api.ts 头部」失实——api.ts 头部实际无 v0.3 专行（v0.3 记于 api.ts unmetMustUse 字段注，系 T-P09 前既有格局）。随 C-1 补正顺手修正措辞（仍属「dish.ts 仅注释」边界）。
- **S-2（建议，主控采纳=保留）**：api.ts v0.7 段末来源标注维持原样。
- **S-3（建议，主控不采纳）**：teardown.cjs 机制级补读不要求，维持现状。
- 补正边界（在原边界表内收紧明确）：api.ts 仅 unmetReasons 字段 JSDoc 注释行、dish.ts 仅 L3 指针注释行；其余文件不动。
- reviewer 移交口径：补正后本卡**即可转 VERIFY，无需再轮 reviewer**。
- **补正回执（2026-09-12）**：fm-dev 补正 C-1（api.ts L201-207 字段 JSDoc 与头部一致、旧措辞零残留）+ S-1（dish.ts L3 事实化措辞），6/6 AC [✓]（shared build EXIT=0、projection-order 冒烟 2 passed|3 skipped、diff 全注释行，api.ts 累计 +10/−4、dish.ts +1/−5），主控抽查确认；记录见 evidence/T-P11-dev-2026-09-11.md §7 + evidence/T-P11-review-2026-09-11.md 第八节。
- **验收结论（2026-09-12）**：fm-verify **PASS**（[evidence/T-P11-verify-2026-09-12.md](../../evidence/T-P11-verify-2026-09-12.md)）——独立复跑根 389/389 首跑即绿、taboo 89/89、tp01 54 PASS、h5 typecheck 0、teardown dry-run W1 PASS 后实删 14 PASS 七表回基线（Dish=48/Menu=13/MenuDish=42/Ingredient=79/Plan=54/Event=93/CookLog=6，REL=29）、:3000/:54329 双清零；6 文件逐 hunk 零行为变更审计与 dev §2 一致；AC2 补强：PG 停止后独立复跑显式 skip 计数可见（2 passed | 3 skipped）。挂账②③⑨⑩全核销。
