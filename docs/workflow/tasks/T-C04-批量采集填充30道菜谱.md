# T-C04 批量采集填充 30 道菜谱（小红书 CDP 流水线实操）

> 状态：**待用户审核**（fm-dev 交付 8/8 AC：30 关键词实采 **29 成功/1 失败**（蚝油生菜候选不足），Dish 19→**48**（+29 全部 FETCHED/DRAFT 三字段有值），87 张图落静态目录；新增 batch-fetch.mjs ~720 行编排脚本（断点续采/赞数排序/视频预过滤，零新增依赖）；报告 [evidence/T-C04-dev-2026-09-10.md](../../../evidence/T-C04-dev-2026-09-10.md)；主控对账复核 EXIT=0（total 48/87 图/fetchedMissing3Fields=[]）；git 提交与否由产品负责人裁决）

## 背景

T-C02（采集）+ T-C03（转换入库）已完结入库（HEAD `050d479`），三段式链路端到端验证通过。用户 2026-09-10 指令：**先走内容轨道实操，填充约 30 道菜谱**。

## AC（逐条 [✓]/[✗] 自检）

- **AC1 关键词清单**：制定 ~30 个关键词（覆盖荤菜/素菜/汤羹/主食，家常菜系优先：如红烧肉、可乐鸡翅、番茄炒蛋、鱼香肉丝、麻婆豆腐、糖醋排骨、清蒸鲈鱼、酸辣土豆丝、蒜蓉西兰花、番茄牛腩汤、香菇滑鸡、青椒肉丝、红烧茄子、蚝油生菜、韭菜盒子、水煮肉片等），清单落报告。
- **AC2 批量采集**：复用 xhs-fetch.mjs 逐关键词搜索，选**高赞（优先 likes 高）且图片≥3** 的笔记，每关键词 1 篇；沿用节流（NAV_GAP_MS≥5300）；**CDP 9222 浏览器与用户登录态禁杀禁动**（只读复用）。
- **AC3 容错**：单篇失败（无合格笔记/解析失败）跳过并记 manifest，整体不中断；检测到登录失效（/login 或验证码）**立即停**报告实采数量，不重试登录。
- **AC4 全链路入库**：每篇 fetch.json → fetch2dish.mjs → import --origin FETCHED（DRAFT）；图片落 apps/api/static/images/dishes/<noteId>/。
- **AC5 manifest**：out/xhs/batch-manifest.json（gitignore 内）：关键词/noteId/标题/赞数/图片数/dishId/失败原因；另落 evidence 报告摘要。
- **AC6 PG 对账**：基线 Dish=19（18 PUBLISHED+1 TESTED）→ 19+N（N=实采成功数，目标 30，允许 ≥25）；全部 origin=FETCHED、status=DRAFT、三字段有值；图片文件数与 manifest 一致。
- **AC7 证据落盘**：evidence/T-C04-dev-2026-09-10.md（命令+退出码+数字）。
- **AC8 teardown 纪律（本卡特殊）**：**产出为真实数据，不删除**（区别于测试数据）；PG 用完 stop（54329=0）；apps/api :3000 用完杀净；CDP 9222 禁碰。

## 边界

- 允许：tools/content-pipeline/**（批量编排脚本如 batch-fetch.mjs 可新增）、out/xhs/**（产物区）、apps/api/static/**（图片落盘）。
- 禁改：apps/h5/**、packages/**、apps/api/src/**（含 app.ts——不需要改动）、prisma、两个产品文档、xhs-fetch.mjs/fetch2dish.mjs/import.ts（T-C03 已定稿，直接复用；发现缺陷停下报告）。
- out/ 与 static/ 已 gitignore，产物不入库。

## 异常升级

缺信息或卡外问题 → 停手写进完成报告。
